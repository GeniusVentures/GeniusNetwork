---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/01-audit/CONCURRENCY-MAP.md"
  - "thirdparty/ipfs-bitswap-cpp/src/bitswap.hpp"
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
  - CONS-04
  - DOCS-02
---

# Phase 03 Plan 01 — Consumer Integration Verification + Documentation

<objective>
Verify that SuperGenius and AsyncIOManager work correctly with Phase 2's thread-safety fixes to ipfs-bitswap-cpp. Zero code changes are required — this plan delivers code review confirmation, build verification, and documentation updates.
</objective>

<must_haves>
## truths (immutable cross-cutting constraints, enforced at verify-work)
- truth:CONS-01 — SuperGenius builds and links with updated ipfs-bitswap-cpp with zero compilation errors
- truth:CONS-02 — AsyncIOManager builds and links with updated ipfs-bitswap-cpp with zero compilation errors
- truth:CONS-03 — Zero Bitswap `#include` or direct API calls exist in CRDT layer source files
- truth:CONS-04 — IPFSSaver PublishFile/PublishDirectory callbacks contain zero blocking I/O, lock acquisition, or long computation
- truth:DOCS-02 — CONCURRENCY-MAP.md contains "Phase 3: Consumer Contract" section with method-by-method table
- truth:DOCS-02 — bitswap.hpp contains @note Doxygen annotations on PublishFile, PublishDirectory, getCacheDir, setCacheDir
- truth:D-03 — Publish callback verification uses Bitswap's own concurrency_publish_test.cpp; no IPFSSaver integration tests needed
- truth:D-04 — Verification is code review only; builds and existing tests already pass on Windows MSVC; no new tests needed
</must_haves>

<tasks>

### Task 01-01: Build verification — SuperGenius and AsyncIOManager

<read_first>
- .planning/REQUIREMENTS.md (CONS-01, CONS-02 specs)
- .planning/phases/03-consumer-integration/03-CONTEXT.md (D-09, D-10 decisions)
- .planning/phases/03-consumer-integration/03-RESEARCH.md (Build/Linkage Analysis section)
- thirdparty/build/CommonTargets.CMake (lines 488-522, 660-692)
- SuperGenius/src/account/CMakeLists.txt (line 98)
- SuperGenius/src/processing/CMakeLists.txt (line 65)
</read_first>

<action>
Verify that the full project builds on the primary dev platform (Windows MSVC). Run `cmake --build . --parallel 8 --config Release` from the build/Windows/Release/ directory. Confirm the following targets compile and link successfully: ipfs-bitswap-cpp, genius_node (SuperGenius account module), processing_service (SuperGenius processing module), AsyncIOManager. Check CI build results for Linux, macOS, iOS, Android. Per D-10, CI failures on non-Windows platforms are non-blocking — document any failures as bugs to fix, not phase blockers.
</action>

<acceptance_criteria>
- `cmake --build . --parallel 8 --config Release` exits 0 from build/Windows/Release/
- Build output contains no errors mentioning Bitswap header changes (GetBlock const, mutex, atomic members)
- Build output contains no linker errors for ipfs-bitswap-cpp symbols
- genius_node, processing_service, and AsyncIOManager targets all appear as successfully built in build log
- If CI results available: report pass/fail per platform (Linux, macOS, iOS, Android)
</acceptance_criteria>

---

### Task 01-02: Code review — PublishFile/PublishDirectory io_context callback safety

<read_first>
- thirdparty/AsyncIOManager/src/IPFSSaver.cpp (full file, focus lines 107-141 and 190-221)
- thirdparty/ipfs-bitswap-cpp/src/bitswap.hpp (PublishFile signature at line ~237, PublishDirectory at line ~238)
- .planning/phases/01-audit/CONCURRENCY-MAP.md (finding C-9: callback-invocation thread context)
- .planning/phases/03-consumer-integration/03-RESEARCH.md (PublishFile/PublishDirectory Callback Safety Analysis section)
</read_first>

<action>
Audit IPFSSaver.cpp PublishFile callback (lines 107-141) and PublishDirectory callback (lines 190-221). For each callback, verify every operation is safe on io_context: (1) std::filesystem::remove is fast and stateless — safe, (2) ContentIdentifierCodec::toString is a pure utility — safe, (3) spdlog logger calls are thread-safe — safe, (4) shared_ptr<string> assignment is safe, (5) boost::asio::post to IPFSSaver's own io_context is safe. Confirm no blocking I/O, no lock acquisition, no long computation exists in either callback. Document findings in a code review note.
</action>

<acceptance_criteria>
- PublishFile callback (IPFSSaver.cpp:107-141): identified all 5 operations, each confirmed io_context-safe with rationale
- PublishDirectory callback (IPFSSaver.cpp:190-221): identified all operations, each confirmed io_context-safe with rationale
- Zero blocking I/O operations found (no file reads, network calls, sleep, wait)
- Zero lock acquisitions found (no mutex.lock, lock_guard, unique_lock)
- Zero long computation found (no loops over large data, no crypto operations)
- All captured values use value semantics or shared_ptr (no dangling references)
</acceptance_criteria>

---

### Task 01-03: Code review — setCacheDir/getCacheDir init-phase thread safety

<read_first>
- SuperGenius/src/account/GeniusNode.cpp (focus lines 1307-1318: InitSubSystems Bitswap section)
- thirdparty/AsyncIOManager/src/FileManager.cpp (focus lines 244-269: setBitswap method)
- thirdparty/ipfs-bitswap-cpp/src/bitswap.hpp (setCacheDir at line ~248, getCacheDir at line ~249)
- .planning/phases/03-consumer-integration/03-CONTEXT.md (D-01, D-05 decisions)
- .planning/phases/03-consumer-integration/03-RESEARCH.md (setCacheDir/getCacheDir Mutex Guard section)
</read_first>

<action>
Audit setCacheDir and getCacheDir call sequences in both consumers. GeniusNode.cpp: verify setCacheDir at line 1314 executes during main-thread InitSubSystems() before any async operations begin, and before Bitswap shared_ptr is propagated to other components via FileManager::setBitswap at line 1317. FileManager.cpp: verify getCacheDir at line 249 executes synchronously during the same InitSubSystems call chain (single-threaded init phase). Confirm both sites precede any async Bitswap operations.
</action>

<acceptance_criteria>
- GeniusNode.cpp:1314 setCacheDir call visually identified in InitSubSystems() method
- GeniusNode.cpp:1317 FileManager::setBitswap call visually identified after setCacheDir (same init sequence)
- FileManager.cpp:249 getCacheDir call visually identified inside setBitswap() method, called synchronously from GeniusNode init
- Confirm no async operations (io_context::post, async calls, thread spawns) occur between setCacheDir and sharing Bitswap with other components in GeniusNode
- Code review note documents the init sequence: setCacheDir(1314) → setBitswap(1317) → all async start
</acceptance_criteria>

---

### Task 01-04: Code review — GetBlock() const removal impact + CRDT audit

<read_first>
- thirdparty/ipfs-bitswap-cpp/src/bitswap.hpp (GetBlock signature, line ~220)
- .planning/phases/03-consumer-integration/03-RESEARCH.md (GetBlock() const Removal — Verification section)
- .planning/phases/03-consumer-integration/03-CONTEXT.md (D-06 CRDT decision)
</read_first>

<action>
Run exhaustive grep for GetBlock() calls across all consumer source trees: `rg "GetBlock\(" SuperGenius/src thirdparty/AsyncIOManager/src`. Confirm zero matches. Also grep CRDT layer for any direct Bitswap `#include` or API usage: `rg "bitswap" SuperGenius/src/crdt --ignore-case`. Confirm zero matches. Document both findings.
</action>

<acceptance_criteria>
- `rg "GetBlock\(" SuperGenius/src thirdparty/AsyncIOManager/src` returns 0 results
- `rg "bitswap" SuperGenius/src/crdt --ignore-case` returns 0 results (CRDT uses graphsync's HasBlock, not Bitswap's)
- GetBlock() const removal causes zero compilation errors — confirmed by Task 01-01 build passing
- CRDT layer has zero Bitswap dependencies — confirmed by grep
</acceptance_criteria>

---

### Task 01-05: Documentation — extend CONCURRENCY-MAP.md with consumer contract section

<read_first>
- .planning/phases/01-audit/CONCURRENCY-MAP.md (full file, 503 lines — understand existing structure, headings, table format)
- .planning/phases/03-consumer-integration/03-CONTEXT.md (D-07 decision with table specification)
- .planning/phases/03-consumer-integration/03-RESEARCH.md (Documentation Scope section with table template)
- .planning/phases/02-fix-test/02-CONTEXT.md (Phase 2 changes summary)
</read_first>

<action>
Extend CONCURRENCY-MAP.md with a new "## Phase 3: Consumer Contract" section inserted after the existing "## Thread Entry-Point Inventory" section. Add a method-by-method table with columns: Method | Caller Thread Requirement | Callback Thread | Mutex/Atomic Guarantee | Phase 2 Change Summary. Populate with all 9 public Bitswap methods: PublishFile, PublishDirectory, HasBlock, RequestContent, GetBlock, setCacheDir, getCacheDir, SetMaxPeerAttempts, SetPeerFailureThreshold. For each row, fill in the Phase 2 change that affected that method (or "Unchanged" if none). Include a brief preamble paragraph explaining that this is the canonical thread-safety reference for Bitswap consumers.
</action>

<acceptance_criteria>
- CONCURRENCY-MAP.md contains heading "## Phase 3: Consumer Contract" (exact string)
- Table has exactly 5 columns: Method, Caller Thread Requirement, Callback Thread, Mutex/Atomic Guarantee, Phase 2 Change Summary
- Table has exactly 9 data rows (one per public Bitswap method listed above)
- PublishFile row shows "Bitswap io_context (was: detached std::thread)" in Callback Thread column
- PublishDirectory row shows "Bitswap io_context (was: detached std::thread)" in Callback Thread column
- GetBlock row shows "const removed" in Phase 2 Change Summary column
- setCacheDir/getCacheDir rows show "Added mutex guard" in Phase 2 Change Summary column
- SetMaxPeerAttempts/SetPeerFailureThreshold rows show "Plain → atomic" in Phase 2 Change Summary column
- Preamble paragraph mentions "canonical thread-safety reference for Bitswap consumers"
</acceptance_criteria>

---

### Task 01-06: Documentation — add Doxygen @note annotations to bitswap.hpp

<read_first>
- thirdparty/ipfs-bitswap-cpp/src/bitswap.hpp (full file, 457 lines — understand existing Doxygen style, method signatures, comment conventions)
- .planning/phases/03-consumer-integration/03-CONTEXT.md (D-02, D-08 decisions)
- .planning/phases/03-consumer-integration/03-RESEARCH.md (Documentation Scope section with annotation text)
- thirdparty/ipfs-bitswap-cpp/src/bitswap.cpp (for implementation context on each method)
</read_first>

<action>
Add Doxygen `@note` annotations to 4 methods in bitswap.hpp:

1. PublishFile (near line ~237): Add `@note Callback fires on Bitswap's internal io_context. Consumer callbacks must not block the io_context thread.`

2. PublishDirectory (near line ~238): Add `@note Callback fires on Bitswap's internal io_context. Consumer callbacks must not block the io_context thread.`

3. getCacheDir (near line ~249): Add `@note Returns a copy of the cache directory under mutex (`mutexCacheDir_`) protection. Thread-safe for concurrent calls.`

4. setCacheDir (near line ~248): Add `@note Sets cache directory under mutex (`mutexCacheDir_`) protection. Call once before starting async operations for deterministic behavior.`

Each `@note` must be placed inside the existing Doxygen comment block (`/** ... */`) for its respective method, before `@param` tags. Match the existing Doxygen indentation style (2-space indent within comment blocks). Do not modify method signatures or implementation.
</action>

<acceptance_criteria>
- PublishFile Doxygen block contains `@note Callback fires on Bitswap's internal io_context. Consumer callbacks must not block the io_context thread.`
- PublishDirectory Doxygen block contains `@note Callback fires on Bitswap's internal io_context. Consumer callbacks must not block the io_context thread.`
- getCacheDir Doxygen block contains `@note Returns a copy of the cache directory under mutex` (with `mutexCacheDir_` reference)
- setCacheDir Doxygen block contains `@note Sets cache directory under mutex` (with `mutexCacheDir_` reference)
- All @note tags appear within existing `/** ... */` Doxygen comment blocks (not as standalone `//` comments)
- All @note tags placed before `@param` tags within their comment blocks (match existing Doxygen convention)
- No method signatures or implementations modified
</acceptance_criteria>

</tasks>

<verification>
## Phase 03 Plan 01 Verification

### Build Verification
- `cmake --build . --parallel 8 --config Release` from build/Windows/Release/ exits 0
- genius_node, processing_service, AsyncIOManager targets all build successfully with updated Bitswap

### Code Review Gate
- All 4 code review tasks (01-02 through 01-04) produce documented findings confirming zero consumer code changes needed
- IPFSSaver callbacks: all 5 operations per callback confirmed io_context-safe
- setCacheDir/getCacheDir: init-phase call sequence confirmed single-threaded
- GetBlock(): grep confirms 0 production call sites
- CRDT: grep confirms 0 Bitswap references

### Documentation Gate
- CONCURRENCY-MAP.md new section grep: `rg "^## Phase 3: Consumer Contract"` returns match
- CONCURRENCY-MAP.md table completeness: `rg "PublishFile|PublishDirectory|HasBlock|RequestContent|GetBlock|setCacheDir|getCacheDir|SetMaxPeerAttempts|SetPeerFailureThreshold"` returns 9 unique method names
- bitswap.hpp @note annotations: `rg "@note" thirdparty/ipfs-bitswap-cpp/src/bitswap.hpp` returns 4 matches
- Each @note references the correct method's threading contract

### Requirements Traceability
- CONS-01: verified by Task 01-01 (SuperGenius build) + Task 01-03 (init sequence) + Task 01-04 (GetBlock)
- CONS-02: verified by Task 01-01 (AsyncIOManager build) + Task 01-02 (callback safety) + Task 01-03 (getCacheDir)
- CONS-03: verified by Task 01-04 (CRDT Bitswap dependency audit grep)
- CONS-04: verified by Task 01-02 (IPFSSaver callback io_context safety audit)
- DOCS-02: verified by Task 01-05 (CONCURRENCY-MAP.md extension) + Task 01-06 (Doxygen @notes)
</verification>

<success_criteria>
- [ ] SuperGenius builds and links successfully with updated ipfs-bitswap-cpp (CONS-01)
- [ ] AsyncIOManager builds and links successfully with updated ipfs-bitswap-cpp (CONS-02)
- [ ] CRDT layer Bitswap integration verified correct with updated threading model — zero Bitswap references in CRDT (CONS-03)
- [ ] AsyncIOManager event-loop integration verified correct — no strand violations at Bitswap boundary (CONS-04)
- [ ] CONCURRENCY-MAP.md extended with Phase 3 Consumer Contract section (DOCS-02)
- [ ] bitswap.hpp annotated with @note Doxygen comments on 4 methods (DOCS-02)
</success_criteria>
