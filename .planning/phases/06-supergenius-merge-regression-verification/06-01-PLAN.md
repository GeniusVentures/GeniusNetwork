---
phase: 06-supergenius-merge-regression-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SuperGenius/src/account/TransactionManager.cpp
  - SuperGenius/src/account/TransactionManager.hpp
  - SuperGenius/src/account/CMakeLists.txt
  - SuperGenius/src/account/GeniusNode.hpp
  - SuperGenius/src/account/GeniusNode.cpp
  - SuperGenius/src/blockchain/Blockchain.hpp
  - SuperGenius/src/blockchain/impl/Blockchain.cpp
  - SuperGenius/src/blockchain/impl/proto/Consensus.proto
  - SuperGenius/test/src/account/CMakeLists.txt
  - SuperGenius/src/account/ProcessingTransaction.hpp
  - SuperGenius/src/account/ProcessingTransaction.cpp
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "All 9 confirmed shared files are staged in a conflict-marker-free state after the merge (no leftover <<<<<<< / ======= / >>>>>>> anywhere in src/, test/, example/)"
    - "ProcessingTransaction is fully retired: absent from both TransactionManager.cpp and CMakeLists.txt, and the source files themselves no longer exist in the working tree"
  artifacts:
    - "SuperGenius/src/account/TransactionManager.cpp with ProcessingTransaction.hpp include dropped, RegistrationTransaction.hpp/RevokeTransaction.hpp retained"
    - "SuperGenius/src/account/CMakeLists.txt with ProcessingTransaction.cpp dropped from GENIUS_NODE_SOURCES, RegistrationTransaction.cpp/RevokeTransaction.cpp retained"
    - "SuperGenius git index mid-merge (MERGE_HEAD present) with all 9 shared files staged, pre-existing docs submodule pointer bump left untouched/unstaged"
  key_links:
    - "TransactionManager.cpp's include-block resolution and CMakeLists.txt's GENIUS_NODE_SOURCES resolution must agree (both drop ProcessingTransaction) or the build breaks with a dangling file reference"
</must_haves>
---

<objective>
Start the `origin/develop` -> `dev_childwallet` merge inside the `SuperGenius` submodule and resolve the 2 genuinely-conflicting files (`src/account/TransactionManager.cpp`, `src/account/CMakeLists.txt`), both of which conflict for the same reason: `origin/develop` retired the deprecated `ProcessingTransaction`/`"process"` tx type at the same list/include positions where `dev_childwallet` inserted `RegistrationTransaction`/`RevokeTransaction`. Leave the merge commit itself un-finalized (mid-merge, `MERGE_HEAD` present, conflicts resolved and staged) so later plans can run the rename sweep, build, and regression suite before anything is committed.

Purpose: MERGE-01 requires all conflicts in the 9 confirmed shared files resolved before a merge commit can be created; this plan performs that resolution and confirms zero conflict markers remain anywhere, without touching the unrelated pre-existing `docs` submodule pointer bump.
Output: SuperGenius working tree mid-merge, 9 shared files staged in a conflict-free, mutually-consistent state; `ProcessingTransaction.{hpp,cpp}` confirmed removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-supergenius-merge-regression-verification/06-RESEARCH.md
@.planning/phases/06-supergenius-merge-regression-verification/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight and start the merge (no-commit)</name>
  <files>SuperGenius/ (git state only, no source edits)</files>
  <read_first>
    06-RESEARCH.md sections "Refreshed Git State" and the "Pre-flight housekeeping note" (docs submodule pointer bump, b68034b0 -> 3293bb6a, must be left untouched); 06-RESEARCH.md "Per-File Conflict Analysis" table.
  </read_first>
  <action>
    In `SuperGenius/`: run `git status` first and confirm the ONLY local modification is `docs` (submodule pointer, new commits) — do not run `git checkout .`, `git reset --hard`, `git stash`, or `git add -A` against it at any point in this plan; it stays untouched and unstaged throughout. Run `git fetch origin develop`. Confirm `git rev-parse origin/develop` returns `2981cd83abb08aba6fdb594103fddb89f8b5dd64` and `git rev-parse HEAD` returns `5fd137dcd13475114b070e070cda374850f8e699` (matches 06-RESEARCH.md's snapshot — if either hash differs, `origin/develop` has moved since research; stop and flag this in the SUMMARY rather than proceeding on stale line-number assumptions). Then run `git merge --no-ff --no-commit origin/develop`. Expect git to report conflicts in exactly `src/account/TransactionManager.cpp` and `src/account/CMakeLists.txt` ("both modified"), with the other 7 shared files (`GeniusNode.hpp`, `GeniusNode.cpp`, `TransactionManager.hpp`, `Blockchain.hpp`, `Blockchain.cpp`, `Consensus.proto`, `test/src/account/CMakeLists.txt`) auto-merged and already staged by git. `--no-commit` is deliberate: it keeps the merge open (MERGE_HEAD present) so later plans can build/test before anything is finalized.
  </action>
  <verify>
    <automated>git -C SuperGenius rev-parse -q --verify MERGE_HEAD</automated>
  </verify>
  <acceptance_criteria>
    `git status` (run inside `SuperGenius/`) reports "You have unmerged paths" listing exactly two files as "both modified": `src/account/TransactionManager.cpp` and `src/account/CMakeLists.txt`. `.git/MERGE_HEAD` exists (or `git rev-parse -q --verify MERGE_HEAD` prints a hash). `docs` still appears separately in `git status` as a pre-existing unstaged modification (not part of the merge conflict set).
  </acceptance_criteria>
  <done>Merge started, MERGE_HEAD present, exactly the 2 expected files show real conflicts, docs pointer untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the 2 real conflicts (retire ProcessingTransaction consistently)</name>
  <files>SuperGenius/src/account/TransactionManager.cpp, SuperGenius/src/account/CMakeLists.txt</files>
  <read_first>
    06-PATTERNS.md sections "`src/account/TransactionManager.cpp` (conflict-resolution)" and "`src/account/CMakeLists.txt` (conflict-resolution)" for the exact current-code excerpts and resolution pattern. Read the live conflicted files themselves (`SuperGenius/src/account/TransactionManager.cpp` around the include block, `SuperGenius/src/account/CMakeLists.txt` around the `GENIUS_NODE_SOURCES` list) to see the actual `<<<<<<<`/`=======`/`>>>>>>>` markers before editing.
  </read_first>
  <action>
    In `SuperGenius/src/account/TransactionManager.cpp`'s include-block conflict: remove the conflict markers, delete the `#include "ProcessingTransaction.hpp"` line entirely, keep `#include "RegistrationTransaction.hpp"` and `#include "RevokeTransaction.hpp"` exactly as they are (both sides already agree these two stay). In `SuperGenius/src/account/CMakeLists.txt`'s `GENIUS_NODE_SOURCES` list conflict: remove the conflict markers, delete the `ProcessingTransaction.cpp` line entirely, keep `RegistrationTransaction.cpp` and `RevokeTransaction.cpp`. Treat both edits as one atomic decision (per 06-PATTERNS.md's "Shared Patterns" note) — do not resolve one file by keeping ProcessingTransaction and the other by dropping it, that produces an inconsistent build.
  </action>
  <verify>
    <automated>grep -c '<<<<<<<' SuperGenius/src/account/TransactionManager.cpp SuperGenius/src/account/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    `grep -c '<<<<<<<' SuperGenius/src/account/TransactionManager.cpp` == 0. `grep -c '<<<<<<<' SuperGenius/src/account/CMakeLists.txt` == 0. `grep -c 'ProcessingTransaction' SuperGenius/src/account/TransactionManager.cpp` == 0. `grep -c 'ProcessingTransaction' SuperGenius/src/account/CMakeLists.txt` == 0. `grep -c 'RegistrationTransaction\|RevokeTransaction' SuperGenius/src/account/TransactionManager.cpp` >= 1 (both still present). `grep -c 'RegistrationTransaction.cpp\|RevokeTransaction.cpp' SuperGenius/src/account/CMakeLists.txt` == 2.
  </acceptance_criteria>
  <done>Both conflicts resolved identically (ProcessingTransaction dropped everywhere, Registration/Revoke retained everywhere), no markers remain in either file.</done>
</task>

<task type="auto">
  <name>Task 3: Repo-wide conflict-marker sweep, retirement confirmation, and explicit staging</name>
  <files>SuperGenius/ (verification + staging, no further edits expected)</files>
  <read_first>
    06-RESEARCH.md "Real Conflict 1" section (the two non-conflicting, auto-merged removals in TransactionManager.cpp that must be spot-checked: the `RegisterDeserializer( "process", ...)` line and the `kProcessing` switch-case arm) and "Common Pitfalls" Pitfall 2.
  </read_first>
  <action>
    Search the whole tree for any leftover conflict markers outside the 2 files already resolved — this proves no other shared or auto-merged file has a stray marker. Separately spot-check that the two adjacent, non-conflicting removals inside `TransactionManager.cpp` (the retired tx-type's deserializer registration call, and its dispatch switch-case arm) are gone while the neighboring registration/revoke deserializer registrations and switch-case arms remain untouched — do not edit either file further in this task, this is confirmation-only. Also confirm the two retired source files described in Task 2 no longer exist anywhere in the working tree (both sides of the merge deleted them — dev_childwallet never modified them, develop deleted them, so git auto-applies the deletion with no conflict). Stage explicitly by path (never `git add -A`, to keep the `docs` submodule pointer bump out of the merge): `git add src/account/TransactionManager.cpp src/account/TransactionManager.hpp src/account/CMakeLists.txt src/account/GeniusNode.hpp src/account/GeniusNode.cpp src/blockchain/Blockchain.hpp src/blockchain/impl/Blockchain.cpp src/blockchain/impl/proto/Consensus.proto test/src/account/CMakeLists.txt`. Then run `git status` and confirm no unmerged paths remain and `docs` is still listed separately as unstaged.
  </action>
  <!-- planner-discipline-allow: ProcessingTransaction -->
  <verify>
    <automated>grep -rn '<<<<<<<' SuperGenius/src SuperGenius/test SuperGenius/example</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn '<<<<<<<' SuperGenius/src SuperGenius/test SuperGenius/example` returns zero lines.
    `grep -c 'RegisterDeserializer( "process"' SuperGenius/src/account/TransactionManager.cpp` returns 0.
    `grep -c 'kProcessing' SuperGenius/src/account/TransactionManager.cpp` returns 0.
    `grep -c 'kRegistration' SuperGenius/src/account/TransactionManager.cpp` returns 1 or more, and `grep -c 'kRevoke' SuperGenius/src/account/TransactionManager.cpp` returns 1 or more (both dispatch arms still present, checked as two independent commands).
    `test -f SuperGenius/src/account/ProcessingTransaction.hpp` fails (file absent).
    `test -f SuperGenius/src/account/ProcessingTransaction.cpp` fails (file absent).
    `git status` (inside `SuperGenius/`) shows no "Unmerged paths" section and the 9 shared files listed under "Changes to be committed"; `docs` still shows as a separate unstaged/uncommitted modification.
  </acceptance_criteria>
  <done>Zero conflict markers repo-wide, the retired tx type fully removed from the tree, all 9 shared files explicitly staged, docs pointer bump left isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `origin/develop` history -> `dev_childwallet` merge | Untrusted (external-to-this-feature) upstream commits are integrated into the branch child-wallet consensus logic lives on |
| Merge conflict resolution (human/agent judgment) -> committed source | The resolution decisions made in this plan become permanent source-of-truth for the retired `ProcessingTransaction` code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | src/account/TransactionManager.cpp, src/account/CMakeLists.txt | high | mitigate | Resolve both conflicts identically (fully adopt develop's ProcessingTransaction retirement in both files, per Task 2); Task 3's acceptance criteria hard-gates on `grep -c 'ProcessingTransaction'` == 0 in both files AND both `.hpp`/`.cpp` source files confirmed absent from the working tree — an inconsistent resolution (e.g. include dropped but CMake source kept) would either fail to build (safe) or leave a stale reference; this explicit dual-file grep catches it either way instead of relying on "build passes" |
| T-06-02 | Tampering | docs submodule pointer (SuperGenius nested submodule) | low | mitigate | Task 1 explicitly forbids `git add -A`/`git stash`/`git reset --hard` against `docs`; Task 3 confirms via `git status` that `docs` remains a separate unstaged modification, never entering the merge commit's staged set |
</threat_model>

<verification>
Repo-wide `grep -rn '<<<<<<<'` across `src/`, `test/`, `example/` inside `SuperGenius/` returns zero matches. `ProcessingTransaction.{hpp,cpp}` absent from the working tree. `git status` shows all 9 shared files staged, zero unmerged paths, `docs` untouched.
</verification>

<success_criteria>
Merge is open (MERGE_HEAD present), both real conflicts resolved consistently (ProcessingTransaction fully retired), zero conflict markers remain anywhere in the repo, and the pre-existing docs submodule pointer bump is unaffected. Nothing has been committed yet — Plan 02 continues on top of this staged state.
</success_criteria>

<output>
Create `.planning/phases/06-supergenius-merge-regression-verification/06-01-SUMMARY.md` when done
</output>
