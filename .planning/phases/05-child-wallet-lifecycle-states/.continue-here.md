---
context: phase
phase: 05-child-wallet-lifecycle-states
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-07-23T00:45:57.147Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: no-auto-commit-on-checkpoint — Plan 05-06 is `autonomous: false` with a blocking `checkpoint:human-verify` (Task 3) specifically because SuperGenius/AgentDocs/CLAUDE.md forbids committing consensus/CRDT code without explicit human approval. Even if `workflow.auto_advance`/auto-mode is active in this project, do NOT auto-approve this checkpoint or auto-commit — this was already caught once this session and must not regress.
- [ ] CONSTRAINT: segfault-on-exit-is-known — Every test run of `registration_transaction_test.exe` in this environment segfaults *after* gtest already prints its PASS/FAIL summary (process exit code 139, `Segmentation fault` from bash). This is a separate, pre-existing issue **another dev is already handling** — do not investigate or fix it, and do not treat exit code 139 as a test failure. Always read the gtest `[  PASSED  ]`/`[  FAILED  ]` lines from the log, never trust the process exit code alone.
- [ ] CONSTRAINT: scope-boundary-already-overridden — Plan 05-06's own text says "this plan does not add, rename, or modify any test code." That boundary has already been explicitly overridden by the user for one specific, necessary fix (see below) — a pre-existing test bug blocked verifying the plan's actual target. Do not re-litigate whether test changes are allowed; they are, for the nonce fix already applied. Do not expand test-code changes beyond what's needed to make the existing tests correctly exercise already-implemented behavior.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Diagnosing from log correlation alone without a stack trace | The original 05-05/05-VERIFICATION diagnosis ("ParseRevokeTransaction's own Put call deadlocks") was inferred from log timing, not a captured stack. The user's own debugger stack trace (captured live) revealed the *real* location was `ConsensusManager::SubmitCertificate` — a completely different call path — which led to the actual root cause below. | advisory | When a hang/deadlock is being diagnosed, prefer an actual stack trace (debugger, or bracket logging around suspected lock-acquisition points) over inferring cause from log timestamps alone. |
| Assuming isolated single-test flakiness is environmental | `ReRegistrationAfterRevoke` failed 4/4 isolated + full-suite runs at the identical assertion, and was initially assumed to be the same "known cert-poll flakiness" seen elsewhere. It was not — it was a 100%-deterministic test bug (see below). A failure that repeats identically every single time is a bug, not flakiness, even if it superficially resembles a known flaky failure mode. | blocking | Before attributing a repeated failure to "known flakiness," check whether it reproduces with 100% consistency (a real bug) vs. genuinely intermittent (true flakiness) — read the actual error log content each time, don't pattern-match on the assertion line number alone. |

<current_state>
Executing Plan 05-06 (gap closure for the ParseRevokeTransaction deadlock found in 05-05-VERIFICATION.md, score 13/18). The plan's stated fix (Task 1: `CrdtDatastore::PutKeyLocal`/`GlobalDB::PutLocal`, routing `ParseRevokeTransaction`'s reg/ write through a local-only, non-broadcast path) turned out to be necessary but **not sufficient** — deep debugging (with the user, using a live debugger + manual stack-trace capture) found the *actual* root cause was a **different** bug: `CrdtSet::mutex_` is a plain non-recursive `std::mutex` held across `PutElems`'s synchronous `putHookFunc_` callback, and that callback chain can re-enter `PutElems` on the same thread (a `/cert/` key's PutHook → `ConsensusManager::CertificateReceived` → confirmed-tx processing → `ParseRevokeTransaction` → `PutKeyLocal` → `set_->Merge` → `PutElems` again). A same-thread relock of a non-recursive mutex just hangs forever. Fix: `CrdtSet::mutex_` changed to `std::recursive_mutex` (SuperGenius/src/crdt/crdt_set.hpp).

Both fixes (PutKeyLocal/PutLocal + recursive_mutex) are **already committed** in the SuperGenius submodule — but this was done by the user directly (commit `be369162` "Debugging results, for now" on branch `dev_childwallet`), NOT by this session's agent, and NOT through the plan's required Task 3 checkpoint approval. **This means the checkpoint approval step for THIS specific code (Task 1 + the recursive_mutex change) has effectively been bypassed by direct user action outside the GSD workflow.** A resuming session should treat this as already-approved by virtue of the user's own commit action, but should confirm this explicitly with the user rather than assuming — the outer GNUS-Network repo's own SUMMARY.md + submodule-bump commit (the "outer repo" half of Task 3's required two-stage commit) has NOT happened yet.

Extensive testing after the recursive_mutex fix: **zero reproductions of the `WaitForJob: Still waiting for CID` deadlock signature across 13+ isolated test runs and 33 full-suite test executions.** The recursive_mutex fix appears to have genuinely eliminated the deadlock.

One remaining test, `RegistrationTransactionE2ETest.ReRegistrationAfterRevoke`, failed 100% of the time (4/4 attempts) — but NOT due to the deadlock. Root cause (confirmed by reading the test source, not guessed): the test's manually-built `rereg_tx` DAGStruct hardcodes `dag.set_nonce(0)` (SuperGenius/test/src/account/registration_transaction_test.cpp, was line ~2250), but the SAME child account already consumed nonce=0 for its original registration via `CertifyChildRegistration`'s own hardcoded `dag.set_nonce(0)` (line ~355). Consensus replay protection correctly rejects the second proposal as `Nonce too low ... nonce=0 confirmed=0` — this is a **pre-existing test bug**, unrelated to the CRDT/consensus production code this plan targets, and it is fully deterministic (not flaky) — it would fail every single time regardless of the CrdtSet fix.

The user has applied the obvious fix (`dag.set_nonce(0)` → `dag.set_nonce(1)` at that line) directly in their editor — **this change is present in the working tree but UNCOMMITTED and UNTESTED.** The user's own words: "we're going to modify the test, but that alone doesn't fix it" — implying they suspect there may be a SECOND issue in this test beyond the nonce collision that will surface once the nonce fix is in place. This has not yet been investigated further. **This is the immediate next action.**
</current_state>

<completed_work>

Completed Tasks:
- Task 1 (plan's original scope): `CrdtDatastore::PutKeyLocal`/`GlobalDB::PutLocal` added, `ParseRevokeTransaction` wired to use `PutLocal` instead of `Put` — Done, committed by user in SuperGenius commit `be369162`.
- Extra fix (discovered mid-session, not in original plan scope): `CrdtSet::mutex_` changed from `std::mutex` to `std::recursive_mutex` in `SuperGenius/src/crdt/crdt_set.hpp` — Done, same commit `be369162`. This is the fix that actually eliminates the deadlock; PutKeyLocal/PutLocal alone did not (see anti-patterns above).
- Task 2 (verification), partial: All 6 originally-blocked/target Revoke tests + the 2 previously-passing Revoke authority tests + all pre-existing Phase 2-5 tests (31 tests total in the binary) have been run, most repeatedly:
  - `RevokeChildEndToEnd`: PASSED (2/3 isolated attempts; 1 hit unrelated cert-poll flakiness, 0 deadlock)
  - `RevokeRejectedForAlreadyDetachedChild`: PASSED
  - `RevokePreservesChildUTXOsKeypairNonce`: PASSED
  - `RevokeRejectedForNonMain`: PASSED
  - `RevokeRejectedForSequenceMismatch`: PASSED
  - `ReRegistrationAfterRevoke`: FAILED 4/4 — root-caused to the test-file nonce bug above, NOT the deadlock. Fix applied uncommitted, not yet re-tested.
  - Full unfiltered suite: 25/25 passed in one continuous run (cut off by an orchestrator timeout, not a hang) + 7/8 passed in a second run covering the remainder (the 1 failure being `ReRegistrationAfterRevoke` again).
  - **Zero `WaitForJob: Still waiting for CID` occurrences in any of the ~46 total test executions this session.**
</completed_work>

<remaining_work>

- Re-run `ReRegistrationAfterRevoke` in isolation with the nonce=1 fix now in place. Investigate whether the user's suspicion ("that alone doesn't fix it") is correct — if it still fails, read the new failure evidence fresh; do not assume it's the same nonce issue or blindly guess a second fix.
- Once `ReRegistrationAfterRevoke` passes reliably (or its remaining issue is understood and fixed), re-run the full Task 2 verification matrix one more time for confidence (all 6 target tests in isolation + the 2 authority tests + full regression suite).
- Confirm with the user explicitly whether SuperGenius commit `be369162` constitutes their Task 3 approval for the PutKeyLocal/PutLocal + recursive_mutex changes, or whether they still want a formal checkpoint presentation for those before the outer-repo commit. Do not assume silently.
- Once approved: commit the test-file nonce fix (and any further test fix) inside the SuperGenius submodule.
- Complete Task 3's outer-repo half: write `.planning/phases/05-child-wallet-lifecycle-states/05-06-SUMMARY.md`, bump the SuperGenius submodule pointer in the outer GNUS-Network repo, commit both together per the plan's `<output>` instruction.
- After SUMMARY.md exists: resume the normal `/gsd-execute-phase 5` flow — it will detect the completed plan, update STATE.md/ROADMAP.md, and run phase-level verification (05-VERIFICATION.md already exists from the original phase run; a fresh verification pass or an update reflecting the gap closure may be appropriate — check `gsd-verifier`'s conventions when resuming).
</remaining_work>

<decisions_made>

- Chose `std::recursive_mutex` over deferring `putHookFunc_` invocation outside the lock (the more "architecturally clean" alternative) for `CrdtSet::mutex_`, because it was the smaller, more surgical change (one declaration site, one lock site, both using CTAD) and testing showed it worked. The deferred-hook-execution alternative remains a candidate if the recursive mutex is later found to mask other reentrancy correctness issues (e.g. in `SetValue`'s priority-check logic under same-thread reentry) — not currently suspected, but not exhaustively ruled out either.
- Used print-based bracket logging (`TEMP-DIAGNOSTIC` markers, temporary, already reverted before the final commit) instead of a live debugger for the initial hypothesis test, because no CLI debugger (`cdb.exe`/`windbg.exe`) is installed in this environment — only the Windows Kit's support DLLs are present, no actual debugger binary. The user separately confirmed they DO have a working debugger (Visual Studio) and provided a live stack trace directly, which is what actually found the real root cause (the bracket-logging approach only got as far as confirming *that* PutKeyLocal's `Merge` call hangs, not *why*).
- Test builds use the `SuperGenius/build/Windows/Release` tree throughout (already CMake-configured, fast incremental rebuilds, ~10-60s per incremental build). `SuperGenius/build/Windows/Debug` exists as a directory but was NOT CMake-configured as of this session (no CMakeCache.txt, zero compiled objects) — the user has since said they built it themselves ("I've already built SuperGenius debug"), so it may now be usable, but was not used for the actual test runs in this session; Release was used throughout instead since print-diagnostics don't need debug symbols.
</decisions_made>

<blockers>
- None hard-blocking. The open question is whether the nonce=1 fix alone resolves `ReRegistrationAfterRevoke`, or whether a second issue remains — this is the immediate next action, not yet a confirmed blocker.
</blockers>

## Required Reading (in order)

1. `.planning/phases/05-child-wallet-lifecycle-states/05-06-PLAN.md` — the plan being executed; note its stated scope (PutKeyLocal/PutLocal only) has already been exceeded by the recursive_mutex fix and the test-file nonce fix, both necessary and already discussed with the user.
2. `.planning/phases/05-child-wallet-lifecycle-states/05-05-SUMMARY.md` and `05-VERIFICATION.md` — original deadlock discovery and the gap this plan closes.
3. `SuperGenius/AgentDocs/CLAUDE.md` — source of the no-auto-commit / interactive-mode / understand-before-committing rules that make this plan `autonomous: false`.
4. This file's `<current_state>` section above — it contains the full root-cause chain (both the CrdtSet::mutex_ reentrancy bug and the test nonce bug) since the diagnostic session logs that document it live only in this session's temporary scratchpad directory, which will NOT be available to a resumed session.

## Infrastructure State

- SuperGenius submodule: on branch `dev_childwallet`, HEAD at commit `be369162` ("Debugging results, for now" — contains the PutKeyLocal/PutLocal + recursive_mutex changes). One further uncommitted change: `test/src/account/registration_transaction_test.cpp` (nonce=0→1 fix, applied by the user, untested).
- Outer GNUS-Network repo: on branch `dev_persisprocresults`. `.planning/STATE.md` has an uncommitted change from this session's `state.begin-phase` bookkeeping call (harmless, safe to include in any future commit). Submodule pointers for GeniusWallet/SuperGenius/thirdparty/zkLLVM show as dirty (expected, since SuperGenius itself has uncommitted/newly-committed changes).
- Two small, harmless leftover test-artifact directories at the outer repo root: `CRDT.Datastore.TEST/` (1K) and `CRDT.Datastore.TEST.unit_25/` (46K) — residue from test runs that hit the known segfault-on-exit before their destructor's `fs::remove_all` cleanup could run. Safe to delete, not required.
- Build tree: `SuperGenius/build/Windows/Release` is configured and incrementally fast — this is what to use for continued testing. Rebuild via: `cmake --build SuperGenius/build/Windows/Release --target registration_transaction_test --config Release --parallel 8`.
- Test binary: `SuperGenius/build/Windows/Release/test_bin/Release/registration_transaction_test.exe`. Always wrap invocations in an OS-level `timeout` (e.g. `timeout 150 ...`) as a safety net — not because hangs are currently expected (they're not, post-fix), but as blast-radius discipline per the user's own instruction earlier this session ("we shouldn't let this run beyond expected times").

<context>
This started as a routine `/gsd-execute-phase 5` run to close the single remaining gap from Phase 5's verification (a deadlock blocking 4/6 Revoke tests). The plan's own proposed fix (PutKeyLocal/PutLocal) builds clean but empirically did NOT eliminate the deadlock on its own — extensive live debugging with the user (they attached an actual debugger and shared a real stack trace, which is what cracked this) found the true mechanism: a non-recursive mutex held across a synchronous callback chain that can recursively re-enter itself. That's now fixed and thoroughly tested with zero deadlock reproductions. The one remaining loose end is a SEPARATE, unrelated, pre-existing test bug (nonce collision) in `ReRegistrationAfterRevoke` that the user is actively fixing directly in their editor — they've expressed a hunch that the obvious one-line fix might not be the whole story, and that's exactly where a resuming session should pick up: test it, and if it's not enough, investigate fresh rather than assuming.
</context>

<next_action>
Start with: rebuild `registration_transaction_test` (Release, incremental) and run `RegistrationTransactionE2ETest.ReRegistrationAfterRevoke` in isolation (bounded with `timeout 150`) to see whether the nonce=1 fix resolves it. Read the actual failure output fresh if it still fails — do not assume it's the same nonce issue.
</next_action>
