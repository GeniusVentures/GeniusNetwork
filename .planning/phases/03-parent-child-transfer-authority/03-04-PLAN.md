---
phase: 03-parent-child-transfer-authority
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - SuperGenius/test/src/account/registration_transaction_test.cpp
autonomous: true
requirements: [CONS-01, CONS-02, REGR-01, REGR-02, REGR-03]
tags: [test, consensus, regression]

must_haves:
  truths:
    - "A main-signed ordinary transfer to a registered child address, and to an arbitrary unregistered address, both pass ValidateTransactionForConsensus unchanged (CONS-01)"
    - "A certified child's RecoverFromChild-constructed transaction, main-signed, recovering to the child's own certified main_address, passes ValidateTransactionForConsensus (CONS-02)"
    - "The same recovery shape with a destination other than the certified main_address is rejected by ValidateTransactionForConsensus (D-21)"
    - "A child-signed transfer to an arbitrary address and to its registered main both pass ValidateTransactionForConsensus unchanged (REGR-01)"
    - "A child-signed transfer to a developer-style address passes ValidateTransactionForConsensus unchanged, proving PayDev's equivalence (REGR-02)"
    - "A transaction claiming src=main_addr but signed with the child's own key is rejected by CheckTransactionAuthorization (REGR-03 — child cannot spend main's UTXOs even claiming delegated authority)"
  artifacts:
    - path: "SuperGenius/test/src/account/registration_transaction_test.cpp"
      provides: "Extended RegistrationTransactionE2ETest fixture (second bare GeniusAccount for the child identity) plus new CONS-01/CONS-02/REGR-01/REGR-02/REGR-03 test cases"
      contains: "child_account_"
  key_links:
    - from: "Test: MainFundsChildApprovedByGate"
      to: "TransactionManager::TransferFunds"
      via: "CONS-01 — ordinary transfer, no new gate interference"
      pattern: "TransferFunds"
    - from: "Test: MainRecoversFromChildApproved"
      to: "TransactionManager::RecoverFromChild"
      via: "CONS-02 happy path"
      pattern: "RecoverFromChild"
    - from: "Test: certification helper"
      to: "Blockchain::CreateConsensusProposal + Blockchain::SubmitProposal"
      via: "single-validator self-vote certification of the manually-injected child registration, mirroring TransactionManager.cpp's own real-transaction submission path"
      pattern: "CreateConsensusProposal"
---

<objective>
Prove, with automated tests extending the existing E2E fixture (D-65 — no new test file), that CONS-01 and CONS-02 work end-to-end through the real consensus pipeline, and that REGR-01/02/03 confirm every existing child-signed path is unaffected by Plan 01-03's changes.

Purpose: Plans 01-03 built the mechanism; this plan is the only place that empirically closes the loop — proving the gate logic table in 03-RESEARCH.md actually holds against the real `ValidateTransactionForConsensus` pipeline, not just in isolation.
Output: New passing GTest cases in the existing `RegistrationTransactionE2ETest` fixture, covering all five requirement IDs this plan carries.
</objective>

## Artifacts this phase produces

Full symbol inventory for Phase 3 (Parent-Child Transfer Authority), so the source-grounding pass excludes newly-created symbols from drift verification regardless of which plan file it reads:

- `Blockchain::CheckCertifiedParent(const std::string &child_addr) const -> std::optional<std::string>` — new method, `Blockchain.hpp`/`Blockchain.cpp` (Plan 01)
- `GeniusTransaction::CheckSignatureAgainst(const std::string &address) const -> bool` — new method, `GeniusTransaction.hpp`/`GeniusTransaction.cpp` (Plan 01)
- `TransactionManager::CheckParentChildAuthority(const GeniusTransaction &tx) const -> bool` — new method, `TransactionManager.hpp`/`TransactionManager.cpp` (Plan 02)
- `TransactionManager::CheckTransactionAuthorization` — extended (new D-60 branch, signature unchanged) (Plan 02)
- `GeniusInputValidator::ValidateWitness` per-input signature check — extended (new D-60 OR-branch, signature unchanged) (Plan 02)
- `TransactionManager::RecoverFromChild(std::string child_address, uint64_t amount, TokenID token_id) -> outcome::result<std::string>` — new method, `TransactionManager.hpp`/`TransactionManager.cpp` (Plan 03)
- `GeniusNode::RecoverFromChild(const std::string &child_address, uint64_t amount, TokenID token_id) -> outcome::result<std::string>` — new method, `GeniusNode.hpp`/`GeniusNode.cpp` (Plan 03)
- `GeniusNode::RecoverFromChild(const std::string &child_address, uint64_t amount, TokenID token_id, std::chrono::milliseconds timeout) -> outcome::result<std::pair<std::string, uint64_t>>` — new method, `GeniusNode.hpp`/`GeniusNode.cpp` (Plan 03)
- `RegistrationTransactionE2ETest::child_account_` — new fixture member, `registration_transaction_test.cpp` (this plan)
- `CertifyChildRegistration(...)` — new test helper, `registration_transaction_test.cpp` (this plan)
- New test cases: `MainFundsChildApprovedByGate`, `MainRecoversFromChildApproved`, `MainRecoveryWrongDestinationRejected`, `ChildTransferToArbitraryAndMainUnaffected`, `ChildTransferToDevWalletUnaffected`, `ChildCannotClaimMainAsSourceRejected` — `registration_transaction_test.cpp` (this plan)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-parent-child-transfer-authority/03-CONTEXT.md
@.planning/phases/03-parent-child-transfer-authority/03-RESEARCH.md
@.planning/phases/03-parent-child-transfer-authority/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the fixture with a second child identity + certification helper; add CONS-01/CONS-02/D-21 tests</name>
  <files>SuperGenius/test/src/account/registration_transaction_test.cpp</files>
  <read_first>
    - SuperGenius/test/src/account/registration_transaction_test.cpp lines 1-54 (top-of-file includes, `kTestTokenId`, `RegistrationTransactionTest` fixture) and lines 190-336 (verified current `RegistrationTransactionE2ETest` fixture — single `account_`/`blockchain_`/`tm_` triple, `TickOnce`-driven READY-state polling pattern in `ChildRegistrationEndToEnd`) and lines 164-179 (verified current `RegistrationE2ETestAccess` friend accessor — already a friend of `TransactionManager` per its `friend class RegistrationE2ETestAccess;` declaration; exposes `GetTransactionByHash`/`FilterRegistration`/`GetRegistrationsForMain` as static wrappers around private `TransactionManager` methods)
    - SuperGenius/test/src/account/transaction_manager_certificate_fallback_test.cpp lines 30-105 (verified current `CertificateFallbackTestAccess` — a SEPARATE file's own friend-accessor pattern; do not import it directly across translation units, but mirror its `BuildCertificate`-style helper shape if a certificate object is needed) — read for pattern only
    - SuperGenius/test/src/blockchain/consensus_certificate_test.cpp lines 495-544 (verified current `SubmitProposalVoteCertificateAndProcess` — the ONLY verified, working single-validator certification recipe in this codebase: `CreateProposal` → `SubmitProposal(proposal, false)` → `CreateVote` (self-signed) → `SubmitVote` → `ConsensusManagerTestAccess::HandleProposal`/`HandleVote` → `IsProposalQuorumReached` → `ProcessCertificates()`. This test operates on a bare `ConsensusManager`, not a `Blockchain`; use it as the reference for WHAT sequence of calls reaches quorum, not as literal code to copy against `blockchain_`)
    - SuperGenius/src/account/TransactionManager.cpp lines 1344-1371 (verified current: how a REAL transaction gets submitted for certification — `blockchain_->CreateConsensusProposal( transaction->GetSrcAddress(), transaction->GetNonce(), transaction->GetHash(), embedded_tx, utxo_commitment, utxo_witness )` then `blockchain_->SubmitProposal( proposal )` — both are PUBLIC `Blockchain` methods, confirmed at Blockchain.hpp lines 220-233, and this is the exact recipe to replicate manually in the test for the child's registration transaction, since there is no second real `TransactionManager` instance driving the child's own submission)
    - SuperGenius/src/blockchain/Blockchain.hpp lines 220-233, 252 (`CreateConsensusProposal`, `SubmitProposal` — both public; `CheckCertificate` — public, to poll for the certification result)
    - SuperGenius/src/account/RegistrationTransaction.hpp / .cpp (full — `New`, `GetMainAddress`, `GetSequence`, `SerializeToEmbeddedTransaction`) and SuperGenius/src/account/GeniusTransaction.hpp (`SerializeToEmbeddedTransaction()` no-arg dispatcher, `MakeSignature`, `GetHash`, `GetNonce`, `GetSrcAddress`)
    - SuperGenius/src/account/TransactionManager.hpp / .cpp — new `RecoverFromChild` (Plan 03), new `CheckParentChildAuthority` (Plan 02), both PUBLIC — callable directly as `tm_->RecoverFromChild(...)` and `tm_->CheckParentChildAuthority(*tx)` with no new friend accessor needed
    - SuperGenius/src/account/GeniusAccount.hpp — `GeniusAccount::New(token_id, path)`, `GetAddress()`, `Sign(...)` — the pattern for constructing a second bare account (no own `TransactionManager`), matching the account construction already used at registration_transaction_test.cpp line 210 and line 343
  </read_first>
  <action>
    Extend the `RegistrationTransactionE2ETest` fixture (currently lines ~200-254) with one new member: `std::shared_ptr&lt;GeniusAccount&gt; child_account_;`, constructed in the fixture constructor via `child_account_ = GeniusAccount::New( kTestTokenId, base_path / "child_account" );` right after the existing `account_` construction, plus its own `LoadUTXOs( db_->GetDataStore() )` call mirroring `account_`'s. In these new tests `account_`/`tm_` play the role of MAIN (the fixture's only real `TransactionManager`); `child_account_` is a bare keypair-only identity with no `TransactionManager` of its own, per the simpler-shape recommendation in 03-RESEARCH.md's Open Question #1 — sufficient because the gates under test are invoked directly on whatever `tx` object reaches `ValidateTransactionForConsensus`, regardless of which node constructed it.

    Add a private helper method on the fixture (or a free function in the anonymous namespace) `CertifyChildRegistration( uint64_t sequence, SGTransaction::RegistrationMetadata metadata ) -> RegistrationTransaction` that: builds a `SGTransaction::DAGStruct` with `source_addr = child_account_->GetAddress()`, `nonce = 0`; constructs `RegistrationTransaction::New( account_->GetAddress(), sequence, metadata, dag )`; signs it with `reg_tx.MakeSignature( *child_account_ )` (child signs its own registration, per D-04/D-05 — matches the existing `ChildRegistrationTamperedSignatureRejected` test's signing pattern); writes it into the CRDT directly at the SAME key format `RegisterChild`/`FilterRegistration` already use (`TransactionManager::GetBlockChainBase() + "reg/" + child_account_->GetAddress()`, callable here since the TEST file itself is compiled as part of `genius_node_test`, which DOES link `TransactionManager.cpp` — this "must not call TransactionManager statics" constraint from Plan 01 applies ONLY to `Blockchain.cpp`/`blockchain_genesis`, not to this test file) via `db_->Put( HierarchicalKey( reg_key ), base::Buffer( reg_tx.SerializeByteVector() ) )` (confirm the exact `Buffer` constructor/`Put` signature against `crdt/globaldb/globaldb.hpp` — mirror however `GlobalDB::Put` is already called elsewhere in this codebase, e.g. `Blockchain.cpp` line ~1013); then certifies it by replicating the exact real-transaction submission recipe verified at TransactionManager.cpp:1363-1371: `auto proposal = blockchain_->CreateConsensusProposal( reg_tx.GetSrcAddress(), reg_tx.GetNonce(), reg_tx.GetHash(), reg_tx.SerializeToEmbeddedTransaction(), std::nullopt, std::nullopt ).value(); blockchain_->SubmitProposal( proposal );` — then polls `blockchain_->CheckCertificate( reg_tx.GetHash() )` in a loop with a short sleep, matching the existing polling style/timeout already used in this file (e.g. the `ChildRegistrationEndToEnd` READY-state poll), until true or a generous timeout (e.g. 10s) elapses; `ASSERT_TRUE` (or equivalent hard assertion in the caller) that certification succeeded before returning. Return the constructed `RegistrationTransaction` (its hash and `GetMainAddress()` are needed by the calling tests).

    Add test case `TEST_F( RegistrationTransactionE2ETest, MainFundsChildApprovedByGate )` (CONS-01): drive `tm_` to READY (reuse the existing polling pattern from `ChildRegistrationEndToEnd`); call `tm_->TransferFunds( amount, child_account_->GetAddress(), kTestTokenId )` — note this is an ordinary transfer FROM main (account_) — assert it succeeds (`result.has_value()`); separately call `tm_->TransferFunds( amount, std::string( 128, 'z' ), kTestTokenId )` (an arbitrary unregistered destination) and assert it also succeeds — both prove the new gate does not interfere with ordinary main-signed transfers regardless of whether the destination is a registered child or not (do NOT call `CertifyChildRegistration` for this test — CONS-01 requires zero registration state at all).

    Add test case `TEST_F( RegistrationTransactionE2ETest, MainRecoversFromChildApproved )` (CONS-02): call `CertifyChildRegistration(...)` to get `child_account_` certified under `account_`'s address; fund `child_account_` first via an ordinary `tm_->TransferFunds(amount, child_account_->GetAddress(), kTestTokenId)` call and poll/wait for that funding tx to reach a state where the child's UTXO is spendable (confirm via `account_->GetUTXOManager().GetBalance(kTestTokenId, child_account_->GetAddress())` — since `tm_`'s own `ParseTransferTransaction`/`PutProducedUTXOs` path runs on ingest of its own submitted transactions, the child's UTXO should appear once the funding tx is processed by the same node's `TickOnce()` loop; poll with a timeout matching the file's existing style); then call `tm_->RecoverFromChild( child_account_->GetAddress(), recover_amount, kTestTokenId )` (recover_amount &lt;= the funded amount) and assert it succeeds; retrieve the resulting transaction via `RegistrationE2ETestAccess::GetTransactionByHash( *tm_, tx_hash )`, and directly assert `tm_->CheckParentChildAuthority( *tx )` returns true and `tm_->CheckTransactionAuthorization( *tx )` returns true (proving both gates independently approve it, not just that no exception was thrown).

    Add test case `TEST_F( RegistrationTransactionE2ETest, MainRecoveryWrongDestinationRejected )` (D-21): after certifying `child_account_` (reuse `CertifyChildRegistration`), manually construct a transfer transaction shaped like a `RecoverFromChild` output (mirroring the manual `dag`+`TransferTransaction::New` construction pattern already used elsewhere in this file) with `source_addr = child_account_->GetAddress()`, a primary output destination that is NOT `account_->GetAddress()` (e.g. an arbitrary 128-hex string), sign the whole tx with `account_->MakeSignature`-equivalent (i.e. `tx.MakeSignature( *account_ )`, main's key — this is what makes it "delegated", not child-self-signed) — assert `tm_->CheckParentChildAuthority( *tx_ptr )` returns false (D-21 destination mismatch correctly rejected), while separately confirming `tm_->CheckTransactionAuthorization( *tx_ptr )` returns true (proving the rejection is specifically the destination-restriction gate, not a signature failure — isolating exactly what D-21 is supposed to catch).
  </action>
  <verify>
    <automated>SuperGenius/build/Windows/Release/test_bin/Release/registration_transaction_test.exe --gtest_filter=RegistrationTransactionE2ETest.MainFundsChildApprovedByGate:RegistrationTransactionE2ETest.MainRecoversFromChildApproved:RegistrationTransactionE2ETest.MainRecoveryWrongDestinationRejected</automated>
  </verify>
  <acceptance_criteria>
    - `RegistrationTransactionE2ETest` fixture has a `child_account_` member constructed alongside `account_`
    - `MainFundsChildApprovedByGate` passes: ordinary main-signed transfers to both a would-be-child address and an arbitrary address succeed
    - `MainRecoversFromChildApproved` passes: a certified child's funds can be recovered by main, and both `CheckParentChildAuthority` and `CheckTransactionAuthorization` independently return true for the resulting transaction
    - `MainRecoveryWrongDestinationRejected` passes: a main-signed, certified-child-sourced transaction with a non-matching destination is rejected by `CheckParentChildAuthority` specifically (not by `CheckTransactionAuthorization`)
  </acceptance_criteria>
  <done>All three new CONS-01/CONS-02/D-21 test cases pass, proving the new gate and RecoverFromChild work correctly end-to-end against the real consensus pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Add REGR-01/REGR-02/REGR-03 regression tests</name>
  <files>SuperGenius/test/src/account/registration_transaction_test.cpp</files>
  <read_first>
    - The `child_account_`/`CertifyChildRegistration` additions from Task 1 (same file, read the version as it exists after Task 1's edits)
    - SuperGenius/src/account/TransactionManager.cpp lines 4066-4076 (extended `CheckTransactionAuthorization` from Plan 02 — the function under direct regression test for REGR-03)
    - SuperGenius/src/account/GeniusNode.cpp line 2331-2334 (verified current `PayDev` — confirmed a thin wrapper: `return TransferFunds( amount, dev_config_.Addr, token_id );`, no separate code path — cited here as the justification for testing REGR-02 via an ordinary transfer to an arbitrary "dev-shaped" address rather than standing up a full `GeniusNode`)
  </read_first>
  <action>
    Add test case `TEST_F( RegistrationTransactionE2ETest, ChildTransferToArbitraryAndMainUnaffected )` (REGR-01): certify `child_account_` via `CertifyChildRegistration(...)`; fund it via `tm_->TransferFunds(...)` as in Task 1's CONS-02 test; then manually construct a transfer transaction with `source_addr = child_account_->GetAddress()`, destination = an arbitrary address, whole-tx signed with `tx.MakeSignature( *child_account_ )` (the CHILD's own key — this is what makes it a genuine self-signed spend, not delegated) and each `InputUTXOInfo.signature_` also signed with `child_account_->Sign(...)` (not `account_->Sign`); assert `tm_->CheckParentChildAuthority( *tx_ptr )` returns true (unconditional approval, no destination restriction, per the gate's `tx.CheckSignature()` branch) and `tm_->CheckTransactionAuthorization( *tx_ptr )` returns true. Repeat the same construction with destination = `account_->GetAddress()` (child transfers back to its registered main via an ordinary self-signed transfer, still REGR-01) and assert the same two gates both approve it.

    Add test case `TEST_F( RegistrationTransactionE2ETest, ChildTransferToDevWalletUnaffected )` (REGR-02): identical construction to the REGR-01 test above, but with an arbitrary destination address representing a "developer wallet" style recipient; assert the same two gates approve it. Document in a comment directly above this test that per `GeniusNode::PayDev`'s verified implementation (a thin wrapper over `TransferFunds` with zero separate code path), this test's coverage — any child-signed transfer to an arbitrary destination — transitively proves REGR-02 without needing to instantiate a full `GeniusNode` in this fixture.

    Add test case `TEST_F( RegistrationTransactionE2ETest, ChildCannotClaimMainAsSourceRejected )` (REGR-03): manually construct a transfer transaction with `source_addr = account_->GetAddress()` (claiming to BE main) but sign the whole tx with `tx.MakeSignature( *child_account_ )` (the child's key, which does not match `account_`'s address) — do NOT certify anything for this test (irrelevant — `account_->GetAddress()` is never registered as anyone's child, so `CheckParentChildAuthority`'s delegated branch never even fires for this src). Assert `tm_->CheckTransactionAuthorization( *tx_ptr )` returns false — this proves the core REGR-03 invariant: even if a child crafts a transaction claiming `src = main_addr`, the whole-tx signature check fails because the child never holds main's private key, and this is unaffected by every change in Plans 01-03 (the new certified-main branch only ever accepts a signature made with the CERTIFIED MAIN's own key, never with a child's key masquerading as main).
  </action>
  <verify>
    <automated>SuperGenius/build/Windows/Release/test_bin/Release/registration_transaction_test.exe --gtest_filter=RegistrationTransactionE2ETest.ChildTransferToArbitraryAndMainUnaffected:RegistrationTransactionE2ETest.ChildTransferToDevWalletUnaffected:RegistrationTransactionE2ETest.ChildCannotClaimMainAsSourceRejected</automated>
  </verify>
  <acceptance_criteria>
    - `ChildTransferToArbitraryAndMainUnaffected` passes: child-self-signed transfers to both an arbitrary address and its registered main are approved by both `CheckParentChildAuthority` and `CheckTransactionAuthorization`
    - `ChildTransferToDevWalletUnaffected` passes, with an explanatory comment citing `PayDev`'s verified thin-wrapper implementation
    - `ChildCannotClaimMainAsSourceRejected` passes: `CheckTransactionAuthorization` rejects a child-signed transaction claiming `src = main_addr`
    - The full `registration_transaction_test` binary (all pre-existing + all new cases from this plan) passes with zero failures
  </acceptance_criteria>
  <done>All three regression tests pass, and the complete extended test file (pre-existing tests + Task 1 + Task 2 additions) runs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-constructed transactions → the real `ValidateTransactionForConsensus`/`ValidateWitness` pipeline | This plan's tests are the only place in the phase where the actual production gate code is exercised end-to-end, not just read/reasoned about |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Elevation of Privilege | Test-only certification helper (`CertifyChildRegistration`) | low | accept | This helper exists only in test code, uses only public `Blockchain`/`TransactionManager` APIs (no test-only backdoor into the certificate store), and exercises the SAME `CreateConsensusProposal`/`SubmitProposal` path real transactions use — it does not weaken or bypass the production certification mechanism |
| T-03-12 | Repudiation | Missing regression coverage silently letting a future refactor reintroduce a REGR-01/02/03 regression | medium | mitigate | This is precisely what Task 2's three tests exist to prevent — each is a direct, automated, CI-visible proof of one specific invariant from the STRIDE register in 03-02-PLAN.md |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new package-manager dependencies in this plan |
</threat_model>

<verification>
## Phase 3 Plan 04 Verification

```
cmake --build SuperGenius/build/Windows/Release --target registration_transaction_test --config Release
SuperGenius/build/Windows/Release/test_bin/Release/registration_transaction_test.exe
```

All pre-existing test cases in this binary (RoundTripSerialization, FactoryFillHash, ChildRegistrationEndToEnd, FilterRegistrationAcceptsValid, etc.) plus all six new cases added by this plan must pass with zero failures. This is the final phase-level proof that CONS-01, CONS-02, CONS-06, REGR-01, REGR-02, and REGR-03 all hold simultaneously.
</verification>

<success_criteria>
- All six new test cases (three per task) pass
- The full `registration_transaction_test` binary passes with zero regressions against its pre-existing test cases
- Every phase requirement (CONS-01, CONS-02, CONS-06, REGR-01, REGR-02, REGR-03) has at least one automated test directly exercising it
</success_criteria>

<output>
Create `.planning/phases/03-parent-child-transfer-authority/03-04-SUMMARY.md` when done
</output>
