---
phase: 01-child-balance-query
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - SuperGenius/test/src/multiaccount/regtest/child_registration.cpp
autonomous: true
requirements: [INTG-01]
tags: [integration-test, balance, child-wallet, crdt-sync]

must_haves:
  truths:
    - "TEST_F(ChildRegistrationIntegrationTest, MainQueriesChildBalance) mints child tokens on child_node_ using its own DevConfig token (child_node_->GetTokenID()), per D-64/D-57 — no cross-node TransferFunds dependency"
    - "The test polls child_node_'s own balance until non-zero (mint landed), then polls main_node_'s GetChildBalance until non-zero (CRDT sync converged), before the final equality assertion — eliminating flakiness from async CRDT propagation (D-65)"
    - "The final assertion confirms main_node_->GetChildBalance(child_address, child_token) equals the exact minted amount"
    - "All existing TEST_F cases in child_registration.cpp (ChildRegistersWithMain, MainDiscoversChild, InvalidRegistrationRejected) continue to pass unchanged — no regressions"
    - "child_registration_test target builds and all 4 TEST_F cases pass, proving INTG-01 end-to-end and ROADMAP Phase 1 Success Criteria 2 and 3"
  artifacts:
    - path: "SuperGenius/test/src/multiaccount/regtest/child_registration.cpp"
      provides: "New TEST_F(ChildRegistrationIntegrationTest, MainQueriesChildBalance) appended after InvalidRegistrationRejected, before the closing namespace brace"
      contains: "TEST_F( ChildRegistrationIntegrationTest, MainQueriesChildBalance )"
      min_lines: 424
  key_links:
    - from: "TEST_F MainQueriesChildBalance"
      to: "main_node_->GetChildBalance( child_address, child_token )"
      via: "CRDT sync convergence (D-49 RegElementCallback pubsub follow), after child_node_->MintTokens(...) lands"
      pattern: "main_node_->GetChildBalance\\("
    - from: "TEST_F MainQueriesChildBalance"
      to: "child_node_->MintTokens( kMintAmount, ... )"
      via: "child_node_->GetTokenID() supplies the child's own token, per D-57"
      pattern: "child_node_->MintTokens\\("
---

<objective>
Add a new multi-node integration test case, `MainQueriesChildBalance`, to the existing shared 3-node `ChildRegistrationIntegrationTest` fixture in `child_registration.cpp`: the child node mints its own token, CRDT sync propagates the UTXO to the main node, and the main node reads the child's balance via `GetChildBalance` — proving BALT-01's production code (Plan 01-01) works end-to-end across independent processes.

Purpose: This is the sole test artifact for INTG-01 and the final proof required by ROADMAP Phase 1 Success Criteria 2 ("child registers with main, receives child token funds, main calls GetChildBalance, returned balance matches the funded amount") and 3 (no regressions to existing registration tests).
Output: A new `TEST_F(ChildRegistrationIntegrationTest, MainQueriesChildBalance)` case appended to `child_registration.cpp`, with the full `child_registration_test` suite (4 cases total) building and passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-child-balance-query/01-CONTEXT.md
@.planning/phases/01-child-balance-query/01-PATTERNS.md
@.planning/phases/01-child-balance-query/01-01-SUMMARY.md

# Test file being modified — read these first
@SuperGenius/test/src/multiaccount/regtest/child_registration.cpp (lines 1-54 — includes; lines 102-214 — fixture + CreateNode; lines 237-270 — MainDiscoversChild poll pattern; lines 395-423 — end of InvalidRegistrationRejected + closing namespace)
@SuperGenius/test/src/multiaccount/multi_account_sync.cpp (lines 197-221 — MintTokens call pattern)

<interfaces>
<!-- Key existing signatures/fixture members the executor needs; extracted from codebase reads during planning. -->

From SuperGenius/test/src/multiaccount/regtest/child_registration.cpp (lines 107-109, fixture statics — already booted once in SetUpTestSuite, shared across all TEST_F cases):
`static std::shared_ptr<sgns::GeniusNode> genesis_node_;`
`static std::shared_ptr<sgns::GeniusNode> main_node_;`
`static std::shared_ptr<sgns::GeniusNode> child_node_;`

From SuperGenius/src/account/GeniusNode.hpp (public methods used by the new test, all already callable in this file today):
`std::string GetAddress() const;`
`[[nodiscard]] TokenID GetTokenID() const { return dev_config_.TokenID; }`
`uint64_t GetBalance( TokenID token_id );` (existing overload, used to poll child_node_'s own view)
`uint64_t GetChildBalance( const std::string &child_address, TokenID token_id );` (new in Plan 01-01 — the method under test)
`outcome::result<std::string> MintTokens( uint64_t amount, ... );` (signature per multi_account_sync.cpp:199-205 call pattern)
`static constexpr std::chrono::milliseconds TIMEOUT_MINT{ ... };`

From SuperGenius/test/testutil/wait_condition.hpp (already included, line 54):
`sgns::test::assertWaitForCondition( lambda_predicate, std::chrono::milliseconds timeout, const std::string &failure_message );`

From SuperGenius/test/testutil/mint_source_hash.hpp (already included, line 51):
`sgns::test::NextMintSourceHash();` — generates a unique mint source hash per call, used by multi_account_sync.cpp's MintTokens calls.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TEST_F(ChildRegistrationIntegrationTest, MainQueriesChildBalance) to child_registration.cpp</name>
  <files>SuperGenius/test/src/multiaccount/regtest/child_registration.cpp</files>
  <read_first>
    - SuperGenius/test/src/multiaccount/regtest/child_registration.cpp — lines 237-270 (`MainDiscoversChild`, the poll-until-convergence pattern to copy), lines 395-423 (end of `InvalidRegistrationRejected` test body, closing brace at line 421, closing `} // namespace sgns` at line 423 — the exact insertion point)
    - SuperGenius/test/src/multiaccount/multi_account_sync.cpp — lines 197-221 (`MintTokens` call pattern: amount, `NextMintSourceHash()`, description string, `TokenID`, empty string, `TIMEOUT_MINT`)
    - .planning/phases/01-child-balance-query/01-CONTEXT.md — D-63 (extend this file, distinct child address for isolation), D-64 (child mints, main queries, no TransferFunds dependency), D-65 (poll-until-sync assertion pattern)
    - .planning/phases/01-child-balance-query/01-PATTERNS.md — "child_registration.cpp (near line 421)" section for the exact test skeleton, mint pattern, and poll pattern to mirror
    - .planning/phases/01-child-balance-query/01-01-SUMMARY.md — confirms `GeniusNode::GetChildBalance` exists and its exact signature before this test calls it
  </read_first>
  <action>
    Insert a new `TEST_F` case into `child_registration.cpp` after the closing brace of `InvalidRegistrationRejected` (line 421) and before `} // namespace sgns` (line 423). Add a banner comment above the test (matching the existing `// ---...--- // TEST-0N: ... // ---...---` style used before `MainDiscoversChild` at lines 233-236) labeling it `TEST-05: MainQueriesChildBalance`.

    Test body, in order:

    1. Read `child_address` from `child_node_->GetAddress()` and `child_token` from `child_node_->GetTokenID()` (per D-57/D-64 — the child's own configured DevConfig token, not the main's; per 01-PATTERNS.md this test uses `child_node_`'s own address as the distinct address for isolation since it is the only test case in this file that mints tokens — no UTXO-namespace collision with the register-only TEST-02/03/04 cases).

    2. Declare `constexpr uint64_t kMintAmount = 500;`.

    3. Call `child_node_->MintTokens(...)` with: `kMintAmount`, `sgns::test::NextMintSourceHash()`, a description string `"test_05_balance"`, `child_token`, an empty string `""`, and `std::chrono::milliseconds( GeniusNode::TIMEOUT_MINT )` — matching the exact argument order and types used by `multi_account_sync.cpp:199-205`. Assert the result with `ASSERT_TRUE( mint_result.has_value() )` and a failure message identifying the mint call ("Mint transaction failed or timed out on child_node_").

    4. First poll (confirms the mint landed on the minting node's own view before checking cross-node sync): `sgns::test::assertWaitForCondition` with a lambda `[&]() { return child_node_->GetBalance( child_token ) > 0; }`, timeout `std::chrono::milliseconds( 60000 )`, failure message "child_node_ balance did not become non-zero after mint".

    5. Second poll (confirms CRDT sync convergence to the main node — the core of what INTG-01 proves): `sgns::test::assertWaitForCondition` with a lambda `[&]() { return main_node_->GetChildBalance( child_address, child_token ) > 0; }`, timeout `std::chrono::milliseconds( 60000 )`, failure message "main_node_ did not observe child balance after CRDT sync".

    6. Final assertion: `EXPECT_EQ( main_node_->GetChildBalance( child_address, child_token ), kMintAmount );` — proves the exact funded amount round-trips through CRDT sync and `GetChildBalance`, satisfying ROADMAP Success Criterion 2.

    Do not modify `SetUpTestSuite`, `TearDownTestSuite`, `CreateNode`, or any existing `TEST_F` case. Do not add any CMakeLists.txt changes — `child_registration.cpp` is already registered as the sole source of the `child_registration_test` target.
  </action>
  <verify>
    <automated>grep -c "TEST_F( ChildRegistrationIntegrationTest, MainQueriesChildBalance )" SuperGenius/test/src/multiaccount/regtest/child_registration.cpp</automated>
    <automated>grep -c "child_node_->MintTokens(" SuperGenius/test/src/multiaccount/regtest/child_registration.cpp</automated>
    <automated>grep -c "main_node_->GetChildBalance(" SuperGenius/test/src/multiaccount/regtest/child_registration.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `child_registration.cpp` contains exactly one `TEST_F( ChildRegistrationIntegrationTest, MainQueriesChildBalance )` definition, inserted between line 421 (`InvalidRegistrationRejected`'s closing brace) and the `} // namespace sgns` line
    - The test calls `child_node_->MintTokens(...)` with `child_node_->GetTokenID()` as the token argument — not a hardcoded `TokenID::FromBytes({0x00})` and not `main_node_`'s token
    - The test contains two distinct `assertWaitForCondition` calls: one polling `child_node_->GetBalance(...)` and one polling `main_node_->GetChildBalance(...)`, each with an explicit timeout and failure message
    - The final assertion is `EXPECT_EQ` (not `ASSERT_EQ`) comparing `main_node_->GetChildBalance( child_address, child_token )` against `kMintAmount`
    - No line within `SetUpTestSuite`, `TearDownTestSuite`, `CreateNode`, `ChildRegistersWithMain`, `MainDiscoversChild`, or `InvalidRegistrationRejected` is modified
  </acceptance_criteria>
  <done>
    The new TEST_F case is present in child_registration.cpp, syntactically complete, and ready to build in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build and run child_registration_test — confirm the new case passes with zero regressions</name>
  <files>SuperGenius/test/src/multiaccount/regtest/child_registration.cpp</files>
  <read_first>
    - SuperGenius/test/src/multiaccount/regtest/child_registration.cpp (post-Task-1 state — confirm the new TEST_F case compiles cleanly alongside the existing 3 cases)
  </read_first>
  <action>
    Build the `child_registration_test` CMake target from the configured Windows Release build directory, then run the full suite via `ctest`. This proves both requirements simultaneously: INTG-01 (the new `MainQueriesChildBalance` case passes) and ROADMAP Success Criterion 3 (the existing 3 cases — `ChildRegistersWithMain`, `MainDiscoversChild`, `InvalidRegistrationRejected` — continue to pass unchanged).

    Note per 01-CONTEXT.md D-51/D-52: `SetUpTestSuite` boots a 3-node network once (~121s GossipPubSub boot), shared across all 4 `TEST_F` cases in this run — expect the full suite invocation to take several minutes, dominated by fixture boot plus the two 60s poll timeouts (which resolve well before their ceiling once CRDT sync converges).
  </action>
  <verify>
    <automated>cd SuperGenius/build/Windows/Release && cmake --build . --target child_registration_test --config Release 2>&1 | tail -30</automated>
    <automated>cd SuperGenius/build/Windows/Release && ctest -R child_registration_test --output-on-failure -C Release</automated>
  </verify>
  <acceptance_criteria>
    - Build output contains zero compiler errors for `child_registration.cpp`
    - `ctest -R child_registration_test` reports all 4 test cases passed: `ChildRegistersWithMain`, `MainDiscoversChild`, `InvalidRegistrationRejected`, `MainQueriesChildBalance`
    - `MainQueriesChildBalance` specifically passes the final `EXPECT_EQ` — `main_node_->GetChildBalance(...)` equals `500` (kMintAmount)
    - No pre-existing test case in this target regresses (0 failed tests reported by ctest, excluding any pre-existing known infra flakiness already documented in prior phase SUMMARYs)
  </acceptance_criteria>
  <done>
    child_registration_test builds and all 4 TEST_F cases pass. INTG-01 is proven end-to-end: child registers/mints, CRDT sync propagates, main queries via GetChildBalance and observes the correct funded amount. ROADMAP Phase 1 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `child_node_` process → `main_node_` process, via CRDT/pubsub sync | Pre-existing boundary (established in Phase 05 of v2.0), unchanged by this plan — this test exercises it with a new UTXO-sync scenario rather than a registration-sync scenario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Test-only `MintTokens` call on `child_node_` | low | accept | `MintTokens` invoked from a GTest fixture exercises an already-existing public production API; no new production code path is introduced. Identical trust model to the pre-existing `multi_account_sync.cpp` usage of the same method. |
| T-02-02 | Denial of Service | Poll-until-condition with 60s timeout (D-65) | low | accept | `assertWaitForCondition` has an explicit timeout and fails the test deterministically (rather than hanging indefinitely) if CRDT sync does not converge — identical pattern to the existing `MainDiscoversChild` poll. |
| T-02-03 | Repudiation | N/A — test-only code, not shipped in production binaries | low | accept | `child_registration.cpp` is a GTest translation unit under `SuperGenius/test/`, never linked into production binaries. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs — pure test-only C++ addition to an already-registered CMake target (`child_registration_test`). |

## Security Summary

This plan adds no new production trust boundary. It exercises the existing child→main CRDT/pubsub sync path (already hardened in v2.0 Phase 5) with a UTXO-balance scenario instead of a registration-discovery scenario, and calls only already-public, already-tested APIs (`MintTokens`, `GetBalance`, and the new `GetChildBalance` from Plan 01-01).
</threat_model>

<verification>
```bash
cd SuperGenius/build/Windows/Release
cmake --build . --target child_registration_test --config Release 2>&1 | tail -30
ctest -R child_registration_test --output-on-failure -C Release
```
Expected: zero compiler errors; ctest reports 4/4 tests passed, including `MainQueriesChildBalance`.
</verification>

<success_criteria>
1. `TEST_F(ChildRegistrationIntegrationTest, MainQueriesChildBalance)` exists in `child_registration.cpp`, mints on `child_node_` using `child_node_->GetTokenID()`, polls until CRDT sync converges, then asserts `main_node_->GetChildBalance(child_address, child_token) == kMintAmount`
2. `child_registration_test` builds and all 4 `TEST_F` cases pass (`ChildRegistersWithMain`, `MainDiscoversChild`, `InvalidRegistrationRejected`, `MainQueriesChildBalance`)
3. ROADMAP Phase 1 Success Criteria 1-3 are all satisfied: GetChildBalance returns correct balance (Criterion 1, proven by Plan 01-01 + this test), end-to-end multi-node test passes (Criterion 2), zero regressions to existing registration tests (Criterion 3)
</success_criteria>

## Artifacts this phase produces

New symbols created across the phase (Plan 01-01 + Plan 01-02). Listed here so downstream drift-verification excludes them as newly-created rather than flagging them as unexplained additions.

- `uint64_t GeniusNode::GetChildBalance( const std::string &child_address, TokenID token_id )` — new method overload (Plan 01-01, Task 1)
- `uint64_t GeniusNode::GetChildBalance( const std::string &child_address )` — new method overload (Plan 01-01, Task 1)
- `TEST_F( ChildRegistrationIntegrationTest, MainQueriesChildBalance )` — new GTest case (this plan, Task 1)

No new fields, no new proto messages, no new files — both new symbols above are added to existing files (`GeniusNode.hpp`, `GeniusNode.cpp`, `child_registration.cpp`).

<output>
Create `.planning/phases/01-child-balance-query/01-02-SUMMARY.md` when done
</output>
