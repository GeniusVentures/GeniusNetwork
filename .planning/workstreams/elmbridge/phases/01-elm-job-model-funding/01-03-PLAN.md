---
phase: 01-elm-job-model-funding
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - SuperGenius/src/processing/processing_node.hpp
  - SuperGenius/src/processing/processing_node.cpp
  - SuperGenius/src/processing/processing_service.cpp
  - SuperGenius/src/processing/processing_subtask_queue_accessor_impl.cpp
  - SuperGenius/test/src/processing/elm_lock_timeout_test.cpp (new)
  - SuperGenius/test/src/processing/CMakeLists.txt
  - SuperGenius/test/src/processing/elm_validation_mode_test.cpp (new)
  - .planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SETTLEMENT.md (new)
  - .planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SUBTASK-MAPPING.md (new)
autonomous: true
requirements: [FUND-02, FUND-03, JOB-03]
must_haves:
  truths:
    - "A queue created for an ELM job carries processing_timeout_length == DeriveElmClocks(hours).lockTimeout (deadline + 60s grace), set via SetProcessingTimeout BEFORE CreateQueue runs — the queue proto (not just the local member) holds the derived value (FUND-02, D-08/D-09)"
    - "An ELM subtask whose lock was taken with the derived timeout is still locked at grab+60s: UnlockExpiredItems(grab+60s) returns false / leaves lock_node_id set (SC-3, D-10)"
    - "FinalizeQueueProcessing reads the ELM validation mode from the re-parsed Task.json_data (D-12) and the defensive assertion accepts mode none and rejects exact|redundant (D-11 corrected to assertion per research F2)"
    - "The over-time terminal-state semantics (BUDGET_EXCEEDED rides the success-shaped result path as a published envelope — never a silent re-grab loop) and the settlement refund design (declared max escrowed, measured grab→publication windows, sum-of-windows billing, milli-hour truncation, refund of remainder to escrow source) are pinned as design artifacts Phase 3/4 implement (FUND-03, D-01/D-02/D-03, OD-3)"
    - "The work_item_id ↔ subtaskid mapping design (one work item → one subtask, map embedded in task JSON, subtask-unique notional chunk id) is pinned for Phase 4's splitter"
    - "A non-ELM job through the same entry points keeps the 15s default timeout, chunk validation, and byte-based cost — regression-locked by tests (SC-5)"
  artifacts:
    - path: "SuperGenius/src/processing/processing_node.cpp"
      provides: "SetProcessingTimeout(derived) called before CreateSubTaskQueue in the ELM path"
      contains: "SetProcessingTimeout"
    - path: "SuperGenius/src/processing/processing_subtask_queue_accessor_impl.cpp"
      provides: "ElmValidationModeOk read from parsed Task.json_data at FinalizeQueueProcessing"
      contains: "ElmValidationModeOk"
    - path: "SuperGenius/test/src/processing/elm_lock_timeout_test.cpp"
      provides: "derived-timeout queue proto assertion + t>60s no-re-grab assertion + 15s default parity"
      min_lines: 100
    - path: "SuperGenius/test/src/processing/elm_validation_mode_test.cpp"
      provides: "defensive assertion matrix"
      min_lines: 60
    - path: ".planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SETTLEMENT.md"
      provides: "settlement/refund/terminal-state design Phase 4 implements"
      contains: "BUDGET_EXCEEDED"
    - path: ".planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SUBTASK-MAPPING.md"
      provides: "splitter mapping design Phase 4 implements"
      contains: "work_item_id"
  key_links:
    - from: "ProcessingNode::New"
      to: "ProcessingSubTaskQueueManager::SetProcessingTimeout"
      via: "derived timeout threaded before CreateSubTaskQueue"
      pattern: "SetProcessingTimeout"
    - from: "FinalizeQueueProcessing"
      to: "parsed Task.json_data validation field"
      via: "sgns::from_json into SgnsProcessing (existing parse block)"
      pattern: "get_validation"
---

<objective>
Wire the derived lock timeout into the queue-creation path (FUND-02), land the D-12 validation-mode defensive assertion (JOB-03), and pin the two design deliverables this phase owes later phases — settlement/refund/terminal-state semantics (FUND-03, D-01/D-02/D-03, OD-3) and the work_item_id↔subtaskid mapping.

Purpose: SC-3 (no re-grab under a valid long lock), SC-4 (terminal published state, refund semantics fixed by design), and the SC-5 regression leg for the queue path.
Output: timeout wiring + assertion + tests in SuperGenius, plus two committed design artifacts in the phase directory.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-CONTEXT.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-RESEARCH.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-PATTERNS.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-02-PLAN.md
@SuperGenius/src/processing/processing_node.cpp
@SuperGenius/src/processing/processing_subtask_queue_manager.cpp
@SuperGenius/src/processing/processing_subtask_queue.cpp
@SuperGenius/src/processing/processing_subtask_queue_accessor_impl.cpp
@SuperGenius/test/src/processing/processing_subtask_queue_manager_test.cpp
</context>

## Artifacts this phase produces

**This plan (01-03) creates:**
- `ProcessingNode::New` overload/parameter accepting an optional derived `processingTimeout` (default: current behavior — no timeout call, 15s constructor default stands) threaded to `m_subtaskQueueManager->SetProcessingTimeout(...)` before `CreateSubTaskQueue` runs
- `ProcessingServiceImpl` call-site feed: parse of `m_pendingTask` json_data → `get_job_type()`/funding → `sgns::processing::DeriveElmClocks` (from plan 01-02) → passed into `ProcessingNode::New` (ELM only; non-ELM passes nothing)
- `SubTaskQueueAccessorImpl::ElmValidationModeOk( const sgns::SgnsProcessing &parsed, std::string &reason )` — static/private helper: job is ELM ⇒ validation must be none-or-absent (D-13 assertion), called from `FinalizeQueueProcessing` inside the existing try/catch fail-safe (adversarial json never crashes — T-15-08 discipline)
- Test targets `elm_lock_timeout_test`, `elm_validation_mode_test`
- `01-DESIGN-SETTLEMENT.md`, `01-DESIGN-SUBTASK-MAPPING.md` (phase-directory design artifacts)

**Plans 01-01/01-02 created (consumed):** schema `get_validation()`, `DeriveElmClocks`, `ElmEscrowMinions`, `kLockGraceSeconds`, `GetElmMaximumProcessingHours`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derived lock-timeout wiring</name>
  <files>SuperGenius/src/processing/processing_node.hpp, SuperGenius/src/processing/processing_node.cpp, SuperGenius/src/processing/processing_service.cpp, SuperGenius/test/src/processing/elm_lock_timeout_test.cpp (new), SuperGenius/test/src/processing/CMakeLists.txt</files>
  <read_first>
  - SuperGenius/src/processing/processing_node.cpp (New :11-46 — wiring site; CreateSubTaskQueue :207-212)
  - SuperGenius/src/processing/processing_subtask_queue_manager.cpp (constructor 15s default :21; SetProcessingTimeout :27-31; CreateQueue proto write :76)
  - SuperGenius/src/processing/processing_subtask_queue.cpp (LockItem :38-56; UnlockExpiredItems :166-196 — strict `now > expiration` comparison)
  - SuperGenius/src/processing/processing_service.cpp (HandleNodeCreationTimeout :661-775; EnqueueSubTasks :526-535)
  - SuperGenius/test/src/processing/processing_subtask_queue_manager_test.cpp (QueueCreating mock-channel pattern :44-89)
  - 01-RESEARCH.md F8 (wiring path verified end-to-end), Pitfall 4 (SetProcessingTimeout after CreateQueue is a silent no-op for peers)
  </read_first>
  <behavior>
  - QueueManager-level: SetProcessingTimeout(3660s) called before CreateQueue(subTasks-with-≥1-chunk) → published queue snapshot's processing_queue().processing_timeout_length() == 3660000 (the :76 proto write)
  - Queue-level SC-3: LockItem(idx, t0) with queue timeout 3660000 → UnlockExpiredItems(t0 + 60000) returns false and lock_node_id stays set (t>60s NOT re-grabbed)
  - Queue-level expiry still works: UnlockExpiredItems(t0 + 3660001) returns true and clears the lock (deadline+grace boundary; note `now > expiration` is strict)
  - Default parity: a queue created with NO SetProcessingTimeout call still carries 15000 (15s default untouched — SC-5)
  - DeriveElmClocks integration: DeriveElmClocks(1.0).lockTimeout == 3660000ms feeds the wiring (ties to plan 01-02)
  </behavior>
  <action>
  1. `processing_node.hpp/.cpp`: extend `ProcessingNode::New` with a trailing parameter `std::chrono::system_clock::duration processingTimeout = std::chrono::seconds( 0 )` (zero = "not derived"). In New, after `node->Initialize(...)` and BEFORE `CreateSubTaskQueue` (Pitfall 4 — later calls never reach the published proto): `if ( processingTimeout.count() > 0 ) { node->m_subtaskQueueManager->SetProcessingTimeout( processingTimeout ); }`. Add the parameter to the protected constructor chain or set it via an initializer step — whichever matches the existing member-init style; Doxygen-update the New block.
  2. `processing_service.cpp` `HandleNodeCreationTimeout`: where the `ProcessingNode::New` call is assembled (the m_pendingTask path), parse the pending task's json_data (nlohmann parse + `sgns::from_json`, wrapped in try/catch — non-ELM/malformed ⇒ pass nothing); if `get_job_type()` is ELM: hours = funding.maximum_processing_hours.value_or(1.0) (D-04 default; belt-and-braces — schema gates guarantee range), derived = DeriveElmClocks(hours).lockTimeout, pass into New. Non-ELM: pass nothing (default 0 → no SetProcessingTimeout call → 15s default — SC-5).
  3. `elm_lock_timeout_test.cpp`: implement the behavior block. Queue-level cases construct `ProcessingSubTaskQueue` directly (localNodeId + now-lambda) following PATTERNS §14; QueueManager-level case uses the mock `ProcessingSubTaskQueueChannelImpl` snapshot pattern (subTasks each with ≥1 chunk in chunkstoprocess — CreateQueue returns false otherwise). Assert proto field equality on the captured snapshot.
  4. Register `addtest(elm_lock_timeout_test elm_lock_timeout_test.cpp)` in test/src/processing/CMakeLists.txt with `target_link_libraries(elm_lock_timeout_test processing_service SGProcessingProto)` (mirror processing_validation_core_test linkage).
  </action>
  <verify>
    <automated>cd SuperGenius; cmake --build build/Windows/Release --target elm_lock_timeout_test --config Release; ctest --test-dir build/Windows/Release -C Release -R elm_lock_timeout_test --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "SetProcessingTimeout" src/processing/processing_node.cpp` matches a call positioned before the CreateSubTaskQueue call in New
  - `grep -n "m_processingTimeout( std::chrono::seconds( 15 ) )" src/processing/processing_subtask_queue_manager.cpp` — constructor default UNCHANGED (SC-5)
  - Published-queue snapshot assertion (3660000) and t+60s still-locked assertion both present and green; t+3660001ms unlock case green
  - Default-parity case (no SetProcessingTimeout → 15000) green
  - ctest -R elm_lock_timeout_test passes
  </acceptance_criteria>
  <done>FUND-02 wired end-to-end at the queue-creation path; SC-3 property test-pinned at queue level and queue-manager level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validation-mode defensive assertion at FinalizeQueueProcessing</name>
  <files>SuperGenius/src/processing/processing_subtask_queue_accessor_impl.cpp, SuperGenius/src/processing/processing_subtask_queue_accessor_impl.hpp, SuperGenius/test/src/processing/elm_validation_mode_test.cpp (new), SuperGenius/test/src/processing/CMakeLists.txt</files>
  <read_first>
  - SuperGenius/src/processing/processing_subtask_queue_accessor_impl.cpp (FinalizeQueueProcessing task-JSON parse block :337-375 including the T-15-08 fail-safe catch; ValidateResults call site)
  - SuperGenius/src/processing/processing_validation_core.cpp (ValidateResults :47-186 — why it is structurally inert for ELM, research F2)
  - 01-PATTERNS.md §11 (exact analog: the existing parse + fail-safe catch)
  - 01-RESEARCH.md F2 (D-11 correction — assertion, not a behavioral gate)
  </read_first>
  <behavior>
  - ElmValidationModeOk(parsed ELM job with validation absent) == true (default none, D-04)
  - ElmValidationModeOk(parsed ELM job with validation "none") == true
  - ElmValidationModeOk(parsed ELM job with validation "exact") == false with reason naming the mode (D-13 — parse-level gate already rejects upstream; this is the validating node's defense-in-depth)
  - ElmValidationModeOk(non-ELM parsed job) == true (assertion scoped to ELM jobs only — non-ELM validation semantics untouched, SC-5)
  - Adversarial json_data (truncated/garbage) still never throws out of FinalizeQueueProcessing (existing catch discipline preserved)
  </behavior>
  <action>
  1. Add `static bool ElmValidationModeOk( const sgns::SgnsProcessing &parsedProcessing, std::string &rejectionReason );` to SubTaskQueueAccessorImpl (hpp) — returns true for non-ELM jobs; for ELM jobs requires get_validation() absent or NONE.
  2. In FinalizeQueueProcessing, after `sgns::from_json( json, parsedProcessing )` succeeds inside the existing try block: call ElmValidationModeOk; on false, `m_logger->error( "FinalizeQueueProcessing: ELM job arrived with unimplemented validation mode: {}", rejectionReason )` and treat as validation-invalid (follow the existing invalid path — do NOT throw; keep the T-15-08 catch untouched around the whole parse). Zero changes to ValidateResults itself (research F2: the cross-subtask comparison is inert for subtask-unique chunk ids; D-11 corrected to this assertion).
  3. `elm_validation_mode_test.cpp`: unit-test ElmValidationModeOk over the four parsed-job cases (build SgnsProcessing via from_json from JSON literals — no queue needed); register with addtest + processing_service linkage as in Task 1.
  </action>
  <verify>
    <automated>cd SuperGenius; cmake --build build/Windows/Release --target elm_validation_mode_test --config Release; ctest --test-dir build/Windows/Release -C Release -R elm_validation_mode_test --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "ElmValidationModeOk" src/processing/processing_subtask_queue_accessor_impl.cpp` >= 2 (definition + call inside the parse try-block)
  - `git diff src/processing/processing_validation_core.cpp` is EMPTY (F2: no ValidateResults change)
  - Four behavior cases green; existing processing_validation_core_test and processing_validate_result_data_test still pass (SC-5)
  </acceptance_criteria>
  <done>D-12 read-from-schema + D-11-as-assertion landed with defense-in-depth that never trusts adversarial json.</done>
</task>

<task type="auto">
  <name>Task 3: Settlement/terminal-state + subtask-mapping design artifacts</name>
  <files>.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SETTLEMENT.md (new), .planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SUBTASK-MAPPING.md (new)</files>
  <read_first>
  - 01-RESEARCH.md F1 (settlement measures nothing today — the full trace), F9 (BUDGET_EXCEEDED gap), Pitfall 5 (result-path shape for terminal states)
  - 01-PATTERNS.md §12 (BuildPayoutOutputs analog — uint128 conservation, malformed-filter, refund source at PayEscrow:1240)
  - 01-CONTEXT.md D-01/D-02/D-03 (billing windows, milli-hour truncation, cancel-at-deadline), OD-3 (proportional-by-measured-window refund split)
  - SuperGenius/src/account/TransactionManager.cpp (BuildPayoutOutputs :1097-1197, PayEscrow :1205-1277)
  - SuperGenius/src/processing/processing_tasksplit.cpp (SplitTask :44-96 — chunk-id minting, the map Phase 4 embeds beside)
  </read_first>
  <action>
  1. Write `01-DESIGN-SETTLEMENT.md` pinning, with file:line anchors, the Phase-4 settlement contract:
     - Measurement: per-subtask wall-clock stamps grab_time_usec/finish_time_usec written by the worker into the result envelope artifact at ProcessingEngine::ProcessSubTask entry and CompleteSubTask publication (schema fields, no proto change); finalizing node reads them via the existing fetchOutputData lambda (:379-411). Model download included by construction (stamp at grab precedes fetch).
     - Billing: billable = Σ per-subtask windows (D-01); each window truncated to milli-hours; minions = Σ milli_hours × 3 / 10 via ElmEscrowMinions-style integer path (D-02); capped at escrow max (D-03).
     - Payout rework in BuildPayoutOutputs (ELM branch only): peers+developer paid from billable, split PROPORTIONAL BY MEASURED WINDOW across work items (OD-3 — supersedes even-split for ELM); refund output of (escrow − billable) to escrow_tx->GetSrcAddress(); total-conservation check preserved (uint128).
     - Rate at settlement: read the elm_rate sibling CRDT key (plan 01-02's record) and/or task JSON funding block.
     - Terminal states: over-time/cancelled jobs publish a success-shaped envelope (artifact URI + one notional chunk hash + payout metadata + finish_reason cancelled|error) so ValidateIndividualResult accepts and the subtask marks processed — closing the F9 re-grab loop (SC-4). Cancellation fires at the per-subtask deadline (D-03/D-10) via the Phase 3 cancel mechanism.
     - Explicit Phase boundaries: measurement stamps (envelope schema) Phase 3; payout rework + wiring Phase 4; Phase 1 ships this design + rate-record + ElmClocks only.
  2. Write `01-DESIGN-SUBTASK-MAPPING.md`: one work item → one subtask via the Phase-4 ELM splitter (one notional chunk with a subtask-unique chunk id — keeps ValidateResults inert per F2); subtaskid↔work_item_id map embedded in Task.json_data (rides beside elms[]); result envelopes echo work_item_id through the existing SubTask.subtaskid keying (validation core and UpdateResultsFromStorage key on subtaskid — mapping resolves at the GCS/requestor side per RES-02 anti-aggregation). Note the interim Phase-1 reality: ProcessImage rejects ELM before splitting (OD-1), so this mapping is consumed first by Phase 4.
  </action>
  <verify>
    <automated>grep -c "BUDGET_EXCEEDED\|milli" .planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SETTLEMENT.md; grep -c "work_item_id" .planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-DESIGN-SUBTASK-MAPPING.md</automated>
  </verify>
  <acceptance_criteria>
  - 01-DESIGN-SETTLEMENT.md contains: milli-hour truncation formula, proportional-by-window split rule (OD-3), refund destination (GetSrcAddress), terminal-envelope shape satisfying ValidateIndividualResult's six structural checks, and at least 6 file:line anchors into TransactionManager/processing code
  - 01-DESIGN-SUBTASK-MAPPING.md contains: 1:1 work-item→subtask rule, task-JSON map placement, subtask-unique chunk-id rationale (F2), Phase-4 consumption note
  - Both committed to the phase directory (root repo, --ws elmbridge)
  </acceptance_criteria>
  <done>The two design deliverables this phase owes are pinned where Phase 3/4 planning will read them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker-published envelope → finalizing node | untrusted result artifacts cross into FinalizeQueueProcessing/ValidateIndividualResult |
| adversarial Task.json_data → validating node | already-guarded parse path extended with one more read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | DoS | adversarial json_data crashes FinalizeQueueProcessing | mitigate | ElmValidationModeOk called inside the existing T-15-08 try/catch; never throws (behavior case pinned) |
| T-01-09 | DoS/Economic | over-time job re-grab loop burns node compute (F9) | mitigate | derived lock (Task 1) bounds re-grab window; terminal-envelope design (Task 3) closes the loop at Phase 4; design pinned now |
| T-01-10 | Tampering | worker self-reports shorter windows to... (n/a — longer windows cost requestor more, capped at escrow max) | accept | billing capped at declared escrow max (D-03); over-max windows refund nothing extra; envelope stamps are worker-attested — noted as accepted residual for v1.0 single-node |
| T-01-11 | Elevation | derived-timeout wiring changes non-ELM lock behavior | mitigate | timeout parameter defaults to "not derived" (no SetProcessingTimeout call); 15s default-parity test pinned (SC-5) |
</threat_model>

<verification>
- Task 1/2 automated commands green from `SuperGenius/`
- Existing processing suites (processing_validation_core_test, processing_validate_result_data_test, task_queue_test) still green — SC-5
- Design artifacts committed; roadmap Phase-1 checkboxes unchanged (execution updates them)
</verification>

<success_criteria>
- SC-3 fully test-pinned (derived timeout in queue proto; t>60s no re-grab)
- SC-4 semantics fixed by design artifact (terminal published state + measured-window refund, OD-3 proportional split)
- JOB-03 call-site leg + FUND-03 design leg complete; SC-5 regression coverage explicit (15s default parity, byte-identical non-ELM assertions across all three plans)
</success_criteria>

<output>
Create `.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-03-SUMMARY.md` when done.
Commit discipline: source commits inside `SuperGenius` on `dev_elmruntime`; design artifacts + summary committed at root with `--ws elmbridge`; root-repo SuperGenius pointer bump last (innermost-first).
</output>
