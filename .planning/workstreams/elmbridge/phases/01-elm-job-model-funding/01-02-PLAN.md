---
phase: 01-elm-job-model-funding
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - SuperGenius (submodule pointer bump for SGProcessingManager)
  - SuperGenius/src/processing/processing_clocks_elm.hpp (new)
  - SuperGenius/src/processing/processing_clocks_elm.cpp (new)
  - SuperGenius/src/processing/CMakeLists.txt
  - SuperGenius/src/account/GeniusNode.hpp
  - SuperGenius/src/account/GeniusNode.cpp
  - SuperGenius/test/src/account/elm_cost_clocks_test.cpp (new)
  - SuperGenius/test/src/account/CMakeLists.txt
autonomous: true
requirements: [FUND-01, FUND-02]
must_haves:
  truths:
    - "GetElmProcessCost returns exactly maximum_processing_hours × $0.0003/hour in minions at PRECISION=6 via integer math, with no GetGNUSPrice/CoinGecko call anywhere on the ELM branch (FUND-01, OD-2)"
    - "Escrow at hold for the D-04 default (1.0h) computes to exactly 300 minions; 24h → 7200; 1.3h → 390 (milli-hour rounding, never bare truncation)"
    - "DeriveElmClocks is the single derivation source: deadline == maximum_processing_hours exactly, lockTimeout == deadline + 60s grace, escrowMinions == the same integer path as GetElmProcessCost (D-08, D-09)"
    - "Submitting an elm_processing job through ProcessImage in Phase 1 parses+validates+prices, then returns a structured ELM_SUBMIT_UNAVAILABLE error BEFORE HoldEscrow — no UTXOs stranded (OD-1)"
    - "The USD→GNUS rate constant used at hold time is defined exactly once and recorded into the escrow CRDT sibling key by the rate-record helper (OD-2)"
    - "The non-ELM path through ProcessImage is untouched: GetProcessCost body byte-identical, cost selection via one isElm conditional only (SC-5)"
  artifacts:
    - path: "SuperGenius/src/processing/processing_clocks_elm.hpp"
      provides: "ElmClocks struct + DeriveElmClocks(hours) + named constants kUsdPerHourElm/kUsdPerGnusRate/kLockGraceSeconds"
      contains: "DeriveElmClocks"
      min_lines: 30
    - path: "SuperGenius/src/account/GeniusNode.hpp"
      provides: "GetElmProcessCost declaration + Error::ELM_SUBMIT_UNAVAILABLE + CreateElmRateRecord helpers"
      contains: "GetElmProcessCost"
    - path: "SuperGenius/src/account/GeniusNode.cpp"
      provides: "isElm cost branch in ProcessImage, interim structured rejection before HoldEscrow, rate-record CRDT Put"
      contains: "ELM_SUBMIT_UNAVAILABLE"
    - path: "SuperGenius/test/src/account/elm_cost_clocks_test.cpp"
      provides: "cost/clocks parameterized matrix + interim-submit rejection tests"
      min_lines: 120
  key_links:
    - from: "GeniusNode::ProcessImage"
      to: "DeriveElmClocks / GetElmProcessCost"
      via: "job_type sniff on procmgr->GetProcessingData()"
      pattern: "GetElmProcessCost"
    - from: "GetElmProcessCost"
      to: "processing_clocks_elm.hpp"
      via: "shared escrowMinions derivation (single source)"
      pattern: "DeriveElmClocks"
    - from: "CreateElmRateRecordCRDTTransaction"
      to: "tx_globaldb_->BeginTransaction/Put"
      via: "HierarchicalKey(escrow_path + \"/elm_rate\")"
      pattern: "elm_rate"
---

<objective>
Land the SuperGenius-side deterministic funding: pointer bump to plan 01-01's schema, the three-clock derivation utility `processing_clocks_elm`, the public `GetElmProcessCost` (no price lookup), the interim ELM submit branch (parse+validate+price then structured rejection before `HoldEscrow`), and the USD→GNUS rate-record CRDT helper.

Purpose: FUND-01 (deterministic hours-based cost) and the derivation half of FUND-02; OD-1 fixes interim submit behavior; OD-2 fixes the rate shape (named constant, recorded at hold time).
Output: clocks utility + cost branch + interim rejection + rate-record helper, all unit-tested without a live node, committed in SuperGenius after the submodule pointer bump.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-CONTEXT.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-RESEARCH.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-PATTERNS.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-01-PLAN.md
@SuperGenius/src/account/GeniusNode.cpp
@SuperGenius/src/account/GeniusNode.hpp
@SuperGenius/src/account/TokenAmount.hpp
@SuperGenius/src/processing/CMakeLists.txt
@SuperGenius/test/src/account/CMakeLists.txt
</context>

## Artifacts this phase produces

**This plan (01-02) creates:**
- `sgns::processing::ElmClocks` struct (fields `deadline`, `lockTimeout` as `std::chrono::milliseconds`; `escrowMinions` as `uint64_t`)
- `sgns::processing::DeriveElmClocks( double maximum_processing_hours ) -> ElmClocks`
- Constants (in `processing_clocks_elm.hpp`): `kUsdPerHourElm = 0.0003`, `kUsdPerGnusRate = 1.0` (OD-2 named rate constant, default 1.0, defined once), `kLockGraceSeconds = 60`, `kMaxProcessingHoursCap = 24.0`
- Internal helper `sgns::processing::ElmEscrowMinions( double hours ) -> uint64_t` (the single integer path: `std::llround(hours * 1000.0) * 3 / 10` at rate 1.0) shared by `DeriveElmClocks` and `GetElmProcessCost`
- `GeniusNode::GetElmProcessCost( const sgns::sgprocessing::ProcessingManager &procmgr ) -> uint64_t` (public)
- `GeniusNode::Error::ELM_SUBMIT_UNAVAILABLE = 17` (next free value after INVALID_NODE_TYPE=16)
- `GeniusNode::CreateElmRateRecordCRDTTransaction( const std::string &escrow_path, double maximum_processing_hours )` — sibling `Put` of `{"usd_per_hour":0.0003,"usd_per_gnus":1.0,"minions":M,"maximum_processing_hours":H}` at `HierarchicalKey(escrow_path + "/elm_rate")`
- Test target `elm_cost_clocks_test`
- The SGProcessingManager submodule pointer bump inside SuperGenius

**Plan 01-01 created (consumed):** `GetElmMaximumProcessingHours()`, `get_job_type()`, generated ELM types.
**Plan 01-03 creates (not this plan):** lock-timeout wiring into `ProcessingNode::New`, `ElmValidationModeOk`, settlement/mapping design notes.

<tasks>

<task type="auto">
  <name>Task 1: Submodule pointer bump</name>
  <files>SuperGenius (gitlink for SGProcessingManager)</files>
  <read_first>
  - SuperGenius/SGProcessingManager git log (confirm plan 01-01's schema/gates commits exist on dev_elmruntime)
  - 01-RESEARCH.md "Common Pitfalls (process-level)" — submodule commit order
  </read_first>
  <action>
  From `SuperGenius/`: verify `git -C SGProcessingManager branch --show-current` is `dev_elmruntime` and `git -C SGProcessingManager log --oneline -3` shows plan 01-01's commits; then `git add SGProcessingManager` and commit the pointer bump as its own commit BEFORE any SuperGenius source changes — SuperGenius code must never compile against uncommitted submodule state.
  </action>
  <verify>
    <automated>cd SuperGenius; git log --oneline -1; git diff HEAD~1 --submodule=short SGProcessingManager | head -20</automated>
  </verify>
  <acceptance_criteria>
  - SuperGenius HEAD commit contains ONLY the SGProcessingManager gitlink change
  - `git -C SGProcessingManager status --short` is empty (submodule tree clean at the bumped SHA)
  </acceptance_criteria>
  <done>Pointer bumped to a committed schema state; SuperGenius work can build against it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three-clock derivation utility</name>
  <files>SuperGenius/src/processing/processing_clocks_elm.hpp (new), SuperGenius/src/processing/processing_clocks_elm.cpp (new), SuperGenius/src/processing/CMakeLists.txt</files>
  <read_first>
  - SuperGenius/src/account/TokenAmount.hpp + TokenAmount.cpp (static-utility class shape, PRECISION=6, :24; outcome::result returns)
  - SuperGenius/src/processing/CMakeLists.txt (processing_service source list — add the new .cpp)
  - 01-PATTERNS.md §6 (TokenAmount analog + RESEARCH sketch; Pitfall 3 integer-math rule)
  - 01-RESEARCH.md F5 (minion arithmetic: milli_hours × 3 / 10 at rate 1.0)
  </read_first>
  <behavior>
  - DeriveElmClocks(1.0) → deadline == 3600000ms, lockTimeout == 3660000ms, escrowMinions == 300
  - DeriveElmClocks(24.0) → deadline == 86400000ms, escrowMinions == 7200
  - DeriveElmClocks(1.3) → escrowMinions == 390 (llround(1300) — NOT 389 from bare truncation of 1299.999…)
  - DeriveElmClocks(0.0000001) → escrowMinions == 0 (sub-milli-hour bills 0, requestor-favorable D-02)
  - deadline == hours exactly (no grace inflation — D-08); lockTimeout - deadline == 60000ms for every input
  </behavior>
  <action>
  1. Create `processing_clocks_elm.hpp` in `namespace sgns::processing` following the TokenAmount constants-first static-utility shape with Doxygen `@brief/@param/@return` on every public symbol:
     - Constants: `inline constexpr double kUsdPerHourElm = 0.0003;` `inline constexpr double kUsdPerGnusRate = 1.0;` (OD-2 — the single named rate; no GetGNUSPrice anywhere), `inline constexpr std::chrono::seconds kLockGraceSeconds{ 60 };` (planner discretion floor: always exceeds publication latency), `inline constexpr double kMaxProcessingHoursCap = 24.0;`
     - `struct ElmClocks { std::chrono::milliseconds deadline; std::chrono::milliseconds lockTimeout; uint64_t escrowMinions; };`
     - `uint64_t ElmEscrowMinions( double maximum_processing_hours );` — pure uint64 path: `std::llround(hours * 1000.0) * 3 / 10` (milli-hours × $0.0003 × 10^6 minions/$ at rate 1.0, floored — Pitfall 3 bans bare `uint64_t(hours*1000)`)
     - `ElmClocks DeriveElmClocks( double maximum_processing_hours );` — deadline = hours exact (D-08), lockTimeout = deadline + kLockGraceSeconds (D-08), escrowMinions = ElmEscrowMinions(hours). Assert/input-contract hours ∈ (0, kMaxProcessingHoursCap] (schema+gates guarantee it upstream; debug-assert only, no clamping — D-05).
  2. Create `processing_clocks_elm.cpp` with the implementations; add `processing_clocks_elm.cpp` to the `processing_service` target's source list in src/processing/CMakeLists.txt (beside processing_tasksplit.cpp). Header-only consumers outside processing_service include the header; the split .cpp keeps UNITY_BUILD clean.
  3. Write the behavior cases above as TEST_P matrix entries in `SuperGenius/test/src/account/elm_cost_clocks_test.cpp` (created fully in Task 4; this task adds the DeriveElmClocks cases and the target skeleton so the RED leg compiles against the new symbols).
  </action>
  <verify>
    <automated>cd SuperGenius; cmake --build build/Windows/Release --target processing_service --config Release</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "kUsdPerGnusRate\|kUsdPerHourElm\|kLockGraceSeconds" src/processing/processing_clocks_elm.hpp` >= 3
  - `grep "ElmEscrowMinions" src/processing/processing_clocks_elm.cpp` matches the llround integer path; `grep -c "GetGNUSPrice\|GetCoinprice\|CoinGecko" src/processing/processing_clocks_elm.* ` == 0 (FUND-01)
  - processing_service builds with the new translation unit
  - All five behavior rows pass in the test target (verified when Task 4 completes the target)
  </acceptance_criteria>
  <done>Single three-clock derivation source exists with deterministic integer escrow math and no price-lookup references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ELM cost branch + interim submit rejection + rate record</name>
  <files>SuperGenius/src/account/GeniusNode.hpp, SuperGenius/src/account/GeniusNode.cpp</files>
  <read_first>
  - SuperGenius/src/account/GeniusNode.cpp (ProcessImage :2257-2357 entry/cost/splitter/escrow/enqueue; GetProcessCost :2381-2410; CreateEscrowInfoCRDTTransaction :3206-3219)
  - SuperGenius/src/account/GeniusNode.hpp (Error enum :190-215 ends INVALID_NODE_TYPE=16; GetProcessCost declaration :367)
  - SuperGenius/src/processing/impl/TaskQueueImpl.cpp (multi-Put atomic transaction precedent :32-67)
  - 01-PATTERNS.md §7-9 (GetProcessCost analog, branch-site shape, rate-record sibling Put)
  - 01-RESEARCH.md F4 (branch-point table + must-not-touch list), F5, F6
  </read_first>
  <behavior>
  - GetElmProcessCost(procmgr with 1.0h ELM job) == 300; with 1.3h == 390; with 24h == 7200; with hours defaulted (absent funding block) == 300 (D-04 default via GetElmMaximumProcessingHours)
  - GetElmProcessCost(non-ELM procmgr) == 0 and logs an error (call-site contract: only invoked when isElm)
  - ProcessImage(valid elm_processing json) on a READY node returns failure ELM_SUBMIT_UNAVAILABLE and NEVER calls HoldEscrow (assert: transaction manager spy/mock or UTXO balance unchanged — follow the account_management_test harness shape)
  - ProcessImage(valid non-ELM json) behaves exactly as before (cost via GetProcessCost, proceeds to splitter) — covered by the existing account/processing test suites still passing (SC-5)
  - CreateElmRateRecordCRDTTransaction produces a CRDT transaction whose Put targets HierarchicalKey(escrow_path + "/elm_rate") with a JSON body containing usd_per_hour, usd_per_gnus, minions, maximum_processing_hours
  </behavior>
  <action>
  1. GeniusNode.hpp: add `ELM_SUBMIT_UNAVAILABLE = 17` to the Error enum with a Doxygen comment ("ELM submission is not yet wired to a splitter; reserved UTXOs are protected by rejecting before escrow hold"); declare `uint64_t GetElmProcessCost( const sgns::sgprocessing::ProcessingManager &procmgr );` directly beside GetProcessCost (:367) with full Doxygen block, and `outcome::result<std::shared_ptr<crdt::AtomicTransaction>> CreateElmRateRecordCRDTTransaction( const std::string &escrow_path, double maximum_processing_hours );` beside CreateEscrowInfoCRDTTransaction.
  2. GeniusNode.cpp — GetElmProcessCost implementation mirroring GetProcessCost's log-and-return-0 shape but: hours = procmgr.GetElmMaximumProcessingHours() (plan 01-01 accessor; 0.0 means not-an-ELM-job → log + return 0); minions = sgns::processing::ElmEscrowMinions( hours ) — NO GetGNUSPrice call, no TokenAmount::CalculateCostMinions (that is the byte-based path, bypassed per FUND-01).
  3. GeniusNode.cpp — ProcessImage branch (SC-5 blast radius: exactly one conditional): after `ProcessingManager::Create` succeeds, sniff `procmgr->GetProcessingData().get_job_type()`; when it equals the generated ELM enum value: compute `auto elmFunds = GetElmProcessCost( *procmgr );` (log-and-fail PROCESS_COST_ERROR if 0, mirroring the existing funds<=0 guard), then `return outcome::failure( Error::ELM_SUBMIT_UNAVAILABLE );` — BEFORE the UTXO balance check, the task/splitter block, and HoldEscrow (:2320) so no UTXOs are reserved (OD-1). Phase 4 replaces this early return with the ELM splitter. The non-ELM path (GetProcessCost call at :2265 through enqueue) stays byte-identical.
  4. GeniusNode.cpp — CreateElmRateRecordCRDTTransaction: `auto tx = tx_globaldb_->BeginTransaction();` then Put a JSON body built with nlohmann::json: {"usd_per_hour": sgns::processing::kUsdPerHourElm, "usd_per_gnus": sgns::processing::kUsdPerGnusRate, "minions": sgns::processing::ElmEscrowMinions( hours ), "maximum_processing_hours": hours} at `sgns::crdt::HierarchicalKey( escrow_path + "/elm_rate" )`, return the transaction (Pattern 4; the escrow record itself stays at the plain escrow_path key). In Phase 1 no production caller wires it into ProcessImage (the interim rejection returns before escrow) — it is the unit-tested building block Phase 4 calls inside the same transaction as CreateEscrowInfoCRDTTransaction; add a `// Phase 4:` comment saying exactly that.
  </action>
  <verify>
    <automated>cd SuperGenius; cmake --build build/Windows/Release --target genius_node --config Release</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "GetGNUSPrice" src/account/GeniusNode.cpp` shows GetGNUSPrice calls ONLY inside GetProcessCost/GetCoinprice — zero new call sites (FUND-01)
  - `git diff src/account/GeniusNode.cpp` hunk containing GetProcessCost body is absent (byte-identical); the ELM branch is one isElm conditional block between Create (:2264) and the funds guard
  - Error enum has ELM_SUBMIT_UNAVAILABLE = 17 with Doxygen
  - `grep "elm_rate" src/account/GeniusNode.cpp` matches the rate-record Put
  - Behavior rows above all pass in elm_cost_clocks_test (Task 4)
  </acceptance_criteria>
  <done>Deterministic ELM cost is selectable at the ProcessImage branch, interim submissions reject before escrow, and the rate-record helper exists with the named-constant rate.</done>
</task>

<task type="auto">
  <name>Task 4: Cost/clocks/interim-submit unit tests</name>
  <files>SuperGenius/test/src/account/elm_cost_clocks_test.cpp (completed), SuperGenius/test/src/account/CMakeLists.txt</files>
  <read_first>
  - SuperGenius/test/src/account/token_amount_test.cpp (TEST_P matrix shape :19-64)
  - SuperGenius/test/src/account/account_management_test.cpp (:296-327 — ProcessingManager::Create + direct GetProcessCost call pattern; node harness for ProcessImage)
  - SuperGenius/test/src/account/CMakeLists.txt (addtest + target_link_libraries genius_node_test pattern)
  - Task 2/3 outputs (the symbols under test)
  </read_first>
  <action>
  1. Complete `elm_cost_clocks_test.cpp`: (a) DeriveElmClocks TEST_P matrix (Task 2's five rows); (b) GetElmProcessCost cases via `ProcessingManager::Create(elm_json)` then direct public call — 1.0h→300, 1.3h→390, 24h→7200, defaulted-funding→300, non-ELM→0; (c) interim-submit: build the account_management_test-style harness, call ProcessImage with a valid elm_processing job, EXPECT error == GeniusNode::Error::ELM_SUBMIT_UNAVAILABLE and requester UTXO balance unchanged (no HoldEscrow side effect); (d) rate-record: drive CreateElmRateRecordCRDTTransaction against the test CRDT stack (base_rocksdb_test linkage precedent from utxo_manager_test) and assert the committed value at HierarchicalKey(escrow_path + "/elm_rate") deserializes to the four-field JSON with minions == ElmEscrowMinions(hours).
  2. Register in test/src/account/CMakeLists.txt: `addtest(elm_cost_clocks_test elm_cost_clocks_test.cpp)` + `target_link_libraries(elm_cost_clocks_test genius_node_test base_crdt_test)` (mirror utmo_manager_test's base_crdt_test linkage for the CRDT leg).
  </action>
  <verify>
    <automated>cd SuperGenius; cmake --build build/Windows/Release --target elm_cost_clocks_test --config Release; ctest --test-dir build/Windows/Release -C Release -R elm_cost_clocks_test --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - ctest green covering all DeriveElmClocks rows, all GetElmProcessCost rows, the ELM_SUBMIT_UNAVAILABLE rejection with unchanged balance, and the elm_rate CRDT content assertion
  - No test requires network (no CoinGecko reachability — the ELM branch is offline-deterministic)
  - `addtest(elm_cost_clocks_test` present in test/src/account/CMakeLists.txt
  </acceptance_criteria>
  <done>FUND-01 and the derivation half of FUND-02 are test-pinned; SC-2's arithmetic locked (hours × 0.0003 → minions, rate recorded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requestor-declared hours → node-side cost | server-side cost computed ONLY from parsed schema fields, never from any client-supplied cost/amount field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation/Economic | requestor under-declares hours to underfund | mitigate | escrow computed from declared hours at submit in GetElmProcessCost (parsed schema source of truth); hours ∈ (0,24] enforced at parse (plan 01-01); interim rejection prevents any hold at all until Phase 4 |
| T-01-05 | Economic/DoS | stranded reserved UTXOs from unwirable ELM holds | mitigate | OD-1: ELM branch returns ELM_SUBMIT_UNAVAILABLE before HoldEscrow (:2320); test asserts balance unchanged |
| T-01-06 | Repudiation | settlement disputes when USD price moves between hold and payout | mitigate | OD-2: rate constant recorded in escrow CRDT sibling key at hold time (CreateElmRateRecordCRDTTransaction); deterministic branch never fetches a live price |
| T-01-07 | Tampering | float truncation under-escrows vs declared max | mitigate | std::llround milli-hour path (Pitfall 3), TEST-pinned 1.3h→390 |
</threat_model>

<verification>
- All four tasks' automated commands green from `SuperGenius/`
- Existing account + processing test suites still pass (SC-5 byte-identical non-ELM path)
- Commit order inside SuperGenius: pointer bump commit (Task 1) precedes source commits; root-repo pointer chain (SuperGenius) bumped after all SuperGenius commits — innermost-first discipline
</verification>

<success_criteria>
- SC-2 arithmetic truth: escrow for ELM job == hours × $0.0003 in minions at PRECISION=6, no price lookup, rate recorded at hold time (helper + tests; live hold wiring is Phase 4)
- SC-3 derivation source: one derivation function feeds deadline/lock/escrow-max
- FUND-01 complete; FUND-02 derivation + wiring-ready (queue wiring is plan 01-03)
</success_criteria>

<output>
Create `.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-02-SUMMARY.md` when done.
Commit discipline: source commits inside `SuperGenius` on `dev_elmruntime` (pointer bump first); planning summary committed at root with `--ws elmbridge`.
</output>
