---
phase: 01-elm-job-model-funding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SuperGenius/SGProcessingManager/gnus-processing-schema.json
  - SuperGenius/SGProcessingManager/generated/* (regenerated — zero hand edits)
  - SuperGenius/SGProcessingManager/include/processingbase/ProcessingManager.hpp
  - SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp
  - SuperGenius/SGProcessingManager/test/processingbase/elm_job_schema_test.cpp (new)
  - SuperGenius/SGProcessingManager/test/processingbase/CMakeLists.txt (new)
  - SuperGenius/SGProcessingManager/test/CMakeLists.txt
autonomous: true
requirements: [JOB-01, JOB-03]
must_haves:
  truths:
    - "A minimal ELM job (name, version, gnus_spec_version==1, job_type=elm_processing, one elms[] entry with work_item_id/manifest uri+hash/input_uri) parses through ProcessingManager::Create successfully"
    - "Malformed ELM jobs — duplicate work_item_id, empty/missing elms, validation mode exact|redundant, hours<=0, top_p<=0, out-of-bounds settings — reject at Create with dedicated structured Error codes (SC-1, D-05/D-07/D-13)"
    - "Non-ELM jobs missing passes/inputs/outputs, or with an empty passes array, are still rejected at Create with INVALID_JSON — pre-change rejection behavior preserved (SC-5)"
    - "ELM optionals default-fill through one normalization read: maximum_processing_hours=1.0, temperature=1.0, top_p=1.0, validation=none (D-04)"
  artifacts:
    - path: "SuperGenius/SGProcessingManager/gnus-processing-schema.json"
      provides: "job_type enum, elms[] work items, generation settings bounds, funding block, validation enum; root required relaxed"
      contains: "elm_processing"
    - path: "SuperGenius/SGProcessingManager/include/processingbase/ProcessingManager.hpp"
      provides: "Error enum values 14-18 + public GetElmMaximumProcessingHours()"
      contains: "DUPLICATE_WORK_ITEM_ID"
    - path: "SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp"
      provides: "CheckElmValidity gates, non-ELM requires-passes gate, ELM default-fill"
      contains: "CheckElmValidity"
    - path: "SuperGenius/SGProcessingManager/test/processingbase/elm_job_schema_test.cpp"
      provides: "parse-rejection + defaults + non-ELM parity matrix"
      min_lines: 150
  key_links:
    - from: "gnus-processing-schema.json"
      to: "generated/*.hpp"
      via: "quicktype regen command (README.md:22-38)"
      pattern: "Elm"
    - from: "ProcessingManager::Create"
      to: "CheckElmValidity / non-ELM gate"
      via: "Init -> CheckProcessValidity chain"
      pattern: "CheckElmValidity"
---

<objective>
Land the ELM job schema in SGProcessingManager: `job_type`/`elms[]`/`generation`/`funding`/`validation` in `gnus-processing-schema.json`, quicktype regen (zero hand-edits to `generated/`), C++ parse gates for everything quicktype cannot enforce, default-fill normalization, and the full parse-rejection test matrix.

Purpose: This is the contract every later phase consumes (JOB-01) and the schema-level validation-mode decision (JOB-03). Per D-06 no new version field is added — the ELM block rides beside `passes[]` under the existing `SgnsProcessing` root governed by `gnus_spec_version==1`.
Output: schema + regenerated headers + gated ProcessingManager + `sgprocbase_elm_job_schema_test` target, committed in the SGProcessingManager repo (innermost-first submodule discipline).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-CONTEXT.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-RESEARCH.md
@.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-PATTERNS.md
@SuperGenius/SGProcessingManager/gnus-processing-schema.json
@SuperGenius/SGProcessingManager/README.md
@SuperGenius/SGProcessingManager/include/processingbase/ProcessingManager.hpp
@SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp
</context>

## Artifacts this phase produces

**This plan (01-01) creates:**
- Schema JSON definitions: `Elm` (work item), `ElmGeneration` (generation settings), `ElmFunding`; root properties `job_type` (enum `["elm_processing"]`), `elms` (array of `Elm`), `funding` (`ElmFunding`), `validation` (enum `["none","exact","redundant"]`)
- Quicktype-generated types (exact spellings per regen output, expected pascal-case): e.g. `sgns::JobType`, `sgns::Elm`, `sgns::ElmGeneration`, `sgns::ElmFunding`, `sgns::ElmValidationMode`, and getters `get_job_type()`, `get_elms()`, `get_funding()`, `get_validation()` on `SgnsProcessing`
- `ProcessingManager::Error` enum values: `DUPLICATE_WORK_ITEM_ID = 14`, `ELM_VALIDATION_UNIMPLEMENTED = 15`, `ELM_WORK_ITEMS_MISSING = 16`, `ELM_FUNDING_INVALID = 17`, `ELM_GENERATION_SETTINGS_INVALID = 18`
- `ProcessingManager::CheckElmValidity()` (private gate, called from `CheckProcessValidity`)
- `ProcessingManager::GetElmMaximumProcessingHours() const -> double` (public; normalized hours, default 1.0 per D-04; returns 0.0 when the job is not `elm_processing` — consumed by plan 01-02's `GetElmProcessCost`)
- Test target `sgprocbase_elm_job_schema_test` (new dir `test/processingbase/`)

**Plans 01-02 / 01-03 create (cross-references, not this plan):** `ElmClocks`/`DeriveElmClocks`, `GetElmProcessCost`, `GeniusNode::Error::ELM_SUBMIT_UNAVAILABLE`, rate-record helpers, `ElmValidationModeOk`, lock-timeout wiring.

<tasks>

<task type="auto">
  <name>Task 1: ELM schema block + quicktype regen</name>
  <files>SuperGenius/SGProcessingManager/gnus-processing-schema.json, SuperGenius/SGProcessingManager/generated/* (regenerated)</files>
  <read_first>
  - SuperGenius/SGProcessingManager/gnus-processing-schema.json (entire file — root required at :7, enum at :117/164, pattern at :12, budget bounds at :235-253, allOf at :255-276)
  - SuperGenius/SGProcessingManager/README.md (regen command, :22-38)
  - SuperGenius/SGProcessingManager/generated/SgnsProcessing.hpp (constraint wiring, :25-36, :73 — D-06 basis)
  - 01-PATTERNS.md §1 (schema analog excerpts: bounds field, enum field, pattern id, root required to relax)
  - 01-RESEARCH.md F3 (quicktype pipeline table), Pitfall 1 (root-required blocks minimal ELM job), Pitfall 6 (constructs quicktype drops)
  </read_first>
  <action>
  Edit `gnus-processing-schema.json` (draft-07) ONLY — never touch `generated/` by hand (SCHEMA-01..05):
  1. Relax root `required` (line 7) from `["name","version","gnus_spec_version","passes","inputs","outputs"]` to `["name","version","gnus_spec_version"]` — keep `passes`/`inputs`/`outputs` as properties (D-04 minimal ELM job; non-ELM enforcement moves to the C++ gate in Task 2). Do NOT add any new version field — `gnus_spec_version` (==1) governs evolution per D-06.
  2. Add new root properties, beside `passes`:
     - `job_type`: string, enum `["elm_processing"]`, optional (absent = existing non-ELM job). Unknown strings reject at parse via the generated enum chain.
     - `elms`: array of `$ref` to a new definition `Elm`. Each `Elm` object: `work_item_id` (string, pattern `^[A-Za-z0-9_-]+$` — same charset as root `name` at :12, required; constrains charset now so later phases never interpolate arbitrary strings into filesystem paths), `elm_type` (string, enum `["causal_lm"]`, required — v1.0 implements causal-LM only), `model_manifest_uri` (string, minLength 1, required), `model_manifest_hash` (string, minLength 1, required), `input_uri` (string, minLength 1, required), `generation` (`$ref` new definition `ElmGeneration`, optional).
     - Definition `ElmGeneration`: `max_output_tokens` (integer, minimum 1, optional), `temperature` (number, minimum 0, maximum 2, optional), `top_p` (number, minimum 0, maximum 1, optional — exclusiveMinimum does NOT survive quicktype codegen per A3/Pitfall 6, the `>0` half is a C++ gate in Task 2), `seed` (integer, minimum 0, optional). Document the defaults in `description` text (temperature 1.0, top_p 1.0 — applied by C++ normalization, quicktype never applies `default`).
     - `funding`: `$ref` new definition `ElmFunding`, optional. `ElmFunding`: `maximum_processing_hours` (number, maximum 24, minimum 0, optional — the `>0` exclusive half is a C++ gate; cap 24h per D-05 discretion).
     - `validation`: string, enum `["none","exact","redundant"]`, optional (schema full enum per D-13; `exact`/`redundant` parse but are refused by the C++ gate as unimplemented).
  3. Regenerate from `SuperGenius/SGProcessingManager/` with the exact README command: quicktype --src-lang schema --lang cpp --top-level SGNSProcessing --code-format with-getter-setter --const-style west-const --namespace sgns --type-style pascal-case --member-style underscore-case --boost --source-style multi-source --include-location global-include --out generated/SGNSProcMain.hpp gnus-processing-schema.json (toolchain verified installed: quicktype 23.2.6, node 24.16.0; CI auto-PR fires only on `main`, we are on `dev_elmruntime` — regen locally and commit).
  4. Record the ACTUAL generated type/enum spellings (e.g. `sgns::JobType::ELM_PROCESSING`, `sgns::ElmValidationMode::NONE`, `get_elms()`) in the commit message — Task 2 and plans 01-02/01-03 reference them.
  No CMake edits needed: `generated/` is already included via `target_include_directories(ProcessingBase ... ../../generated)` (src/processingbase/CMakeLists.txt:8).
  </action>
  <verify>
    <automated>cd SuperGenius/SGProcessingManager; git diff --stat generated/ ; grep -c "elm_processing" generated/SgnsProcessing.hpp; grep -c "ElmValidationMode" generated/SGNSProcMain.hpp; cmake --build build/Windows/Release --target ProcessingBase --config Release</automated>
  </verify>
  <acceptance_criteria>
  - Root schema `required` is exactly `["name", "version", "gnus_spec_version"]`
  - `grep "elm_processing" gnus-processing-schema.json` matches the job_type enum entry
  - `grep -c "min_double_value\|max_double_value" generated/ElmGeneration.hpp` is > 0 (bounds survived codegen — if the file name differs, grep the regenerated header containing temperature constraints)
  - Regenerated `SgnsProcessing` root exposes `get_job_type`, `get_elms`, `get_funding`, `get_validation` accessors (verify via grep in the regenerated root header)
  - `git diff generated/` shows ONLY quicktype output (no manual edits); `ProcessingBase` builds clean
  - The pre-existing `gnus_spec_version == 1` constraint is still present in the regenerated root header (D-06)
  </acceptance_criteria>
  <done>Schema carries the full ELM block with bounds/pattern/enums; headers regenerated; ProcessingBase compiles against them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error enum + parse gates + default-fill normalization</name>
  <files>SuperGenius/SGProcessingManager/include/processingbase/ProcessingManager.hpp, SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp</files>
  <read_first>
  - SuperGenius/SGProcessingManager/include/processingbase/ProcessingManager.hpp (Error enum :82-95 — hand-written, ends at UNKNOWN_PASS_TYPE=13)
  - SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp (Create :419, Init pre-parse interception :523-561, catch ladder :566-578, CheckProcessValidity :586+, value_or default precedent :1311-1313)
  - 01-PATTERNS.md §4 (Analog A pre-parse, Analog C typed post-parse, Analog D default-fill)
  - 01-RESEARCH.md F7 (structured-error pattern), Pitfall 7 (enum rejection context), Pitfall 1 (regression surface for relaxed root required)
  </read_first>
  <behavior>
  - elm_processing job with elms ["a","a"] (duplicate work_item_id) → Create fails with Error::DUPLICATE_WORK_ITEM_ID
  - elm_processing job with "elms": [] or elms absent → Create fails with Error::ELM_WORK_ITEMS_MISSING
  - elm_processing job with "validation": "exact" or "redundant" → Create fails with Error::ELM_VALIDATION_UNIMPLEMENTED (D-13 — they parse fine, refused by policy)
  - elm_processing job with "validation": "bogus" → Create fails with Error::INVALID_JSON (generated enum chain)
  - elm_processing job with funding.maximum_processing_hours = 0 or negative → Create fails with Error::ELM_FUNDING_INVALID
  - elm_processing job with hours = 25 → Create fails with Error::INVALID_JSON (schema maximum 24, CheckConstraint ValueTooHighException)
  - elm_processing job with generation.top_p = 0 → Create fails with Error::ELM_GENERATION_SETTINGS_INVALID; top_p = 1.0 → parses
  - generation.temperature = 2.5 or max_output_tokens = 0 → Create fails with Error::INVALID_JSON (schema bounds)
  - work_item_id "bad id!" → Create fails with Error::INVALID_JSON (pattern)
  - elms present but job_type absent → Create fails with Error::INVALID_JSON (defensive: ELM payload requires the discriminator)
  - non-ELM job missing passes OR inputs OR outputs, or with "passes": [] → Create fails with Error::INVALID_JSON at the same entry point as before the schema change (SC-5 parity)
  - Valid minimal ELM job (defaults absent) → GetElmMaximumProcessingHours() == 1.0; explicit 1.3h → 1.3; non-ELM job → 0.0
  </behavior>
  <action>
  1. Extend the hand-written Error enum (include/processingbase/ProcessingManager.hpp:82-95) with: DUPLICATE_WORK_ITEM_ID = 14, ELM_VALIDATION_UNIMPLEMENTED = 15, ELM_WORK_ITEMS_MISSING = 16, ELM_FUNDING_INVALID = 17, ELM_GENERATION_SETTINGS_INVALID = 18 (Doxygen one-liners each).
  2. In ProcessingManager.cpp add a private method `outcome::result<void> CheckElmValidity( const sgns::SgnsProcessing &data )` called from CheckProcessValidity when job_type indicates elm_processing (use the actual generated enum spelling from Task 1). Gates, each `m_logger->error(...)` then `outcome::failure(...)` per Analog C:
     - elms absent or empty → ELM_WORK_ITEMS_MISSING (quicktype drops minItems — Pitfall 6)
     - loop elms with std::set<std::string> seen; second insert failure → DUPLICATE_WORK_ITEM_ID (D-07)
     - validation present and value != none → ELM_VALIDATION_UNIMPLEMENTED (D-13)
     - funding.maximum_processing_hours present and <= 0 → ELM_FUNDING_INVALID (schema minimum is 0 inclusive; the exclusive >0 half is ours)
     - any elm generation.top_p present and <= 0 → ELM_GENERATION_SETTINGS_INVALID (A3 belt-and-braces)
     - elms present while job_type is absent/non-ELM → INVALID_JSON (add to the non-ELM gate below)
  3. Non-ELM parity gate (SC-5, Pitfall 1): in Init after parse (before CheckProcessValidity returns success), when job_type is absent: require passes/inputs/outputs present AND passes non-empty, else log + return outcome::failure( Error::INVALID_JSON ) — preserves today's reject-at-Create outcome for jobs that the relaxed root `required` would now let through, including the previously-downstream `"passes": []` case.
  4. Default-fill normalization (D-04, Pattern 3): add public `double GetElmMaximumProcessingHours() const` — returns 0.0 when the parsed job is not elm_processing; otherwise funding.maximum_processing_hours.value_or(1.0). Single defaulted read site so plan 01-02's cost branch and three-clock derivation consume exactly one view.
  </action>
  <verify>
    <automated>cd SuperGenius/SGProcessingManager; cmake --build build/Windows/Release --target ProcessingBase --config Release</automated>
  </verify>
  <acceptance_criteria>
  - Error enum contains contiguous values 14-18 with the five names above (grep in ProcessingManager.hpp)
  - `grep -c "CheckElmValidity" src/processingbase/ProcessingManager.cpp` >= 2 (definition + call)
  - `grep "GetElmMaximumProcessingHours" include/processingbase/ProcessingManager.hpp` matches the public declaration
  - Every behavior listed above is exercised by a test in Task 3 (matrix below maps 1:1)
  - No change to any existing non-ELM error path other than the added parity gate (git diff scoped review)
  </acceptance_criteria>
  <done>All D-05/D-07/D-13 rejections that quicktype cannot express are enforced at Create with structured codes; defaults normalize through one accessor.</done>
</task>

<task type="auto">
  <name>Task 3: Parse-rejection matrix tests + target registration</name>
  <files>SuperGenius/SGProcessingManager/test/processingbase/elm_job_schema_test.cpp (new), SuperGenius/SGProcessingManager/test/processingbase/CMakeLists.txt (new), SuperGenius/SGProcessingManager/test/CMakeLists.txt</files>
  <read_first>
  - SuperGenius/SGProcessingManager/test/execution/CMakeLists.txt (SGPROC_TEST_DISCOVERY gating pattern :3-20)
  - SuperGenius/SGProcessingManager/test/CMakeLists.txt (add_subdirectory list)
  - 01-PATTERNS.md §5 (CMake gating analog + parameterized-matrix analog)
  - The Task 1/2 state of gnus-processing-schema.json and ProcessingManager.cpp
  </read_first>
  <action>
  1. Create `test/processingbase/elm_job_schema_test.cpp` implementing every case in Task 2's behavior block. Structure: a `BuildElmJobJson(...)` helper producing a valid minimal ELM job (name/version/gnus_spec_version=1/job_type=elm_processing/one elm), then per-mutation TEST/TEST_P assertions on `ProcessingManager::Create(json)->error()` — follow the parameterized variant-matrix shape from PATTERNS §5B where the input+expected-error pair is the parameter. Include the four non-ELM parity cases (missing passes / missing inputs / missing outputs / empty passes) and the defaults cases via GetElmMaximumProcessingHours (1.0 default, explicit 1.3, non-ELM 0.0).
  2. Create `test/processingbase/CMakeLists.txt` mirroring the execution gating pattern: add_executable(sgprocbase_elm_job_schema_test elm_job_schema_test.cpp); target_link_libraries(... PRIVATE GTest::gtest_main ProcessingBase); unconditional add_test(NAME sgprocbase_elm_job_schema_test ...); gtest_discover_tests gated on `if(SGPROC_TEST_DISCOVERY AND NOT CMAKE_CROSSCOMPILING)` — wrong gating re-breaks the SuperGenius parent build.
  3. Register `add_subdirectory(processingbase)` in test/CMakeLists.txt beside the existing six entries.
  </action>
  <verify>
    <automated>cd SuperGenius/SGProcessingManager; cmake --build build/Windows/Release --target sgprocbase_elm_job_schema_test --config Release; ctest --test-dir build/Windows/Release -C Release -R sgprocbase_elm_job_schema_test --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
  - `ctest -R sgprocbase_elm_job_schema_test` passes with >= 15 distinct cases covering: both duplicate/missing-elms/unimplemented-mode/bounds rejections AND the four non-ELM parity rejections AND the three defaults assertions
  - `grep "SGPROC_TEST_DISCOVERY" test/processingbase/CMakeLists.txt` matches the gated discovery block
  - Test binary runs offline (no network, no fixtures — pure JSON strings)
  </acceptance_criteria>
  <done>sgprocbase_elm_job_schema_test green in the standalone build; rejection matrix and SC-5 parity locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requestor JSON → validating node | untrusted `Task.json_data` crosses into `ProcessingManager::Create` on every node that re-parses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering/DoS | ProcessingManager::Create on adversarial json | mitigate | schema bounds/pattern/enums throw typed exceptions caught as INVALID_JSON (Init catch ladder :566-578); C++ gates never dereference absent optionals |
| T-01-02 | Tampering | work_item_id (filesystem-path hazard in Phases 2/4) | mitigate | schema pattern `^[A-Za-z0-9_-]+$` rejects arbitrary strings at parse |
| T-01-03 | DoS | unknown enum strings crash parse | accept | generated enum chain throws std::runtime_error, caught by the existing std::exception handler (verified F3/Pitfall 7) |
| T-01-SC | Tampering | package installs | accept | zero installs this phase (quicktype/node pre-installed, verified) |
</threat_model>

<verification>
- All three tasks' automated commands green from `SuperGenius/SGProcessingManager/`
- `git status` in SGProcessingManager shows schema + regenerated headers + hand-written gate files + tests; commit lands in the SGProcessingManager repo FIRST (innermost-first: this repo's commit precedes any SuperGenius pointer bump — plan 01-02 task 1)
- Existing SGProcessingManager tests still pass (run ctest across the standalone build)
</verification>

<success_criteria>
- SC-1 (parse-level): ELM jobs express work items/funding/validation entirely in Task.json_data; malformed jobs reject at parse with structured errors; no protobuf change (grep: zero .proto diffs)
- JOB-01/JOB-03 schema legs complete; D-04/D-05/D-06/D-07/D-13 implemented and test-pinned
- SC-5 parse-parity leg: non-ELM rejection outcomes unchanged and test-locked
</success_criteria>

<output>
Create `.planning/workstreams/elmbridge/phases/01-elm-job-model-funding/01-01-SUMMARY.md` when done.
Commit discipline: code commits inside `SuperGenius/SGProcessingManager` on `dev_elmruntime`; planning summary committed at root via the gsd commit tool with `--ws elmbridge`.
</output>
