---
phase: 17-render-path-cross-hardware-tolerance
plan: 08
type: execute
wave: 7
depends_on: ["17-06", "17-07"]
files_modified:
  - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/
  - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md
  - .planning/workstreams/sgproc-render/ROADMAP.md
autonomous: false
requirements: [RENDTOL-02]

must_haves:
  truths:
    - "All three fixtures are re-captured fresh on both machines (D-09) with each fixture's final empirically-derived byteQuantMode now wired into its parameters array -- a NEW capture, not a reinterpretation of Round 1's raw-divergence numbers (mirrors 13-05-PLAN.md's own re-capture-after-quantization-change precedent)."
    - "SC4's outcome is reported per-fixture with the same honesty convention as VALD-01/13-SCOPE-BOUNDARY.md: hash match confirmed, OR a residual gap is characterized explicitly (which fixture, how large, why) -- never silently declared passing to satisfy SC4's letter (D-08)."
    - "ROADMAP.md's Phase 17 entry is updated to reflect the phase's actual completion status once this plan's results are in, mirroring how prior phases (13, 16) closed out their own Success Criteria sections with real outcomes."
  artifacts:
    - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md
  key_links:
    - "17-06/17-07's final per-fixture byteQuantMode values <-> this plan's fresh capture_diff re-run <-> SC4's per-fixture hash-match verdict -- the entire empirical chain this phase exists to prove, closed here."
---

<objective>
Run Round 2 -- the final, post-derivation cross-machine capture (D-09) -- for all three fixtures now that each carries its own empirically-derived `byteQuantMode` (17-06/17-07), and report SC4's hash-match outcome per fixture with the same honesty convention as VALD-01/`13-SCOPE-BOUNDARY.md`.

Purpose: this is the phase's final empirical proof point -- RENDTOL-02's success criterion 4 requires a FRESH capture with the real tolerance value wired in, not a reinterpretation of Round 1's raw numbers, and D-08 requires any residual gap to be characterized honestly rather than silently declared passing.
Output: six more `.cap` files (Round 2) + three fresh `capture_diff` JSON reports, `17-TOLERANCE-RESULTS.md` documenting each fixture's final SC4 verdict, and a ROADMAP.md update closing out Phase 17's Success Criteria with real outcomes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-capture all three fixtures on this (Windows) machine with the final derived tolerances</name>
  <files>.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/</files>
  <read_first>
    SuperGenius/test/src/processing_dispatch/lighting-fixture-definition.json, SuperGenius/test/src/processing_dispatch/blending-fixture-definition.json, SuperGenius/test/src/processing_dispatch/texturing-fixture-definition.json (confirm each now declares its final `byteQuantMode` from 17-06/17-07 before capturing -- do not proceed if any is still missing its `parameters` entry)
  </read_first>
  <action>
    Rebuild `capture_harness` (`cmake --build SuperGenius/build/Windows/Debug --target capture_harness --config Debug`) and re-run all three fixtures exactly as in 17-05-PLAN.md Task 1, `--repeat 3`, with distinct Round-2 labels (`--label xhw-render-lighting-r2`, `-blending-r2`, `-texturing-r2`) so Round 1's `.cap` files are never overwritten -- both rounds must remain on disk for comparison/audit. Confirm each run's stdout reports stability and a "wrote" success line.
  </action>
  <verify>
    <automated>ls .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/ | grep -c "xhw-render-lighting-r2_\|xhw-render-blending-r2_\|xhw-render-texturing-r2_" | grep -qE "^[3-9]$|^[1-9][0-9]+$"</automated>
  </verify>
  <acceptance_criteria>
    - Three new Round-2 `.cap` files exist, distinct from Round 1's files (both sets present on disk).
    - Each of the three fixture JSON files' `parameters` array was confirmed to declare a `byteQuantMode` before capturing (not still absent).
  </acceptance_criteria>
  <done>This machine's half of Round 2's six-file capture set exists, using the final empirically-derived tolerances.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Hands-on capture on the Mac mini (Round 2, all three fixtures with final tolerances)</name>
  <files>.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/ (3 new Mac-origin Round-2 .cap files)</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-05-PLAN.md (Task 2 -- this plan's own Round 1 checkpoint, the exact pattern this task repeats for Round 2)
  </read_first>
  <action>
    Present the following to the user and wait for their confirmation. Three Round-2 `capture_harness` runs already completed on this (Windows) machine (Task 1), now using each fixture's final empirically-derived `byteQuantMode`.

    On the Mac mini, with the repo updated to include 17-06/17-07's fixture JSON changes and `capture_harness` rebuilt, run the identical three Round-2 commands (`--label xhw-render-lighting-r2`/`-blending-r2`/`-texturing-r2`, `--repeat 3`, same `--fixture-root`/`--fixture` pairs as Task 1). Transfer the three resulting `.cap` files into `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/` in this repo.
  </action>
  <resume-signal>Reply "captured" once all three Round-2 Mac-side `.cap` files are present in this phase's `captures/` directory.</resume-signal>
  <verify>
    <automated>captures_dir=".planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures"; [ -d "$captures_dir" ] && [ "$(ls "$captures_dir" | grep -c "xhw-render-lighting-r2_\|xhw-render-blending-r2_\|xhw-render-texturing-r2_")" -ge "6" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `captures/` contains at least 6 `xhw-render-<technique>-r2_*.cap` files total (3 from Task 1's Windows run, 3 from this task's Mac run).
    - Both machines captured using a checkout that includes 17-06/17-07's final `byteQuantMode` values.
  </acceptance_criteria>
  <done>All three fixtures are re-captured on both machines using their final empirically-derived tolerances, completing Round 2's six-file set.</done>
</task>

<task type="auto">
  <name>Task 3: Diff Round 2, report SC4's verdict per fixture, and close out ROADMAP.md's Phase 17 Success Criteria</name>
  <files>.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/, .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md, .planning/workstreams/sgproc-render/ROADMAP.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md (the exact honest-reporting convention for a hash-match vs. residual-gap outcome -- SC1 Refit section -- this task's per-fixture reporting mirrors)
    .planning/workstreams/sgproc-render/ROADMAP.md (Phase 17 section -- Success Criteria 1-5, to be annotated with real per-fixture outcomes, mirroring how Phase 13/16 closed out their own roadmap sections)
  </read_first>
  <action>
    Run `capture_diff --element-type uint8` for each of the three Round-2 Mac-vs-Windows pairs, `--json-output captures/diff-render-<technique>-r2.json`.

    Create `17-TOLERANCE-RESULTS.md`: one subsection per fixture reporting Round 2's `contentHashMatch`/`chunkHashesMatch`/`maxAbsDelta` alongside the fixture's final `byteQuantMode` value and Round 1's original raw `maxAbsDelta` (side by side, so the before/after effect of quantization is visible). For each fixture, state plainly one of three outcomes: (a) hash match now confirmed (SC4 fully satisfied for that fixture); (b) a residual gap remains, characterized with the same specificity `13-SCOPE-BOUNDARY.md` used for VALD-01 (which specific hash comparison still mismatches, by how much, and an honest hypothesis if one is warranted -- never silently reinterpreted as passing); (c) the fixture never diverged in the first place (Round 1 already showed zero delta), so SC4 is trivially satisfied but this is noted as a "did not stress cross-hardware behavior as much as hoped" finding, not hidden.

    Edit `ROADMAP.md`'s Phase 17 section (scoped `Edit` calls only) to reflect completion: change the phase's checkbox/status line to closed, and annotate Success Criteria 1-5 each with a one-line real outcome (e.g. "SC4: lighting hash-matched; blending hash-matched; texturing residual gap of &lt;N&gt; documented in 17-TOLERANCE-RESULTS.md"), mirroring Phase 13's own SC-annotation-on-close precedent. Update the `Plans` count/list to the actual 8 plans this phase produced, and the `Progress` table's Phase 17 row (Plans Complete, Status, Completed date).
  </action>
  <verify>
    <automated>test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md &amp;&amp; test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-lighting-r2.json &amp;&amp; test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-blending-r2.json &amp;&amp; test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-texturing-r2.json &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three Round-2 `capture_diff` JSON reports exist.
    - `17-TOLERANCE-RESULTS.md` states an explicit, real (a)/(b)/(c) outcome for each of the three fixtures, sourced directly from the JSON reports -- no invented numbers, no silent reinterpretation of a mismatch as a pass.
    - `ROADMAP.md`'s Phase 17 Success Criteria 1-5 are each annotated with a real per-fixture outcome, applied via scoped `Edit` calls (the rest of the file is untouched).
  </acceptance_criteria>
  <done>SC4's cross-hardware hash-match claim is proven or its residual gap is honestly characterized, per fixture, and ROADMAP.md accurately reflects Phase 17's real completion state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator-supplied Round-2 Mac-side `.cap` files -> relocated into this repo | Same accepted boundary as 17-05/T-17-12. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-19 | Repudiation | Reporting SC4 as satisfied without a fresh Round-2 capture (reusing Round 1's raw numbers) | high | mitigate | Task 1/2 perform a genuinely NEW two-machine capture with the final `byteQuantMode` wired in, distinct `.cap` files from Round 1, mirroring 13-05-PLAN.md's own re-capture-after-change precedent -- never a reinterpretation of stale data. |
| T-17-20 | Repudiation | A residual cross-hardware gap silently reported as "passing" to satisfy SC4's letter | high | mitigate | 17-TOLERANCE-RESULTS.md's action explicitly requires one of three honest outcomes per fixture, mirroring `13-SCOPE-BOUNDARY.md`'s convention -- outcome (b) exists specifically so a real gap is never hidden. |

No package-manager installs occur in this plan. The npm/pip/cargo Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. Round 2's six `.cap` files exist, distinct from Round 1's.
2. Three fresh `capture_diff` JSON reports exist.
3. `17-TOLERANCE-RESULTS.md` states a real, honest outcome for each fixture.
4. `ROADMAP.md`'s Phase 17 section reflects the phase's actual completion state.
</verification>

<success_criteria>
Phase 17's final empirical proof point is complete: each fixture's real, empirically-derived tolerance is validated (or its residual gap honestly characterized) against a fresh two-machine capture -- RENDTOL-01 and RENDTOL-02 both fully closed, with the phase's own documentation reflecting the true outcome rather than an assumed one.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-08-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New directory contents under `captures/`: six Round-2 `.cap` files, three `diff-render-<technique>-r2.json` reports.
- New file `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md`.
- Updated `ROADMAP.md` Phase 17 section (Success Criteria annotations, Plans list/count, Progress table row).
