---
phase: 17-render-path-cross-hardware-tolerance
plan: 09
type: execute
wave: 8
depends_on: ["17-08"]
files_modified:
  - SuperGenius/SGProcessingManager/include/util/diff_utils.hpp
  - SuperGenius/SGProcessingManager/src/util/diff_utils.cpp
  - SuperGenius/SGProcessingManager/test/util/diff_utils_test.cpp
  - SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp
  - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/
  - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md
  - .planning/workstreams/sgproc-render/REQUIREMENTS.md
  - .planning/workstreams/sgproc-render/ROADMAP.md
autonomous: true
gap_closure: true
requirements: [RENDTOL-02]

must_haves:
  truths:
    - "RENDTOL-02's tolerance mechanism is now proven independently against real cross-hardware capture data from EACH of RENDTOL-01's three fixtures: lighting and texturing via the existing strict hash-match (already proven in Wave 7, unchanged by this plan) and blending via a new raw-buffer numeric-tolerance check (this plan) that reuses production's own IsByteChunkWithinTolerance bound formula, unmodified, against blending's already-captured Round 2 preQuantizeBytes -- closing the last of the three fixtures without a new hardware capture round (D-10/D-11)."
    - "The pre-existing strict hash-based capture_diff comparison for blending (quantizedBytes-based, contentHashMatch=false, maxAbsDelta=64.0) is unchanged and is never silently reinterpreted as passing -- the new raw-tolerance result is reported alongside it, in the same JSON file and the same markdown doc, so a future reader sees both numbers and understands why RENDTOL-02 is now satisfied despite the strict number still showing a mismatch."
    - "capture_diff's own SC4 verification methodology now applies the identical raw-buffer tolerance bound production's ValidateResults/AttemptToleranceFallback already applies (IsByteChunkWithinTolerance, called on preQuantizeBytes) -- not a new, second tolerance mechanism, and QuantizeByteBuffer/IsByteChunkWithinTolerance/AttemptToleranceFallback are not modified by this plan (D-11's explicit constraint)."
    - "REQUIREMENTS.md's RENDTOL-02 row/traceability table and ROADMAP.md's Phase 17 SC4 annotation and phase-summary bullet are updated to cite this plan's proof, replacing the now-resolved 'human decision needed' language (D-10 recorded the decision; this plan proves it)."
  artifacts:
    - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-blending-r2-rawtolerance.json
    - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md
  key_links:
    - "Fixture's declared byteQuantMode -> new capture_diff --byte-quant-mode CLI flag -> new IsByteChunkWithinToleranceForMode wrapper -> existing IsByteChunkWithinTolerance (unmodified) -> the exact bound formula AttemptToleranceFallback already applies in production -- the connection proving Phase 17's own verification methodology now matches what production enforces."
    - "Round 2's already-on-disk .cap files' CaptureRecord::preQuantizeBytes field (recorded unconditionally at capture time, never previously read by capture_diff) -> the new raw-tolerance check -- the connection proving no new hardware capture round was required to close this gap."
---

<objective>
Close 17-VERIFICATION.md's Gap 1 (RENDTOL-02's blending residual gap) by implementing the user's locked resolution (17-CONTEXT.md D-10: numeric-tolerance fallback) via D-11's grounded finding: extend `capture_diff`'s own SC4 verification methodology to apply the same raw-buffer tolerance check (`IsByteChunkWithinTolerance`) production's `ValidateResults`/`AttemptToleranceFallback` already applies to raw pre-quantization bytes -- then prove it against blending's Round 2 capture data that already exists on disk, with no new hardware capture round. Also close 17-VERIFICATION.md's Gap 2 residual (REQUIREMENTS.md's RENDTOL-02 annotation, the one Gap-2 item not already resolved by the prior "sync REQUIREMENTS.md with verified phase outcome" commit).

Purpose: `capture_diff.cpp` (Phase 10) never called the tolerance-check functions Phase 15 added to `diff_utils.cpp` -- it only ever compared `contentHashMatch` on already-quantized bytes, a strictly stricter bar than production's own real validation path. Blending's raw cross-hardware divergence (maxAbsDelta=1.0, confirmed in both Round 1 and preserved unchanged in Round 2's `preQuantizeBytes`) is trivially within byteQuantMode=6's tolerance bound (63) -- exactly the numeric-tolerance fallback the user chose (D-10), already proven correct in production, just never checked by this phase's own offline verification tool.
Output: a new `IsByteChunkWithinToleranceForMode` helper in `diff_utils.hpp`/`.cpp` (+ unit tests), a new `--byte-quant-mode` raw-tolerance mode in `capture_diff.cpp`, a new evidence file proving blending's tolerance check passes against already-captured data, and REQUIREMENTS.md/ROADMAP.md updated to reflect RENDTOL-02's closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-VERIFICATION.md
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IsByteChunkWithinToleranceForMode to diff_utils (reuses IsByteChunkWithinTolerance unmodified)</name>
  <files>SuperGenius/SGProcessingManager/include/util/diff_utils.hpp, SuperGenius/SGProcessingManager/src/util/diff_utils.cpp, SuperGenius/SGProcessingManager/test/util/diff_utils_test.cpp</files>
  <read_first>
    SuperGenius/SGProcessingManager/include/util/diff_utils.hpp (full file -- existing declarations, ElementDiffStats shape, IsByteChunkWithinTolerance's exact signature/doc comment to mirror)
    SuperGenius/SGProcessingManager/src/util/diff_utils.cpp (full file -- IsByteChunkWithinTolerance's implementation at the bottom of the file; TryGetDeclaredByteQuantMode's [0,8] validation; do not modify either)
    SuperGenius/SGProcessingManager/test/util/diff_utils_test.cpp:56-67 (the MakeParameters helper's exact set_name/set_type/set_parameter_default construction pattern to mirror for the new wrapper's internal Parameter construction), and lines 196-214 (IsByteChunkWithinToleranceUsesMaskBoundWhenDeclaredPasses/Fails -- the exact test structure/assertions the new tests below extend)
    SuperGenius/SGProcessingManager/generated/Parameter.hpp (confirms set_parameter_default takes a plain nlohmann::json, so a bare int argument converts implicitly -- no manual JSON construction needed)
    SuperGenius/SGProcessingManager/test/util/CMakeLists.txt (confirms diff_utils_test is already registered as ctest target DiffUtilsTest against library sgprocmanagerdiff -- no CMakeLists edit needed for this task)
  </read_first>
  <behavior>
    - Test 1: IsByteChunkWithinToleranceForMode(a, b, byteQuantMode=6, statsOut) where a/b are single-byte buffers differing by exactly 63 ((1&lt;&lt;6)-1, the mask bound) -&gt; returns true.
    - Test 2: same byteQuantMode=6, a/b differing by exactly 64 (one past the bound) -&gt; returns false.
    - Test 3 (regression anchor tied to this gap): byteQuantMode=6, a/b differing by exactly 1 -- blending's real measured raw maxAbsDelta from both Round 1's diff-render-blending.json and Round 2's preserved preQuantizeBytes -&gt; returns true. This is the exact numeric fact Task 2 later confirms against real capture-file bytes.
  </behavior>
  <action>
    In diff_utils.hpp, declare a new free function IsByteChunkWithinToleranceForMode in namespace sgns::sgprocmanagerdiff, taking (const std::vector&lt;uint8_t&gt; &amp;a, const std::vector&lt;uint8_t&gt; &amp;b, int byteQuantMode, ElementDiffStats &amp;statsOut) and returning bool. Document it as a convenience wrapper for CLI/tool callers (capture_diff) that know a numeric byteQuantMode directly from a command-line flag and have no job Parameter array to build IsByteChunkWithinTolerance's existing parameters argument from -- it must build a single-entry std::vector&lt;sgns::Parameter&gt; declaring "byteQuantMode" of type INT with the given value (mirroring diff_utils_test.cpp's MakeParameters construction exactly: default-construct a Parameter, call set_name/set_type/set_parameter_default) and delegate to IsByteChunkWithinTolerance unmodified, passing the address of that vector. Do not alter IsByteChunkWithinTolerance's own body or TryGetDeclaredByteQuantMode's validation logic -- this is a pure additive wrapper, not a refactor, matching diff_utils.cpp's existing "isolated duplicate, never call into quantization.cpp's own resolver" discipline for parameter lookups.

    Implement the function in diff_utils.cpp near the bottom of the file, after IsByteChunkWithinTolerance's existing definition.

    Add the three TEST_F cases from the behavior list above to diff_utils_test.cpp's DiffUtilsTest fixture, placed after the existing IsByteChunkWithinTolerance* test group. Name them IsByteChunkWithinToleranceForModeUsesMaskBoundWhenDeclaredPasses, IsByteChunkWithinToleranceForModeUsesMaskBoundWhenDeclaredFails, and IsByteChunkWithinToleranceForModeMatchesBlendingRealDelta respectively, following the file's existing BuildByteBytes/ElementDiffStats usage pattern exactly (no new helper needed beyond what already exists in this file).
  </action>
  <verify>
    <automated>cd SuperGenius &amp;&amp; cmake --build build/Windows/Debug --target diff_utils_test --config Debug &amp;&amp; ctest --test-dir build/Windows/Debug -R DiffUtilsTest -C Debug --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - IsByteChunkWithinToleranceForMode exists in diff_utils.hpp/.cpp, delegates to IsByteChunkWithinTolerance without modifying it.
    - All three new TEST_F cases pass, plus every pre-existing DiffUtilsTest case still passes (no regression).
    - No edit was made to IsByteChunkWithinTolerance, TryGetDeclaredByteQuantMode, or any quantization.cpp/.hpp symbol.
  </acceptance_criteria>
  <done>A production-identical raw-buffer tolerance check is available to any caller that only has a bare byteQuantMode integer (not a full job Parameter array), unit-proven against the exact mask-boundary values and against blending's real 1-unit raw delta.</done>
</task>

<task type="auto">
  <name>Task 2: Wire --byte-quant-mode into capture_diff and prove it against blending's already-captured Round 2 data</name>
  <files>SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp, .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-blending-r2-rawtolerance.json</files>
  <read_first>
    SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp (full file -- CliArgs/ParseArgs's existing argv-pair convention for --element-type/--json-output; the haveRecords-guarded block that computes lastRecordA/lastRecordB and the per-element numeric pass over their quantizedBytes; the nlohmann::json report-building block and its console-output block, both to extend additively)
    SuperGenius/SGProcessingManager/tools/capture/capture_file_format.hpp:52-59 (CaptureRecord::preQuantizeBytes/quantizedBytes -- confirms preQuantizeBytes is recorded unconditionally at capture time, independent of whatever maskBits was active, so Round 2's blending .cap files already contain the real raw pre-quantization bytes even though byteQuantMode=6 was declared when they were captured)
    .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md (Blending Fixture section -- Round 1's maxAbsDelta=1.0 and Round 2's quantized maxAbsDelta=64.0, the two numbers this task's new report must reconcile without contradicting either)
  </read_first>
  <action>
    Add a new CLI argument --byte-quant-mode &lt;N&gt; to capture_diff's CliArgs/ParseArgs, following the existing argv-pair parsing convention (an optional int field plus a "declared" boolean, since 0 is a valid value distinct from "not supplied"). At parse time, validate the argument parses as an integer in the inclusive range [0,8]; on any parse failure or out-of-range value, print a usage error naming the offending value and fail closed (return false from ParseArgs), mirroring --element-type's existing strict-validation style -- do not silently fall back to an unchecked or default value here.

    In main(), inside the existing haveRecords-guarded block (where lastRecordA/lastRecordB are already computed from rawRecordsPerArtifact[0].back()), add a new step that runs only when --byte-quant-mode was supplied AND args.elementType == "uint8": call the new IsByteChunkWithinToleranceForMode(lastRecordA.preQuantizeBytes, lastRecordB.preQuantizeBytes, byteQuantMode, rawStats) from diff_utils.hpp (already linked -- capture_diff already depends on sgprocmanagerdiff), capturing its bool return as withinTolerance. When --byte-quant-mode was supplied but args.elementType == "float32", print an informational stderr note that byteQuantMode only applies to the uint8 render path and skip the check (do not fail the whole run).

    Extend the JSON report with a new nested object "rawToleranceCheck" containing: "checked" (true only when the check actually ran), and, only in that case, "byteQuantMode" (the supplied N), "withinTolerance", and every ElementDiffStats field from rawStats (elementCount, maxAbsDelta, maxRelDelta, maxUlpDistance, percentExceedingThreshold, sizeMismatch). When not checked, emit only the "checked": false field (no numeric fields). This is purely additive -- every existing top-level report field (built from quantizedBytes, unchanged) keeps its current meaning and current values; this new nested object is the only new content. Add a matching console-output line reporting the raw-tolerance verdict when checked.

    Rebuild capture_diff, then run it against the two Round 2 blending capture files already present on disk in this phase's captures/ directory (xhw-render-blending-r2_Fuus-Mac-mini.local---macOS_20260820T070308.cap and xhw-render-blending-r2_Mofu---Windows_20260820T031711.cap -- locate via the xhw-render-blending-r2_* glob if timestamps differ from what is named here) with --element-type uint8 --byte-quant-mode 6, writing to captures/diff-render-blending-r2-rawtolerance.json. No new capture_harness run and no new hardware session are required -- these two .cap files already hold each machine's real preQuantizeBytes recorded during Wave 7's Round 2 capture.
  </action>
  <verify>
    <automated>captures_dir=".planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures"; grep -A 12 '"rawToleranceCheck"' "$captures_dir/diff-render-blending-r2-rawtolerance.json" | grep -q '"withinTolerance": true' &amp;&amp; grep -A 12 '"rawToleranceCheck"' "$captures_dir/diff-render-blending-r2-rawtolerance.json" | grep -q '"byteQuantMode": 6' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - capture_diff accepts --byte-quant-mode and rejects an out-of-range/non-numeric value with a clear usage error.
    - diff-render-blending-r2-rawtolerance.json exists, its rawToleranceCheck.byteQuantMode is 6, its rawToleranceCheck.maxAbsDelta equals 1.0 (matching Round 1's independently-measured raw delta exactly, confirming preQuantizeBytes coherence across rounds), and its rawToleranceCheck.withinTolerance is true.
    - Every pre-existing top-level field in the report (contentHashMatch, maxAbsDelta from quantizedBytes, etc.) is present and numerically unchanged from what Round 2's original diff-render-blending-r2.json already showed (64.0/false) -- the new field is additive, not a replacement.
  </acceptance_criteria>
  <done>capture_diff can independently prove, from already-captured evidence and without any new hardware session, that blending's byteQuantMode=6 is a valid numeric-tolerance fallback for its real raw cross-hardware divergence -- mirroring the exact check production's ValidateResults already performs.</done>
</task>

<task type="auto">
  <name>Task 3: Document the gap closure and update REQUIREMENTS.md/ROADMAP.md</name>
  <files>.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md, .planning/workstreams/sgproc-render/REQUIREMENTS.md, .planning/workstreams/sgproc-render/ROADMAP.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md (full file -- append after, do not alter, the existing Blending Fixture section and Summary Table; this task's new section must not contradict or restate incorrect numbers from what is already there)
    .planning/workstreams/sgproc-render/REQUIREMENTS.md (lines 19-22 RENDTOL-01/02 bullets, lines 52-62 Traceability table -- the exact current wording to replace via scoped Edit)
    .planning/workstreams/sgproc-render/ROADMAP.md (Phase 17 milestone-list bullet around line 78, Success Criteria item 4 around lines 119-121, Plans section lines 124-154, Progress table row for Phase 17 around line 204 -- all via scoped Edit calls, never a full-file Write)
  </read_first>
  <action>
    Append a new "## Gap Closure Addendum (RENDTOL-02 numeric-tolerance fallback, D-10/D-11)" section to the end of 17-TOLERANCE-RESULTS.md via Edit. State plainly: the strict quantized-hash comparison capture_diff already performed for blending remains unchanged (contentHashMatch=false, maxAbsDelta=64.0, cite the existing Blending Fixture section above rather than re-deriving); capture_diff was extended with an opt-in --byte-quant-mode raw-tolerance mode that calls IsByteChunkWithinTolerance (the same function/bound formula production's AttemptToleranceFallback already applies to raw pre-quantization bytes) against the already-captured Round 2 blending .cap files' preQuantizeBytes; the result (diff-render-blending-r2-rawtolerance.json) shows the real raw cross-hardware delta (1.0) is within byteQuantMode=6's tolerance bound (63), withinTolerance=true; conclude that RENDTOL-02 is now satisfied for blending via the user's chosen numeric-tolerance-fallback path (17-CONTEXT.md D-10), proven with the exact mechanism production already relies on (D-11), without any change to QuantizeByteBuffer, IsByteChunkWithinTolerance, or any ValidateResults code. State explicitly that the strict hash-based number is not reinterpreted as passing -- both results coexist and answer different questions, exactly as D-11's grounding research anticipated.

    Edit REQUIREMENTS.md: change RENDTOL-02's checkbox from [ ] to [x] and rewrite its bullet to state completion via the numeric-tolerance-fallback proof, citing 17-TOLERANCE-RESULTS.md's new addendum section (keep RENDTOL-01's already-correct bullet untouched). Update the Traceability table's RENDTOL-02 row from "Partial -- blending residual gap open (17-VERIFICATION.md gap #1), human decision needed" to "Complete -- numeric-tolerance fallback proven (17-TOLERANCE-RESULTS.md Gap Closure Addendum)". Leave the Coverage summary counts as-is (they track requirement-to-phase mapping, not completion status, and are already correct at 8/8 mapped).

    Edit ROADMAP.md's Phase 17 section: update the milestone-list bullet (currently "closed with a residual gap -- blending's byteQuantMode=6 does not close cross-hardware divergence...") to reflect the gap-closure outcome (mechanism proven via numeric-tolerance fallback, citing this plan). Update Success Criteria item 4's blending outcome text to note the residual gap is now closed via the raw-tolerance check while still honestly noting the quantized-hash number is unchanged. Add a "**Wave 8**" heading under Phase 17's Plans list with a "- [x] 17-09-PLAN.md -- ..." entry, update the "**Plans**:" count from 8/8 to 9/9, and update the "## Progress" table's Phase 17 row "Plans Complete" column from 8/8 to 9/9.
  </action>
  <verify>
    <automated>grep -A2 "RENDTOL-02" .planning/workstreams/sgproc-render/REQUIREMENTS.md | grep -qi "complete" &amp;&amp; grep -q "Gap Closure Addendum" .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md &amp;&amp; grep -q "17-09" .planning/workstreams/sgproc-render/ROADMAP.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md's RENDTOL-02 checkbox is [x] and its bullet/traceability row cite the new evidence, replacing "human decision needed".
    - 17-TOLERANCE-RESULTS.md contains a new Gap Closure Addendum section documenting both the unchanged strict-hash number and the new raw-tolerance proof, side by side, honestly.
    - ROADMAP.md's Phase 17 section lists 17-09-PLAN.md, updates the Plans count to 9/9, and its SC4/milestone-bullet wording reflects the closed gap without erasing the historical record that the quantized-hash comparison itself still mismatches.
  </acceptance_criteria>
  <done>RENDTOL-02 is fully closed for all three RENDTOL-01 fixtures, documented honestly, with REQUIREMENTS.md/ROADMAP.md reflecting the true, evidence-backed completion state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Existing on-disk Round 1/Round 2 `.cap` files (already accepted per T-17-12/T-17-19) -> re-read by the extended `capture_diff` | Same operator-produced-hardware boundary already accepted in Waves 4/7 -- no new external input, no new trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-24 | Tampering | New `--byte-quant-mode` CLI argument (`capture_diff`) | medium | mitigate | `ParseArgs` validates the value is an integer in `[0,8]` at parse time and fails closed (usage error, non-zero exit) on any invalid/out-of-range value, mirroring `--element-type`'s existing strict-validation convention -- never a silent fallback that could make a misconfigured check look like a pass. |
| T-17-25 | Repudiation | Reporting RENDTOL-02 as "Complete" while the strict quantized-hash comparison for blending still fails | high | mitigate | Task 3's addendum and the REQUIREMENTS.md/ROADMAP.md edits explicitly require both numbers (quantized-hash `maxAbsDelta=64.0`/`false`, raw-tolerance `maxAbsDelta=1.0`/`true`) documented side by side with an explanation of why both are true simultaneously -- never a claim of unqualified pass. |
| T-17-26 | Tampering | Malformed/oversized `.cap` file content read by `capture_diff` | low | accept | Unchanged, pre-existing mitigation: `DeserializeCaptureFile`'s bounds-checked reads (T-10-02/T-10-01a) already guard every length/count field before allocation; this plan adds no new deserialization path, only a new comparison over already-validated `preQuantizeBytes`. |

No package-manager installs occur in this plan (existing `capture_diff`/`diff_utils_test` targets, rebuilt as-is). The npm/pip/cargo Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. `IsByteChunkWithinToleranceForMode` exists, is unit-tested (3 new cases + no regressions), and delegates to `IsByteChunkWithinTolerance` unmodified.
2. `capture_diff` accepts `--byte-quant-mode`, validates it, and produces a `rawToleranceCheck` object additively alongside its existing report fields.
3. `diff-render-blending-r2-rawtolerance.json` proves blending's byteQuantMode=6 is within tolerance of the real raw cross-hardware delta, using already-captured Round 2 data (no new hardware capture).
4. `17-TOLERANCE-RESULTS.md`/`REQUIREMENTS.md`/`ROADMAP.md` all reflect RENDTOL-02's closure honestly, without erasing the still-true strict-hash-mismatch fact.
</verification>

<success_criteria>
RENDTOL-02 is fully closed: the render output path's schema-configurable tolerance mechanism is proven, for every one of RENDTOL-01's three fixtures, against real cross-hardware capture data -- lighting/texturing via the existing strict hash-match, blending via the newly-added raw-buffer numeric-tolerance check that mirrors production's own `AttemptToleranceFallback` exactly. No production code (`QuantizeByteBuffer`, `IsByteChunkWithinTolerance`, `ValidateResults`) was modified. REQUIREMENTS.md and ROADMAP.md accurately reflect this closure.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New function `sgns::sgprocmanagerdiff::IsByteChunkWithinToleranceForMode` in `SuperGenius/SGProcessingManager/include/util/diff_utils.hpp` / `src/util/diff_utils.cpp`.
- New unit tests in `SuperGenius/SGProcessingManager/test/util/diff_utils_test.cpp`: `IsByteChunkWithinToleranceForModeUsesMaskBoundWhenDeclaredPasses`, `IsByteChunkWithinToleranceForModeUsesMaskBoundWhenDeclaredFails`, `IsByteChunkWithinToleranceForModeMatchesBlendingRealDelta`.
- New `--byte-quant-mode <N>` CLI argument and `rawToleranceCheck` JSON/console output in `SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp`.
- New evidence file `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-blending-r2-rawtolerance.json`.
- New "Gap Closure Addendum" section appended to `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-TOLERANCE-RESULTS.md`.
- Updated `REQUIREMENTS.md` (RENDTOL-02 bullet + Traceability table row) and `ROADMAP.md` (Phase 17 milestone bullet, SC4 annotation, Plans list/count, Progress table row).
