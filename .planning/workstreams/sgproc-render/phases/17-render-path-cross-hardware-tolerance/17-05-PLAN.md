---
phase: 17-render-path-cross-hardware-tolerance
plan: 05
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-04"]
files_modified:
  - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/
  - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md
autonomous: false
requirements: [RENDTOL-01, RENDTOL-02]

must_haves:
  truths:
    - "All three new fixtures (lighting, blending, texturing) are captured via capture_harness --repeat 3 on both machines of Phase 11's exact two-machine dataset (Mac mini + Mofu/Windows, D-09), with byteQuantMode undeclared so ResolveByteQuantMode's N=0 identity fallback applies -- measuring genuine raw divergence, per D-06 methodology step 1."
    - "capture_diff --element-type uint8 is run for all three Mac-vs-Windows pairs, and the resulting maxAbsDelta/contentHashMatch/chunkHashesMatch numbers are recorded honestly in 17-CAPTURE-RESULTS-ROUND1.md -- whether divergence is large, small, or zero -- mirroring 11-CAPTURE-RESULTS.md's and 13-SCOPE-BOUNDARY.md's honest-reporting convention, never silently declared passing or interpreted favorably."
  artifacts:
    - .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md
  key_links:
    - "This plan's raw maxAbsDelta-per-fixture numbers <-> Wave 5's 17-06/17-07 binary-search derivation -- D-06's methodology cannot begin without this plan's real captured data; the derivation plans read this file's numbers directly, not a guess."
---

<objective>
Gather Round 1 of this phase's two required cross-machine empirical rounds (D-09): run all three new fixtures (lighting, blending, texturing) through `capture_harness` on both machines of Phase 11's exact dataset, with no `byteQuantMode` declared (the raw, unquantized N=0 baseline), then `capture_diff` each Mac-vs-Windows pair and record the results honestly. This is the empirical input Wave 5's tolerance-derivation plans (17-06/17-07) need before they can meaningfully binary-search a `byteQuantMode` value for any fixture.

Purpose: satisfies RENDTOL-01's SC2 (real, non-zero-or-honestly-zero captured divergence, proving the fixtures actually stress cross-hardware floating-point behavior) and supplies the raw-delta numbers RENDTOL-02's tolerance derivation is built on.
Output: six new `.cap` files (3 fixtures x 2 machines) plus three `capture_diff` JSON reports, all under this phase's `captures/` directory, and `17-CAPTURE-RESULTS-ROUND1.md` documenting each fixture's `maxAbsDelta`/`contentHashMatch`/`chunkHashesMatch` honestly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture all three fixtures on this (Windows) machine</name>
  <files>.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/</files>
  <read_first>
    SuperGenius/SGProcessingManager/tools/capture/capture_harness.cpp:1-30,60-130 (CLI usage, `--repeat` >= 2 requirement, `--fixture-root`/`--fixture`/`--label`/`--output-dir` semantics)
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md (the exact `xhw-render_<hostname>---<OS>_<timestamp>.cap` naming convention this task's `--label` values must continue, and the results-table format Task 3 mirrors)
  </read_first>
  <action>
    Confirm `capture_harness` is current: `cmake --build SuperGenius/build/Windows/Debug --target capture_harness --config Debug`. Run it three times, once per fixture, `--repeat 3` (matching Phase 11's own repeat count, satisfying `capture_harness`'s built-in same-machine stability self-check), each with a distinct `--label` continuing the `xhw-render-<technique>` naming convention: `--label xhw-render-lighting`, `--label xhw-render-blending`, `--label xhw-render-texturing`, each `--fixture-root SuperGenius/test/src --fixture processing_dispatch/<technique>-fixture-definition.json --output-dir .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/`. None of the three fixture JSON files declares a `byteQuantMode` parameter (confirmed by Waves 1-3), so `ResolveByteQuantMode`'s documented N=0 identity fallback applies automatically -- no `--` flag is needed to force this. Confirm each run's stdout reports stability (no "instability detected" message) and prints its "wrote ... bytes" success line.
  </action>
  <verify>
    <automated>ls .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/ | grep -c "xhw-render-lighting_\|xhw-render-blending_\|xhw-render-texturing_" | grep -qE "^[3-9]$|^[1-9][0-9]+$"</automated>
  </verify>
  <acceptance_criteria>
    - Three new `.cap` files exist under `captures/`, one per fixture, named `xhw-render-<technique>_<hostname>---<OS>_<timestamp>.cap`.
    - None of the three `capture_harness` runs reported an "instability detected" warning (same-machine, cross-repeat stability confirmed).
  </acceptance_criteria>
  <done>This machine's half of Round 1's six-file capture set exists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Hands-on capture on the Mac mini (Round 1, all three fixtures)</name>
  <files>.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/ (3 new Mac-origin .cap files)</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-04-PLAN.md (Task 2 -- the exact checkpoint:human-action pattern this task mirrors: sync checkout, rebuild capture_harness, run it per-fixture, consolidate onto this Windows checkout)
  </read_first>
  <action>
    Present the following to the user and wait for their confirmation. Three `capture_harness` runs already completed on this (Windows) machine for the lighting/blending/texturing fixtures (Task 1), each producing a `.cap` file under this phase's `captures/` directory. Round 1's other half -- the same three runs on the Mac mini -- requires physical access this session does not have (D-09, the same hard, non-automatable requirement Phase 11/13 already worked within).

    On the Mac mini (`Fuus-Mac-mini.local`), with this repo checked out at the same commit and `capture_harness` rebuilt (same build convention as Phase 11/13), run the identical three commands from Task 1's action but with `--output-dir` pointed at wherever is convenient on the Mac: `--fixture-root SuperGenius/test/src --fixture processing_dispatch/lighting-fixture-definition.json --label xhw-render-lighting --repeat 3`; the same for `blending-fixture-definition.json`/`xhw-render-blending` and `texturing-fixture-definition.json`/`xhw-render-texturing`. Transfer the three resulting `.cap` files into `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/` in this repo (matching Phase 11/13's own relocate-after-capture precedent).
  </action>
  <resume-signal>Reply "captured" once all three Mac-side `.cap` files are present in this phase's `captures/` directory (mirrors 13-01-PLAN.md's accepted resume-signal precedent -- not independently re-derivable from `.cap` bytes alone).</resume-signal>
  <verify>
    <automated>captures_dir=".planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures"; [ -d "$captures_dir" ] && [ "$(ls "$captures_dir" | grep -c "xhw-render-lighting_\|xhw-render-blending_\|xhw-render-texturing_")" -ge "6" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `captures/` contains at least 6 `xhw-render-<technique>_*.cap` files total (3 from Task 1's Windows run, 3 from this task's Mac run).
    - Both machines' `capture_harness` binaries were rebuilt from the same commit before capturing (user-confirmed via the resume signal).
  </acceptance_criteria>
  <done>All three fixtures are captured on both machines of Phase 11's dataset, completing Round 1's six-file set.</done>
</task>

<task type="auto">
  <name>Task 3: Diff all three Mac-vs-Windows pairs and document Round 1 results honestly</name>
  <files>.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/, .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md</files>
  <read_first>
    SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp:1-90 (CLI usage -- `--a`/`--b`/`--element-type`/`--json-output`)
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md (full file -- the exact results-table structure, per-fixture interpretation prose style, and honest-reporting convention this task's new document must mirror)
  </read_first>
  <action>
    Build `capture_diff` if not already current: `cmake --build SuperGenius/build/Windows/Debug --target capture_diff --config Debug`. Run it three times, once per fixture, `--element-type uint8` (render output is always raw RGBA/RGB pixel bytes, never `float32`, matching the happy-path fixture's own Phase 11 convention): `--a <Mac .cap path> --b <Windows .cap path> --json-output captures/diff-render-<technique>.json` for each of lighting/blending/texturing.

    Create `17-CAPTURE-RESULTS-ROUND1.md` (mirroring `11-CAPTURE-RESULTS.md`'s structure): a short header naming this as Round 1 (raw, byteQuantMode-absent divergence, D-06 methodology step 1 -- Round 2 with the derived tolerance comes later in Wave 6), then one subsection per fixture reporting `elementCount`, `maxAbsDelta`, `maxRelDelta`, `maxUlpDistance` (if applicable to uint8), `contentHashMatch`, `combinedHashMatch`, `chunkHashesMatch`, and the raw machine identifiers from each `.cap` file's own machine tag. Report every number exactly as `capture_diff` produced it -- if a fixture shows zero divergence (mirroring the original happy-path fixture's own surprising zero-divergence result), say so plainly and flag it as a real finding requiring Wave 5's D-08 honest-gap handling if it recurs, not a favorable reinterpretation. Do not average, round, or omit any of the three fixtures' numbers.
  </action>
  <verify>
    <automated>test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md &amp;&amp; test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-lighting.json &amp;&amp; test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-blending.json &amp;&amp; test -f .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/diff-render-texturing.json &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three `capture_diff` JSON reports exist, one per fixture, each run with `--element-type uint8`.
    - `17-CAPTURE-RESULTS-ROUND1.md` names a real numeric `maxAbsDelta` (which may legitimately be `0`) and boolean `contentHashMatch` for each of the three fixtures, sourced directly from the JSON reports (no invented or estimated numbers).
  </acceptance_criteria>
  <done>Round 1's raw cross-hardware divergence data exists for all three fixtures, documented with the same honesty convention as 11-CAPTURE-RESULTS.md/13-SCOPE-BOUNDARY.md, ready for Wave 5's binary-search derivation to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator-supplied Mac-side `.cap` files -> relocated into this repo's `captures/` directory | Same boundary Phase 11/13 already accepted -- files originate from the same operator's own hardware this session already trusts, not an external/untrusted source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-12 | Tampering | Relocated Mac `.cap` files | low | accept | Mirrors T-11-01's already-accepted rationale verbatim -- a plain filesystem move from a directory the same operator just produced, content already self-checked by `capture_harness`'s own stability check before these files existed. |

No package-manager installs occur in this plan (already-built `capture_harness`/`capture_diff` binaries, run as-is). The npm/pip/cargo Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. Six `.cap` files exist (3 fixtures x 2 machines) under this phase's `captures/` directory.
2. Three `capture_diff` JSON reports exist, one per fixture.
3. `17-CAPTURE-RESULTS-ROUND1.md` honestly documents each fixture's raw divergence numbers.
</verification>

<success_criteria>
Round 1's real, two-machine, byteQuantMode-absent divergence data exists for all three fixtures and is documented honestly -- the empirical foundation Wave 5's tolerance derivation requires, and SC2's "real, non-zero captured divergence" claim is either confirmed or its absence is honestly flagged per-fixture.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New directory contents under `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/captures/`: six `.cap` files, three `diff-render-<technique>.json` reports.
- New file `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md`.
