---
phase: 17-render-path-cross-hardware-tolerance
plan: 07
type: execute
wave: 6
depends_on: ["17-06"]
files_modified:
  - SuperGenius/test/src/processing_conformance_security/secv01_render_texturing_counter_test.cpp
  - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw
  - SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
  - SuperGenius/test/src/processing_dispatch/texturing-fixture-definition.json
autonomous: true
requirements: [RENDTOL-02]

must_haves:
  truths:
    - "Texturing's byteQuantMode is derived independently from the other two fixtures' values (D-07 -- no shared/global tolerance), via the same binary-search-against-a-counter-test discipline, starting from texturing's own real captured maxAbsDelta in 17-CAPTURE-RESULTS-ROUND1.md."
    - "The 'wrong' variant substitutes a corrupted source texture image (a fully bit-inverted copy of texturing-source-image.raw) -- the closest structural analog to secv01_tex3d_counter_test.cpp's byte-perturbed-model precedent, applied to a sampled image instead of an MNN model."
    - "If Pitfall 1's filtering-divergence risk materialized in Round 1 (texturing's maxAbsDelta unusually large, or the fixture is already using NEAREST so no filtering divergence should exist at all), that outcome is reflected honestly in this task's derivation and citation trail, per D-08."
  artifacts:
    - SuperGenius/test/src/processing_conformance_security/secv01_render_texturing_counter_test.cpp
    - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw
  key_links:
    - "17-CAPTURE-RESULTS-ROUND1.md's texturing maxAbsDelta <-> this plan's first candidate N -- independent of lighting/blending's own already-derived values from 17-06."
    - "The final chosen N <-> both the counter-test's two inline JSON literals AND texturing-fixture-definition.json's new parameters array -- exact match required."
---

<objective>
Derive texturing's own independent `byteQuantMode` value via binary-search-against-a-counter-test (D-06/D-07), starting from Wave 4's real captured divergence for the texturing fixture specifically -- then prove it via a new dedicated counter-test whose "deliberately wrong" variant substitutes a corrupted source texture image, the closest structural analog to `secv01_tex3d_counter_test.cpp`'s byte-perturbed-model precedent.

Purpose: completes RENDTOL-02's empirical derivation for all three fixtures (the third and largest-scope technique per D-05); sequenced after 17-06 (not parallel with it) because both plans edit the same `processing_conformance_security_test`'s `CMakeLists.txt` source list.
Output: `secv01_render_texturing_counter_test.cpp` (registered in `processing_conformance_security_test`), a bit-inverted corrupted texture asset, and `texturing-fixture-definition.json` updated with its final `byteQuantMode` and citation trail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the corrupted texture asset and prove it diverges before committing</name>
  <files>SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw</files>
  <read_first>
    SuperGenius/test/src/processing_dispatch/texturing-source-image.raw (17-03's real 64x64 RGBA8 checkerboard -- the source this fixture is a corrupted copy of)
    .planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-02-PLAN.md (Task 1 -- the project's established "corrupt a fixture, empirically verify it still loads and diverges before committing" methodology, adapted here to a trivially-parseable raw byte buffer rather than a structured model format)
  </read_first>
  <action>
    Create `secv01-wrong-texture-source-image.raw`: exactly 16384 bytes, byte-for-byte bit-inversion of `texturing-source-image.raw` (`wrongByte = 255 - correctByte` for every one of the 16384 bytes, generated by a short one-off script, not committed -- only the resulting `.raw` output is committed, same convention as 17-03's original asset). Unlike a structured model format (`.mnn`'s flatbuffers header/vtable region), a raw RGBA8 buffer has no parseable structure to accidentally break -- every possible byte value is a valid pixel component, so no fallback-offset search is needed here (unlike 12-02-PLAN.md's model-corruption task). Confirm before committing: the file is exactly 16384 bytes, and every byte differs from the corresponding byte in `texturing-source-image.raw` (a full inversion, not a partial one -- this is the maximal-divergence "wrong" variant, deliberately simpler and more robust than a single-byte flip for a small asset with no structural fragility to avoid).
  </action>
  <verify>
    <automated>test -f SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw &amp;&amp; [ "$(wc -c < SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw)" = "16384" ] &amp;&amp; ! cmp -s SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw SuperGenius/test/src/processing_dispatch/texturing-source-image.raw &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `secv01-wrong-texture-source-image.raw` is exactly 16384 bytes.
    - Every byte differs from `texturing-source-image.raw`'s corresponding byte (full inversion, confirmed via `cmp`/checksum).
  </acceptance_criteria>
  <done>A maximally-corrupted, still-well-formed copy of the texturing source image exists on disk, checked into git.</done>
</task>

<task type="auto">
  <name>Task 2: Write the texturing counter-test and derive the final byteQuantMode</name>
  <files>SuperGenius/test/src/processing_conformance_security/secv01_render_texturing_counter_test.cpp, SuperGenius/test/src/processing_conformance_security/CMakeLists.txt, SuperGenius/test/src/processing_dispatch/texturing-fixture-definition.json</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_security/secv01_render_lighting_counter_test.cpp, SuperGenius/test/src/processing_conformance_security/secv01_render_blending_counter_test.cpp (17-06's just-completed files -- reuse their exact structural shape)
    SuperGenius/test/src/processing_conformance_security/secv01_tex3d_counter_test.cpp (the byte-perturbed-input corruption precedent this task's texture substitution most closely mirrors)
    SuperGenius/test/src/processing_dispatch/texturing-fixture-definition.json (current shape, to mirror inline and update)
    .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md (texturing's real captured `maxAbsDelta` -- read the actual number, do not assume; note Pitfall 1's open question about whether NEAREST filtering produced any measurable divergence at all)
  </read_first>
  <action>
    Create `secv01_render_texturing_counter_test.cpp`, class `Secv01RenderTexturingCounterTest`, `TEST_F` case `TexturingWrongSourceImageStillDiverges`: two inline JSON jobs mirroring `texturing-fixture-definition.json` -- "correct" job's `texture_buffer.source` resolves to `file://processing_dispatch/texturing-source-image.raw`; "wrong" job's second input (`textureData`-equivalent) instead resolves to `file://processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw` (Task 1's fully-inverted asset) -- everything else (shaders, vertex data, `pipeline_state`, `render_target`) byte-identical between the two. Both declare the identical candidate `byteQuantMode` in a `parameters` array. Same `HasUsableVulkanDevice()` skip guard, `Create()`+`Process()`+binary `memcmp` assertion shape as 17-06's two counter-tests.

    Register the new source in `CMakeLists.txt` as the sixth source alongside the five now present. Also add `"${CMAKE_CURRENT_SOURCE_DIR}/fixtures/"`'s existing `copy_directory` POST_BUILD step already covers Task 1's new `fixtures/secv01-wrong-texture-source-image.raw` automatically (no CMake change needed for that file specifically, confirm by inspecting the existing `copy_directory` command).

    Binary-search texturing's final N using the identical D-06 methodology and search-direction discipline as 17-06's two tasks, starting from texturing's own real `maxAbsDelta` (read from `17-CAPTURE-RESULTS-ROUND1.md`). If Round 1 showed zero divergence for texturing (a real possible outcome per Pitfall 1 -- NEAREST filtering is bit-reproducible by construction, so genuine zero cross-hardware divergence is a legitimate, not-a-bug finding, mirroring the original happy-path fixture's own zero-divergence result), the divergence-absorption floor is simply N=0 (no widening needed) -- still run the full binary search against the counter-test to confirm N=0 (or the smallest safe N) does not mask the deliberately-corrupted texture, and document the zero-divergence finding honestly in this task's citation trail rather than skipping the derivation. Apply the same three possible outcomes as 17-06 (failure boundary found -> one step below it; no failure boundary in [0,8] -> D-08 divergence-absorption floor; cannot pass at any N -> honest documented gap).

    Wire the final chosen N into both inline JSON literals and into `texturing-fixture-definition.json`'s new `parameters` array, citing texturing's real measured `maxAbsDelta` (including an explicit "measured zero divergence" note if that was Round 1's actual finding).
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug &amp;&amp; ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv01RenderTexturingCounterTest -V | grep -qE "100% tests passed"</automated>
  </verify>
  <acceptance_criteria>
    - `secv01_render_texturing_counter_test.cpp` asserts via `std::memcmp` on `.artifacts[0].artifactId`.
    - `CMakeLists.txt` lists all six sources (three original + 17-06's two + this task's one).
    - `texturing-fixture-definition.json`'s new `byteQuantMode` default exactly matches both of this task's counter-test inline JSON literals, with a real-`maxAbsDelta`-citing description (including an explicit statement if the measured value was zero).
    - `ctest -R Secv01RenderTexturingCounterTest` reports 100% tests passed at the final chosen N -- OR the SUMMARY documents a D-08 honest gap explicitly.
  </acceptance_criteria>
  <done>Texturing's byteQuantMode is empirically derived (independent of lighting/blending's own values) and proven (or honestly documented as unable to be proven) not to mask a fully-corrupted source texture -- RENDTOL-02's derivation complete for all three fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Deliberately-inverted texture image -> real sampled-texture render pipeline -> post-quantization `artifactId` | The adversarial-simulation boundary this counter-test exists to prove, for the largest-scope (D-05) of the three fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-16 | Tampering / Spoofing | Substituted source texture image at texturing's own derived tolerance | critical | mitigate | `TexturingWrongSourceImageStillDiverges` asserts the corrupted-texture job's post-quantization `artifactId` differs from the correct job's at the final chosen N. |
| T-17-17 | Tampering | Choosing an N wide enough for texturing's real divergence but loose enough to mask a substituted image | high | mitigate | D-06's binary-search-against-this-counter-test methodology, same discipline as 17-06, applied independently to texturing's own real data (D-07 -- no shared tolerance across fixtures). |
| T-17-18 | Repudiation | texturing's final `byteQuantMode` chosen without a traceable citation, including the zero-divergence case | medium | mitigate | `parameters[].description` cites the real measured `maxAbsDelta` (explicitly noting zero if that was the Round 1 finding) -- never an unsourced constant. |

No package-manager installs occur in this plan. The npm/pip/cargo Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. The corrupted texture asset exists, fully inverted, exactly 16384 bytes.
2. The new counter-test asserts via `.artifacts[0].artifactId`, registered as the sixth `processing_conformance_security_test` source.
3. Texturing's final `byteQuantMode` is derived from its own real captured `maxAbsDelta` (including the zero-divergence case, if that is what Round 1 found), independent of lighting/blending's own values.
4. Production fixture JSON and counter-test agree exactly on the final value.
</verification>

<success_criteria>
Texturing has a real, empirically-derived, citation-traceable `byteQuantMode` (or an honestly-documented D-08 gap), proven not to mask a fully-corrupted source texture -- RENDTOL-02 fully satisfied across all three fixtures.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New file `SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-texture-source-image.raw` (16384-byte fully-inverted corrupted texture).
- New C++ class `sgns::Secv01RenderTexturingCounterTest`, `TEST_F` case `TexturingWrongSourceImageStillDiverges`.
- Sixth source registered in `processing_conformance_security_test`'s CMake target.
- New `parameters[].byteQuantMode` entry in `texturing-fixture-definition.json`.
