---
phase: 17-render-path-cross-hardware-tolerance
plan: 06
type: execute
wave: 5
depends_on: ["17-05"]
files_modified:
  - SuperGenius/test/src/processing_conformance_security/secv01_render_lighting_counter_test.cpp
  - SuperGenius/test/src/processing_conformance_security/secv01_render_blending_counter_test.cpp
  - SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
  - SuperGenius/test/src/processing_dispatch/lighting-fixture-definition.json
  - SuperGenius/test/src/processing_dispatch/blending-fixture-definition.json
autonomous: true
requirements: [RENDTOL-02]

must_haves:
  truths:
    - "Lighting's and blending's byteQuantMode values are each derived independently via binary-search-against-a-counter-test (D-06), starting from 17-CAPTURE-RESULTS-ROUND1.md's real measured maxAbsDelta for that fixture -- never guessed a priori."
    - "The search direction is correct: increasing N (masking more low bits) is the MORE tolerant direction for byteQuantMode, the mirror image of quantScale's decreasing-S convention (RESEARCH.md Tolerance Derivation Methodology step 4) -- the final N is chosen one integer step below the first N that starts masking the counter-test's deliberate corruption, or (if no failure boundary exists in [0,8]) the smallest N such that 2^N > maxAbsDelta."
    - "Each fixture gets its own dedicated counter-test file (mirrors secv01_tex3d_counter_test.cpp's per-fixture-file precedent, D-07) -- not a shared/parameterized test, and not an edit to the existing trivial-fixture secv01_counter_test.cpp."
    - "If no byteQuantMode in [0,8] closes a fixture's captured divergence (or if the divergence was already zero and no derivation is meaningful), that outcome is documented honestly per D-08, never silently forced to look like a pass."
  artifacts:
    - SuperGenius/test/src/processing_conformance_security/secv01_render_lighting_counter_test.cpp
    - SuperGenius/test/src/processing_conformance_security/secv01_render_blending_counter_test.cpp
  key_links:
    - "17-CAPTURE-RESULTS-ROUND1.md's per-fixture maxAbsDelta <-> this plan's first candidate N for each fixture's binary search -- the search starts from real data, not a blind guess."
    - "The final chosen N <-> both the counter-test's two inline JSON literals AND the production fixture JSON's new parameters array -- both must carry the identical final value, mirroring 14-03-PLAN.md's exact-match acceptance criterion."
---

<objective>
Derive real, empirically-justified `byteQuantMode` values for the lighting and blending fixtures via the same binary-search-against-a-counter-test discipline Phase 13/14 used for `quantScale` (D-06), starting from Wave 4's real captured raw divergence (17-CAPTURE-RESULTS-ROUND1.md) -- then prove each chosen value still catches a deliberately corrupted result via a new dedicated SECV-01-style counter-test (D-07), mirroring `secv01_tex3d_counter_test.cpp`'s structure exactly.

Purpose: this is RENDTOL-02's central empirical proof for two of the three fixtures -- the mechanism (`ResolveByteQuantMode`/`QuantizeByteBuffer`) already exists (Phase 14, D-01); this plan supplies real per-fixture values and proves they are not loose enough to also mask corruption.
Output: `secv01_render_lighting_counter_test.cpp` + `secv01_render_blending_counter_test.cpp` (registered in `processing_conformance_security_test`), each fixture's production JSON updated with its final `byteQuantMode` and a citation trail to the real divergence data it was derived from.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md
@.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive lighting's byteQuantMode and write its counter-test</name>
  <files>SuperGenius/test/src/processing_conformance_security/secv01_render_lighting_counter_test.cpp, SuperGenius/test/src/processing_conformance_security/CMakeLists.txt, SuperGenius/test/src/processing_dispatch/lighting-fixture-definition.json</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp:200-390 (RenderWrongShaderConstantStillDiverges -- the exact render-job counter-test shape, `HasUsableVulkanDevice()`/`GTEST_SKIP` guard, `ModelNode.set_source("input:...")` convention, and the binary `std::memcmp` on `.artifacts[0].artifactId` to replicate verbatim)
    SuperGenius/test/src/processing_conformance_security/secv01_tex3d_counter_test.cpp (full file -- the exact per-fixture-dedicated-file template, `parameters` array declaring the candidate value under test in both jobs)
    SuperGenius/test/src/processing_conformance_security/CMakeLists.txt (current `addtest(processing_conformance_security_test ...)` source list to extend)
    SuperGenius/test/src/processing_dispatch/lighting-fixture-definition.json (the real fixture's current shape, to mirror inline and to update with the final `parameters` entry)
    .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md (lighting's real captured `maxAbsDelta`/`contentHashMatch` -- the starting point for this task's search, read this file's actual numbers, do not assume a value)
  </read_first>
  <action>
    Create `secv01_render_lighting_counter_test.cpp` under namespace `sgns`, class `Secv01RenderLightingCounterTest : public ProcessorConformanceFixture {}` (no `SetUpTestSuite` override, mirroring `Secv01CounterTest`'s shape). One `TEST_F` case `LightingWrongColorStillDiverges`: two inline JSON job strings shaped exactly like `lighting-fixture-definition.json` (same `render_shader.stages`/`render_target`/`vertex_buffer`/`vertex_layout`/`pipeline_state`, using `file://processing_dispatch/lighting_vertex_shader.glsl`/`lighting_fragment_shader.glsl`/`lighting-vertex-data.raw` paths) -- the "correct" job with `lightColor: [1.0, 1.0, 1.0]`, the "wrong" job with `lightColor: [0.5, 0.5, 0.5]` (RESEARCH.md's own suggested corruption), everything else byte-identical between the two. Both jobs declare an identical `parameters` array with a single entry `{"name": "byteQuantMode", "type": "int", "default": &lt;candidate N&gt;}` (mirrors `secv01_tex3d_counter_test.cpp`'s `quantScale` declaration shape exactly, per RESEARCH.md's Shared Pattern). Guard the test with `if (!sgns::sgprocessing::HasUsableVulkanDevice()) { GTEST_SKIP() << "..."; }` first. `Create()` + `Process()` each (mirroring `RenderWrongShaderConstantStillDiverges`'s exact `ModelNode.set_source("input:lightingVertexInput")`/`Process(ioc, chunkhashes, modelNode, outputLocations)` call shape). `ASSERT_TRUE` both results, `ASSERT_GE` both `.artifacts.size() >= 1`, then `EXPECT_NE(std::memcmp(prCorrect.value().artifacts[0].artifactId, prWrong.value().artifacts[0].artifactId, sgns::sgprocessing::SHA256_HASH_SIZE), 0)`.

    Register `secv01_render_lighting_counter_test.cpp` as a new source in `CMakeLists.txt`'s `addtest(processing_conformance_security_test ...)` call, alongside the three existing sources.

    Binary-search the final candidate N (D-06, direction note: INCREASING N is the MORE tolerant direction for `byteQuantMode`, opposite of `quantScale`'s decreasing-S convention -- RESEARCH.md's Tolerance Derivation Methodology step 4). Read lighting's real `maxAbsDelta` from `17-CAPTURE-RESULTS-ROUND1.md`. Compute the smallest integer N such that `2^N > maxAbsDelta` as your first candidate (D-08's divergence-absorption floor). Set both inline JSON literals' `byteQuantMode` to that candidate, rebuild (`cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug`), run (`ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv01RenderLightingCounterTest -V`). If it passes, try increasing N by 1 repeatedly until you find the first N where the test FAILS (the two `artifactId`s collide) -- that is the failure boundary; choose the final N as one integer step BELOW that boundary (never the exact edge), capped at 8. If N=8 itself still passes (no failure boundary found anywhere in [0,8] -- a legitimate, already-precedented outcome per Phase 14's tex3d search), fall back to D-08: ship the divergence-absorption floor computed above (smallest N with `2^N > maxAbsDelta`) as the final value, since no tighter empirical constraint exists. If even N=8 cannot make the counter-test pass at all (both artifactIds always collide, or maxAbsDelta is so large no mask width in [0,8] plausibly absorbs it), document this as an honest D-08 gap in this task's contribution to the phase's tolerance-results document (do not force a passing report).

    Once the final N is chosen, hardcode it into both of `secv01_render_lighting_counter_test.cpp`'s inline JSON literals, and add a matching `parameters: [{"name": "byteQuantMode", "type": "int", "default": &lt;final N&gt;, "description": "&lt;cites lighting's real measured maxAbsDelta from 17-CAPTURE-RESULTS-ROUND1.md and the binary-search range/margin, mirroring 13-04-PLAN.md's citation-trail discipline&gt;"}]` entry to the production `lighting-fixture-definition.json`.
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug &amp;&amp; ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv01RenderLightingCounterTest -V | grep -qE "100% tests passed"</automated>
  </verify>
  <acceptance_criteria>
    - `secv01_render_lighting_counter_test.cpp` asserts via `std::memcmp` on `.artifacts[0].artifactId` (never `.combinedHash`).
    - `CMakeLists.txt` lists `secv01_render_lighting_counter_test.cpp` as a fourth source alongside the three pre-existing ones.
    - `lighting-fixture-definition.json`'s new `byteQuantMode` default exactly matches the value hardcoded in both of the counter-test's inline JSON literals.
    - `lighting-fixture-definition.json`'s new `parameters[0].description` field contains a citation to a real numeric `maxAbsDelta` value (not a placeholder string).
    - `ctest -R Secv01RenderLightingCounterTest` reports 100% tests passed at the final chosen N -- OR, if D-08's honest-gap case applies, the SUMMARY documents it explicitly and this criterion is waived with that documented rationale.
  </acceptance_criteria>
  <done>Lighting's byteQuantMode is empirically derived from real captured divergence and proven (or honestly documented as unable to be proven) not to mask a deliberately wrong light color.</done>
</task>

<task type="auto">
  <name>Task 2: Derive blending's byteQuantMode and write its counter-test</name>
  <files>SuperGenius/test/src/processing_conformance_security/secv01_render_blending_counter_test.cpp, SuperGenius/test/src/processing_conformance_security/CMakeLists.txt, SuperGenius/test/src/processing_dispatch/blending-fixture-definition.json</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp:200-390 (same render-job counter-test template as Task 1)
    SuperGenius/test/src/processing_conformance_security/secv01_render_lighting_counter_test.cpp (Task 1's just-completed file -- reuse its exact structural shape for blending, changing only the job body and corruption)
    SuperGenius/test/src/processing_dispatch/blending-fixture-definition.json (current shape, to mirror inline and update)
    .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-CAPTURE-RESULTS-ROUND1.md (blending's real captured `maxAbsDelta` -- read the actual number, do not assume)
  </read_first>
  <action>
    Create `secv01_render_blending_counter_test.cpp`, class `Secv01RenderBlendingCounterTest`, `TEST_F` case `BlendingWrongFactorStillDiverges`: two inline JSON jobs mirroring `blending-fixture-definition.json` -- "correct" job with `pipeline_state.blend_src_factor: "src_alpha"` / `blend_dst_factor: "one_minus_src_alpha"`; "wrong" job with `blend_src_factor: "one"` / `blend_dst_factor: "zero"` (RESEARCH.md's own suggested corruption -- this disables the destination's contribution entirely, a materially different final blended color). Both declare the identical candidate `byteQuantMode` in a `parameters` array. Same `HasUsableVulkanDevice()` skip guard, `Create()`+`Process()`+binary `memcmp` assertion shape as Task 1.

    Register the new source in `CMakeLists.txt` as a fifth source alongside the four now present (three original + Task 1's lighting file).

    Binary-search blending's final N using the identical D-06 methodology and search-direction discipline as Task 1, starting from blending's own real `maxAbsDelta` (read from `17-CAPTURE-RESULTS-ROUND1.md` -- this will generally differ from lighting's value; each fixture's search is fully independent, D-06/D-07). Rebuild+run via `ctest -R Secv01RenderBlendingCounterTest -V` at each candidate. Apply the same three possible outcomes (failure boundary found -> one step of margin below it; no failure boundary in [0,8] -> D-08's divergence-absorption floor; cannot pass at any N -> honest documented gap).

    Wire the final chosen N into both inline JSON literals and into `blending-fixture-definition.json`'s new `parameters` array, with the same citation-trail discipline as Task 1.
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug &amp;&amp; ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv01RenderBlendingCounterTest -V | grep -qE "100% tests passed"</automated>
  </verify>
  <acceptance_criteria>
    - `secv01_render_blending_counter_test.cpp` asserts via `std::memcmp` on `.artifacts[0].artifactId`.
    - `CMakeLists.txt` lists both Task 1's and Task 2's new counter-test files (five sources total).
    - `blending-fixture-definition.json`'s new `byteQuantMode` default exactly matches both of Task 2's counter-test inline JSON literals, with a real-`maxAbsDelta`-citing description.
    - `ctest -R Secv01RenderBlendingCounterTest` reports 100% tests passed at the final chosen N -- OR the SUMMARY documents a D-08 honest gap explicitly.
  </acceptance_criteria>
  <done>Blending's byteQuantMode is empirically derived and proven (or honestly documented as unable to be proven) not to mask a deliberately wrong blend factor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Deliberately-corrupted lighting/blending job JSON -> real render pipeline -> post-quantization `artifactId` | The adversarial-simulation boundary both counter-tests exist to prove: does each fixture's own empirically-derived `byteQuantMode` still reject a plausible-but-wrong result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-13 | Tampering / Spoofing | Wrong light color (lighting) / wrong blend factor (blending) substituted at each fixture's own derived tolerance | critical | mitigate | `LightingWrongColorStillDiverges`/`BlendingWrongFactorStillDiverges` assert the corrupted job's post-quantization `artifactId` differs from the correct job's at the final chosen N -- direct empirical proof neither derived tolerance is loose enough to also mask a substituted result. |
| T-17-14 | Tampering | Choosing an N wide enough to close real captured cross-hardware divergence but ALSO wide enough to mask corruption | high | mitigate | D-06's binary-search-against-this-counter-test methodology empirically finds the pass/fail boundary per fixture and picks one integer step of margin on the safe (lower-N) side, rather than guessing -- mirrors 13-04/14-03's already-precedented discipline. |
| T-17-15 | Repudiation | A fixture's final `byteQuantMode` is chosen without a traceable citation to the real captured divergence it addresses | medium | mitigate | Both fixtures' new `parameters[].description` fields cite the real numeric `maxAbsDelta` from `17-CAPTURE-RESULTS-ROUND1.md`, mirroring 13-04/14-03's citation-trail convention -- never an unsourced constant. |

No package-manager installs occur in this plan. The npm/pip/cargo Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. Both new counter-test files exist, registered in `CMakeLists.txt`, asserting via `.artifacts[0].artifactId`.
2. Each fixture's final `byteQuantMode` is derived from that fixture's own real captured `maxAbsDelta`, via the correct (increasing-N-is-more-tolerant) search direction.
3. Each fixture's production JSON and counter-test inline JSON agree exactly on the final chosen value.
4. Any fixture where no N in [0,8] closes the gap is documented honestly, not silently passed.
</verification>

<success_criteria>
Lighting and blending each have a real, empirically-derived, citation-traceable `byteQuantMode`, proven (or honestly documented as unprovable) not to mask deliberate corruption -- RENDTOL-02 satisfied for two of the three fixtures.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New C++ classes `sgns::Secv01RenderLightingCounterTest` / `sgns::Secv01RenderBlendingCounterTest`.
- New `TEST_F` cases `LightingWrongColorStillDiverges` / `BlendingWrongFactorStillDiverges`.
- Two new source files registered in `processing_conformance_security_test`'s CMake target.
- New `parameters[].byteQuantMode` entries in `lighting-fixture-definition.json` and `blending-fixture-definition.json`.
