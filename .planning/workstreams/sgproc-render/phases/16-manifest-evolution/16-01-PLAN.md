---
phase: 16-manifest-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/sgproc-render/REQUIREMENTS.md
  - .planning/workstreams/sgproc-render/ROADMAP.md
autonomous: true
requirements: [ARTF-07, ARTF-08]

must_haves:
  truths:
    - "REQUIREMENTS.md's traceability table and requirement bullets show ARTF-07 and ARTF-08 as 'Won't implement — not applicable' (not 'Pending'), each citing the D-01..D-08 rationale from 16-CONTEXT.md."
    - "ROADMAP.md's Phase 16 section no longer lists a Merkle-tree or content-defined-chunking success criterion as open work; both are documented as this phase's own closed architectural conclusion, not a future deferral."
    - "ARTF-09 and ARTF-10's REQUIREMENTS.md/ROADMAP.md entries are left unchanged (still open/Pending) — this plan only corrects the two won't-implement items, not the two items Plans 16-02/16-03 actually build."
  artifacts:
    - .planning/workstreams/sgproc-render/REQUIREMENTS.md
    - .planning/workstreams/sgproc-render/ROADMAP.md
  key_links:
    - "REQUIREMENTS.md's traceability table <-> ROADMAP.md's Phase 16 section — both must cite the same D-01..D-08 rationale so a future reader finds consistent bookkeeping, not a contradictory status between the two documents."
---

<objective>
Correct REQUIREMENTS.md and ROADMAP.md so Phase 16's bookkeeping reflects the actual, discussed scope: ARTF-07 (Merkle tree over chunk hashes) and ARTF-08 (content-defined chunking) are closed as "Won't implement — not applicable" (16-CONTEXT.md D-01 through D-08), not silently dropped and not left describing a stale goal. ARTF-09 and ARTF-10 (the two requirements Plans 16-02/16-03 actually implement) are untouched by this plan.

Cited decisions for ARTF-07 ("Won't implement"): D-01 (Artifact::chunkHashes already gives full per-chunk localization), D-02 (graphsync/protobuf already deliver complete chunk-hash lists; verifiers redo processing and compare directly), D-03 (the mismatch-fallback path is XNODE-01c territory, already out of scope — not reopened here), D-04 (status reads "Won't implement," not "deferred," and this documentation correction is itself a Phase 16 plan task). Cited decisions for ARTF-08 ("Won't implement"): D-05 (block_len is job-poster-owned per Dimensions.hpp), D-06 (a block_len/source-data mismatch is a bad-job failure, not a chunking gap), D-07 (CID-addressed inputs are immune to the small-edit problem by construction; FileManager's non-CID http/file sources exist but the mismatch remains the job poster's responsibility per D-06), D-08 (same "Won't implement, not a deferral" status convention as D-04, with this same documentation task closing it out).

Purpose: mirrors Phase 13's own in-plan ROADMAP.md scope-correction precedent (STATE.md: "REQUIREMENTS.md VALD-01 / ROADMAP.md Phase 13 SC1 wording corrected... via scoped Edit calls") — a future reader of either document must not chase a Merkle-tree/CDC goal that this phase's own discussion already concluded is architecturally unnecessary.
Output: REQUIREMENTS.md and ROADMAP.md edited in place (via scoped Edit, never a whole-file Write) to reflect the "Won't implement" status and its rationale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/phases/16-manifest-evolution/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark ARTF-07/ARTF-08 as "Won't implement — not applicable" in REQUIREMENTS.md</name>
  <files>.planning/workstreams/sgproc-render/REQUIREMENTS.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/REQUIREMENTS.md (the "Manifest Evolution" bullet list, lines 12-17, and the Traceability table, lines 48-61 — read the exact current text before editing)
    .planning/workstreams/sgproc-render/phases/16-manifest-evolution/16-CONTEXT.md (D-01 through D-08 — the exact rationale to cite; do not paraphrase away the "graphsync/protobuf already deliver full chunk-hash lists" and "block_len is job-poster-owned" points)
  </read_first>
  <action>
    Using Edit (scoped replacement, not Write): (1) On the ARTF-07 bullet line, change the checkbox from "- [ ]" to "- [x]" and append, after the existing description, " — **Won't implement, not applicable** (16-CONTEXT.md D-01..D-04: Artifact::chunkHashes already gives full per-chunk localization; graphsync/protobuf already deliver the complete chunk-hash list to every real verifier, so a root-only Merkle proof serves no scenario this system's actual verification flow has)". (2) On the ARTF-08 bullet line, change the checkbox from "- [ ]" to "- [x]" and append " — **Won't implement, not applicable** (16-CONTEXT.md D-05..D-08: block_len is a job-poster-owned schema parameter (Dimensions.hpp); a source-data/block_len mismatch is a bad-job failure to surface, not a chunking gap for SGProcessingManager to reconcile)". (3) In the Traceability table, change the ARTF-07 row's Status cell from "Pending" to "Won't implement — not applicable (D-01..D-04)" and the ARTF-08 row's Status cell from "Pending" to "Won't implement — not applicable (D-05..D-08)". Leave the ARTF-09 and ARTF-10 bullets' checkboxes ("- [ ]") and their Traceability rows' Status cells ("Pending") exactly as they are today — this task does not touch either. Do not edit the "Coverage" counts block (8 total / 8 mapped / 0 unmapped remains literally true — it tracks phase mapping, not implementation status).
  </action>
  <verify>
    <automated>grep -c "Won't implement" .planning/workstreams/sgproc-render/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the exact substring "Won't implement" appears at least 4 times in REQUIREMENTS.md after the edit (2 occurrences per requirement: once in the bullet line, once in the traceability table row, for both ARTF-07 and ARTF-08).
    - Behavior: the ARTF-09 and ARTF-10 rows in the Traceability table still read "Pending" — unchanged by this task.
    - Source: after editing, Read the file and confirm the Markdown table's pipe alignment is intact (no broken `|` columns) around the edited rows.
  </acceptance_criteria>
  <done>REQUIREMENTS.md's ARTF-07/ARTF-08 bullets and traceability rows read "Won't implement — not applicable" with a cited D-01..D-08 rationale; ARTF-09/ARTF-10 remain unchanged and still "Pending".</done>
</task>

<task type="auto">
  <name>Task 2: Correct ROADMAP.md's Phase 16 Goal/Success Criteria to drop the stale Merkle/CDC goal</name>
  <files>.planning/workstreams/sgproc-render/ROADMAP.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/ROADMAP.md (the Phase 16 phase-list bullet near line 77, and the full Phase 16 section — Goal/Depends on/Requirements/Success Criteria — near lines 84-96; read the exact current text before editing)
    .planning/workstreams/sgproc-render/phases/16-manifest-evolution/16-CONTEXT.md (D-01..D-08 rationale for the won't-implement items; D-09..D-15 for what the remaining Success Criteria describe)
  </read_first>
  <action>
    Using Edit (scoped replacement, not Write): (1) In the milestone's Phase 16 phase-list bullet ("- [ ] **Phase 16: Manifest Evolution** - Merkle-tree chunk integrity, content-defined chunking, human-readable error messages, and a schema-evolvable binary manifest format — closing Phase 08's deferred scope"), replace the clause after the dash with: "Human-readable error messages retrievable from the manifest, and a schema-evolvable binary manifest format — closing Phase 08's deferred scope (Merkle-tree chunk integrity and content-defined chunking concluded 'Won't implement — not applicable' during phase discussion; see 16-CONTEXT.md D-01..D-08)". (2) Replace the Phase 16 section's **Goal** line so it describes only the ARTF-09/ARTF-10 scope actually implemented (a human-readable error message retrievable from the manifest by a manifest-only caller, and additive schema evolution proven in both compatibility directions), followed by one sentence noting ARTF-07 (Merkle tree) and ARTF-08 (content-defined chunking) were concluded "Won't implement — not applicable" because graphsync/protobuf already deliver full per-chunk hash lists to every real verifier and block_len is a job-poster-owned schema parameter, not SGProcessingManager's to renegotiate (cite 16-CONTEXT.md D-01..D-08). (3) Replace the 4-item **Success Criteria** list with exactly 2 renumbered items: item 1 is the existing SC3 text (human-readable error message retrievable from the manifest by a manifest-only caller), item 2 is the existing SC4 text (both-directions schema-evolution parse compatibility). Delete the existing SC1 (Merkle) and SC2 (CDC) items entirely — do not keep them renumbered as "moot," remove them from the numbered list. (4) Immediately after the renumbered Success Criteria list, add a new subsection titled "**Won't implement (this phase's own conclusion, not a deferral):**" with two bullets: one for ARTF-07 citing D-01..D-04's graphsync/protobuf rationale, one for ARTF-08 citing D-05..D-08's job-poster-owned block_len rationale — matching the phrasing used in Task 1's REQUIREMENTS.md edit for consistency between the two documents. Leave the "**Requirements**: ARTF-07, ARTF-08, ARTF-09, ARTF-10" line completely unchanged — all four IDs remain this phase's requirements (two implemented this milestone, two closed as won't-implement); do not remove any ID from that line, and do not change "**Depends on**" or "**Plans**: TBD" (the latter is updated by the planner's own update_roadmap step after this task, not by this task).
  </action>
  <verify>
    <automated>grep -c "Won't implement" .planning/workstreams/sgproc-render/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the Phase 16 section's Success Criteria list contains exactly 2 numbered items after the edit.
    - Behavior: the substring "Merkle tree" does not appear anywhere inside the numbered Success Criteria list body (it may still appear in the new "Won't implement" subsection immediately below it, and in the phase-list bullet's parenthetical — both expected).
    - Behavior: the exact substring "Won't implement" appears at least 3 times in ROADMAP.md after the edit (phase-list bullet + Goal line + the new subsection).
    - Behavior: the line "**Requirements**: ARTF-07, ARTF-08, ARTF-09, ARTF-10" is unchanged — all 4 IDs still present, comma-separated, in that exact order.
    - Source: after editing, Read the file and confirm no broken Markdown structure around the edited section.
  </acceptance_criteria>
  <done>ROADMAP.md's Phase 16 Goal and Success Criteria describe only the ARTF-09/ARTF-10 scope this milestone actually delivers, with ARTF-07/ARTF-08 explicitly documented as "Won't implement — not applicable" (with rationale) rather than silently dropped or left describing a stale Merkle/CDC goal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan edits only planning documentation (REQUIREMENTS.md, ROADMAP.md) via scoped text replacement; no source code is touched, no runtime data flow exists, and no trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-00 | N/A | Planning documentation only | low | accept | No code changes in this plan — nothing to mitigate. Recorded for structural completeness of the phase's STRIDE register (security_enforcement is enabled project-wide per .planning/config.json). |

No package-manager installs occur in this plan.
</threat_model>

<verification>
1. REQUIREMENTS.md's ARTF-07/ARTF-08 bullets and traceability rows read "Won't implement — not applicable" with a cited rationale; ARTF-09/ARTF-10 are unchanged.
2. ROADMAP.md's Phase 16 Success Criteria list contains exactly 2 items (the former SC3/SC4), with SC1 (Merkle) and SC2 (CDC) removed and replaced by an explicit "Won't implement" subsection.
3. Both documents cite the same D-01..D-08 rationale, so a future reader finds consistent, non-contradictory bookkeeping between REQUIREMENTS.md and ROADMAP.md.
</verification>

<success_criteria>
REQUIREMENTS.md and ROADMAP.md both accurately describe Phase 16's actual, discussed scope — ARTF-07/ARTF-08 closed as "Won't implement — not applicable" with cited rationale, ARTF-09/ARTF-10 left open for Plans 16-02/16-03 to implement.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/16-manifest-evolution/16-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `.planning/workstreams/sgproc-render/REQUIREMENTS.md` — ARTF-07/ARTF-08 bullets and traceability rows edited to read "Won't implement — not applicable" with cited rationale.
- `.planning/workstreams/sgproc-render/ROADMAP.md` — Phase 16 phase-list bullet, Goal line, and Success Criteria list edited to drop the stale Merkle-tree/content-defined-chunking goal and document it as a closed, won't-implement conclusion.
