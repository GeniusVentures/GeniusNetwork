---
phase: 04-cross-platform-build-ci-end-to-end-verification
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: true
requirements: [E2E-03]

must_haves:
  truths:
    - "The existing Phase 1 concurrent Vulkan-init stress test (vulkan_init_concurrency_test) still passes with zero races/crashes after plans 04-01/04-02's changes"
    - "The full existing MNN inference/retrain test suite (processing_datatypes_test) passes with zero regressions"
    - "The hardware-independent tests (shader_compiler_test, processing_schema_test, and processing_dispatch_test's 7 non-execution cases) remain ungated and pass unconditionally"
  artifacts: []
  key_links:
    - "processing_dispatch_test's rebuild picks up plan 04-01's GTEST_SKIP() retrofit and new E2E-01 test before this confirmation run"
---

<objective>
Confirm, per 04-CONTEXT.md D-37, that E2E-03's "zero regressions" claim holds after this phase's CI/test-infrastructure changes: the existing Phase 1 concurrent-init stress test (`vulkan_init_concurrency_test`) still passes, and the full existing MNN inference/retrain suite (`processing_datatypes_test`) still passes, with no new authoring required unless a genuine gap surfaces. This is a verification/confirmation task, not new test-authoring (D-37) -- 04-PATTERNS.md found no coverage gap in the existing 25-iteration/3-thread concurrency test warranting new code.

Purpose: close out the phase's "zero regressions to existing MNN inference/retrain behavior" success criterion with a concrete, re-run confirmation rather than an assumption.
Output: a pass/fail confirmation result for the 5 relevant CTest targets, using the closest available proxy to CI's per-platform matrix run (the existing local Windows Release build directory) since the actual GitHub Actions self-hosted runners are not reachable from this environment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/04-cross-platform-build-ci-end-to-end-verification/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm zero regressions across the concurrency stress test and full MNN suite</name>
  <files></files>
  <read_first>
    SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp (the concurrency stress test to re-verify -- RepeatedConcurrentInitNoRaceOrCrash, 25 iterations x 3 threads: MNN string job, MNN texture3d job, RenderProcessor::StartProcessing directly)
    SuperGenius/test/src/processing_datatypes/CMakeLists.txt (confirms processing_datatypes_test is the CTest target name for the MNN inference/retrain suite's 13 migration-candidate *ProcessingTest cases)
    04-CONTEXT.md D-37 (E2E-03 scope: confirm existing tests stay green, not new authoring, unless research surfaces a specific gap -- none was found)
  </read_first>
  <action>
    Using the existing local build directory `SuperGenius/build/Windows/Release` (or the freshest available build directory that already contains plans 04-01/04-02's changes rebuilt), run an incremental build (`cmake --build . --config Release -j`) to pick up plan 04-01's `processing_dispatch_test` source changes and the new `vulkan_gpu_probe.cpp`/`SGProcessors` additions. Then run `ctest` filtered to exactly these 5 targets: `vulkan_init_concurrency_test`, `processing_datatypes_test`, `processing_dispatch_test`, `processing_schema_test`, `shader_compiler_test`. This is the closest available proxy to CI's per-platform matrix confirmation (D-37) since the actual self-hosted GitHub Actions runners (`sg-ubuntu-linux`/`sg-arm-linux`/`SG-WIN11`/`gv-OSX-Large`) are not reachable from this environment.

    Do not author any new test cases for E2E-03 -- this task is a confirmation gate only. If any of these 5 targets shows a genuine new failure (not merely "no GPU available" for the two GTEST_SKIP-guarded cases inside `processing_dispatch_test`, and not a pre-existing already-tracked failure unrelated to this phase), stop and report the specific failing test name and assertion message rather than silently patching around it or marking the phase complete.
  </action>
  <verify>
    <automated>ctest . -C Release --output-on-failure -R "^(vulkan_init_concurrency_test|processing_datatypes_test|processing_dispatch_test|processing_schema_test|shader_compiler_test)$"</automated>
  </verify>
  <acceptance_criteria>
    - `ctest . -C Release --output-on-failure -R "^(vulkan_init_concurrency_test|processing_datatypes_test|processing_dispatch_test|processing_schema_test|shader_compiler_test)$"` (run from the build directory) exits 0, reporting exactly 5 tests attempted with 0 marked Failed
    - `vulkan_init_concurrency_test` specifically completes its 25-iteration/3-thread `RepeatedConcurrentInitNoRaceOrCrash` run and reaches its `SUCCEED()` call -- no crash, no hang, no race-detected failure
    - `processing_datatypes_test` (the MNN inference/retrain suite) reports 0 failures across all of its `*ProcessingTest` cases
    - `processing_dispatch_test`'s xunit output shows its two GPU-gated cases (`RenderPassSameNodeRepeatedExecutionProducesBitExactHash`, `RenderPassEndToEndProducesVerifiedOutputHash`) as either PASSED (real device present) or SKIPPED (no device) -- never FAILED -- and its other 7 cases PASSED unconditionally
    - `processing_schema_test` and `shader_compiler_test` (hardware-independent, no VkDevice dependency) report 0 failures
  </acceptance_criteria>
  <done>All 5 targets pass, or cleanly skip only their GPU-gated cases, with zero regressions attributable to plans 04-01/04-02's changes; any genuine failure is reported by exact test name and assertion rather than silently absorbed or waved through.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build environment ↔ real Vulkan/MNN test execution | Runs existing, already-reviewed test binaries against the local host's real hardware/drivers -- no new code, no new attack surface introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Repudiation | Confirmation result | low | accept | This task's acceptance criteria require reporting the exact failing test name/assertion on any genuine regression rather than a generic pass/fail, so a false "all green" claim is falsifiable against the recorded ctest output |

</threat_model>

<verification>
- `ctest . -C Release --output-on-failure -R "^(vulkan_init_concurrency_test|processing_datatypes_test|processing_dispatch_test|processing_schema_test|shader_compiler_test)$"` exits 0
</verification>

<success_criteria>
E2E-03 is confirmed, not assumed: the Phase 1 concurrency stress test and the full existing MNN inference/retrain suite both pass with zero regressions after this phase's CI/test changes, and the two newly GPU-gated render tests skip cleanly (never fail) in the absence of a usable device.
</success_criteria>

## Artifacts This Phase Produces (this plan's contribution)

- None -- this is a verification/confirmation-only plan per 04-CONTEXT.md D-37. No new files, no new CMake targets, no new TEST() cases, no new CI steps.

<output>
Create `.planning/workstreams/sgproc-render/phases/04-cross-platform-build-ci-end-to-end-verification/04-03-SUMMARY.md` when done
</output>
