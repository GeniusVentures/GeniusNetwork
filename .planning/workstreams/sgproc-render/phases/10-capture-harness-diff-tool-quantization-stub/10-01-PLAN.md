---
phase: 10-capture-harness-diff-tool-quantization-stub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SuperGenius/SGProcessingManager/include/util/quantization.hpp
  - SuperGenius/SGProcessingManager/src/util/quantization.cpp
  - SuperGenius/SGProcessingManager/src/util/CMakeLists.txt
  - SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt
  - SuperGenius/SGProcessingManager/include/execution/execution_context.hpp
autonomous: true
requirements:
  - CAPT-02

must_haves:
  truths:
    - "A shared sgprocmanagerquant library exists with two functions (QuantizeFloatBuffer, QuantizeByteBuffer) that all 14 processor files can call"
    - "ExecutionContext carries one new opt-in capture callback that stays nullptr unless a caller (only capture_harness) explicitly sets it, so production job execution pays zero cost"
  artifacts:
    - path: "SuperGenius/SGProcessingManager/include/util/quantization.hpp"
      provides: "sgns::sgprocmanagerquant::QuantizeFloatBuffer / QuantizeByteBuffer declarations"
      exports: ["QuantizeFloatBuffer", "QuantizeByteBuffer"]
    - path: "SuperGenius/SGProcessingManager/src/util/quantization.cpp"
      provides: "Phase 10 no-op/identity stub implementations (Phase 12 replaces internals)"
    - path: "SuperGenius/SGProcessingManager/include/execution/execution_context.hpp"
      provides: "ExecutionContext::rawOutputCapture opt-in callback field"
      exports: ["ExecutionContext::rawOutputCapture"]
  key_links:
    - from: "src/processors/CMakeLists.txt target_link_libraries(SGProcessors ...)"
      to: "sgprocmanagerquant"
      via: "static library link, mirrors existing sgprocmanagersha link"
      pattern: "sgprocmanagerquant"
    - from: "ExecutionContext::rawOutputCapture"
      to: "tools/capture/capture_harness.cpp (Plan 10-05)"
      via: "caller sets the std::function before calling ProcessingManager::Process()'s 5-arg overload"
---

<objective>
Create the foundation two new plans in Wave 2 build on: the `sgprocmanagerquant` utility library (Phase 10 no-op/identity stub; Phase 12 fills in real IEEE-754 canonicalization + fixed-precision rounding) and one new opt-in `ExecutionContext::rawOutputCapture` callback field that lets a caller observe the literal bytes fed into each processor's existing `sha256()` call site.

Purpose: Phase 10's goal is proving the full invasive plumbing (14 processor files + one `ExecutionContext` field) end-to-end once, with quantization wired in as an identity stub, so Phase 12 only has to change two small function bodies later — never revisit the plumbing. This plan builds the two pieces every other Wave-2/3 plan depends on: the quantization library every processor links against, and the capture callback field every processor invokes.
Output: `sgprocmanagerquant` CMake target (mirrors the existing `sgprocmanagersha` library exactly), and `ExecutionContext::rawOutputCapture`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
@.planning/workstreams/sgproc-render/phases/10-capture-harness-diff-tool-quantization-stub/10-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/10-capture-harness-diff-tool-quantization-stub/10-PATTERNS.md
@.planning/workstreams/sgproc-render/research/ARCHITECTURE.md
@SuperGenius/AgentDocs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sgprocmanagerquant utility library (identity stub)</name>
  <files>SuperGenius/SGProcessingManager/include/util/quantization.hpp, SuperGenius/SGProcessingManager/src/util/quantization.cpp, SuperGenius/SGProcessingManager/src/util/CMakeLists.txt, SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt</files>
  <read_first>
    SuperGenius/SGProcessingManager/include/util/sha256.hpp
    SuperGenius/SGProcessingManager/src/util/sha256.cpp
    SuperGenius/SGProcessingManager/src/util/CMakeLists.txt
    SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt
  </read_first>
  <action>
Create `include/util/quantization.hpp` mirroring `sha256.hpp`'s exact shape: include guard `SGPROCMGR_QUANTIZATION_HPP`, `#include <cstddef>` and `<cstdint>`, namespace `sgns::sgprocmanagerquant`. Declare exactly two free functions, matching the signatures research already vetted against all 14 call sites: `void QuantizeFloatBuffer( float *data, size_t count );` (used by all 13 MNN processor files — the shared float-output path, regardless of each processor's own declared DataType) and `void QuantizeByteBuffer( uint8_t *data, size_t count );` (used only by `processing_processor_render.cpp`'s uint8 RGBA8/RGB8 readback). Add a Doxygen-style doc comment on each function stating this is a Phase 10 literal no-op/identity stub — Phase 12 replaces the body with real IEEE-754 canonicalization (NaN/Inf/denormal/signed-zero) plus fixed-precision scale-round-cast for the float path, and integer tolerance-banding for the byte path, once Phase 11's empirical cross-machine capture data justifies a real constant. Do not implement any rounding/canonicalization arithmetic in this phase.

Create `src/util/quantization.cpp`: `#include "util/quantization.hpp"`, same namespace, both function bodies are literal no-ops — mark both parameters `(void)data; (void)count;` and add a one-line comment referencing the header's stub doc comment. No arithmetic on `data` anywhere in this file.

Edit `src/util/CMakeLists.txt`: append a new `add_library(sgprocmanagerquant ...)` block after the existing `sgprocmanagersha` block (after its `sgnus_install(sgprocmanagersha)` line), mirroring lines 14-26 exactly but WITHOUT the `OPENSSL_INCLUDE_DIR`/`OpenSSL::Crypto` lines (quantization has zero third-party dependencies): sources `quantization.cpp` + `../../include/util/quantization.hpp`; `target_include_directories(sgprocmanagerquant PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../include>)`; no `target_link_libraries` call is needed; end with `sgnus_install(sgprocmanagerquant)`.

Edit `src/processors/CMakeLists.txt`: in the existing `target_link_libraries(SGProcessors PUBLIC ...)` block, add `sgprocmanagerquant` on its own line immediately after the existing `sgprocmanagersha` entry, so every processor file (linked via `SGProcessors`) can call into it. `SGProcessors` is a PUBLIC dependency of `ProcessingBase`, so this also transitively propagates to `tools/capture/`'s executables in Wave 3 without any further CMake change there.
  </action>
  <verify>
    <automated>grep -c "QuantizeFloatBuffer\|QuantizeByteBuffer" SuperGenius/SGProcessingManager/include/util/quantization.hpp</automated>
  </verify>
  <acceptance_criteria>
    - `SuperGenius/SGProcessingManager/include/util/quantization.hpp` declares `namespace sgns::sgprocmanagerquant` containing exactly `void QuantizeFloatBuffer( float *data, size_t count );` and `void QuantizeByteBuffer( uint8_t *data, size_t count );` — no other public functions.
    - `SuperGenius/SGProcessingManager/src/util/quantization.cpp` implements both functions; neither body performs any arithmetic operation on the `data` pointer or dereferences it (grep for `data[` or `*data` inside this file returns no matches).
    - `SuperGenius/SGProcessingManager/src/util/CMakeLists.txt` contains `add_library(sgprocmanagerquant` and `sgnus_install(sgprocmanagerquant)`; does not reference `OpenSSL::Crypto` or `OPENSSL_INCLUDE_DIR` inside that new block.
    - `SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt`'s `target_link_libraries(SGProcessors ...)` block lists `sgprocmanagerquant`.
    - CMake configure + build of the `sgprocmanagerquant` and `SGProcessors` targets succeeds with exit code 0 (per `SuperGenius/AgentDocs/CLAUDE.md`'s documented `cd build/<Platform>/<BuildType> && cmake .. -G "Ninja" && ninja` convention).
  </acceptance_criteria>
  <done>sgprocmanagerquant library builds, links into SGProcessors, and exposes the two identity-stub functions with zero third-party dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add ExecutionContext::rawOutputCapture opt-in capture callback</name>
  <files>SuperGenius/SGProcessingManager/include/execution/execution_context.hpp</files>
  <read_first>
    SuperGenius/SGProcessingManager/include/execution/execution_context.hpp
  </read_first>
  <action>
In the `ExecutionContext` struct (currently declaring `cancelToken`, `progressCallback`, `deadlineMs`, `gpuMemoryBudget`, `maxOutputArtifactBytes`), add one new field immediately after `progressCallback`: `std::function<void( const std::vector<uint8_t> &quantizedBytes, const std::vector<uint8_t> &preQuantizeBytes )> rawOutputCapture;`. `<functional>` is already included by this header; also add `#include <vector>` (not yet included) since the new field's signature needs `std::vector<uint8_t>`.

Add a Doxygen-style doc comment directly above the field (matching this struct's existing per-field trailing-comment convention): state it is a capture-only hook that fires with (quantized bytes, pre-quantize bytes) at each processor's existing hash call site, stays unset (nullptr) in production job execution, and is only ever set by `tools/capture/capture_harness.cpp` (Wave 3) — mirroring `progressCallback`'s opt-in injection pattern via `ExecutionContext`, not a new `StartProcessing()` parameter.

Do NOT modify `ExecutionContext::NoOp()`'s body to assign a lambda to `rawOutputCapture` — unlike `progressCallback`, this field must stay unset (default-constructed `std::function`, which is falsy in an `if (execCtx.rawOutputCapture)` check) even from `NoOp()`, so every production and test caller that never explicitly sets it pays zero capture-path cost. This is a deliberate divergence from how `NoOp()` treats `progressCallback` — document it as a one-line comment inside `NoOp()` itself so a future reader does not "fix" it to match `progressCallback`.
  </action>
  <verify>
    <automated>grep -c "rawOutputCapture" SuperGenius/SGProcessingManager/include/execution/execution_context.hpp</automated>
  </verify>
  <acceptance_criteria>
    - `ExecutionContext` struct declares a field named exactly `rawOutputCapture` of type `std::function<void( const std::vector<uint8_t> &, const std::vector<uint8_t> & )>` (parameter names may vary but both parameters are `const std::vector<uint8_t> &`).
    - `ExecutionContext::NoOp()`'s body does not contain the token `rawOutputCapture` (region-scoped: within the `NoOp()` function body only — the field declaration itself, outside `NoOp()`, is expected to contain this token).
    - The header still compiles standalone (no missing `#include <vector>`).
  </acceptance_criteria>
  <done>ExecutionContext exposes rawOutputCapture as an opt-in, no-op-by-default std::function field; NoOp() leaves it unset.</done>
</task>

</tasks>

## Artifacts this phase produces

- New file: `SuperGenius/SGProcessingManager/include/util/quantization.hpp`
- New file: `SuperGenius/SGProcessingManager/src/util/quantization.cpp`
- New namespace: `sgns::sgprocmanagerquant`
- New function: `QuantizeFloatBuffer( float *data, size_t count )`
- New function: `QuantizeByteBuffer( uint8_t *data, size_t count )`
- New CMake target: `sgprocmanagerquant`
- New struct field: `ExecutionContext::rawOutputCapture` (`std::function<void( const std::vector<uint8_t> &, const std::vector<uint8_t> & )>`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture_harness (Wave 3, external caller) → ExecutionContext::rawOutputCapture | The only trust boundary this plan creates: a caller-supplied callback invoked with raw output bytes from inside production processor code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Tampering | ExecutionContext::rawOutputCapture default state | low | mitigate | Field defaults to an unset std::function (falsy); every processor call site (Wave 2) must guard with `if (execCtx.rawOutputCapture)` before invoking it, so a caller that never sets it (all production job execution) cannot be affected by this new field at all |
| T-10-08 | Elevation of Privilege | sgprocmanagerquant identity-stub functions | low | accept | No behavior change this phase (literal no-op); nothing to elevate — real logic and its own review lands in Phase 12 |

No new package-manager installs in this plan (pure in-tree C++/CMake addition, zero third-party dependency) — the npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `sgprocmanagerquant` and `SGProcessors` targets build with the toolchain documented in `SuperGenius/AgentDocs/CLAUDE.md`.
2. `execution_context.hpp` compiles standalone and `rawOutputCapture` is reachable as `sgns::sgprocessing::ExecutionContext{}.rawOutputCapture`.
3. `ExecutionContext::NoOp()` produces a context whose `rawOutputCapture` evaluates false in a boolean context (unset).
</verification>

<success_criteria>
sgprocmanagerquant exists, links into SGProcessors with zero new third-party dependencies, and ExecutionContext exposes rawOutputCapture as an opt-in field that defaults to unset even via NoOp(). Both are ready for Wave 2 plans (10-02, 10-03, 10-04) to call at each processor's existing hash call site.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/10-capture-harness-diff-tool-quantization-stub/10-01-SUMMARY.md` when done
</output>
