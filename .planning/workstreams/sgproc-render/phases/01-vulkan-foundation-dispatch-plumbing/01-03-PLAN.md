---
phase: 01-vulkan-foundation-dispatch-plumbing
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - thirdparty/.gitmodules
  - thirdparty/build/CommonTargets.cmake
  - SuperGenius/SGProcessingManager/cmake/CommonBuildParameters.cmake
  - SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md
  - thirdparty/Vulkan-Headers (submodule pin — only if a version bump is required for vk-bootstrap compatibility)
autonomous: true

requirements: [CTX-01, CTX-04]

must_haves:
  truths:
    - "vk-bootstrap is vendored as a git submodule pinned to the tag confirmed by plan 01-02's human checkpoint, not a moving branch"
    - "vk-bootstrap's build is wired to use the project's existing thirdparty/Vulkan-Headers via vk-bootstrap's own external-header-directory CMake option, not a second independently-fetched copy of Vulkan-Headers, with Vulkan-Headers version compatibility explicitly checked (and bumped if required)"
    - "Vulkan-ValidationLayers vendoring deferral is a written, discoverable decision, not silently absent"
  artifacts:
    - thirdparty/vk-bootstrap (submodule)
    - SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md
  key_links:
    - "SGProcessingManager/cmake/CommonBuildParameters.cmake's find_package(vk-bootstrap CONFIG REQUIRED) resolves to the ExternalProject_Add(vk-bootstrap ...) target built by thirdparty/build/CommonTargets.cmake"
---

<objective>
Vendor `vk-bootstrap` (per D-01, resolved in plan 01-02) as a pinned git submodule with matching CMake build wiring, and produce the CTX-04 written decision documenting that Vulkan-ValidationLayers vendoring is explicitly deferred to v1.x.

Purpose: Give plan 01-04's `RenderProcessor` a real, buildable `vk-bootstrap` target to link against, and close CTX-04 with a discoverable artifact rather than a silent omission.
Output: `thirdparty/vk-bootstrap` submodule, CMake `ExternalProject_Add`/`find_package` wiring, `SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor vk-bootstrap as a pinned git submodule</name>
  <files>thirdparty/.gitmodules</files>
  <read_first>thirdparty/.gitmodules (lines 70-78, the Vulkan-Headers/Vulkan-Loader/MoltenVK submodule entries), plan 01-02's SUMMARY.md (confirmed release tag)</read_first>
  <action>Using the exact release tag confirmed in plan 01-02's checkpoint (read from `01-02-SUMMARY.md`), add a new `[submodule "vk-bootstrap"]` entry to `thirdparty/.gitmodules` mirroring the existing `Vulkan-Headers` entry's shape exactly: `path = vk-bootstrap`, `url = https://github.com/charles-lunarg/vk-bootstrap.git`. Then run `git submodule add https://github.com/charles-lunarg/vk-bootstrap.git thirdparty/vk-bootstrap` from the repo root, and `git -C thirdparty/vk-bootstrap checkout &lt;confirmed-tag&gt;` to pin exactly the confirmed tag — never track a moving branch (`main`/`master`).</action>
  <verify>
    <automated>git submodule status thirdparty/vk-bootstrap</automated>
  </verify>
  <acceptance_criteria>`thirdparty/.gitmodules` contains a `[submodule "vk-bootstrap"]` block matching the existing entries' format; `thirdparty/vk-bootstrap/` exists as a checked-out submodule at the exact tag confirmed by plan 01-02 (not a branch HEAD); no other submodule entry in `.gitmodules` was modified.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add CMake build wiring for vk-bootstrap</name>
  <files>thirdparty/build/CommonTargets.cmake, SuperGenius/SGProcessingManager/cmake/CommonBuildParameters.cmake</files>
  <read_first>thirdparty/build/CommonTargets.cmake (lines 362-392, the Vulkan-Headers/Vulkan-Loader ExternalProject_Add blocks and their enclosing `if(NOT ANDROID)` guard), SuperGenius/SGProcessingManager/cmake/CommonBuildParameters.cmake (lines 175-178, the libp2p find_package pattern), thirdparty/vk-bootstrap/CMakeLists.txt (the vendored submodule's own build file, added by Task 1 — read this before finalizing CMAKE_CACHE_ARGS, since RESEARCH.md flagged the exact args as unverified at research time), thirdparty/.gitmodules (current `Vulkan-Headers` pin, currently `v1.3.302-1-g9dff1f5` per `git submodule status thirdparty/Vulkan-Headers`)</read_first>
  <action>**vk-bootstrap header-collision guard (user-flagged, blocking sub-requirement):** vk-bootstrap's own `CMakeLists.txt` by default fetches or bundles its own copy of Vulkan-Headers when built standalone — this project already vendors `thirdparty/Vulkan-Headers` as a pinned submodule and must not end up with two independently-sourced copies of the Vulkan headers in the build. Read `thirdparty/vk-bootstrap/CMakeLists.txt` and identify the cache variable it exposes for pointing at an externally-supplied Vulkan-Headers location (do not assume a name — vk-bootstrap has historically exposed a `VK_BOOTSTRAP_VULKAN_HEADER_DIR`-style option for exactly this; confirm the actual variable name and expected form — raw include directory vs. installed `VulkanHeaders` CMake package — by reading the file directly). Set that variable via `CMAKE_CACHE_ARGS` in the `ExternalProject_Add(vk-bootstrap ...)` block below to point at this project's already-vendored `thirdparty/Vulkan-Headers` (or its installed package location under `<INSTALL_DIR>/share/cmake/VulkanHeaders`, matching whatever form the discovered option expects), so vk-bootstrap never fetches or builds a second copy. Separately, check vk-bootstrap's documented minimum-supported Vulkan-Headers version against the currently pinned `thirdparty/Vulkan-Headers` tag (`v1.3.302-1-g9dff1f5`); if vk-bootstrap requires a newer Vulkan-Headers release, bump the `thirdparty/Vulkan-Headers` submodule to a compatible tag as part of this task — pin to an explicit tag (never track `main`/`master`), matching Task 1's pinning discipline — and record the version bump explicitly in `01-03-SUMMARY.md`. If no bump is needed, state that explicitly in the summary too (not a silent no-op).

In `thirdparty/build/CommonTargets.cmake`, inside the existing `if(NOT ANDROID)` block (lines 362-392), immediately after the `Vulkan-Loader` `ExternalProject_Add` block and before that block's closing `endif()`, add a new `ExternalProject_Add(vk-bootstrap PREFIX vk-bootstrap SOURCE_DIR "${THIRDPARTY_DIR}/vk-bootstrap" CMAKE_CACHE_ARGS -DCMAKE_INSTALL_PREFIX:PATH=&lt;INSTALL_DIR&gt; -DVulkanHeaders_DIR:PATH=&lt;INSTALL_DIR&gt;/share/cmake/VulkanHeaders [+ the external-header-dir cache var discovered above] ${_CMAKE_COMMON_CACHE_ARGS} DEPENDS Vulkan-Headers)` block, matching the `-DVulkanHeaders_DIR` value the existing `Vulkan-Loader` block already uses. Adjust the exact `CMAKE_CACHE_ARGS` to match whatever options `thirdparty/vk-bootstrap/CMakeLists.txt` actually declares (verify by reading that file directly — do not assume). In `SuperGenius/SGProcessingManager/cmake/CommonBuildParameters.cmake`, add `set(vk-bootstrap_DIR "${_THIRDPARTY_BUILD_DIR}/vk-bootstrap/lib/cmake/vk-bootstrap")` followed by `find_package(vk-bootstrap CONFIG REQUIRED)`, mirroring the existing libp2p pattern at lines 175-178 exactly — adjust the package/target name to match whatever CMake package name `thirdparty/vk-bootstrap`'s installed config actually exports (verify by inspecting the vendored source's `CMakeLists.txt`/`cmake/` directory).</action>
  <verify>
    <automated>grep -c "ExternalProject_Add(vk-bootstrap" thirdparty/build/CommonTargets.cmake</automated>
  </verify>
  <acceptance_criteria>`thirdparty/build/CommonTargets.cmake` defines an `ExternalProject_Add(vk-bootstrap ...)` target that `DEPENDS Vulkan-Headers`; `SGProcessingManager/cmake/CommonBuildParameters.cmake` calls `find_package(vk-bootstrap CONFIG REQUIRED)`; no existing `ExternalProject_Add` block (`Vulkan-Headers`, `Vulkan-Loader`, `MNN`) was modified; a CMake configure pass for the `thirdparty` build tree succeeds with the new target present (verified when plan 01-04 links against it); vk-bootstrap's build is configured to use this project's existing `thirdparty/Vulkan-Headers` via whichever CMake option vk-bootstrap exposes for an external header directory — not a second, independently-fetched copy of Vulkan-Headers; if a Vulkan-Headers version bump was required for compatibility, `thirdparty/Vulkan-Headers` is pinned to the new tag and this is stated explicitly in `01-03-SUMMARY.md` (or the summary states no bump was needed).</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document the CTX-04 validation-layers deferral decision</name>
  <files>SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md</files>
  <read_first>SuperGenius/SGProcessingManager/doc/processing-json-guide.md (existing doc/ directory convention — confirm heading/tone style), REQUIREMENTS.md CTX-04 and v2 VALLAYER-01 entries</read_first>
  <action>Create `SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md` with four sections: (1) **Decision** — Vulkan-ValidationLayers is explicitly NOT vendored or wired into debug or CI builds for v1.0. (2) **Rationale** — CTX-04/D-07 scoped this out of v1 to keep Phase 1's new-vendoring surface to `vk-bootstrap` only; validation layers are a debug/CI tooling aid, not a runtime-correctness requirement for CTX-01..03/DISP-01..03, and this project's CI has no macOS Vulkan signal today regardless of validation-layer presence. (3) **Deferred to** — tracked as v2 requirement `VALLAYER-01` in `.planning/workstreams/sgproc-render/REQUIREMENTS.md` ("Vulkan-ValidationLayers wired into debug/CI builds"), to be implemented in v1.x. (4) **Future hook point** — if implemented later, `vkb::InstanceBuilder::request_validation_layers(true)` (vk-bootstrap has direct built-in support for this) would be gated behind a new CMake debug-only option, analogous to the existing `SANITIZE_CODE` debug-only toggle already present in `CommonCompilerOptions.cmake`.</action>
  <verify>
    <automated>test -f SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md && grep -c "VALLAYER-01" SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md</automated>
  </verify>
  <acceptance_criteria>The file exists and is committed; it explicitly states the decision, the rationale, and the `VALLAYER-01`/v1.x tracking reference — not a bare "TODO: add validation layers later" with no rationale.</acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces (this plan)

- `thirdparty/vk-bootstrap` — new vendored git submodule (pinned tag confirmed by plan 01-02).
- `ExternalProject_Add(vk-bootstrap ...)` CMake target in `thirdparty/build/CommonTargets.cmake`.
- `SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md` — new CTX-04 decision document.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `thirdparty/` vendoring pipeline | External code entering the build via git submodule, now materialized as an actual pinned checkout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-SC | Tampering | `thirdparty/vk-bootstrap` submodule pin | high | mitigate | Task 1 pins to the exact tag confirmed by plan 01-02's human checkpoint; never tracks a branch, so a future upstream compromise cannot silently flow in via an untracked `git submodule update --remote` |
| T-01-04 | (n/a — documentation gap, not a runtime threat) Absence of validation-layer tooling in debug/CI builds | low | accept | Explicitly documented in Task 3's decision doc per CTX-04; tracked as `VALLAYER-01` for v1.x — accepted for v1.0 because validation layers are a development/debugging aid, not a runtime correctness control for this phase's shipped code path |

</threat_model>

<verification>
- `git submodule status thirdparty/vk-bootstrap` reports a pinned commit matching the confirmed tag.
- `thirdparty/build/CommonTargets.cmake` and `SGProcessingManager/cmake/CommonBuildParameters.cmake` both reference `vk-bootstrap` consistently (ExternalProject_Add target name matches the find_package package name).
- `SuperGenius/SGProcessingManager/doc/vulkan-validation-layers-decision.md` exists and names `VALLAYER-01`.
</verification>

<success_criteria>
`vk-bootstrap` is vendored, pinned, and buildable as a CMake target that plan 01-04 can link against. CTX-04's written decision exists and is discoverable in the codebase, not silently dropped.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-03-SUMMARY.md` when done
</output>
