---
phase: 01-vulkan-foundation-dispatch-plumbing
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - SuperGenius/SGProcessingManager/include/processors/processing_processor_render.hpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp
  - SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt
autonomous: true

requirements: [CTX-01, CTX-03]

must_haves:
  truths:
    - "A headless VkInstance/VkPhysicalDevice/VkDevice/VkQueue can be created for RenderProcessor with no VkSurfaceKHR, swapchain, or WSI extension present, on Windows/Linux/macOS-MoltenVK"
    - "Physical-device selection for RenderProcessor follows a documented, deterministic scoring policy (discrete > integrated > reject CPU/software, VRAM-heap tie-break) — never 'pick index 0'"
    - "RenderProcessor's Vulkan context is created lazily on first StartProcessing call, not eagerly at construction"
  artifacts:
    - SuperGenius/SGProcessingManager/include/processors/processing_processor_render.hpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp
  key_links:
    - "RenderProcessor::StartProcessing() -> InitializeContext() -> sgns::sgprocessing::VulkanInitMutex() -> vkb::InstanceBuilder/PhysicalDeviceSelector/DeviceBuilder"
---

<objective>
Implement `RenderProcessor`, a new `ProcessingProcessor` sibling to the MNN processor family, that lazily creates a headless Vulkan instance/device/queue (no WSI/swapchain, ever) via `vk-bootstrap`, applies this phase's deterministic device-selection scoring, and serializes its init through the shared `VulkanInitMutex()` from plan 01-01.

Purpose: This is CTX-01/CTX-03's core deliverable — a real, coexistence-safe headless Vulkan context, proven architecturally correct for macOS/MoltenVK even without macOS CI signal yet (Phase 4's gap to close). Pipeline construction, draw calls, and readback are explicitly out of scope (Phase 3) — a minimal stub `StartProcessing` that proves context creation + coexistence + dispatch-reachability is sufficient here.
Output: `processing_processor_render.hpp`/`.cpp`, wired into the `SGProcessors` CMake target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-01-SUMMARY.md
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the RenderProcessor header</name>
  <files>SuperGenius/SGProcessingManager/include/processors/processing_processor_render.hpp</files>
  <read_first>SuperGenius/SGProcessingManager/include/processors/processing_processor_mnn_image.hpp (full file, closest analog), SuperGenius/SGProcessingManager/include/processors/processing_processor.hpp (base interface, full file)</read_first>
  <action>Create `include/processors/processing_processor_render.hpp` with `#pragma once`, `#include &lt;vulkan/vulkan.h&gt;` (for the `VkInstance`/`VkPhysicalDevice`/`VkDevice`/`VkQueue` handle typedefs) and `#include "processing_processor.hpp"`. Declare `namespace sgns::sgprocessing { class RenderProcessor : public ProcessingProcessor { ... } }`. Public members: a default constructor with an empty body (per D-06, no Vulkan calls at construction time), `~RenderProcessor() override = default;`, and `ProcessingResult StartProcessing( std::vector&lt;std::vector&lt;uint8_t&gt;&gt; &chunkhashes, const sgns::IoDeclaration &proc, std::vector&lt;char&gt; &imageData, std::vector&lt;char&gt; &modelFile, const std::vector&lt;sgns::Parameter&gt; *parameters ) override;` matching the base class signature exactly. Private members: `VkInstance m_instance{VK_NULL_HANDLE}; VkPhysicalDevice m_physicalDevice{VK_NULL_HANDLE}; VkDevice m_device{VK_NULL_HANDLE}; VkQueue m_queue{VK_NULL_HANDLE}; bool m_contextInitialized{false};` and a private helper declaration `bool InitializeContext();` (implemented in the .cpp, invoked from `StartProcessing` under the lazy-init guard).</action>
  <verify>
    <automated>grep -c "class RenderProcessor" SuperGenius/SGProcessingManager/include/processors/processing_processor_render.hpp</automated>
  </verify>
  <acceptance_criteria>`RenderProcessor` publicly inherits `ProcessingProcessor` and overrides `StartProcessing` with the exact base signature; no `VkSurfaceKHR`/`VkSwapchainKHR`-typed member or symbol appears anywhere in the file (`grep -c "VkSurfaceKHR\|VkSwapchainKHR" processing_processor_render.hpp` equals 0); the constructor body is empty.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement lazy headless context creation via vk-bootstrap with deterministic device selection</name>
  <files>SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp</files>
  <read_first>SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_image.cpp (lines 105-160, error-handling/logging convention: m_logger->error + early-return, never throw), SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp, RESEARCH.md "Pattern 4: Deterministic physical-device selection" and "Decision Point: Vulkan Bootstrap Approach" sections, thirdparty/vk-bootstrap/src/VkBootstrap.h (the vendored header from plan 01-03 — confirm exact `vkb::InstanceBuilder`/`vkb::PhysicalDeviceSelector`/`vkb::DeviceBuilder` API shape before writing this task's code, since RESEARCH.md flagged the exact API as WebSearch-sourced and unverified against a live checkout)</read_first>
  <action>Implement `bool RenderProcessor::InitializeContext()`: first check `if ( m_contextInitialized ) { return true; }` outside any lock (fast path). Then acquire `std::lock_guard&lt;std::mutex&gt; lock( sgns::sgprocessing::VulkanInitMutex() );` and re-check `if ( m_contextInitialized ) { return true; }` inside the lock (double-checked pattern, since another thread could have finished initializing between the fast-path check and acquiring the lock — though in this codebase's current per-call processor-instantiation pattern a fresh `RenderProcessor` is created per `StartProcessing` invocation, this guard is still correct and cheap). Build the instance via `vkb::InstanceBuilder`, requesting zero WSI/surface extensions and `.request_validation_layers(false)` (per CTX-04's deferral decision from plan 01-03), calling `.build()`. On failure, log via `m_logger-&gt;error(...)` and return `false` without mutating any member handle. Enumerate physical device candidates via `vkb::PhysicalDeviceSelector(vkb_instance).select_devices()` (NOT `.select()`, which silently falls back to any GPU type including CPU/software) to get all suitable candidates. Apply this phase's own deterministic scoring over the raw `VkPhysicalDevice` handles (extract them from the candidate wrapper type — confirm the exact field/accessor name against the vendored header): implement `static bool IsAcceptable( VkPhysicalDeviceType type )` returning true only for `VK_PHYSICAL_DEVICE_TYPE_DISCRETE_GPU` or `VK_PHYSICAL_DEVICE_TYPE_INTEGRATED_GPU` (hard-rejecting CPU/software per D-03), and `static VkDeviceSize LargestDeviceLocalHeap( VkPhysicalDevice device )` summing the largest `VK_MEMORY_HEAP_DEVICE_LOCAL_BIT`-flagged heap from `vkGetPhysicalDeviceMemoryProperties()`. Rank: discrete above integrated; within the same rank, the largest `LargestDeviceLocalHeap()` wins (per D-02). If no candidate is acceptable, log an error and return `false`. Build the logical device via `vkb::DeviceBuilder` from the selected physical device, call `.build()`, then retrieve the graphics queue via `vkb::Device::get_queue(vkb::QueueType::graphics)`. On full success, store the resulting raw `VkInstance`/`VkPhysicalDevice`/`VkDevice`/`VkQueue` handles into the member variables and set `m_contextInitialized = true`; return `true`. In `StartProcessing()`, call `InitializeContext()`; if it returns `false`, log and return a default-constructed `ProcessingResult{}`. On success, also return a default-constructed `ProcessingResult{}` — Phase 3 implements the actual pipeline/draw/readback. Never throw an exception from any path in this file.</action>
  <verify>
    <automated>grep -c "VK_KHR_surface\|VkSurfaceKHR\|VkSwapchainKHR" SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp</automated>
  </verify>
  <acceptance_criteria>The grep count above is 0 (no WSI/surface/swapchain symbol anywhere in the file); `sgns::sgprocessing::VulkanInitMutex()` is acquired before any `vkb`/`vkCreate*`/`vkEnumeratePhysicalDevices` call; device selection explicitly rejects `VK_PHYSICAL_DEVICE_TYPE_CPU` and `VK_PHYSICAL_DEVICE_TYPE_OTHER`; `select_devices()` is used, not the default `select()`; `StartProcessing` never throws and always returns a `ProcessingResult` value on both success and failure paths.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire RenderProcessor into the SGProcessors build target</name>
  <files>SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt</files>
  <read_first>SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt (full file, already confirmed 87 lines)</read_first>
  <action>Add `processing_processor_render.cpp` to the `add_library(SGProcessors STATIC ...)` source list (alongside the other `processing_processor_mnn_*.cpp` entries, lines 2-19) and `../../include/processors/processing_processor_render.hpp` to the header list (lines 20-38), following the exact relative-path convention already used for every other entry. Add the vk-bootstrap CMake target (the exact target name exported by `thirdparty/vk-bootstrap`'s installed CMake config, as determined while completing plan 01-03's Task 2) to the existing `target_link_libraries(SGProcessors PUBLIC ...)` block (lines 50-61), alongside `Vulkan::Vulkan`.</action>
  <verify>
    <automated>grep -c "processing_processor_render.cpp" SuperGenius/SGProcessingManager/src/processors/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>`SGProcessors`'s source list includes `processing_processor_render.cpp` and its header list includes `processing_processor_render.hpp` (grep count above equals 1); `target_link_libraries` includes the vk-bootstrap target; no existing source/header entry was removed; the `SGProcessors` target builds successfully.</acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces (this plan)

- `sgns::sgprocessing::RenderProcessor` — new class in `include/processors/processing_processor_render.hpp` / `src/processors/processing_processor_render.cpp`, implementing `ProcessingProcessor`.
- `RenderProcessor::InitializeContext()` — new private method performing lazy headless Vulkan context creation.
- `RenderProcessor::IsAcceptable(VkPhysicalDeviceType)`, `RenderProcessor::LargestDeviceLocalHeap(VkPhysicalDevice)` — new static/free helper functions implementing deterministic device-selection scoring.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-process, cross-processor | `RenderProcessor`'s Vulkan init joins the same shared-mutex boundary established in plan 01-01 |
| Local hardware enumeration | `vkEnumeratePhysicalDevices`-derived device selection — no external/network input, pure local-machine hardware query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Denial of Service | `RenderProcessor::InitializeContext()` concurrent with MNN's Vulkan-init call sites | high | mitigate | Acquire the same shared `sgns::sgprocessing::VulkanInitMutex()` established in plan 01-01 before any `vkb`/`vkCreate*` call (Task 2); runtime-verified end-to-end by plan 01-06's concurrent stress test |
| T-01-06 | Elevation of Privilege / Resource Exhaustion | Physical-device selection silently falling back to CPU/software on a multi-GPU or GPU-less node | medium | mitigate | Task 2's explicit `IsAcceptable()` rejection of `VK_PHYSICAL_DEVICE_TYPE_CPU`/`VK_PHYSICAL_DEVICE_TYPE_OTHER`, and use of `select_devices()` rather than vk-bootstrap's default `select()` (which defaults `allow_any_gpu_device_type()` to `true`) |

</threat_model>

<verification>
- `RenderProcessor` compiles as part of the `SGProcessors` static library.
- No WSI/surface/swapchain symbol appears anywhere in `processing_processor_render.hpp`/`.cpp`.
- `InitializeContext()` acquires `VulkanInitMutex()` before any Vulkan instance/device/enumeration call.
- Device selection rejects CPU/software device types unconditionally.
</verification>

<success_criteria>
CTX-01 and CTX-03 are satisfied: a headless, coexistence-safe Vulkan context can be created for `RenderProcessor` with zero WSI surface, and physical-device selection is deterministic and documented, never "index 0".
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-04-SUMMARY.md` when done
</output>
