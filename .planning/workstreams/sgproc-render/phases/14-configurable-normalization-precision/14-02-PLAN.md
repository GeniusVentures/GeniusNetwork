---
phase: 14-configurable-normalization-precision
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_float.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_buffer.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_bool.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_image.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_string.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat4.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat3.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat2.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_int.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texture1d.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texturecube.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_volume.cpp
  - SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp
autonomous: true
requirements: [QUANT-CFG-01, QUANT-CFG-02]

must_haves:
  truths:
    - "Every one of the 21 existing QuantizeFloatBuffer/QuantizeByteBuffer call sites across these 14 processor files now passes a value resolved via ResolveQuantScale/ResolveByteQuantMode -- a bare 2-arg call no longer compiles anywhere in this codebase, closing RESEARCH.md Pitfall 1's 'missed call site' risk by construction."
    - "A job schema declaring nothing for quantScale/byteQuantMode still produces byte-for-byte identical output to v2.1 at all 14 of these processors, since ResolveQuantScale/ResolveByteQuantMode's fallback values (32768.0f / 0, built in Plan 14-01) exactly match v2.1's prior hardcoded constants (QUANT-CFG-02)."
    - "Every processor file's StartProcessing signature is unchanged -- the resolved scale/maskBits value is obtained from the already-in-scope parameters argument each function already receives, no new function parameter is added anywhere in this plan."
  artifacts:
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_float.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_buffer.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_bool.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_image.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_string.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat4.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat3.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat2.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_int.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texture1d.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texturecube.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_volume.cpp
    - SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp
  key_links:
    - "Each processor's already-in-scope StartProcessing parameters argument <-> the new ResolveQuantScale (or ResolveByteQuantMode for render.cpp) call added near the top of each function body -- zero StartProcessing signature changes anywhere in this plan."
    - "mnn_volume.cpp's newly-wired ResolveQuantScale call <-> Plan 14-03's tex3d/spleen_ct_seg binary search -- Plan 14-03 cannot measure or choose a tex3d-specific quantScale value until this plan's mnn_volume.cpp call site actually reads and uses the schema-declared value instead of the hardcoded constant."
---

<objective>
Wire the resolved quantization value into all 21 existing `QuantizeFloatBuffer`/`QuantizeByteBuffer` call sites across the 14 processor files that currently call them with only 2 arguments. Every file already receives `parameters` as a `StartProcessing` argument (13 of 14 currently silence it with an unused-parameter suppression statement that becomes genuinely obsolete the moment this plan's resolve call is added); this plan adds exactly one resolve call per file and threads its result through each file's existing call site(s) unchanged otherwise.

Purpose: this is the mechanical, high-file-count wiring step RESEARCH.md's Pitfall 1 warns is easy to undercount by hand -- every one of the grep-confirmed 21 call sites across these 14 files is individually assigned to one of this plan's 3 tasks below, none left implicit.
Output: all 14 processor files compile against Plan 14-01's new 3-arg `QuantizeFloatBuffer`/`QuantizeByteBuffer` signatures, each resolving its own scale/maskBits value from the job's schema exactly once per `StartProcessing` call.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire quantScale resolution into the 5 single/no-precedent float processors (mnn_float, mnn_buffer, mnn_bool, mnn_image, mnn_string)</name>
  <files>SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_float.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_buffer.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_bool.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_image.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_string.cpp</files>
  <read_first>
    SuperGenius/SGProcessingManager/include/util/quantization.hpp (Plan 14-01's final ResolveQuantScale signature and contract)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_float.cpp (StartProcessing signature at line 168; unused-parameter suppression statement at line 175; QuantizeFloatBuffer call sites at lines 311 and 349)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_buffer.cpp (StartProcessing at line 136; suppression statement at line 143; call site at line 270)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_bool.cpp (StartProcessing at line 185; suppression statement at line 192; call site at line 339)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_image.cpp (StartProcessing at line 21; suppression statement at line 28; call site at line 110)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_string.cpp (StartProcessing at line 46; the existing maxLength lookup block at lines 74-89 -- this file has no suppression statement to remove, parameters is already actively used; call site at line 147)
  </read_first>
  <action>
    In `processing_processor_mnn_float.cpp`: replace the existing unused-parameter suppression statement at line 175 with a single new line, `const float scale = sgprocmanagerquant::ResolveQuantScale( parameters );`. Update both existing `QuantizeFloatBuffer` calls (the per-chunk call at line 311 and the stitched-output call at line 349) to pass `scale` as a third argument, unchanged otherwise.
    In `processing_processor_mnn_buffer.cpp`: replace the suppression statement at line 143 with the identical resolve line. Update the single `QuantizeFloatBuffer` call at line 270 to pass `scale` as a third argument.
    In `processing_processor_mnn_bool.cpp`: replace the suppression statement at line 192 with the identical resolve line. Update the single `QuantizeFloatBuffer` call at line 339 to pass `scale`.
    In `processing_processor_mnn_image.cpp`: replace the suppression statement at line 28 with the identical resolve line. Update the single `QuantizeFloatBuffer` call at line 110 to pass `scale`.
    In `processing_processor_mnn_string.cpp`: this file has no suppression statement to remove -- `parameters` is already read for its existing `maxLength` lookup (lines 74-89). Insert the identical resolve line immediately before that block (before the `int maxLength = 128;` declaration around line 74). Update the single `QuantizeFloatBuffer` call at line 147 to pass `scale`.
    None of these 5 files need a new `#include` -- each already includes `util/quantization.hpp` (every one already calls `QuantizeFloatBuffer`).
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target SGProcessors --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - For each of these 5 files, `grep -c "sgprocmanagerquant::ResolveQuantScale( parameters )" &lt;file&gt;` returns exactly 1.
    - For `mnn_float.cpp`, `mnn_buffer.cpp`, `mnn_bool.cpp`, `mnn_image.cpp`: `grep -c "(void)parameters;" &lt;file&gt;` returns 0 (the vestigial suppression line is gone in all four).
    - `mnn_string.cpp` still contains its existing `maxLength` lookup block unmodified in structure, with the new resolve line preceding it.
    - Every `QuantizeFloatBuffer(` call in these 5 files takes exactly 3 comma-separated arguments, the third being `scale`.
    - `cmake --build SuperGenius/build/Windows/Debug --target SGProcessors --config Debug` succeeds (compile-only check; this target statically links all 14 processor files without executing any test binary).
  </acceptance_criteria>
  <done>All 6 QuantizeFloatBuffer call sites across these 5 files resolve their scale from the job's own schema via ResolveQuantScale instead of relying on quantization.cpp's internal fallback implicitly; the SGProcessors library target compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire quantScale resolution into the 5 double-call-site matrix/tensor/int processors (mnn_mat4, mnn_mat3, mnn_mat2, mnn_int, mnn_tensor)</name>
  <files>SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat4.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat3.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat2.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_int.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp</files>
  <read_first>
    SuperGenius/SGProcessingManager/include/util/quantization.hpp (Plan 14-01's final ResolveQuantScale signature and contract)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat4.cpp (StartProcessing at line 186; suppression statement at line 193; call sites at lines 339 and 377)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat3.cpp (StartProcessing at line 186; suppression statement at line 193; call sites at lines 339 and 377)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_mat2.cpp (StartProcessing at line 186; suppression statement at line 193; call sites at lines 339 and 377)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_int.cpp (StartProcessing at line 120; suppression statement at line 127; call sites at lines 277 and 315)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_tensor.cpp (StartProcessing at line 186; suppression statement at line 193; call sites at lines 362 and 400)
  </read_first>
  <action>
    All 5 files in this task share the identical shape: one unused-parameter suppression statement immediately inside `StartProcessing`, followed later by exactly two `QuantizeFloatBuffer` call sites (a per-chunk-loop call and a stitched-output call). For each file, replace the suppression statement at its listed line with `const float scale = sgprocmanagerquant::ResolveQuantScale( parameters );`, then update both of that file's `QuantizeFloatBuffer` calls (the listed per-chunk and stitched-output line numbers) to pass `scale` as a third argument, unchanged otherwise. Apply this identically to: `mnn_mat4.cpp` (suppression at 193; calls at 339, 377), `mnn_mat3.cpp` (suppression at 193; calls at 339, 377), `mnn_mat2.cpp` (suppression at 193; calls at 339, 377), `mnn_int.cpp` (suppression at 127; calls at 277, 315), `mnn_tensor.cpp` (suppression at 193; calls at 362, 400). None of these 5 files need a new `#include` -- each already includes `util/quantization.hpp`.
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target SGProcessors --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - For each of these 5 files, `grep -c "sgprocmanagerquant::ResolveQuantScale( parameters )" &lt;file&gt;` returns exactly 1, and `grep -c "(void)parameters;" &lt;file&gt;` returns 0.
    - Every `QuantizeFloatBuffer(` call across these 5 files (10 call sites total) takes exactly 3 comma-separated arguments, the third being `scale`.
    - `cmake --build SuperGenius/build/Windows/Debug --target SGProcessors --config Debug` succeeds.
  </acceptance_criteria>
  <done>All 10 QuantizeFloatBuffer call sites across these 5 files resolve their scale from the job's own schema via ResolveQuantScale; the SGProcessors library target compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the 3 ParseLayout-family processors (mnn_texture1d, mnn_texturecube, mnn_volume) and render.cpp's byte-path call site</name>
  <files>SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texture1d.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texturecube.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_volume.cpp, SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp</files>
  <read_first>
    SuperGenius/SGProcessingManager/include/util/quantization.hpp (Plan 14-01's final ResolveQuantScale/ResolveByteQuantMode signatures and contracts)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_volume.cpp:209-265 (StartProcessing at line 209; suppression statement at line 216; ParseLayout already consumes parameters at line 261; QuantizeFloatBuffer call site at line 521)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texture1d.cpp (StartProcessing at line 257; suppression statement at line 264; QuantizeFloatBuffer call site at line 411)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_texturecube.cpp (StartProcessing at line 257; suppression statement at line 264; QuantizeFloatBuffer call sites at lines 480 and 526)
    SuperGenius/SGProcessingManager/src/processors/processing_processor_render.cpp:2026-2211 (StartProcessing at line 2026; ResolveUniforms already consumes parameters at line 2090 -- no suppression statement exists in this file; QuantizeByteBuffer call site at line 2207)
  </read_first>
  <action>
    In `processing_processor_mnn_volume.cpp`: replace the existing unused-parameter suppression statement at line 216 with `const float scale = sgprocmanagerquant::ResolveQuantScale( parameters );` -- this line is now genuinely redundant anyway, since `ParseLayout( parameters, ... )` at line 261 already actively consumes `parameters` two statements later in the same function. Update the single `QuantizeFloatBuffer` call at line 521 to pass `scale` as a third argument.
    In `processing_processor_mnn_texture1d.cpp`: replace the suppression statement at line 264 with the identical resolve line. Update the single `QuantizeFloatBuffer` call at line 411 to pass `scale`.
    In `processing_processor_mnn_texturecube.cpp`: replace the suppression statement at line 264 with the identical resolve line. Update both `QuantizeFloatBuffer` calls (lines 480 and 526) to pass `scale`.
    In `processing_processor_render.cpp`: this file has no suppression statement to remove -- `parameters` is already actively consumed by the existing `ResolveUniforms( uniformsMap, parameters, resolvedUniforms, errorOut )` call at line 2090. Insert `const int maskBits = sgns::sgprocmanagerquant::ResolveByteQuantMode( parameters );` immediately after that `ResolveUniforms` call succeeds (before line 2096's comment). Update the single `QuantizeByteBuffer` call at line 2207 to pass `maskBits` as a third argument -- note this is the byte path (`ResolveByteQuantMode`/`maskBits`), not the float path used by the other 3 files in this task.
    None of these 4 files need a new `#include` -- each already includes `util/quantization.hpp` (every one already calls `QuantizeFloatBuffer`/`QuantizeByteBuffer`).
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target SGProcessors --config Debug</automated>
  </verify>
  <acceptance_criteria>
    - For `mnn_volume.cpp`, `mnn_texture1d.cpp`, `mnn_texturecube.cpp`: `grep -c "sgprocmanagerquant::ResolveQuantScale( parameters )" &lt;file&gt;` returns exactly 1, and `grep -c "(void)parameters;" &lt;file&gt;` returns 0.
    - `render.cpp`: `grep -c "sgns::sgprocmanagerquant::ResolveByteQuantMode( parameters )" processing_processor_render.cpp` returns exactly 1.
    - Every `QuantizeFloatBuffer(` call in `mnn_volume.cpp`/`mnn_texture1d.cpp`/`mnn_texturecube.cpp` (4 call sites total) takes exactly 3 arguments, the third being `scale`; the single `QuantizeByteBuffer(` call in `render.cpp` takes exactly 3 arguments, the third being `maskBits`.
    - `cmake --build SuperGenius/build/Windows/Debug --target SGProcessors --config Debug` succeeds.
  </acceptance_criteria>
  <done>All 4 remaining float call sites (mnn_volume/texture1d/texturecube) resolve scale via ResolveQuantScale, and render.cpp's byte-path call site resolves maskBits via ResolveByteQuantMode; the SGProcessors library target compiles clean. All 21 call sites across all 14 processor files are now wired -- none left calling the old 2-arg signature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Per-file mechanical wiring consistency across 14 independently-maintained processor files | The risk this plan exists to close: a missed or miswired call site silently keeps one processor on the old hardcoded behavior even though its schema declares a custom value, defeating QUANT-CFG-01's guarantee for that one data type only -- invisible until a future cross-hardware divergence investigation for that specific processor (RESEARCH.md Pitfall 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | Any of the 21 call sites across 14 processor files | high | mitigate | Plan 14-01 made `scale`/`maskBits` required (non-defaulted) function parameters specifically so a missed call site fails to compile rather than silently continuing to use stale hardcoded behavior; this plan's own acceptance criteria additionally grep-verify, per file, that the vestigial suppression statement is gone and the resolve call is present exactly once -- both a compiler-enforced and a grep-enforced check for the same risk. |
| T-14-06 | Tampering | render.cpp's byte path resolving the wrong function (ResolveQuantScale instead of ResolveByteQuantMode, or vice versa for a float file) | medium | mitigate | This plan's Task 3 explicitly calls out render.cpp as the one byte-path file (`ResolveByteQuantMode`/`maskBits`) versus the other 13 float-path files (`ResolveQuantScale`/`scale`); acceptance criteria grep for the correct function name per file, catching a swapped resolver at review time rather than silently at runtime. |

No package-manager installs occur in this plan -- every file touched is an existing in-repo `.cpp` source file already compiled by the existing `SGProcessors` CMake target; only the value passed to an already-present function call changes. The npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `grep -rc "(void)parameters;" SuperGenius/SGProcessingManager/src/processors/processing_processor_mnn_{float,buffer,bool,image,mat4,mat3,mat2,int,tensor,texture1d,texturecube,volume}.cpp` returns 0 for every one of these 12 files.
2. Every one of the 21 grep-confirmed `QuantizeFloatBuffer`/`QuantizeByteBuffer` call sites across all 14 files now takes exactly 3 arguments.
3. `cmake --build SuperGenius/build/Windows/Debug --target SGProcessors --config Debug` succeeds with zero compile errors across all 14 modified files.
</verification>

<success_criteria>
All 21 existing quantization call sites across all 14 processor files resolve their scale/mask value from the job's own schema via ResolveQuantScale/ResolveByteQuantMode; zero call sites remain on the old 2-arg signature; the SGProcessors library compiles clean.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- No new symbols -- this plan exclusively wires Plan 14-01's `ResolveQuantScale`/`ResolveByteQuantMode` into 21 pre-existing call sites across 14 pre-existing files. Every `QuantizeFloatBuffer`/`QuantizeByteBuffer` call site across `processing_processor_mnn_{float,buffer,bool,image,string,mat4,mat3,mat2,int,tensor,texture1d,texturecube,volume}.cpp` and `processing_processor_render.cpp` now passes a resolved `scale`/`maskBits` value instead of relying on a 2-arg call.
</output>
