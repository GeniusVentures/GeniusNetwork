---
phase: 14-configurable-normalization-precision
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - SuperGenius/test/src/processing_conformance_security/secv01_tex3d_counter_test.cpp
  - SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
  - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-corrupted-spleen_ct_seg.mnn
  - SuperGenius/test/src/processing_datatypes/texture3d-processing-definition.json
autonomous: true
requirements: [QUANT-CFG-03]

must_haves:
  truths:
    - "A byte-perturbed copy of spleen_ct_seg.mnn, run through the same MNN volume pipeline shape as texture3d-processing-definition.json with a schema-declared quantScale, produces a post-quantization artifactId that differs from the correct model's artifactId (D-09/D-10, mirrors SECV-01)."
    - "tex3d/spleen_ct_seg's final quantScale value is chosen via the same binary-search-against-a-counter-test methodology Phase 13 used for the small MNN model (13-04-PLAN.md), not guessed a priori (D-10, QUANT-CFG-03)."
    - "The final chosen quantScale value is documented with direct citation to the real captured cross-machine divergence data for this specific workload (delta 0.005126953125, 168x the current S=2^15 grid step) -- mirroring QUANT-04's empirical-derivation discipline (QUANT-CFG-03)."
    - "texture3d-processing-definition.json declares the final chosen quantScale as a schema parameter, so the production fixture itself (not just the new test) benefits from the empirically-derived value."
  artifacts:
    - SuperGenius/test/src/processing_conformance_security/secv01_tex3d_counter_test.cpp
    - SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
    - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-corrupted-spleen_ct_seg.mnn
    - SuperGenius/test/src/processing_datatypes/texture3d-processing-definition.json
  key_links:
    - "Plan 14-02's mnn_volume.cpp ResolveQuantScale wiring <-> this plan's binary search -- the search cannot measure anything meaningful until the real schema-declared quantScale actually reaches QuantizeFloatBuffer through mnn_volume.cpp's StartProcessing."
    - "output.artifacts[0].artifactId (ComputeArtifactIdentity) <-> the D-01-compliant hash target for the new counter-test -- never output.combinedHash / ExecutionManifest.manifestHash (Pitfall 3)."
---

<objective>
Prove that tex3d/spleen_ct_seg's own, much coarser quantization precision (previously locked to the same S=2^15 constant tuned for a tiny unrelated fixture) still catches a genuinely corrupted model: add a new SECV-01-style counter-test (`secv01_tex3d_counter_test.cpp`), binary-search the widest power-of-two `quantScale` that still passes it, and commit that empirically-derived value into `texture3d-processing-definition.json`'s schema parameters.

Purpose: this directly closes QUANT-CFG-03 -- the `tex3d`/`spleen_ct_seg` gap this whole milestone exists to fix (STATE.md: all 25 chunk hashes mismatch cross-hardware at S=2^15, real delta 168x the current grid step) -- with a value chosen by the same empirical discipline Phase 13 used for the small model, not guessed.
Output: new `secv01_tex3d_counter_test.cpp` + corrupted-model fixture, wired into the existing `processing_conformance_security_test` CTest target; `texture3d-processing-definition.json` updated with the final `quantScale` value and its citation trail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the corrupted spleen_ct_seg.mnn fixture and verify it loads with materially different output</name>
  <files>SuperGenius/test/src/processing_conformance_security/fixtures/secv01-corrupted-spleen_ct_seg.mnn</files>
  <read_first>
    SuperGenius/test/src/processing_datatypes/spleen_ct_seg.mnn (source model, 19339764 bytes -- the file this fixture is a byte-perturbed copy of)
    .planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-02-PLAN.md (Task 1 -- the exact corrupted-fixture creation technique this task replicates at a larger scale: single-byte XOR with 0xFF well into the file's back region, past the flatbuffers schema/vtable region near the front, then empirically verify the corrupted file still loads and diverges before committing)
    SuperGenius/test/src/processing_datatypes/texture3d-processing-definition.json (confirms the exact input shape spleen_ct_seg.mnn is exercised with: input_nodes shape [1,1,96,96,96], output_nodes shape [1,2,96,96,96])
  </read_first>
  <action>
    Copy `spleen_ct_seg.mnn`'s 19339764 bytes to the new fixture path. Pick a starting candidate byte offset well into the back ~75-80% region of the file (e.g. offset 15000000, comfortably past the flatbuffers schema/vtable region concentrated near the front) and flip that single byte by XORing it with 0xFF. Verify empirically, before committing anything, via a small standalone check (a scratch program or an ad hoc local harness invoking `MNN::Interpreter::createFromBuffer()` directly) that: (a) the corrupted bytes still produce a non-null interpreter and a session can be created, and (b) running the same volume input (`spleen_15.raw`) through both the original and corrupted model produces at least one materially different output element -- not merely a bit-flip MNN's graph structure silently absorbs. If offset 15000000 breaks parsing (interpreter returns null) or produces byte-identical output, try 17000000, then 10000000, then 5000000 in that order, keeping the first offset that both loads successfully and diverges. Commit only the final working corrupted `.mnn` file, exactly 19339764 bytes (same length as the original) except for the single flipped byte.
  </action>
  <verify>
    <automated>MISSING -- Task 2 creates secv01_tex3d_counter_test.cpp, which is the actual automated check that exercises this fixture end-to-end via the real MNN::Interpreter load path and the full ProcessingManager pipeline; there is no standalone CTest target for the fixture file in isolation, mirroring 12-02-PLAN.md Task 1's identical precedent.</automated>
  </verify>
  <acceptance_criteria>
    - `secv01-corrupted-spleen_ct_seg.mnn` is exactly 19339764 bytes and differs from `spleen_ct_seg.mnn` in at least one byte (confirm via a byte-level diff or checksum comparison).
    - `MNN::Interpreter::createFromBuffer()` on the corrupted bytes returns non-null and a session can be created (empirically confirmed before committing the file).
    - Running the same `spleen_15.raw` input through both models produces at least one differing output element (empirically confirmed before committing).
  </acceptance_criteria>
  <done>A byte-perturbed, still-loadable copy of spleen_ct_seg.mnn exists on disk, checked into git, confirmed to produce materially different inference output than the original.</done>
</task>

<task type="auto">
  <name>Task 2: Write the tex3d SECV-01-style counter-test, binary-search the final quantScale, and commit the fixture JSON update</name>
  <files>SuperGenius/test/src/processing_conformance_security/secv01_tex3d_counter_test.cpp, SuperGenius/test/src/processing_conformance_security/CMakeLists.txt, SuperGenius/test/src/processing_datatypes/texture3d-processing-definition.json</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp (full existing file -- the exact Secv01CounterTest class shape, PatchJsonUrisToAbsolute/Create/Process pattern, and the binary std::memcmp on .artifacts[0].artifactId to replicate verbatim)
    SuperGenius/test/src/processing_conformance_security/CMakeLists.txt (current single-source addtest() call and its POST_BUILD fixtures/ copy step)
    SuperGenius/test/src/processing_datatypes/texture3d-processing-definition.json (current structure, dimensions, parameters array -- the exact job shape to mirror inline)
    SuperGenius/test/src/processing_datatypes/processing_datatypes_test.cpp:328-372 (Texture3DProcessingTest -- the OLDER Process()-return-value convention, `.value().size()`/`.empty()`, to explicitly NOT replicate here -- Pitfall 3)
    .planning/workstreams/sgproc-render/STATE.md ("New evidence for QUANT-CFG-01" paragraph -- the real captured tex3d/spleen_ct_seg divergence: all 25 chunk hashes mismatch, delta 0.005126953125 = 168x the current S=2^15 grid step of 3.0517578125e-05)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-04-PLAN.md (Task 1 -- the exact local rebuild+ctest binary-search loop shape this task replicates for tex3d's own value, including the "one power-of-two step of margin above the first confirmed failure boundary" choice discipline)
  </read_first>
  <action>
    Create `secv01_tex3d_counter_test.cpp` under namespace `sgns` with `class Secv01Tex3dCounterTest : public ProcessorConformanceFixture {}` (no `SetUpTestSuite` override, mirroring `Secv01CounterTest`'s exact shape). Add one `TEST_F` case, `MnnCorruptedSpleenCtSegModelStillDiverges`: construct two inline JSON string literals shaped exactly like `texture3d-processing-definition.json` (same `inputs`/`outputs`/`passes` structure: `inputVolume` from `"file://processing_datatypes/spleen_15.raw"`, dimensions width 253/height 253/chunk_count 94/chunk_subchunk_width 96/chunk_subchunk_height 96/block_len 96/chunk_stride 48/chunk_line_stride 48/block_stride 48, format FLOAT32, output `segmentationOutput`, single `volume_inference` pass with `input_nodes` shape [1,1,96,96,96] and `output_nodes` shape [1,2,96,96,96]) plus a `parameters` array containing both the existing `modelUri` entry and a new `quantScale` entry (type `"float"`) -- one job with `model.source_uri_param` and the `modelUri` parameter both set to `"file://processing_datatypes/spleen_ct_seg.mnn"` (correct model), the other set to `"file://processing_conformance_security/fixtures/secv01-corrupted-spleen_ct_seg.mnn"` (Task 1's corrupted copy); both jobs declare the identical candidate `quantScale` value under test in their `parameters` array. Patch both via `PatchJsonUrisToAbsolute( json_str, BinPath() )`. `Create()` + `Process()` (4-arg overload, exactly mirroring `processing_datatypes_test.cpp`'s `Texture3DProcessingTest` call shape: `manager->Process( ioc, chunkhashes, model_node, output_locations )` with `model_node` taken from `passes[0].get_model().value().get_input_nodes()[0]`) each; `ASSERT_TRUE` both `pr.has_value()`. Assert `std::memcmp( prCorrect.value().artifacts[0].artifactId, prCorrupted.value().artifacts[0].artifactId, sgns::sgprocessing::SHA256_HASH_SIZE ) != 0` -- the identical binary `EXPECT_NE` pattern `secv01_counter_test.cpp` uses; never read `.value().size()`/`.empty()` anywhere in this new file (Pitfall 3).

    Register the new source in `SuperGenius/test/src/processing_conformance_security/CMakeLists.txt`'s existing `addtest(processing_conformance_security_test ...)` call as a second source file alongside `secv01_counter_test.cpp` -- this reuses the existing `POST_BUILD` `fixtures/` copy step already in that file (which already generalizes to any file placed under `fixtures/`, including Task 1's new corrupted `.mnn`); no other line in this CMakeLists.txt needs to change.

    Binary-search the final `quantScale` value locally (D-10, mirrors 13-04-PLAN.md's process exactly -- no cross-machine capture needed for this step, it is a local pass/fail check). Start from the candidate power-of-two set implied by STATE.md's estimate (measured real delta 0.005126953125 is ~168x the current S=2^15 grid step of 3.0517578125e-05): try 128.0 (2^7) and 256.0 (2^8) first. For each candidate `S`, set both inline JSON literals' `quantScale` value to `S`, then rebuild and run: `cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug` followed by `ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv01Tex3dCounterTest -V`. Narrow the power-of-two search (halving/doubling `S`) until you bracket the exact boundary between the widest `S` where the test passes (correct and corrupted `artifactId`s differ) and the narrowest `S` where it starts failing (the two `artifactId`s collide). Choose the final `S` as one full power-of-two step above the first confirmed failure boundary -- the same margin discipline Phase 13 used choosing S=2^15 for the small model -- never the exact edge value.

    Once the final `S` is chosen, hardcode it into both inline JSON literals in `secv01_tex3d_counter_test.cpp`, and add a matching `quantScale` parameter entry to `texture3d-processing-definition.json`'s `parameters` array (`"name": "quantScale"`, `"type": "float"`, `"default": &lt;final S&gt;`) with a `"description"` field citing the real captured divergence this value is derived from: the measured delta 0.005126953125 (168x the current S=2^15 grid step of 3.0517578125e-05), the binary-search range searched, and the final chosen margin -- mirroring 13-04-PLAN.md's citation-trail discipline for the small model's S=2^15 choice.
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug && ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv01Tex3dCounterTest -V | grep -qE "100% tests passed"</automated>
  </verify>
  <acceptance_criteria>
    - `secv01_tex3d_counter_test.cpp` asserts via `std::memcmp` on `.artifacts[0].artifactId` (`grep -c "artifacts\[0\].artifactId"` returns at least 2, one per job); this file never contains `.combinedHash` or `.value().size()`/`.value().empty()`.
    - `CMakeLists.txt`'s `addtest(processing_conformance_security_test ...)` call lists both `secv01_counter_test.cpp` and `secv01_tex3d_counter_test.cpp` as sources.
    - `texture3d-processing-definition.json`'s `parameters` array contains a new entry with `"name": "quantScale"`, a numeric `"default"` that is a power of two, and a `"description"` field containing the substring `"0.005126953125"`.
    - The final chosen `quantScale` value hardcoded in `secv01_tex3d_counter_test.cpp`'s two inline JSON literals exactly matches `texture3d-processing-definition.json`'s new `quantScale` default.
    - `ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv01Tex3dCounterTest -V` reports 100% tests passed at the final chosen value.
  </acceptance_criteria>
  <done>A new automated counter-test proves tex3d/spleen_ct_seg's own empirically-derived quantScale (cited against real captured cross-machine divergence data, chosen via the same binary-search discipline as Phase 13's S=2^15) still catches a deliberately corrupted model, and the production fixture JSON declares that same value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Corrupted spleen_ct_seg.mnn fixture -> real MNN inference pipeline -> post-quantization hash | The deliberate adversarial-simulation boundary this plan's counter-test exists to prove: does a much coarser, tex3d-specific quantScale still reject a plausible-but-wrong result, the same question SECV-01 already answered for the small model at S=2^15. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering / Spoofing | MNN model-file substitution (secv01-corrupted-spleen_ct_seg.mnn) at tex3d's own coarser precision | critical | mitigate | Secv01Tex3dCounterTest.MnnCorruptedSpleenCtSegModelStillDiverges asserts the corrupted model's post-quantization artifactId differs from the correct model's at the final chosen quantScale -- direct empirical proof that a grid coarse enough to serve this workload's real divergence is not simultaneously loose enough to also mask a substituted model. |
| T-14-08 | Tampering | Choosing a tex3d quantScale wide enough to pass STATE.md's cross-hardware divergence but wide enough to also mask a corrupted model | high | mitigate | D-10's binary-search-against-this-counter-test methodology (mirroring 13-04-PLAN.md) empirically finds the exact pass/fail boundary and picks one full power-of-two step of margin above it, rather than guessing a value a priori -- this is precisely what distinguishes an empirically-derived value from an unverified one. |
| T-14-09 | Denial of Service | Corruption offset lands in the flatbuffers header/vtable region, breaking model parsing outright | low | accept | Task 1's action specifies an empirical load-and-diverge verification loop with 4 fallback offsets tried in order (mirroring 12-02-PLAN.md's Pitfall 4 precedent) -- a broken offset is caught locally before the fixture is ever committed, not discovered later via a flaky CI failure. |

No package-manager installs occur in this plan -- it adds one new in-repo test source file, one new binary fixture file, and one new CMakeLists.txt source-list entry, all within the existing `processing_conformance_security_test` CTest target. The npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `secv01-corrupted-spleen_ct_seg.mnn` exists, is exactly 19339764 bytes, differs from the original by exactly one byte, and is confirmed loadable/divergent before commit.
2. `secv01_tex3d_counter_test.cpp` asserts via the D-01-compliant `.artifacts[0].artifactId` `std::memcmp` pattern, never `.combinedHash`.
3. `ctest -R Secv01Tex3dCounterTest` passes at the final empirically-chosen `quantScale` value.
4. `texture3d-processing-definition.json`'s new `quantScale` parameter cites the real measured delta (0.005126953125) and matches the test's hardcoded value exactly.
</verification>

<success_criteria>
tex3d/spleen_ct_seg is configured with its own empirically-derived quantScale, chosen via the same binary-search-against-a-counter-test discipline Phase 13 used for the small model and cited against real captured cross-machine divergence data (not guessed a priori) -- QUANT-CFG-03 fully satisfied.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/14-configurable-normalization-precision/14-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New C++ class `sgns::Secv01Tex3dCounterTest` (extends `ProcessorConformanceFixture`) in `secv01_tex3d_counter_test.cpp`.
- New `TEST_F` case: `Secv01Tex3dCounterTest.MnnCorruptedSpleenCtSegModelStillDiverges`.
- New fixture file: `SuperGenius/test/src/processing_conformance_security/fixtures/secv01-corrupted-spleen_ct_seg.mnn`.
- `secv01_tex3d_counter_test.cpp` added as a second source in the existing `processing_conformance_security_test` CTest target (`CMakeLists.txt`).
- New `quantScale` parameter entry in `texture3d-processing-definition.json`'s `parameters` array, with the final empirically-derived value and its citation trail.
</output>
