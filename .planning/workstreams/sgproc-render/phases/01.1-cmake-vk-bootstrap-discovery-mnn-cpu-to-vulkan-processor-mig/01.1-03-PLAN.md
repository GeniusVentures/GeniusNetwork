---
phase: 01.1-cmake-vk-bootstrap-discovery-mnn-cpu-to-vulkan-processor-mig
plan: 03
type: execute
wave: 2
depends_on: ["01.1-02"]
files_modified:
  - SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp
  - SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp
autonomous: true
requirements: [MIGR-02, COV-01]

must_haves:
  truths:
    - "vulkan_init_guard.hpp's doc comment no longer states a stale, undercounting fixed number of Vulkan-init call sites (it previously said 'MNN's 3 existing... call sites plus RenderProcessor') — it now describes the guarded pattern generically or states a freshly-verified count with an explicit re-verify-on-future-migration note"
    - "vulkan_init_concurrency_test.cpp documents that its 3 exercised MNN call sites (String, Volume) plus RenderProcessor are representative of the full, larger set of callers now sharing VulkanInitMutex() post-migration, following the same shared-accessor generalization argument 01-06-SUMMARY.md already used to justify not separately exercising MNN_Image"
    - "The 13 migration-candidate *ProcessingTest cases in ProcessingDatatypesTest (Bool/Buffer/Float/Int/Mat2/Mat3/Mat4/Tensor/Texture1D/TextureCube/Vec2/Vec3/Vec4) are re-run against the now-Vulkan-backed processors and their existing tolerance-based comparisons (mean_abs_diff < 1e-3, max_abs_diff < 1e-2) are confirmed to still hold — no new coverage tooling (gcov/lcov/OpenCppCoverage) is introduced, per RESEARCH.md's resolved reading of 'coverage'"
  artifacts:
    - "SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp (modified: doc comment only, mutex implementation unchanged)"
    - "SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp (modified: scope-clarifying comment only, test logic unchanged)"
  key_links:
    - "This plan depends on 01.1-02 completing first — the doc comment's accurate call-site count and the coverage re-verification both require the migration to already be in place, not planned"
</must_haves>
---

<objective>
Close out this phase's two remaining threads now that migration (01.1-02) is complete: (1) update `vulkan_init_guard.hpp`'s doc comment so it no longer undercounts the real number of Vulkan-init call sites sharing the mutex (MIGR-02), and (2) resolve "coverage" per RESEARCH.md's evidence-backed reading — re-verify the existing `ProcessingDatatypesTest` suite's 13 migration-candidate cases against the newly-Vulkan-backed processors, and update the concurrent-init stress test's scope documentation to acknowledge the larger caller set it now generalizes over (COV-01). This explicitly does NOT introduce gcov/lcov/OpenCppCoverage or any new code-coverage tooling — RESEARCH.md found none exists anywhere in this workstream's CI, and the concrete, already-available mechanism (the existing local, tolerance-based GTest fixtures) is the actionable interpretation of "coverage" for this migration.

Purpose: Prevent the stale-documentation trap RESEARCH.md's Pitfall 3 flags (a future engineer undercounting real Vulkan-init call sites by 4x+), and prove the migration didn't regress processor output, using infrastructure that already exists rather than standing up new tooling.
Output: An accurate `vulkan_init_guard.hpp` doc comment, an updated scope note in `vulkan_init_concurrency_test.cpp`, and a re-verification pass of the 13 `*ProcessingTest` cases against the migrated processors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/01.1-cmake-vk-bootstrap-discovery-mnn-cpu-to-vulkan-processor-mig/01.1-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/01.1-cmake-vk-bootstrap-discovery-mnn-cpu-to-vulkan-processor-mig/01.1-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update vulkan_init_guard.hpp's doc comment to reflect the post-migration call-site set</name>
  <files>SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp</files>
  <read_first>
    SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp:1-17 (full file — the doc comment at lines 6-11 is the only thing changing; the mutex implementation at lines 12-16 is untouched)
  </read_first>
  <action>
    First, re-verify the current real call-site count by running `grep -rc MNN_FORWARD_VULKAN SuperGenius/SGProcessingManager/src/processors/*.cpp` (do not reuse a number from RESEARCH.md's estimate without re-checking — RESEARCH.md's own summary text and its detailed per-file findings disagree by one, so confirm the true figure directly against the now-fully-migrated source). Then rewrite the header comment above `VulkanInitMutex()` (currently a three-sentence block describing a fixed, now-inaccurate small count of call sites) to describe the guarded set as a pattern rather than a hardcoded number: every MNN processor's `createSession()` call using the Vulkan backend, plus `RenderProcessor`'s lazy-init path — instructing a reader to grep `MNN_FORWARD_VULKAN` across `src/processors/*.cpp` for the current count rather than trusting a comment that can drift out of date on the next backend migration. Keep the rest of the comment's content (process-wide scope, repeated-acquisition semantics, magic-static thread-safety note) intact — only the stale fixed-count sentence changes. Do not modify the `inline std::mutex &VulkanInitMutex()` implementation itself.
  </action>
  <verify>
    <automated>test "$(grep -c 'MNN.s 3' SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp)" = "0" && grep -q 'VulkanInitMutex' SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp</automated>
  </verify>
  <acceptance_criteria>
    - The doc comment no longer hardcodes a specific small call-site count tied to "MNN's 3 existing" sites
    - The doc comment still accurately describes: process-wide scope, repeated (not run-once) acquisition, and that it covers every MNN Vulkan-backend `createSession()` call site plus `RenderProcessor`
    - `inline std::mutex &VulkanInitMutex()` and its magic-static implementation are byte-identical to before this task
  </acceptance_criteria>
  <done>grep confirms the stale hardcoded-count phrasing is gone and the mutex accessor implementation is unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update vulkan_init_concurrency_test.cpp's scope documentation for the larger post-migration caller set</name>
  <files>SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp</files>
  <read_first>
    SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp:1-152 (full file — the test body/thread logic is unchanged; only a scope-clarifying comment is added/updated)
    .planning/workstreams/sgproc-render/phases/01-vulkan-foundation-dispatch-plumbing/01-06-SUMMARY.md (key-decisions: the existing generalization argument — "correct serialization for 3 distinct callers implies correct serialization for the 4th" via the identical shared accessor — this task extends that same argument in a comment, it does not change the test's structure)
  </read_first>
  <action>
    Add or update a comment near the `VulkanConcurrentInitTest` fixture class (or immediately above `RepeatedConcurrentInitNoRaceOrCrash`) documenting that this test exercises 3 representative call sites (MNN_String, MNN_Volume, RenderProcessor) as a sample of the full set of callers that now share `VulkanInitMutex()` after this phase's migration (16 MNN processors + RenderProcessor — re-verify the exact MNN processor count via the same grep used in Task 1 rather than hardcoding a number that can drift), and that this is the same shared-accessor generalization argument 01-06's plan rationale already established (correct serialization for a representative subset, all going through the identical accessor function, implies correct serialization for the full set). Do not add new threads, new call sites, or change `kIterations` — this is a documentation-only change per RESEARCH.md's resolution of Open Question 2 (extending thread variety is a nice-to-have, not required).
  </action>
  <verify>
    <automated>grep -q 'VulkanInitMutex' SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp && test "$(grep -c 'TEST_F( VulkanConcurrentInitTest, RepeatedConcurrentInitNoRaceOrCrash )' SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - A comment referencing the representative-subset/shared-accessor generalization argument is present near the test fixture or test case
    - `kIterations`, the 3-thread structure, and `TEST_F( VulkanConcurrentInitTest, RepeatedConcurrentInitNoRaceOrCrash )` are unchanged from before this task
  </acceptance_criteria>
  <done>grep confirms the test signature and iteration structure are unchanged; a scope-clarifying comment is present</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify ProcessingDatatypesTest's 13 migration-candidate cases against the Vulkan-backed processors</name>
  <files>SuperGenius/test/src/processing_datatypes/processing_datatypes_test.cpp</files>
  <read_first>
    SuperGenius/test/src/processing_datatypes/processing_datatypes_test.cpp:977-1026 (Mat2ProcessingTest — representative example of the existing tolerance-based comparison pattern shared by all 13 migration-candidate *ProcessingTest cases)
  </read_first>
  <action>
    No new test code is written in this task — per RESEARCH.md's resolved reading of "coverage" (test/verification coverage of the migration via existing fixtures, not new code-coverage tooling), re-run the 13 existing `*ProcessingTest` cases that correspond to this phase's migrated processors (BoolProcessingTest, BufferProcessingTest, FloatProcessingTest, IntProcessingTest, Mat2ProcessingTest, Mat3ProcessingTest, Mat4ProcessingTest, TensorProcessingTest, Texture1DProcessingTest, TextureCubeProcessingTest, Vec2ProcessingTest, Vec3ProcessingTest, Vec4ProcessingTest) against the now-Vulkan-backed processors from 01.1-02, using the already-built `processing_datatypes_test` binary/CTest target if the local build environment supports it (build directories with existing CMakeCache.txt are present under `SuperGenius/build/Windows/`). Confirm each case's existing tolerance comparison (`mean_abs_diff < 1e-3`, `max_abs_diff < 1e-2`) still passes with Vulkan-backend output. If the full build+run cycle cannot complete in this session (missing toolchain step, environment gap unrelated to this migration), record which of the 13 cases were structurally reviewed vs. actually executed as an explicit `human_judgment: true` coverage note in the SUMMARY — per this project's own established convention (Phase 1's `01-03`/`01-06` SUMMARYs) — rather than silently claiming full runtime verification.
  </action>
  <verify>
    <automated>test "$(grep -cE 'TEST_F\( ProcessingDatatypesTest, (Bool|Buffer|Float|Int|Mat2|Mat3|Mat4|Tensor|Texture1D|TextureCube|Vec2|Vec3|Vec4)ProcessingTest \)' SuperGenius/test/src/processing_datatypes/processing_datatypes_test.cpp)" = "13"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 migration-candidate `*ProcessingTest` cases are confirmed present (structural gate) and an attempt is made to actually execute them against the migrated build
    - No gcov/lcov/OpenCppCoverage instrumentation, CI wiring, or coverage-percentage tooling is added anywhere in this task
    - Any case not actually executed in this session is explicitly listed with a `human_judgment: true` rationale in the resulting SUMMARY, not silently assumed passing
  </acceptance_criteria>
  <done>grep confirms all 13 target *ProcessingTest cases exist in the suite; execution status (pass, or documented human_judgment gap) for each is recorded in the plan SUMMARY</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation / test-scope accuracy | No runtime trust boundary — stale documentation is a reliability/maintainability risk (a future engineer under-serializing new Vulkan-init call sites believing only 3-4 exist), not an injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-011-05 | Tampering (documentation-integrity) | `vulkan_init_guard.hpp` doc comment | low | mitigate | Rewrite to describe the guarded pattern rather than a hardcoded, driftable count, per RESEARCH.md Pitfall 3 — prevents this specific staleness from recurring on the next backend migration |
| T-011-06 | Repudiation (test-evidence gap) | Determinism/correctness evidence for the migrated processors | medium | mitigate | Re-run the existing 13 tolerance-based `*ProcessingTest` cases against the migrated Vulkan-backend output; any case not actually executed in this session is explicitly flagged `human_judgment: true` rather than silently assumed passing |

</threat_model>

<verification>
`vulkan_init_guard.hpp`'s doc comment accurately describes the guarded pattern (not a stale count); `vulkan_init_concurrency_test.cpp` documents that its 3 exercised call sites represent the larger post-migration caller set via the same shared-accessor generalization argument already established in 01-06; all 13 migration-candidate `*ProcessingTest` cases are confirmed present and an execution attempt (with explicit gap notes where unverifiable) is recorded.
</verification>

<success_criteria>
- No file in this phase's scope contains a stale, hardcoded Vulkan-init call-site count that undercounts the real post-migration total.
- The migration's correctness is evidenced by the existing local regression suite, not by newly-introduced code-coverage tooling.
</success_criteria>

## Artifacts This Plan Produces

| File | Change | Symbol/Block |
|------|--------|---------------|
| `SuperGenius/SGProcessingManager/include/processingbase/vulkan_init_guard.hpp` | Modified | Doc comment above `VulkanInitMutex()` rewritten (implementation unchanged) |
| `SuperGenius/test/src/processing_vulkan_concurrency/vulkan_init_concurrency_test.cpp` | Modified | Scope-clarifying comment added/updated near `VulkanConcurrentInitTest` (test body unchanged) |

<output>
Create `.planning/workstreams/sgproc-render/phases/01.1-cmake-vk-bootstrap-discovery-mnn-cpu-to-vulkan-processor-mig/01.1-03-SUMMARY.md` when done
</output>
