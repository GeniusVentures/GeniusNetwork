---
phase: 19-validation-re-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SuperGenius/test/src/processing/processing_validation_core_test.cpp
  - SuperGenius/test/src/processing/CMakeLists.txt
  - .planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-REVERIFICATION.md
  - .planning/workstreams/sgproc-render/REQUIREMENTS.md
  - .planning/workstreams/sgproc-render/ROADMAP.md
  - .planning/workstreams/sgproc-render/STATE.md
autonomous: true
requirements: [VALD-02]

must_haves:
  truths:
    - "The exact VALD-01 fixture pair named in 19-CONTEXT.md D-01 (xhw-mnn-float_Fuus-Mac-mini.local---macOS_20260812T232347.cap vs xhw-mnn-float_Mofu---Windows_20260812T232430.cap) is fed into the real, unmodified ProcessingValidationCore::ValidateResults/AttemptToleranceFallback production code path -- not a re-derived or substitute fixture, not a reimplementation of the tolerance formula."
    - "All 15 chunks of the fixture (per D-03) are exercised through a single ValidateResults call, with per-chunk hash bytes read directly from each .cap file's Artifact::chunkHashes -- not hand-typed synthetic values."
    - "The re-run's outcome is captured as concrete evidence (per-chunk hash-match self-check, chunk 10's measured maxAbsDelta against the D-03 tolerance bound, and ValidateResults' final invalidSubTaskIds/error state) in a new outcome document -- not inferred from the general test suite passing."
    - "19-REVERIFICATION.md explicitly classifies VALD-01's gap as closed, partially closed, or still open (per D-04), citing the exact maxAbsDelta/tolerance-bound numbers observed, mirroring 13-SCOPE-BOUNDARY.md's honest-reporting convention."
    - "No production code (quantScale's value, IsFloatChunkWithinTolerance's bound formula, QuantizeFloatBuffer) is modified by this phase -- per D-04's scope boundary, the only code change is the new test cases plus CMake wiring to reach them."
    - "Per D-02, the captured Mac/Windows chunk bytes are fed into ValidateResults/AttemptToleranceFallback via a direct unit-level call, mirroring processing_validation_core_test.cpp's existing ProcessingValidationCoreTest style -- since ChunkContribution is a private struct that cannot be constructed directly by a test (per processing_validation_core.hpp's ChunkContribution fields being non-public), the harness instead builds public SGProcessing::SubTaskCollection/SubTaskResult protobuf objects and calls the real 5-arg ValidateResults, which is still the direct-call route D-02 asks for and explicitly not SECV-02's heavier full-pipeline style (real ProcessTaskSplitter::SplitTask/SubTaskQueueAccessorImpl)."
  artifacts:
    - SuperGenius/test/src/processing/processing_validation_core_test.cpp (2 new TEST cases + a ReadFileBytes helper)
    - SuperGenius/test/src/processing/CMakeLists.txt (sgproccapture link + VALD02_CAPTURES_DIR compile definition on processing_validation_core_test)
    - .planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-REVERIFICATION.md
  key_links:
    - "New TEST cases -> sgns::sgproccapture::DeserializeCaptureFile -> Phase 13's real .cap files on disk (via VALD02_CAPTURES_DIR), not synthetic FloatToBytes buffers"
    - "New TEST cases -> sgns::processing::ProcessingValidationCore::ValidateResults (5-arg overload) -> AttemptToleranceFallback -> sgns::sgprocmanagerdiff::IsFloatChunkWithinTolerance (unchanged production formula, D-03's 2.0/quantScale bound)"
    - "19-REVERIFICATION.md's stated classification -> the actual ctest console output captured while running processing_validation_core_test (cited verbatim, not paraphrased)"
---

<objective>
Determine, with real evidence rather than assumption, whether Phase 15's `ProcessingValidationCore::ValidateResults` tolerance-fallback mechanism (XNODE-02) actually closes VALD-01's original MNN float32 gap -- Phase 13's finding that 1 of 15 chunk hashes (chunk 10, `maxAbsDelta = 3.0517578125e-05`, exactly one S=2^15 grid step) still diverges cross-hardware after quantization -- and document the outcome honestly (closed / partially closed / still open), per 19-CONTEXT.md D-01 through D-04.

This is a re-verification phase, not a fix phase: it exercises an already-shipped mechanism (Phase 15, v2.2) against an already-characterized fixture (Phase 13, v2.1). Per D-04, no production code (`quantScale`, `IsFloatChunkWithinTolerance`'s bound, `QuantizeFloatBuffer`) is modified this phase, regardless of what the re-run shows -- only test/harness-layer code and an outcome document are in scope.

Purpose: Close out VALD-02, the last open requirement of v2.3 (Deferred Gap Closure), and give the project a definitive, evidence-backed answer to a question that has been carried as an open item since 2026-08-13 (see STATE.md's Deferred Items table).
Output: Two new `TEST` cases in `processing_validation_core_test.cpp` that feed Phase 13's real captured Mac/Windows `.cap` bytes through the real `ValidateResults`/`AttemptToleranceFallback` code path across all 15 chunks, plus `19-REVERIFICATION.md` stating the honest outcome with cited evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/PROJECT.md
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md
@SuperGenius/AgentDocs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VALD-02 re-verification TEST cases to processing_validation_core_test.cpp + wire CMake</name>
  <files>SuperGenius/test/src/processing/processing_validation_core_test.cpp, SuperGenius/test/src/processing/CMakeLists.txt</files>
  <read_first>
    SuperGenius/test/src/processing/processing_validation_core_test.cpp (whole file -- extend this file's existing plain-TEST(), no-shared-fixture style; note the anonymous namespace at lines 137-154 already declaring FloatToBytes and MakeQuantScaleParameters(double quantScale) -- reuse MakeQuantScaleParameters(32768.0) unchanged, do not redeclare it)
    SuperGenius/test/src/processing/CMakeLists.txt (lines 31-38: the processing_validation_core_test addtest() block and its current target_link_libraries -- processing_service, SGProcessingProto only)
    SuperGenius/src/processing/processing_validation_core.hpp (ValidateResults' 5-arg signature lines 61-67; ChunkContribution's chunkIdx/totalChunksForSubtask fields lines 86-91 -- private, cannot be constructed directly by the test)
    SuperGenius/src/processing/processing_validation_core.cpp (lines 63-146: chunksBySubtask keying by chunkstoprocess(chunkIdx).SerializeAsString(), the allMatch/AttemptToleranceFallback dispatch; lines 257-355: AttemptToleranceFallback's fetch-then-slice loop, especially lines 301-321's uniform-division slicing branch used for totalChunksForSubtask > 1)
    SuperGenius/SGProcessingManager/include/util/diff_utils.hpp (IsFloatChunkWithinTolerance's signature and doc comment lines 92-117; ElementDiffStats struct lines 36-44)
    SuperGenius/SGProcessingManager/src/util/diff_utils.cpp (lines 202-228: IsFloatChunkWithinTolerance's actual bound formula -- `statsOut.maxAbsDelta <= 2.0 / static_cast<double>(*declaredScale)` when quantScale is validly declared)
    SuperGenius/SGProcessingManager/include/util/quantization.hpp (lines 78-120: current S=32768.0f/2^15 constant and its SECV-01-boundary derivation history -- use this exact value, do not invent a different scale)
    SuperGenius/SGProcessingManager/tools/capture/capture_file_format.hpp (CaptureRecord/CaptureFile struct layout lines 55-84; rawRecordsPerArtifact's index-alignment doc comment lines 72-79; DeserializeCaptureFile signature line 97)
    SuperGenius/SGProcessingManager/include/artifacts/artifact_types.hpp (Artifact struct lines 45-68: contentHash[32], chunkHashCount, chunkHashes[1024][32]; SHA256_HASH_SIZE=32 at line 21)
    SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp (lines 122-132: the ReadFileBytes helper to mirror; lines 145-184: DeserializeCaptureFile call + one-artifact-per-file + chunkHashCount-match validation pattern)
    SuperGenius/SGProcessingManager/test/capture/capture_smoke_test.cpp (lines 12-21: the FIXTURE_ROOT_PATH target_compile_definitions pattern for referencing fixture files that live outside the test's own CMakeLists.txt directory -- mirror this for VALD02_CAPTURES_DIR)
    SuperGenius/SGProcessingManager/tools/capture/CMakeLists.txt (lines 5-16: sgproccapture's PUBLIC include dirs, which is why `#include "tools/capture/capture_file_format.hpp"` resolves once sgproccapture is linked)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md ("SC1 Refit" and "SC1 Diagnostic: Chunk 10 Per-Element Numeric Characterization" sections -- the exact published numbers this task's assertions cross-check against; note per D-01-REVISED, Phase 13's own archived `.cap` files are no longer readable by current tooling -- this task uses a fresh 2-machine capture that independently reproduces the identical numbers, not Phase 13's original files)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit.json (chunkHashesMatch array: true at every index except index 10 -- Phase 13's original published reference; historical baseline only, not the fixture this task reads)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit-chunkdiag.json (chunkDiffs[10].maxAbsDelta = 3.0517578125e-05, elementCount = 64 -- Phase 13's original published reference; historical baseline only, not the fixture this task reads)
    .planning/workstreams/sgproc-render/phases/19-validation-re-verification/captures/diff-mnn-float-vald02-fresh.json (per D-01-REVISED, an independent capture_diff cross-check of the fresh fixture pair this task actually reads -- confirms chunkHashesMatch is true for every index except index 10, and chunk 10's maxAbsDelta = 3.0517578125e-05, maxUlpDistance = 2048, an exact match to Phase 13's original numbers despite being a wholly new 2-machine capture)
  </read_first>
  <action>
    NOTE: This task's code was already committed in the SuperGenius submodule as commit `a99a47c8`, built against Phase 13's archived (now-unreadable per D-01-REVISED) fixture filenames/path. The already-committed test logic, structure, and assertions are correct and must NOT be rewritten from scratch -- this is a targeted follow-up commit updating only the fixture filenames and the VALD02_CAPTURES_DIR path below, then re-verifying the build still passes against the new fixture.

    In SuperGenius/test/src/processing/CMakeLists.txt, add `sgproccapture` to processing_validation_core_test's target_link_libraries (alongside the existing processing_service and SGProcessingProto -- do not remove either). Add a target_compile_definitions(processing_validation_core_test PRIVATE ...) declaring VALD02_CAPTURES_DIR as the string literal `${CMAKE_CURRENT_SOURCE_DIR}/../../../../.planning/workstreams/sgproc-render/phases/19-validation-re-verification/captures` -- four `../` levels walk from SuperGenius/test/src/processing up through SuperGenius/test, SuperGenius/, to the outer repo root, landing in the outer repo's .planning tree (same depth as before; only the leaf phase-directory name changed, from `13-re-validation-scope-boundary-documentation` to `19-validation-re-verification`, per D-01-REVISED's fresh capture living in this phase's own captures directory). This mirrors capture_smoke_test.cpp's FIXTURE_ROOT_PATH pattern for referencing fixture files that live outside the CMakeLists.txt's own directory tree (no .cap files are copied or duplicated into SuperGenius). If commit `a99a47c8` already declared VALD02_CAPTURES_DIR pointing at Phase 13's captures directory, update that existing definition in place -- do not add a second, duplicate definition.

    In processing_validation_core_test.cpp, add `#include "tools/capture/capture_file_format.hpp"` (root-relative, resolves via sgproccapture's PUBLIC include dir once linked) below the existing includes. Add a `ReadFileBytes(const std::string &path, std::vector<uint8_t> &out)` free function to the existing anonymous namespace (alongside FloatToBytes/MakeQuantScaleParameters), mirroring capture_diff.cpp's ReadFileBytes exactly: open an std::ifstream in binary mode, return false immediately if `!stream.is_open()`, otherwise `out.assign(std::istreambuf_iterator<char>(stream), std::istreambuf_iterator<char>())` and return true.

    Add `TEST(ProcessingValidationCoreTest, Vald02ChunkHashSelfCheckMatchesPublishedRefit)`: build the Mac path as `std::string(VALD02_CAPTURES_DIR) + "/mnn-float_Fuus-Mac-mini.local---macOS_20260821T221542.cap"` and the Windows path with `"/mnn-float_Mofu---Windows_20260821T221735.cap"` (per D-01-REVISED's fresh fixture filenames, replacing the original D-01 `xhw-mnn-float_...` names -- if commit `a99a47c8` already hard-coded the old filenames in this TEST case, update both string literals in place); ReadFileBytes both, ASSERT_TRUE each read succeeds; `sgns::sgproccapture::DeserializeCaptureFile` both into `CaptureFile captureA`/`captureB`, ASSERT_TRUE both deserialize; ASSERT_EQ(1u, captureA.artifacts.size()) and the same for captureB (Phase 10's one-artifact-per-file scope, mirroring capture_diff.cpp's own check); ASSERT_EQ(15u, artifactA.chunkHashCount) and the same for artifactB. Loop j from 0 to 14: compare `artifactA.chunkHashes[j]` vs `artifactB.chunkHashes[j]` via `std::equal` over `sgns::sgprocessing::SHA256_HASH_SIZE` bytes (mirroring capture_diff.cpp lines 194-199's own chunkHashesMatch computation); assert the boolean is true for every j except j==10, and false for j==10 -- this is the independent proof that this test's own file-parsing produces the identical chunk-hash-match pattern already published in diff-mnn-float-refit.json, before any new conclusion is drawn from ValidateResults' behavior.

    Add `TEST(ProcessingValidationCoreTest, Vald02FullFixtureValidateResultsToleranceOutcome)`: reload both `.cap` files (repeat the ReadFileBytes + DeserializeCaptureFile steps -- do not share state across TEST cases, matching this file's existing no-fixture-class convention). Build a `SGProcessing::SubTaskCollection subTasks` with two subtasks, `subtaskid` "SUBTASK_MAC" and "SUBTASK_WIN"; give each subtask 15 `SGProcessing::ProcessingChunk` entries added via `add_chunkstoprocess()->CopyFrom(...)` in index order 0 to 14, each with a distinct `chunkid` (e.g. "MNN_FLOAT_CHUNK_0".."MNN_FLOAT_CHUNK_14") and `n_subchunks(1)` -- both subtasks must add the identical 15 chunk keys in the identical order so processing_validation_core.cpp's per-index `SerializeAsString()` keying groups each chunk's two contributions together. Build two `SGProcessing::SubTaskResult` entries: for SUBTASK_MAC, call `add_chunk_hashes(...)` 15 times in order j=0..14 with `std::string(reinterpret_cast<const char*>(artifactA.chunkHashes[j]), sgns::sgprocessing::SHA256_HASH_SIZE)`, and `set_ipfs_results_data_id("stub://vald02-mac")`; build SUBTASK_WIN analogously from artifactB.chunkHashes[j] and URI "stub://vald02-win". Build `jobParameters` via the file's existing `MakeQuantScaleParameters(32768.0)` helper -- do not invent a different scale value; 32768.0 is quantization.hpp's current shipped S=2^15 constant. Build a `fetchOutputData` lambda: for "stub://vald02-mac", concatenate `captureA.rawRecordsPerArtifact[0][j].quantizedBytes` for j=0..14 in order into one `std::vector<uint8_t>` (15 chunks * 256 bytes/chunk = 3840 bytes -- this reconstructs the full per-machine raw output blob that AttemptToleranceFallback's uniform-division slicing, processing_validation_core.cpp lines 306-315, expects for a 15-chunk subtask); return the Windows-equivalent concatenation for "stub://vald02-win"; for any other URI return `outcome::failure(std::make_error_code(std::errc::io_error))` (mirroring the file's existing fetchOutputData error convention). Call `ProcessingValidationCore().ValidateResults(subTasks, results, invalidSubTaskIds, &jobParameters, fetchOutputData)` (the 5-arg overload).

    In the same test, independently extract chunk 10's two 256-byte slices directly from `captureA.rawRecordsPerArtifact[0][10].quantizedBytes` and `captureB.rawRecordsPerArtifact[0][10].quantizedBytes`, and call `sgns::sgprocmanagerdiff::IsFloatChunkWithinTolerance(sliceA, sliceB, &jobParameters, statsOut)` directly (a second, independent code path from ValidateResults, proving this test's own byte extraction is correct regardless of ValidateResults' internal slicing). ASSERT_DOUBLE_EQ(3.0517578125e-05, statsOut.maxAbsDelta) against 13-SCOPE-BOUNDARY.md's published chunk-10 diagnostic number. Record the boolean IsFloatChunkWithinTolerance returns; per diff_utils.cpp's D-03 bound (2.0/32768.0 = 6.103515625e-05), 3.0517578125e-05 sits within that bound, so this direct call is expected to return true.

    Finally assert ValidateResults' own outcome. Given chunks 0-9/11-14 already hash-match trivially (Test 1's self-check, no fallback invoked) and chunk 10's direct IsFloatChunkWithinTolerance cross-check above is expected true, the expected ValidateResults outcome is `ASSERT_FALSE(validate_res.has_error())` and `ASSERT_TRUE(invalidSubTaskIds.empty())`. If the actual build-and-run contradicts this (validate_res.has_error() is true, or invalidSubTaskIds is non-empty), do NOT edit the assertion to force a pass -- per D-04's narrow exception, first check whether the discrepancy is a harness construction bug in this new test (wrong chunk ordering, URI mismatch, incorrect byte slicing) versus a genuine still-open production-level gap; fix only a confirmed harness bug, otherwise flip the assertion to match the actually-observed outcome and carry that observed result into Task 3's outcome document as the honest finding. Do not modify quantScale, IsFloatChunkWithinTolerance's bound formula, or QuantizeFloatBuffer under any circumstance this task.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "TEST(ProcessingValidationCoreTest, Vald02" SuperGenius/test/src/processing/processing_validation_core_test.cpp` returns exactly 2.
    - Source assertion: `grep -c "sgproccapture" SuperGenius/test/src/processing/CMakeLists.txt` returns at least 1.
    - Source assertion: `grep -c "VALD02_CAPTURES_DIR" SuperGenius/test/src/processing/CMakeLists.txt` returns at least 1.
    - Build command: from SuperGenius/build/Windows/Debug, `cmake --build . --target processing_validation_core_test --config Debug` completes with exit code 0 (0 compile errors).
    - No production file outside `SuperGenius/test/src/processing/` is modified by this task (per D-04): `git -C SuperGenius diff --name-only` shows only `test/src/processing/processing_validation_core_test.cpp` and `test/src/processing/CMakeLists.txt`.
  </acceptance_criteria>
  <verify>
    <automated>cd SuperGenius/build/Windows/Debug && cmake --build . --target processing_validation_core_test --config Debug</automated>
  </verify>
  <done>processing_validation_core_test builds cleanly with the 2 new Vald02* TEST cases added; no other SuperGenius source file changed; CMakeLists.txt links sgproccapture and declares VALD02_CAPTURES_DIR for the target.</done>
</task>

<task type="auto">
  <name>Task 2: Run the re-verification tests and capture per-chunk evidence</name>
  <files>(none -- this task runs already-built binaries and captures their output; no source files are modified)</files>
  <read_first>
    SuperGenius/build/Windows/Debug/test/src/processing/CTestTestfile.cmake (confirms processing_validation_core_test is already a registered CTest target in this exact build tree)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md (the "SC2: SECV-01 Re-Confirmation" section's verbatim-ctest-output citation convention -- Task 3 must cite this task's output the same way, not paraphrase it)
  </read_first>
  <action>
    From SuperGenius/build/Windows/Debug (the project's already-configured, live Visual Studio-generator build tree -- do not reconfigure, delete, or recreate this directory; do not run a separate scratch/isolated build), run `ctest -C Debug -R ^processing_validation_core_test$ -V` to execute the full processing_validation_core_test binary (all cases, including this plan's 2 new ones plus the file's 5 pre-existing cases) with verbose output, so ProcessingValidationCore's internal `AttemptToleranceFallback: maxAbsDelta=... maxRelDelta=... withinTolerance=...` debug log line is visible for chunk 10's mismatch. Scope this run to the processing_validation_core_test target only (per this project's established test-gate-scoping convention) -- do not run the full SuperGenius ctest suite. If verbose ctest output is truncated or the debug log line is not visible at the default log level, instead run the built test binary directly (e.g. `test_bin\Debug\processing_validation_core_test.exe --gtest_filter=ProcessingValidationCoreTest.Vald02*`) to capture full gtest console output for just the 2 new cases. Capture the complete, unedited console output (both new TEST cases' RUN/OK-or-FAILED lines, the exact numeric values logged) verbatim into a scratch text file for Task 3 to quote from directly -- do not paraphrase or round any number, mirroring 13-SCOPE-BOUNDARY.md's own verbatim-citation convention for `13-SECV01-RERUN.txt`.
  </action>
  <acceptance_criteria>
    - CLI output assertion: the captured console output contains the literal substring "Vald02ChunkHashSelfCheckMatchesPublishedRefit" paired with either "OK" or "FAILED".
    - CLI output assertion: the captured console output contains the literal substring "Vald02FullFixtureValidateResultsToleranceOutcome" paired with either "OK" or "FAILED".
    - Behavior assertion: the captured output includes at least one numeric maxAbsDelta value attributable to chunk 10 (either via the AttemptToleranceFallback debug log line or via a value the test itself prints/records), usable as cited evidence in Task 3 -- a bare pass/fail with no numeric evidence does not satisfy this task.
  </acceptance_criteria>
  <verify>
    <automated>cd SuperGenius/build/Windows/Debug && ctest -C Debug -R ^processing_validation_core_test$ -V</automated>
  </verify>
  <done>Both new TEST cases have been actually executed (not just compiled) against the real Phase 13 .cap fixture pair, and their console output -- including chunk 10's numeric maxAbsDelta and the final pass/fail verdict -- has been captured verbatim for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Write 19-REVERIFICATION.md and close out REQUIREMENTS.md/ROADMAP.md/STATE.md</name>
  <files>.planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-REVERIFICATION.md, .planning/workstreams/sgproc-render/REQUIREMENTS.md, .planning/workstreams/sgproc-render/ROADMAP.md, .planning/workstreams/sgproc-render/STATE.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md (the full document -- mirror its frontmatter shape, its per-fixture "Interpretation"/"Outcome" prose style, and its explicit refusal to round a partial result into a pass)
    .planning/workstreams/sgproc-render/phases/17-render-path-cross-hardware-tolerance/17-09-SUMMARY.md and 17-TOLERANCE-RESULTS.md (the "Gap Closure Addendum" convention -- documenting two numbers side by side rather than reinterpreting one as the other)
    .planning/workstreams/sgproc-render/REQUIREMENTS.md (VALD-02's current `- [ ]` line and the Traceability table's "VALD-02 | Phase 19 | Pending" row -- lines 30 and 61)
    .planning/workstreams/sgproc-render/ROADMAP.md (Phase 19's section -- Goal/Depends on/Requirements/Success Criteria/`**Plans**: TBD` at approximately lines 179-190; the Progress table's Phase 19 row near line 215; Phase 17/18's "Outcome: ..." bolded-inline annotation convention used elsewhere in this file as the pattern to mirror for Phase 19's 3 success criteria)
    .planning/workstreams/sgproc-render/STATE.md (the Deferred Items table row: "v2.1 | VALD-01 MNN float32 fixture 12/15 chunk-hash gap -- never re-checked against Phase 15's tolerance fallback | Queued for v2.3 Phase 19 (VALD-02) | 2026-08-13")
    .planning/workstreams/sgproc-render/phases/19-validation-re-verification/captures/diff-mnn-float-vald02-fresh.json (per D-01-REVISED, an independent capture_diff cross-check of the fresh fixture pair actually used this phase -- fresh evidence for this document, alongside Phase 13's historical diff-mnn-float-refit.json/diff-mnn-float-refit-chunkdiag.json which remain relevant for baseline comparison)
    Task 2's captured verbatim ctest/gtest console output (this task's primary evidence source)
  </read_first>
  <action>
    Create `.planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-REVERIFICATION.md` with YAML frontmatter (phase: 19-validation-re-verification, captured: today's date, status: a one-line summary of the classification chosen below) followed by sections mirroring 13-SCOPE-BOUNDARY.md's structure: (1) restate the phase goal and, per D-01-REVISED, the fresh fixture pair actually used (mnn-float_Fuus-Mac-mini.local---macOS_20260821T221542.cap, mnn-float_Mofu---Windows_20260821T221735.cap), noting briefly why D-01's original archived Phase 13 `.cap` pair was replaced (unreadable by current tooling -- a backward-compat regression unrelated to this phase); (2) "Self-Check" section citing Task 1's Vald02ChunkHashSelfCheckMatchesPublishedRefit result, noting explicitly that this is a *fresh* capture (not Phase 13's archived one), citing diff-mnn-float-vald02-fresh.json's own capture_diff cross-check as corroborating evidence, and stating that this fresh pair reproduces the identical chunkHashesMatch pattern originally published in Phase 13's diff-mnn-float-refit.json (14 true, 1 false at index 10) -- an exact match to the historical baseline despite being an independently re-captured pair, itself a notable, citable fact; (3) "Chunk 10 Tolerance Cross-Check" section citing the direct IsFloatChunkWithinTolerance call's statsOut.maxAbsDelta (3.0517578125e-05) against the D-03 bound (2.0/32768.0 = 6.103515625e-05) and its boolean result, quoting Task 2's captured console output verbatim, and noting this exactly matches both Phase 13's original chunk-10 diagnostic number and diff-mnn-float-vald02-fresh.json's independently-computed maxAbsDelta/maxUlpDistance (2048); (4) "ValidateResults Full-Fixture Outcome" section citing the actual observed validate_res.has_error()/invalidSubTaskIds result from Task 2's run, quoted verbatim, not paraphrased; (5) a final "Classification" section stating explicitly, in those words, whether VALD-01's gap is CLOSED, PARTIALLY CLOSED, or STILL OPEN as of this re-verification, per D-04's required framing -- if Task 2's actual run matched the expected pass (tolerance engaged, no subtask invalidated), state CLOSED and explain that AttemptToleranceFallback genuinely engaged for chunk 10 and resolved it (not that the gap was already absent); if the run instead showed a failure, state STILL OPEN or PARTIALLY CLOSED as accurately fits the observed data, with the same honesty 13-SCOPE-BOUNDARY.md's "Open Gap Against SC1" section used, and do not propose a production-code fix (D-04).

    Update `.planning/workstreams/sgproc-render/REQUIREMENTS.md` via scoped Edit: change VALD-02's `- [ ]` line to `- [x]` (only if the classification is CLOSED; otherwise leave unchecked but append the classification and a citation to 19-REVERIFICATION.md, mirroring how RENDTOL-02's line documents a nuanced outcome rather than a bare checkbox) and append `-- {classification}, see 19-REVERIFICATION.md` to its description; update the Traceability table's `| VALD-02 | Phase 19 | Pending |` row to reflect the actual classification (e.g. `Complete` or `Partial -- see 19-REVERIFICATION.md`); update the Coverage summary counts if "Unmapped" or completion counts reference VALD-02's status.

    Update `.planning/workstreams/sgproc-render/ROADMAP.md` via scoped Edit: replace Phase 19's `**Plans**: TBD` with `**Plans**: 1/1 plans complete`; add a `Plans:` list with `- [x] 19-01-PLAN.md -- {one-line objective}` under a `**Wave 1**` heading; annotate each of Phase 19's 3 Success Criteria with a bolded `**Outcome: ...**` sentence citing the actual observed numbers (mirroring Phase 17's SC-outcome annotation convention already present in this same file); update the top-of-file milestone bullet's `[ ] **Phase 19: ...**` to `[x]` if the classification is CLOSED (leave as `[ ]` with an honest partial-outcome note otherwise, mirroring Phase 17's own precedent of not checking a box prematurely); update the Progress table's Phase 19 row (Plans Complete: 1/1, Status, Completed date).

    Update `.planning/workstreams/sgproc-render/STATE.md` via scoped Edit: change the Deferred Items table's VALD-01/Phase 19 row's Status column from "Queued for v2.3 Phase 19 (VALD-02)" to the actual outcome (e.g. "Resolved -- Phase 19 re-verification confirmed AttemptToleranceFallback closes the gap, see 19-REVERIFICATION.md" or "Still partial -- see 19-REVERIFICATION.md", matching whichever classification Task 3's own document states) and update `current_phase`/`status`/`progress` frontmatter fields and the "Current Position" section to reflect Phase 19 completion, following this file's own established per-phase update convention (see how Phase 18's completion was recorded).
  </action>
  <acceptance_criteria>
    - Source assertion: `.planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-REVERIFICATION.md` exists and contains exactly one of the literal words "CLOSED", "PARTIALLY CLOSED", or "STILL OPEN" in its Classification section.
    - Source assertion: 19-REVERIFICATION.md quotes at least one numeric value (3.0517578125e-05 and/or 6.103515625e-05) taken from Task 1/Task 2's actual run, not merely restated from 13-SCOPE-BOUNDARY.md without a fresh citation.
    - Source assertion: `grep -A2 "VALD-02" .planning/workstreams/sgproc-render/REQUIREMENTS.md` no longer contains the literal word "Pending" in the Traceability row.
    - Source assertion: `grep -c "Plans.*TBD" .planning/workstreams/sgproc-render/ROADMAP.md` returns 0 (Phase 19's placeholder is replaced).
    - Source assertion: STATE.md's Deferred Items row for the VALD-01/Phase-19 item no longer reads "Queued for v2.3 Phase 19 (VALD-02)" unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -Ec "CLOSED|STILL OPEN" .planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-REVERIFICATION.md</automated>
  </verify>
  <done>19-REVERIFICATION.md exists with an explicit, evidence-cited closed/partially-closed/still-open classification (this gate matches any of the three -- "CLOSED" also matches "PARTIALLY CLOSED" -- it does not require a specific outcome); REQUIREMENTS.md, ROADMAP.md, and STATE.md all reflect that same classification consistently, with no remaining "TBD"/"Pending" placeholders for Phase 19/VALD-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test binary -> local `.cap` fixture files | The new TEST cases read pre-existing, already-checked-in `.cap` files from disk via `DeserializeCaptureFile` -- not attacker-controlled input at runtime, but the same parsing function production capture tooling relies on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | `DeserializeCaptureFile` (via the new test's `.cap` reads) | low | accept | Already mitigated by existing, unmodified production code: `DeserializeCaptureFile` validates every declared length/count against remaining buffer bytes and rejects (returns false, no partial population) any malformed/truncated/oversized input before allocating -- this phase adds no new call path beyond what `capture_diff`/`capture_smoke_test` already exercise identically |
| T-19-02 | Denial of Service | New `TEST` cases reading `.cap` files via a CMake-injected absolute path (`VALD02_CAPTURES_DIR`) | low | accept | Path points at fixed, checked-in files under `.planning/`, not user/network input; a missing/moved file fails the ReadFileBytes step with an ASSERT_TRUE failure (test fails loudly), not a silent hang or crash |
| T-19-SC | Tampering | No package-manager installs in this phase | n/a | accept | Not applicable -- this phase adds no new npm/pip/cargo dependencies; the Package Legitimacy Gate does not apply |

</threat_model>

<verification>
Run `ctest -C Debug -R ^processing_validation_core_test$ -V` from `SuperGenius/build/Windows/Debug` and confirm all 7 TEST cases in the file (5 pre-existing + 2 new Vald02* cases) execute and report a definitive OK/FAILED verdict with no crash. Confirm 19-REVERIFICATION.md's classification is internally consistent with the actual captured ctest output (the document must not claim CLOSED if the captured output shows a FAILED verdict for `Vald02FullFixtureValidateResultsToleranceOutcome`, and vice versa).
</verification>

<success_criteria>
1. The exact VALD-01 MNN float32 fixture (D-01's named `.cap` pair) is re-run end-to-end through the current `ValidateResults` tolerance-fallback path, not a re-derived or substitute fixture.
2. The re-run's outcome is captured as concrete evidence -- which chunks match/mismatch post-fallback, and whether the numeric-tolerance fallback actually engaged for chunk 10 -- not inferred from Phase 14/15's general test suite passing.
3. The outcome is documented in 19-REVERIFICATION.md as explicitly closed, partially closed, or still open, with any remaining gap characterized rather than left implicit, and REQUIREMENTS.md/ROADMAP.md/STATE.md all consistently reflect that same classification.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `TEST(ProcessingValidationCoreTest, Vald02ChunkHashSelfCheckMatchesPublishedRefit)` -- new GTest case in `processing_validation_core_test.cpp`
- `TEST(ProcessingValidationCoreTest, Vald02FullFixtureValidateResultsToleranceOutcome)` -- new GTest case in `processing_validation_core_test.cpp`
- `ReadFileBytes(const std::string &path, std::vector<uint8_t> &out)` -- new anonymous-namespace helper function in `processing_validation_core_test.cpp`
- `VALD02_CAPTURES_DIR` -- new CMake compile definition on the `processing_validation_core_test` target
- `sgproccapture` -- new `target_link_libraries` entry on the `processing_validation_core_test` target
- `.planning/workstreams/sgproc-render/phases/19-validation-re-verification/19-REVERIFICATION.md` -- new outcome document

