---
phase: 13-re-validation-scope-boundary-documentation
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float.json"
  - ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-render.json"
  - ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md"
autonomous: true
requirements: [VALD-01]

must_haves:
  truths:
    - "capture_diff, run against the 4 fresh .cap files Plan 13-01 delivered, reports contentHashMatch and chunkHashesMatch for the MNN float32 fixture (Mac vs Windows, post-Phase-12-quantization) -- the literal empirical target VALD-01/SC1 require -- distinct from combinedHashMatch, which is expected to stay false by design (D-03, D-01 of 12-CONTEXT.md)"
    - "13-SCOPE-BOUNDARY.md states the explicit scope boundary in plain terms: hash comparison is now cross-hardware tolerant; ProcessingValidationCore::ValidateResults's concatenation bug remains unfixed; no cross-node consensus/redundant-execution plumbing was built (D-09, quoting REQUIREMENTS.md's own Out of Scope table wording)"
    - "13-SCOPE-BOUNDARY.md cites the re-validation results (SC1 processor-level hash match citation, SC2 SECV-01 fresh-pass citation from Plan 13-02's 13-SECV01-RERUN.txt) and SC4's final normalization constants with their empirical derivation citing Phase 11's exact captured numbers verbatim (D-08/D-09)"
    - "If the fresh diff instead shows contentHashMatch:false or any chunkHashesMatch entry false for the MNN fixture, 13-SCOPE-BOUNDARY.md states that honestly as an open gap against SC1 rather than asserting success -- this task records ground truth, it does not guarantee a particular outcome"
  artifacts:
    - path: ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float.json"
      provides: "Fresh Mac-vs-Windows post-quantization divergence stats for the MNN float32 fixture"
    - path: ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-render.json"
      provides: "Fresh Mac-vs-Windows post-quantization divergence stats for the render uint8 fixture"
    - path: ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md"
      provides: "The milestone's literal VALD-01 re-validation record + explicit scope-boundary statement, satisfying ROADMAP Phase 13 SC1-SC4 in one combined doc (D-08/D-09)"
  key_links:
    - from: "captures/xhw-mnn-float_*.cap, captures/xhw-render_*.cap (Plan 13-01)"
      to: "captures/diff-mnn-float.json, captures/diff-render.json"
      via: "capture_diff --a --b --element-type --json-output"
      pattern: "contentHashMatch|chunkHashesMatch"
    - from: "captures/diff-mnn-float.json, captures/diff-render.json + 13-SECV01-RERUN.txt (Plan 13-02)"
      to: "13-SCOPE-BOUNDARY.md"
      via: "verbatim-cited JSON fields + ctest output lines"
      pattern: "contentHashMatch|MnnCorruptedModelStillDiverges"
    - from: "REQUIREMENTS.md ## Out of Scope table"
      to: "13-SCOPE-BOUNDARY.md Scope Boundary section"
      via: "quoted canonical wording (not paraphrased)"
      pattern: "ValidateResults|cross-node consensus"
---

<objective>
Diff the fresh Mac-vs-Windows captures Plan 13-01 delivered at the processor-level hash layer (D-03), and write `13-SCOPE-BOUNDARY.md` -- Phase 13's single combined re-validation-results + scope-boundary record (D-08/D-09) -- closing out VALD-01 and the v2.1 milestone's traceability loop.

Purpose: this is the literal empirical re-confirmation ROADMAP Phase 13 SC1/SC2 require, plus the written scope-boundary statement (SC3) and final-constants derivation (SC4) that prevent any downstream consumer from mistaking this milestone's cross-hardware hash tolerance for "verification" or "consensus-ready" (per ROADMAP's own explicit warning).
Output: `captures/diff-mnn-float.json`, `captures/diff-render.json` (both new), `13-SCOPE-BOUNDARY.md` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md
@.planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-CONTEXT.md
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run capture_diff on the fresh Mac-vs-Windows captures for both fixtures</name>
  <files>.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float.json (new), .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-render.json (new)</files>
  <read_first>
    SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp (CLI usage, JSON field names -- unmodified since Phase 10)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/ (Plan 13-01's 4 delivered .cap files -- list the directory to find the actual generated filenames, each embeds a runtime UTC timestamp that cannot be known in advance)
  </read_first>
  <action>
List this phase's `captures/` directory to find the exact filenames Plan 13-01's checkpoint produced (each embeds a runtime timestamp, e.g. `xhw-mnn-float_Fuus-Mac-mini.local---macOS_<timestamp>.cap` and `xhw-mnn-float_Mofu---Windows_<timestamp>.cap` -- do not assume Phase 11's exact timestamps, these are freshly generated).

Rebuild `capture_diff` if not already current (`cmake --build SuperGenius/build/Windows/Debug --target capture_diff --config Debug`), then run it twice from the repository root, once per fixture, comparing the Mac-origin `.cap` against the Windows-origin `.cap` (`--a`/`--b` order does not affect the reported stats):

`capture_diff --a captures/xhw-mnn-float_<mac-file>.cap --b captures/xhw-mnn-float_<windows-file>.cap --element-type float32 --json-output .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float.json`
`capture_diff --a captures/xhw-render_<mac-file>.cap --b captures/xhw-render_<windows-file>.cap --element-type uint8 --json-output .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-render.json`

(substitute the real filenames found by the directory listing above; adjust the `--a`/`--b` source paths to point at wherever the 4 `.cap` files actually live in this phase's `captures/` directory). Never compare Mac-vs-Mac or Windows-vs-Windows -- each invocation must pair exactly one Mac-origin file with one Windows-origin file for the same fixture.

Print both JSON reports to console as part of this task's own evidence trail -- Task 2 (same plan) reads them from disk directly.
  </action>
  <verify>
    <automated>d=".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures"; test -f "$d/diff-mnn-float.json" && test -f "$d/diff-render.json" && grep -q "contentHashMatch" "$d/diff-mnn-float.json" && grep -q "chunkHashesMatch" "$d/diff-mnn-float.json" && grep -q "contentHashMatch" "$d/diff-render.json" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `captures/diff-mnn-float.json` and `captures/diff-render.json` both exist and are valid JSON containing `contentHashMatch`, `chunkHashesMatch`, `combinedHashMatch`, `maxAbsDelta`, `maxRelDelta`, `maxUlpDistance`, `percentExceedingThreshold` fields.
    - Both diffs compare exactly one Mac-origin `.cap` against one Windows-origin `.cap` per fixture.
  </acceptance_criteria>
  <done>Fresh Mac-vs-Windows post-quantization divergence stats exist for both fixtures, ready for 13-SCOPE-BOUNDARY.md to cite verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Write 13-SCOPE-BOUNDARY.md -- combined re-validation results + explicit scope boundary</name>
  <files>.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md (new)</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md (D-01 through D-09 -- this doc's full required-content list)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-PATTERNS.md (the section-by-section template this task follows)
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md (structural analog, already read in full during planning)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float.json, .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-render.json (this plan's Task 1 output)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SECV01-RERUN.txt (Plan 13-02's fresh SECV-01 pass evidence)
    .planning/workstreams/sgproc-render/REQUIREMENTS.md (## Out of Scope table -- exact wording to quote)
    SuperGenius/SGProcessingManager/include/util/quantization.hpp (the final constants: S=2^20/1048576.0f, grid step ~1e-6, NaN->0x7FC00000, +Inf/-Inf->0x7F800000/0xFF800000, +/-0.0->0x00000000, render stays byte-identity)
  </read_first>
  <action>
Create `13-SCOPE-BOUNDARY.md` in this phase's directory, mirroring `11-CAPTURE-RESULTS.md`'s structure (frontmatter block, per-fixture verbatim-JSON-plus-interpretation subsections) per D-08, but as a single combined doc covering both the re-validation results AND the scope boundary per D-09 -- not split into two files.

Open with a metadata header (phase, captured date, status noting this is the real-quantization re-validation of Phase 11's 2-machine dataset) plus a one-line restatement of the Phase 13 Goal.

Include a "Re-Validation Scope" section restating D-01 (same accepted 2-machine Mac+Windows dataset as Phase 11 -- no genuine 3rd machine has appeared since), with a forward reference to `13-CONTEXT.md`'s own D-01/D-02, and an explicit contrast against Phase 11's original run (quantization still a no-op stub there vs. Phase 12's real quantization active here).

For each fixture (MNN float32, render uint8), embed this plan's Task 1 diff JSON verbatim as a fenced `json` block (byte-for-byte, unrounded -- the same verbatim-citation discipline `11-CAPTURE-RESULTS.md` used), followed by prose interpretation naming each field by its JSON key. Per D-03, the interpretation MUST explicitly frame `contentHashMatch`/`chunkHashesMatch` as the processor-level `ProcessingResult` hash/chunk-hash comparison (`Artifact.contentHash`/`Artifact.chunkHashes`) that SC1/VALD-01 actually require, and MUST explicitly note that `combinedHashMatch` is expected to remain `false` regardless -- `ProcessOutput.combinedHash`/`ExecutionManifest.manifestHash` bakes in machine-specific `executorIdentity`/`gpuMemoryUsedBytes` by construction (see `ProcessingManager.cpp:1500-1510` and `12-CONTEXT.md` D-01) -- a `false` `combinedHashMatch` is NOT a Phase 13 failure and must not be reported as one.
If the MNN fixture's `contentHashMatch` or any `chunkHashesMatch` entry reports `false`, state that plainly as an open gap against SC1 rather than asserting success -- do not round, omit, or reinterpret an unfavorable result into a passing one.

Include an "SC2: SECV-01 Re-Confirmation" section citing `13-SECV01-RERUN.txt`'s actual PASSED/SKIPPED lines for both `TEST_F` cases verbatim (per D-07 -- no new test logic, this section only cites the fresh re-run's output).

Include an explicit "Scope Boundary" section (satisfies ROADMAP SC3) stating, in REQUIREMENTS.md's own Out of Scope table's quoted wording: (1) hash comparison is now cross-hardware tolerant -- this milestone's actual delivered scope; (2) `ProcessingValidationCore::ValidateResults`'s concatenation bug remains unfixed (quote REQUIREMENTS.md's own phrasing verbatim); (3) no cross-node consensus/redundant-execution plumbing was built (quote REQUIREMENTS.md's own phrasing verbatim). Add a one-line explicit statement that no downstream consumer should read this milestone's output as "verification" or "consensus-ready" (ROADMAP SC3's own literal wording).

Include an "SC4: Final Normalization Constants" section quoting `quantization.hpp`'s actual final constants (S = 2^20 / 1048576.0f, grid step ~1e-6, NaN canonicalizes to `0x7FC00000`, +Inf/-Inf to `0x7F800000`/`0xFF800000` respectively, +0.0/-0.0 to `0x00000000`, render stays byte-identity) and their empirical derivation citing Phase 11's exact captured numbers verbatim: `maxAbsDelta ≈ 1.043081283569336e-07`, `maxRelDelta ≈ 7.269731577252969e-05`, `maxUlpDistance = 768` (MNN float32, 512 elements, Mac vs Windows, pre-quantization, from `11-CAPTURE-RESULTS.md`) -- note the constant's ~10x safety margin over this measured delta. For the render uint8 path, cite Phase 11's `contentHashMatch: true` / all-deltas-zero finding as the empirical justification for `quantization.hpp`'s deliberate byte-identity (no lossy tolerance) design choice.

Close with a short "Traceability" section noting REQUIREMENTS.md VALD-01 and ROADMAP.md Phase 13 SC1 now read "2 distinct physical machines" / "processor-level result/chunk hash" (Plan 13-02's edit), completing the v2.1 milestone's requirement coverage (12/12 requirements, VALD-01 the last).
  </action>
  <verify>
    <automated>file=".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md"; grep -q "1.043081283569336e-07" "$file" && grep -q "7.269731577252969e-05" "$file" && grep -q "768" "$file" && grep -qi "ValidateResults" "$file" && grep -qi "cross-node consensus" "$file" && grep -qE "processing_conformance_security_test|MnnCorruptedModelStillDiverges" "$file" && grep -qi "contentHashMatch" "$file" && grep -qi "chunkHashesMatch" "$file" && grep -qi "combinedHashMatch" "$file" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `13-SCOPE-BOUNDARY.md` exists, embeds both fresh diff JSONs verbatim, and states D-01 (2-machine scope), the processor-level-vs-manifest hash distinction (D-03), the SECV-01 fresh-pass citation (D-07), the explicit scope-boundary language (ROADMAP SC3, quoting REQUIREMENTS.md's Out of Scope table), and SC4's final constants + Phase 11 empirical derivation (D-05/D-08/D-09).
    - If `contentHashMatch`/`chunkHashesMatch` for MNN report `false`, the doc states this honestly as an open gap rather than a pass.
    - `combinedHashMatch`'s expected-false status is explicitly explained, not silently omitted.
  </acceptance_criteria>
  <done>13-SCOPE-BOUNDARY.md exists as Phase 13's single combined re-validation-results + scope-boundary record, citing the fresh Mac-vs-Windows diff data, the fresh SECV-01 pass, the milestone's explicit out-of-scope boundary, and the final normalization constants' empirical derivation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Numeric citation into a permanent milestone-closing record | `13-SCOPE-BOUNDARY.md` is the document future phases/consumers will read to understand this milestone's actual guarantees -- an imprecise or optimistic citation here misleads every future reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|-----------------|
| T-13-05 | Repudiation | Numeric citations in 13-SCOPE-BOUNDARY.md could be paraphrased/rounded, misrepresenting the milestone's actual empirical result | medium | mitigate | Task 2 requires embedding both fresh diff JSONs verbatim (not paraphrased or rounded), mirroring `11-CAPTURE-RESULTS.md`/T-11-02's precedent -- any future disagreement about what Phase 13 actually measured is resolvable by diffing the doc against `captures/*.json`. |
| T-13-06 | Repudiation / Information Disclosure | Claiming VALD-01/SC1 satisfied when the fresh data does not actually show a processor-level hash match | medium | mitigate | Task 2's action explicitly requires an honest "open gap" statement if `contentHashMatch`/`chunkHashesMatch` report `false` for MNN, rather than silently converting an unfavorable result into a claimed pass. |

No package-manager installs occur in this plan -- it touches no build files, no dependencies. The npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `captures/diff-mnn-float.json` and `captures/diff-render.json` exist, comparing exactly one Mac-origin and one Windows-origin `.cap` per fixture.
2. `13-SCOPE-BOUNDARY.md` cites both diffs' exact fields, the fresh SECV-01 pass, the explicit scope-boundary language (quoting REQUIREMENTS.md's Out of Scope table), and SC4's final constants + Phase 11 empirical derivation.
3. Any unfavorable diff result (contentHashMatch/chunkHashesMatch false for MNN) is reported honestly as an open gap, not silently converted to a pass.
</verification>

<success_criteria>
Phase 13's re-validation is complete and traceable: fresh Mac-vs-Windows processor-level hash comparison data exists for both fixtures, SECV-01's fresh pass is cited, and `13-SCOPE-BOUNDARY.md` records the milestone's explicit scope boundary and final normalization constants' empirical derivation -- closing the v2.1 milestone's traceability loop (12/12 requirements, VALD-01 last).
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New file: `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float.json`.
- New file: `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-render.json`.
- New file: `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md`.
- No code symbols, no `SuperGenius`/`SGProcessingManager` source changes.

## Artifacts this phase produces (full phase, across all 3 plans)

- New directory: `captures/` (4 fresh `.cap` files from Plan 13-01, 2 fresh diff JSONs from this plan).
- New file: `13-SECV01-RERUN.txt` (Plan 13-02).
- New file: `13-SCOPE-BOUNDARY.md` (this plan) -- Phase 13's literal deliverable.
- Edited file: `.planning/workstreams/sgproc-render/REQUIREMENTS.md` (VALD-01 wording, Plan 13-02).
- Edited file: `.planning/workstreams/sgproc-render/ROADMAP.md` (Phase 13 SC1 + summary-bullet wording, Plan 13-02).
- No `SuperGenius`/`SGProcessingManager` source file is created or modified anywhere in this phase.
