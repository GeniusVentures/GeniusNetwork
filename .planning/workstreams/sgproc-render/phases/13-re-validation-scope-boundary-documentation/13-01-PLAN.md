---
phase: 13-re-validation-scope-boundary-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/ (new directory; 4 .cap files written by capture_harness -- exact filenames follow xhw-mnn-float_{machine}---{OS}_{timestamp}.cap and xhw-render_{machine}---{OS}_{timestamp}.cap, generated at runtime)"
autonomous: false
requirements: []

must_haves:
  truths:
    - "captures/ (new, Phase-13-owned directory, distinct from Phase 11's own captures/ dir per D-06) contains exactly 4 .cap files: xhw-mnn-float and xhw-render, one of each from the Mac and one of each from Windows"
    - "All 4 .cap files were produced by binaries rebuilt from a checkout that includes Phase 12's real-quantization commits (SGProcessingManager 76f6ae6; SuperGenius 5538b606/1c16248b) -- not a stale pre-Phase-12 build that would silently reproduce Phase 10/11's no-op-stub numbers"
    - "The user ran capture_harness personally on both machines per D-05 -- this session did not invoke capture_harness on either machine's behalf, even though the Windows leg is technically reachable via this environment's Bash tool, to keep the re-capture procedure identical and comparable across both machines"
  artifacts:
    - path: ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/"
      provides: "4 fresh .cap files (2 fixtures x 2 machines) captured with Phase 12's real quantization active -- the raw evidence Plan 13-03's capture_diff run and 13-SCOPE-BOUNDARY.md depend on"
  key_links:
    - from: "Phase 12's rebuilt quantization.cpp/hpp (76f6ae6) + SECV-01 test commits (5538b606/1c16248b)"
      to: "capture_harness's --output-dir captures/ .cap output"
      via: "a plain incremental rebuild of the capture_harness target on each machine -- no source changes to the tool itself"
      pattern: "xhw-mnn-float_.*\\.cap|xhw-render_.*\\.cap"
---

<objective>
Get real, empirical, post-Phase-12-quantization cross-machine capture data onto disk: the user personally rebuilds `capture_harness` on both the Mac and Windows machines against Phase 12's real normalization logic, runs it for both Phase 09 fixtures on each machine, and lands all 4 resulting `.cap` files together in a new, phase-13-owned `captures/` directory (D-05, D-06).

This is a hands-on data-gathering checkpoint, not a coding task -- no `SuperGenius`/`SGProcessingManager` source file is created or modified. Per D-05, Claude does not run `capture_harness` on the user's behalf on either machine (not even the Windows one this session can technically reach), so both legs of the re-capture stay procedurally identical.

Purpose: produce the fresh, real-quantization evidence Plan 13-03's `capture_diff` run and `13-SCOPE-BOUNDARY.md` need to empirically re-confirm VALD-01/SC1.
Output: 4 fresh `.cap` files in the new `captures/` directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Hands-on re-capture on Mac + Windows with Phase 12's real quantization active (D-05)</name>
  <files>.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/ (new; 4 .cap files)</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md (D-01, D-05, D-06)
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md (machine-label + .cap naming convention precedent: Fuus-Mac-mini.local---macOS, Mofu---Windows)
    SuperGenius/SGProcessingManager/tools/capture/capture_harness.cpp (CLI usage -- --fixture-root/--fixture/--label/--repeat/--output-dir/--model-input-source/--write-render-vertex-fixture; unmodified since Phase 10)
  </read_first>
  <action>
Present the following to the user and wait for their confirmation before proceeding to Plan 13-02/13-03. Per D-05, both machines are run by the user personally -- not automated by this session even on the Windows leg -- to keep the re-capture procedure identical and comparable across machines, matching Phase 11's own hands-on precedent (11-CONTEXT.md D-01/D-07).

**Step 1 -- sync both checkouts to include Phase 12's commits.** Before rebuilding on either machine, confirm each machine's checkout has Phase 12's landed changes: the top-level `SuperGenius` repo needs commits `5538b606` (test(12-02): add SECV-01 counter-test) and `1c16248b` (test(12-02): add SECV-01 fixtures) or later; the nested `SGProcessingManager` submodule needs commit `76f6ae6` (feat(12-01): implement real QuantizeFloatBuffer/QuantizeByteBuffer) or later. A stale checkout would rebuild against Phase 10/11's no-op quantization stub and silently reproduce last time's uncorrected divergence numbers -- this is the single most important precondition for this task to mean anything.

**Step 2 -- rebuild capture_harness on each machine**, from the repository root:
  - Windows: `cmake --build SuperGenius/build/Windows/Debug --target capture_harness --config Debug`
  - Mac: the equivalent build command for whichever build directory produced Phase 11's Mac capture (e.g. `cmake --build SuperGenius/build/OSX/Debug --target capture_harness --config Debug`) -- reuse the same build tree Phase 11 already used successfully on that machine.

**Step 3 -- run capture_harness for both fixtures, on each machine**, from the repository root, pointing `--output-dir` directly at this phase's new `captures/` directory (created automatically if missing -- no manual `mkdir` needed):

MNN float32 fixture (repeat=3, matching Phase 10/11's own invocation):
`<path-to-freshly-built-capture_harness> --fixture-root SuperGenius/test/src --fixture processing_datatypes/float-processing-definition.json --label xhw-mnn-float --repeat 3 --output-dir .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures`

Render happy-path fixture (repeat=2, matching Phase 10/11's own invocation):
`<path-to-freshly-built-capture_harness> --fixture-root SuperGenius/test/src --fixture processing_dispatch/render-pass-happy-path-definition.json --label xhw-render --repeat 2 --model-input-source input:renderInput --write-render-vertex-fixture --output-dir .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures`

(`<path-to-freshly-built-capture_harness>` is wherever your CMake generator placed the rebuilt binary for this target -- the same one used for Phase 11's capture on that machine.) Confirm each run's console output reports "N/N stable runs" (CAPT-03 passing) before moving on -- `capture_harness` writes no file at all if the same-node stability check fails.

**Step 4 -- consolidate onto the Windows checkout.** This session can only see this Windows checkout's filesystem. Transfer the 2 Mac-produced `.cap` files (one `xhw-mnn-float_*`, one `xhw-render_*`) into the same `captures/` directory on this Windows checkout -- via shared network drive, cloud sync, USB, `scp`, or whatever transfer mechanism is convenient. All 4 `.cap` files (2 fixtures x 2 machines) must end up together in this one directory before resuming.

Do not move, rename, or otherwise touch Phase 11's own `captures/` directory (`.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/`) -- per D-06, this phase's fresh evidence stays in its own directory, keeping stub-era and real-quantization-era evidence unambiguously separated.
  </action>
  <resume-signal>Type "captured" once all 4 .cap files (xhw-mnn-float x2, xhw-render x2) exist together in this phase's captures/ directory, or describe what went wrong (e.g. a stability-check failure, a stale checkout, a build error) if the capture could not be completed.</resume-signal>
  <verify>
    <automated>captures_dir=".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures"; [ -d "$captures_dir" ] && [ "$(ls "$captures_dir"/xhw-mnn-float_*.cap 2>/dev/null | wc -l)" = "2" ] && [ "$(ls "$captures_dir"/xhw-render_*.cap 2>/dev/null | wc -l)" = "2" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `captures/` (this phase's own directory) contains exactly 2 `xhw-mnn-float_*.cap` files (one Mac-origin, one Windows-origin) and 2 `xhw-render_*.cap` files (one Mac-origin, one Windows-origin) -- 4 total.
    - None of the 4 files are copies of, or collide with, any file already present in Phase 11's own `captures/` directory.
    - Both machines' binaries were rebuilt from a checkout including Phase 12's quantization commits before capturing (user-confirmed via the resume signal).
    - No file under `SuperGenius/` is created or modified by this task -- `capture_harness` runs unmodified.
  </acceptance_criteria>
  <done>4 fresh .cap files (2 fixtures x 2 machines), captured with Phase 12's real quantization active, exist together in Phase 13's own captures/ directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Mac-to-Windows manual file transfer of `.cap` files | Untrusted-in-transit binary data crossing from a machine this session cannot reach to one it can, via a user-chosen transfer mechanism |
| Stale-checkout rebuild | The rebuilt-binary precondition (Phase 12's commits present) is asserted by the user, not independently verifiable from the `.cap` bytes alone by this task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|-----------------|
| T-13-01 | Tampering | `.cap` files transferred Mac -> Windows by hand | low | accept | `capture_harness`'s own CAPT-02 self-check (independently re-hashes every captured buffer against the artifact's own chunk/content hashes) and CAPT-03 stability check already ran on each machine before the file was ever written -- a file that exists already passed both checks. Plan 13-03's `capture_diff` run additionally surfaces a `sizeMismatch` flag if a transferred file were truncated or corrupted in transit. |
| T-13-02 | Repudiation | Stale (pre-Phase-12) binary silently reproducing Phase 10/11's no-op-stub numbers | medium | mitigate | This task's action names the exact commit hashes (SGProcessingManager `76f6ae6`; SuperGenius `5538b606`/`1c16248b`) the user must confirm present before rebuilding. Plan 13-03's doc-writing task cross-checks the fresh diff numbers against Phase 11's stub-era numbers (11-CAPTURE-RESULTS.md) -- an MNN chunk-hash mismatch pattern identical to Phase 11's all-`false` `chunkHashesMatch` would be a red flag worth surfacing, since real quantization is expected to change that outcome. |

No package-manager installs occur in this task -- it touches no build files, no dependencies. The npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `captures/` (Phase 13's own directory) contains exactly 4 `.cap` files: 2 `xhw-mnn-float_*` (Mac + Windows) and 2 `xhw-render_*` (Mac + Windows).
2. The user has confirmed both machines' binaries were rebuilt from a checkout including Phase 12's real-quantization commits before capturing.
3. Phase 11's own `captures/` directory is untouched.
4. No file under `SuperGenius/` is created or modified by this task.
</verification>

<success_criteria>
Fresh, real-quantization-era cross-machine capture evidence (4 `.cap` files, 2 fixtures x 2 machines) exists in Phase 13's own `captures/` directory, ready for Plan 13-03's `capture_diff` comparison.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New directory: `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/` (holds 4 fresh `.cap` files: `xhw-mnn-float_*` x2, `xhw-render_*` x2 -- exact filenames include a runtime timestamp).
- No code symbols, no `SuperGenius`/`SGProcessingManager` source changes -- this plan re-runs existing, unmodified tooling on the user's own machines.
