---
phase: 13-re-validation-scope-boundary-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SECV01-RERUN.txt"
  - ".planning/workstreams/sgproc-render/REQUIREMENTS.md"
  - ".planning/workstreams/sgproc-render/ROADMAP.md"
autonomous: true
requirements: []

must_haves:
  truths:
    - "processing_conformance_security_test (secv01_counter_test.cpp, unmodified since Phase 12) still passes when re-run against Phase 12's final quantization constants -- D-07: no new test code is written, Phase 12's constants ARE the final precision"
    - "REQUIREMENTS.md VALD-01 and ROADMAP.md Phase 13 Success Criterion 1 both read '2 distinct physical machines' and 'processor-level result/chunk hash(es)' instead of the stale '3 different machines'/'combined hash' wording (D-02/D-04)"
    - "Phase 13's Goal line, SC2, SC3, SC4, and every other requirement/phase entry in both files are untouched by this wording fix"
    - "VALD-01's checkbox and its Traceability-table status stay exactly as they were (Pending / unchecked) -- flipping to complete happens only after this phase is verified, not during this wording-only edit"
  artifacts:
    - path: ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SECV01-RERUN.txt"
      provides: "Verbatim ctest -V console output citing the fresh SECV-01 pass, for Plan 13-03's 13-SCOPE-BOUNDARY.md to quote"
    - path: ".planning/workstreams/sgproc-render/REQUIREMENTS.md"
      provides: "VALD-01 reconciled to the accepted 2-machine scope (D-01) and the processor-level hash target (D-03)"
    - path: ".planning/workstreams/sgproc-render/ROADMAP.md"
      provides: "Phase 13 Success Criterion 1 reconciled to the same two corrections"
  key_links:
    - from: "SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp (Phase 12, unmodified)"
      to: "13-SECV01-RERUN.txt"
      via: "ctest -V console capture, redirected to file"
      pattern: "PASSED|SKIPPED"
    - from: "13-CONTEXT.md D-02/D-03/D-04"
      to: "REQUIREMENTS.md VALD-01 + ROADMAP.md Phase 13 SC1"
      via: "scoped Edit text substitution (never a whole-file Write)"
      pattern: "2 distinct physical machines|processor-level result"
---

<objective>
Re-confirm SECV-01's existing counter-test still holds at Phase 12's final quantization precision (D-07 -- no new test code), and reconcile REQUIREMENTS.md/ROADMAP.md's stale "≥3 different machines" / "combined hash" wording to the scope and hash target this milestone actually settled on (D-02 through D-04) -- the second instance of the exact wording-fix pattern Phase 11's own D-03 established.

Purpose: closes out the "re-confirm SECV-01" half of ROADMAP Phase 13 SC2, and removes the internal contradiction between REQUIREMENTS.md/ROADMAP.md's literal text and what Phase 12 actually built (a processor-level-hash cross-hardware tolerance, not a manifest-level one, across the accepted 2-machine dataset).
Output: `13-SECV01-RERUN.txt` (new), reconciled `REQUIREMENTS.md` + `ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-01-PLAN.md
@.planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run SECV-01 CTest suite at final precision, capture verbatim output</name>
  <files>.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SECV01-RERUN.txt (new)</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp
    SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md (D-07)
    .planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-CONTEXT.md (D-10 through D-13)
    .planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-VERIFICATION.md (the prior confirmed-pass evidence format to mirror)
  </read_first>
  <action>
Per D-07, this is a literal re-run of the existing, unmodified `secv01_counter_test.cpp` CTest target -- no new test logic, no fixture changes, no source edits of any kind. Phase 12's constants (S=2^20 fixed-point grid, the IEEE-754 canonicalization bit patterns) are the final precision; nothing changes on the SECV-01 side between Phase 12 and Phase 13.

From the repository root, rebuild the target incrementally (guarantees an up-to-date binary; nothing new should actually need recompiling since Phase 12 already landed everything this suite exercises) and re-run it verbosely so both named `TEST_F` cases' own PASSED/SKIPPED lines are visible for citation, not just the aggregate ctest summary:

`cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug`
`ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R processing_conformance_security_test -V > .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SECV01-RERUN.txt 2>&1`

Confirm the redirected file shows `MnnCorruptedModelStillDiverges` passing, and `RenderWrongShaderConstantStillDiverges` either passing (a real Vulkan device is present on this machine) or reporting SKIPPED (no usable Vulkan device) -- never FAILED, per the existing `GTEST_SKIP()` pattern this suite already implements (Phase 09 D-05 precedent, carried into Phase 12 D-11's discretion note).

Do not modify `secv01_counter_test.cpp`, its `CMakeLists.txt`, or either of its two fixture files (`fixtures/secv01-wrong-color.frag`, `fixtures/secv01-corrupted-float_model.mnn`) -- this task only re-runs existing, unmodified tooling and tests.
  </action>
  <verify>
    <automated>file=".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SECV01-RERUN.txt"; grep -q "MnnCorruptedModelStillDiverges" "$file" && grep -qE "PASSED|OK" "$file" && ! grep -q "FAILED" "$file" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `13-SECV01-RERUN.txt` exists and contains `ctest -V`'s verbatim console output for `processing_conformance_security_test`.
    - `MnnCorruptedModelStillDiverges` shows as passed; `RenderWrongShaderConstantStillDiverges` shows as passed or skipped -- never failed.
    - `secv01_counter_test.cpp`, its `CMakeLists.txt`, and its two fixture files are byte-for-byte unmodified by this task (`git diff` shows no changes to any of the four).
  </acceptance_criteria>
  <done>Fresh, citable evidence exists that SECV-01's existing counter-test still passes at Phase 12's final quantization precision, with no new test code written.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS.md/ROADMAP.md wording to the accepted 2-machine scope and processor-level hash target</name>
  <files>.planning/workstreams/sgproc-render/REQUIREMENTS.md, .planning/workstreams/sgproc-render/ROADMAP.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md (D-01 through D-04)
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-01-PLAN.md (Task 3 -- the exact scoped-Edit-not-Write precedent this task repeats)
    .planning/workstreams/sgproc-render/REQUIREMENTS.md (VALD-01 line, Traceability table)
    .planning/workstreams/sgproc-render/ROADMAP.md (Phase 13 section: Goal, SC1-SC4, and the Phase 13 phase-list summary bullet)
    SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp:1500-1510 (confirms ComputeManifestHash/combinedHash assembly, backing the processor-level-vs-manifest distinction being written into the corrected wording)
  </read_first>
  <action>
Use the Edit tool (scoped replacement) on both files -- never a whole-file `Write` rewrite of either, matching 11-01-PLAN.md Task 3's exact discipline, since both files contain many unrelated phase/requirement entries outside this task's scope.

In REQUIREMENTS.md, replace VALD-01's current line -- it reads "run on ≥3 different machines (including the user's Mac and PC), produce matching post-normalization combined hashes" -- so it lands on two corrected phrases: "≥2 distinct physical machines" (plus a short parenthetical noting a third machine's only available device was a software rasterizer, excluded -- pointing at `13-SCOPE-BOUNDARY.md` for the full rationale, mirroring how Phase 11's own SC1/QUANT-04 cite `11-CAPTURE-RESULTS.md`) and "processor-level result/chunk hashes" (with a short parenthetical distinguishing this from `ProcessOutput.combinedHash`/`ExecutionManifest.manifestHash`, which bake in machine-specific `executorIdentity`/`gpuMemoryUsedBytes` by construction -- see `ProcessingManager.cpp:1500-1510` and `12-CONTEXT.md` D-01/D-02). This is D-02/D-04's explicitly mandated edit. Do NOT flip VALD-01's checkbox from unchecked to checked, and do NOT change its Traceability-table status from "Pending" -- that flips only once this phase is verified complete, mirroring 11-01-PLAN.md's identical instruction for QUANT-04.

In ROADMAP.md, replace Phase 13 Success Criterion 1's current text -- the same "≥3 different machines" / "combined hashes" phrasing -- with the same two corrections: "≥2 distinct physical machines" (same software-rasterizer-exclusion caveat, pointing at `13-SCOPE-BOUNDARY.md`) and "processor-level result/chunk hashes" (same parenthetical distinguishing it from `combinedHash`/`manifestHash`). This is D-02/D-04's explicitly mandated edit for this file.

As a discretionary consistency fix in the same document (mirrors 11-01-PLAN.md Task 3's own precedent of also fixing an adjacent stale mention in the same phase block, beyond the literally-mandated line): also correct the Phase 13 one-line phase-list summary bullet's stale machine-count phrasing to the same "2-machine" correction, leaving the rest of that bullet's wording alone.

Do NOT touch Phase 13's Goal line, SC2, SC3, or SC4 -- 13-PATTERNS.md confirms these are unaffected by D-02/D-04 and are the actual content this phase's execution must satisfy, not edit targets. Do NOT touch any other phase's machine-count wording -- Phase 11's own SC1/Goal line already read "2 distinct physical machines" per its own D-03 (11-01-PLAN.md Task 3), and Phase 12's success criteria are untouched by this phase per 12-CONTEXT.md D-02's own note that Phase 12 does not own Phase 13's roadmap section.
  </action>
  <verify>
    <automated>reqs=".planning/workstreams/sgproc-render/REQUIREMENTS.md"; roadmap=".planning/workstreams/sgproc-render/ROADMAP.md"; grep -qi "2 distinct physical machines" "$reqs" && grep -qi "processor-level result" "$reqs" && grep -qi "2 distinct physical machines" "$roadmap" && grep -qi "processor-level result" "$roadmap" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md VALD-01 reads "2 distinct physical machines" and "processor-level result/chunk hash(es)" (or equivalent wording landing on both target phrases).
    - ROADMAP.md Phase 13 Success Criterion 1 reads the same two corrections.
    - VALD-01's checkbox/Traceability status, Phase 13's Goal/SC2/SC3/SC4, and every other phase/requirement entry in both files are unchanged.
  </acceptance_criteria>
  <done>REQUIREMENTS.md VALD-01 and ROADMAP.md Phase 13 SC1 both read "≥2 distinct physical machines" and "processor-level result/chunk hash," reconciling the roadmap/requirements' literal wording with the accepted scope (D-01) and the correct hash target (D-03), mirroring Phase 11's own wording-fix precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Existing, unmodified CTest binary re-run | No new attack surface -- the exact same test/fixtures Phase 12 already committed and proved out |
| Scoped-Edit text substitution on shared cross-phase docs | REQUIREMENTS.md/ROADMAP.md are read and edited by every future phase's planner/executor -- an imprecise edit here propagates confusion downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|-----------------|
| T-13-03 | Repudiation | REQUIREMENTS.md/ROADMAP.md wording edit could silently drift from the two mandated landing phrases (D-02/D-04) | low | mitigate | Task 2's `<verify>` greps both files for both target phrases ("2 distinct physical machines", "processor-level result") before the task is considered done. |
| T-13-04 | Tampering | Re-running an existing, unmodified CTest binary | low | accept | No source or fixture file is touched by Task 1; `git diff` on the four named files is asserted empty in acceptance criteria. |

No package-manager installs occur in this plan -- it touches no build files, no dependencies. The npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `13-SECV01-RERUN.txt` exists, citing `ctest -V`'s verbatim output, with `MnnCorruptedModelStillDiverges` passed and `RenderWrongShaderConstantStillDiverges` passed-or-skipped (never failed).
2. `secv01_counter_test.cpp`, its `CMakeLists.txt`, and both fixture files are byte-for-byte unmodified.
3. REQUIREMENTS.md VALD-01 and ROADMAP.md Phase 13 SC1 both read "≥2 distinct physical machines" and "processor-level result/chunk hash," with VALD-01's checkbox/Traceability status and Phase 13's SC2-SC4 left untouched.
</verification>

<success_criteria>
SECV-01's existing counter-test is proven to still pass at Phase 12's final precision (cited, not assumed), and REQUIREMENTS.md/ROADMAP.md no longer contradict the milestone's actual accepted scope (2 machines) and actual hash target (processor-level, not manifest-level).
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New file: `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SECV01-RERUN.txt` (verbatim `ctest -V` output).
- Edited file: `.planning/workstreams/sgproc-render/REQUIREMENTS.md` (VALD-01 wording only).
- Edited file: `.planning/workstreams/sgproc-render/ROADMAP.md` (Phase 13 SC1 + phase-list summary bullet wording only).
- No code symbols, no `SuperGenius`/`SGProcessingManager` source changes.
