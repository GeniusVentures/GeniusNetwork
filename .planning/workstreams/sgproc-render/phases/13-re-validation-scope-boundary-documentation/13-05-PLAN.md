---
phase: 13-re-validation-scope-boundary-documentation
plan: 05
type: execute
wave: 4
depends_on: ["13-04"]
files_modified:
  - ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit.json"
  - ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md"
  - ".planning/workstreams/sgproc-render/REQUIREMENTS.md"
  - ".planning/workstreams/sgproc-render/ROADMAP.md"
autonomous: true
requirements: [VALD-01]

must_haves:
  truths:
    - "capture_diff, run against Plan 13-04's 2 fresh xhw-mnn-float-refit_*.cap files (Mac vs Windows, widened-grid quantization active), reports contentHashMatch and chunkHashesMatch for the MNN float32 fixture -- the literal fresh empirical answer to whether the S=2^14 fix closes VALD-01/SC1's remaining gap"
    - "13-SCOPE-BOUNDARY.md gains a new Refit section stating the outcome honestly either way: if contentHashMatch is true AND every chunkHashesMatch entry is true, it declares SC1 now fully satisfied for the MNN fixture; if any entry is still false, it states that plainly as a continuing open gap rather than reinterpreting an unfavorable result into a passing one -- mirroring Plan 13-03's own honesty discipline for the original round"
    - "REQUIREMENTS.md's VALD-01 line/traceability-row and ROADMAP.md's Phase 13 SC1/Progress-table/phase-list-bullet status are updated to match the fresh Refit outcome exactly: flipped from Partial to fully satisfied ONLY if the fresh diff is unconditionally clean for the MNN fixture; otherwise left as Partial, with the evidence citation updated to point at the fresh Refit numbers (not silently left pointing only at the superseded pre-fix numbers)"
  artifacts:
    - path: ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit.json"
      provides: "Fresh Mac-vs-Windows post-widened-grid-quantization divergence stats for the MNN float32 fixture"
    - path: ".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md"
      provides: "New Refit section recording the fresh empirical outcome (pass or continuing honest gap) alongside the original round's data, which stays in place for comparison"
    - path: ".planning/workstreams/sgproc-render/REQUIREMENTS.md"
      provides: "VALD-01 status/citation updated to reflect the Refit outcome"
    - path: ".planning/workstreams/sgproc-render/ROADMAP.md"
      provides: "Phase 13 SC1/Progress-table/phase-list-bullet status updated to reflect the Refit outcome"
  key_links:
    - from: "captures/xhw-mnn-float-refit_*.cap (Plan 13-04)"
      to: "captures/diff-mnn-float-refit.json"
      via: "capture_diff --a --b --element-type float32 --json-output"
      pattern: "contentHashMatch|chunkHashesMatch"
    - from: "captures/diff-mnn-float-refit.json"
      to: "13-SCOPE-BOUNDARY.md Refit section"
      via: "verbatim-cited JSON fields, same discipline as the original round's SC1 section"
      pattern: "contentHashMatch"
    - from: "13-SCOPE-BOUNDARY.md Refit outcome"
      to: "REQUIREMENTS.md VALD-01 + ROADMAP.md Phase 13 SC1/Progress table"
      via: "conditional scoped Edit -- flip to Complete only if the fresh diff is unconditionally clean, else update evidence citation only"
      pattern: "VALD-01|SC1"
---

<objective>
Diff Plan 13-04's fresh Mac-vs-Windows MNN captures (widened-grid quantization active) at the processor-level hash layer, and record the honest outcome in `13-SCOPE-BOUNDARY.md`'s new Refit section -- closing VALD-01's traceability loop either by confirming the gap is now closed, or by honestly reporting that it persists with updated evidence for whoever picks it up next.

Purpose: this is the literal empirical check of whether Plan 13-04's S=2^14 grid-widening actually closes the MNN fixture's remaining VALD-01/SC1 gap, continuing 13-VERIFICATION.md's gap report and 13-SCOPE-BOUNDARY.md's original "Open Gap Against SC1" statement.
Output: `captures/diff-mnn-float-refit.json` (new), `13-SCOPE-BOUNDARY.md` (edited, new Refit section), `REQUIREMENTS.md`/`ROADMAP.md` (edited conditionally on the outcome).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-03-PLAN.md
@.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-04-PLAN.md
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run capture_diff on Plan 13-04's fresh widened-grid MNN captures</name>
  <files>.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit.json (new)</files>
  <read_first>
    SuperGenius/SGProcessingManager/tools/capture/capture_diff.cpp (CLI usage, JSON field names -- unmodified since Phase 10; lines ~279-368 for exactly what contentHashMatch/chunkHashesMatch/maxAbsDelta compute)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/ (Plan 13-04's 2 delivered xhw-mnn-float-refit_*.cap files -- list the directory to find the actual generated filenames, each embeds a runtime UTC timestamp that cannot be known in advance)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-03-PLAN.md (Task 1 -- the exact capture_diff invocation pattern this task repeats)
  </read_first>
  <action>
List this phase's `captures/` directory to find the exact filenames Plan 13-04's checkpoint produced (each embeds a runtime timestamp, e.g. `xhw-mnn-float-refit_Fuus-Mac-mini.local---macOS_<timestamp>.cap` and `xhw-mnn-float-refit_Mofu---Windows_<timestamp>.cap` -- do not assume any prior round's exact timestamps). Do not confuse these with Plan 13-01's original `xhw-mnn-float_*.cap` files (no `-refit` suffix) still present in the same directory.

Rebuild `capture_diff` if not already current (`cmake --build SuperGenius/build/Windows/Debug --target capture_diff --config Debug`), then run it once, comparing the Mac-origin `xhw-mnn-float-refit_*.cap` against the Windows-origin `xhw-mnn-float-refit_*.cap` (`--a`/`--b` order does not affect the reported stats):

`capture_diff --a captures/xhw-mnn-float-refit_<mac-file>.cap --b captures/xhw-mnn-float-refit_<windows-file>.cap --element-type float32 --json-output .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit.json`

(substitute the real filenames found by the directory listing above). Never compare Mac-vs-Mac, Windows-vs-Windows, or a `-refit` file against a non-`-refit` file from the original round -- this diff must pair exactly one Mac-origin `-refit` file with one Windows-origin `-refit` file.

Print the JSON report to console as part of this task's own evidence trail -- Task 2 (same plan) reads it from disk directly.
  </action>
  <verify>
    <automated>d=".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures"; test -f "$d/diff-mnn-float-refit.json" && grep -q "contentHashMatch" "$d/diff-mnn-float-refit.json" && grep -q "chunkHashesMatch" "$d/diff-mnn-float-refit.json" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `captures/diff-mnn-float-refit.json` exists and is valid JSON containing `contentHashMatch`, `chunkHashesMatch`, `combinedHashMatch`, `maxAbsDelta`, `maxRelDelta`, `maxUlpDistance`, `percentExceedingThreshold` fields.
    - The diff compares exactly one Mac-origin `xhw-mnn-float-refit_*.cap` against one Windows-origin `xhw-mnn-float-refit_*.cap` -- never a `-refit` file against a non-`-refit` file, and never same-machine.
  </acceptance_criteria>
  <done>Fresh Mac-vs-Windows post-widened-grid-quantization divergence stats exist for the MNN fixture, ready for 13-SCOPE-BOUNDARY.md's Refit section to cite verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Write 13-SCOPE-BOUNDARY.md's Refit section, and update REQUIREMENTS.md/ROADMAP.md's VALD-01 status conditionally on the fresh outcome</name>
  <files>.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md, .planning/workstreams/sgproc-render/REQUIREMENTS.md, .planning/workstreams/sgproc-render/ROADMAP.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit.json (this plan's Task 1 output -- the fresh outcome to cite verbatim and to condition this task's REQUIREMENTS.md/ROADMAP.md edit on)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md (existing structure -- SC1/SC4 sections this task's new Refit section is appended after, not a replacement of)
    .planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-04-PLAN.md (the widened-grid derivation numbers to cite: old grid 9.5367431640625e-07, new grid 6.103515625e-05, margins 64x/~585x)
    .planning/workstreams/sgproc-render/REQUIREMENTS.md (VALD-01 line and Traceability table row -- current "Partial" wording from Plan 13-02's edit)
    .planning/workstreams/sgproc-render/ROADMAP.md (Phase 13 SC1 text, Progress table row, phase-list summary bullet -- current "Gaps found (VALD-01 partial)" wording)
  </read_first>
  <action>
Read `captures/diff-mnn-float-refit.json`'s fresh `contentHashMatch` and `chunkHashesMatch` fields first -- this task's REQUIREMENTS.md/ROADMAP.md edit branches on this actual value, not on an assumed outcome.

Append a new "## SC1 Refit: Widened-Grid Re-Measurement" section to `13-SCOPE-BOUNDARY.md`, after the existing MNN Float32 Fixture subsection (do not delete or rewrite the original round's data -- both rounds stay in the document so a future reader can see the before/after). This new section must: (1) restate that Plan 13-04 widened the quantization grid from S=2^20 (9.5367431640625e-07 grid step) to S=2^14 (6.103515625e-05 grid step), citing the same margin numbers as `quantization.hpp`'s updated doc comment (64x the old grid step, ~585x Phase 11's original maxAbsDelta 1.043081283569336e-07); (2) embed `captures/diff-mnn-float-refit.json`'s full verbatim contents as a fenced `json` block, byte-for-byte, unrounded -- the same discipline the original SC1 section already used; (3) state the outcome plainly.

If the fresh `contentHashMatch` is `true` AND every `chunkHashesMatch` entry is `true`: state that SC1 is now **fully satisfied** for the MNN fixture -- the widened grid closed the gap for this fixture's real data. In this case, also update `REQUIREMENTS.md`'s VALD-01 line and Traceability-table row (currently "Partial — render OK, MNN open gap") to state both fixtures now match, check VALD-01's checkbox, and update `ROADMAP.md`'s Phase 13 SC1 text, Progress-table row (currently "Gaps found (VALD-01 partial)"), and phase-list summary bullet to reflect full completion -- using scoped `Edit` calls only, never a whole-file `Write`, per this phase's established discipline (13-PATTERNS.md).

If the fresh `contentHashMatch` is `false` OR any `chunkHashesMatch` entry is `false`: state plainly that the gap **persists** after the widened-grid fix -- do not round this away or claim partial credit as full success. Report the fresh `maxAbsDelta`/`maxRelDelta`/`maxUlpDistance` and the count of divergent chunks, and compare them numerically against the original round's numbers (contentHashMatch false, 12/15 divergent, maxAbsDelta 9.5367431640625e-07) to show whether the fix measurably reduced divergence even if it did not eliminate it. In this case, leave `REQUIREMENTS.md`'s VALD-01 and `ROADMAP.md`'s Phase 13 SC1/Progress-table/phase-list-bullet status exactly as "Partial", but update their evidence citation (via scoped `Edit`) to reference this fresh Refit section and `captures/diff-mnn-float-refit.json` alongside the original round's citation -- do not leave the wording pointing only at the now-superseded pre-fix numbers as if this fix attempt never happened.

Either way, close the new Refit section with an honest one-line statement restating the phase's own core caveat (already present in `quantization.hpp`'s updated doc comment and this plan's own objective): a fixed rounding grid is a probabilistic mitigation, not a mathematical guarantee, for arbitrary per-element cross-hardware deltas landing close to a rounding boundary.
  </action>
  <verify>
    <automated>file=".planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md"; grep -qi "SC1 Refit" "$file" && grep -q "diff-mnn-float-refit" "$file" && grep -qi "contentHashMatch" "$file" && grep -qi "16384\|2\^14\|6.103515625e-05" "$file" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `13-SCOPE-BOUNDARY.md` gains a new "SC1 Refit" section (the original round's SC1/SC4 sections remain unmodified below/above it) embedding `diff-mnn-float-refit.json` verbatim and citing the widened-grid derivation numbers (2^14, 6.103515625e-05, 64x/~585x margins).
    - If the fresh diff is unconditionally clean (contentHashMatch true, all chunkHashesMatch true): REQUIREMENTS.md VALD-01 and ROADMAP.md Phase 13 SC1/Progress-table/phase-list-bullet all flip from Partial to fully satisfied, via scoped Edit only.
    - If the fresh diff still shows any divergence: REQUIREMENTS.md/ROADMAP.md status stays "Partial" but their evidence citation is updated (via scoped Edit) to also reference the fresh Refit section -- not left pointing only at the pre-fix numbers.
    - The Refit section's stated outcome (pass or continuing gap) matches `diff-mnn-float-refit.json`'s actual `contentHashMatch`/`chunkHashesMatch` values exactly -- no rounding an unfavorable result into a passing one.
  </acceptance_criteria>
  <done>13-SCOPE-BOUNDARY.md's Refit section honestly records the widened-grid fix's fresh empirical outcome, and REQUIREMENTS.md/ROADMAP.md's VALD-01/SC1 status accurately reflects that outcome (fully satisfied only if genuinely unconditionally clean, otherwise Partial with updated evidence).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Numeric citation + status flip into a permanent milestone-closing record | `13-SCOPE-BOUNDARY.md`'s Refit section and REQUIREMENTS.md/ROADMAP.md's VALD-01/SC1 status are what future consumers read to judge whether this milestone's central empirical claim actually holds -- an optimistic status flip here misleads every future reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|-----------------|
| T-13-10 | Repudiation / Information Disclosure | Claiming VALD-01/SC1 fully satisfied when the fresh Refit diff does not actually show an unconditional processor-level hash match | high | mitigate | Task 2's action requires reading `diff-mnn-float-refit.json`'s actual `contentHashMatch`/`chunkHashesMatch` fields first and branches the REQUIREMENTS.md/ROADMAP.md edit strictly on that real value -- a status flip to "Complete" is only permitted if every relevant field is true, mirroring 13-03's T-13-06 discipline. |
| T-13-11 | Repudiation | Numeric citations in the new Refit section could be paraphrased/rounded, obscuring whether the fix measurably helped | medium | mitigate | Task 2 requires embedding `diff-mnn-float-refit.json` verbatim (not paraphrased) and numerically comparing it against the original round's cited numbers, mirroring 13-03's T-13-05 precedent. |

No package-manager installs occur in this plan -- it touches no build files, no dependencies. The npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `captures/diff-mnn-float-refit.json` exists, comparing exactly one Mac-origin and one Windows-origin `xhw-mnn-float-refit_*.cap`.
2. `13-SCOPE-BOUNDARY.md`'s new Refit section cites the fresh diff's exact fields and the widened-grid derivation numbers, without deleting the original round's data.
3. REQUIREMENTS.md/ROADMAP.md's VALD-01/SC1 status matches the fresh diff's actual outcome exactly (Complete only if unconditionally clean; Partial with updated evidence otherwise).
</verification>

<success_criteria>
VALD-01's traceability loop is closed with fresh, honestly-reported evidence: either the MNN fixture's processor-level hash now matches cross-hardware under the widened-grid fix (and REQUIREMENTS.md/ROADMAP.md reflect full completion), or it still does not (and REQUIREMENTS.md/ROADMAP.md stay Partial, now citing the fresh attempt's numbers alongside the original round's).
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New file: `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/captures/diff-mnn-float-refit.json`.
- Edited file: `.planning/workstreams/sgproc-render/phases/13-re-validation-scope-boundary-documentation/13-SCOPE-BOUNDARY.md` (new Refit section appended).
- Edited file: `.planning/workstreams/sgproc-render/REQUIREMENTS.md` (VALD-01 wording/status, conditional on outcome).
- Edited file: `.planning/workstreams/sgproc-render/ROADMAP.md` (Phase 13 SC1/Progress-table/phase-list-bullet wording/status, conditional on outcome).
- No `SuperGenius`/`SGProcessingManager` source file is created or modified by this plan.

## Artifacts this gap-closure round produces (both new plans, 13-04 + 13-05)

- Modified: `SuperGenius/SGProcessingManager/include/util/quantization.hpp`, `SuperGenius/SGProcessingManager/src/util/quantization.cpp`, `SuperGenius/SGProcessingManager/test/util/quantization_test.cpp` (Plan 13-04 -- widened S=2^20 to S=2^14).
- New: `captures/xhw-mnn-float-refit_*.cap` (2 files, Plan 13-04), `captures/diff-mnn-float-refit.json` (this plan).
- Edited: `13-SCOPE-BOUNDARY.md` (new Refit section, this plan), `REQUIREMENTS.md`, `ROADMAP.md` (this plan, conditional on outcome).
- Render fixture and `QuantizeByteBuffer` are untouched throughout this gap-closure round -- the render path already fully satisfied SC1 and required no rework.