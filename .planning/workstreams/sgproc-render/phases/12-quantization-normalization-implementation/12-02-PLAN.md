---
phase: 12-quantization-normalization-implementation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp
  - SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
  - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-color.frag
  - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-corrupted-float_model.mnn
  - SuperGenius/test/src/CMakeLists.txt
autonomous: true
requirements: [SECV-01]

must_haves:
  truths:
    - "A byte-perturbed copy of float_model.mnn, run through the same MNN float pipeline shape as float-processing-definition.json, produces a post-quantization artifactId that differs from the correct model's artifactId (D-10, SECV-01)."
    - "A render job whose fragment shader outputs a different constant color than passthrough.frag, run through the same render pipeline, produces a post-quantization artifactId that differs from the correct shader's artifactId (D-11, SECV-01)."
    - "Both counter-test cases live in one new CTest target that runs automatically under ctest in CI, not a standalone manual tool like capture_harness (D-12)."
    - "Both assertions are a binary not-equal check on hash bytes with no secondary magnitude/divergence-threshold check (D-13)."
  artifacts:
    - SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp
    - SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
    - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-color.frag
    - SuperGenius/test/src/processing_conformance_security/fixtures/secv01-corrupted-float_model.mnn
  key_links:
    - "Plan 12-01's real QuantizeFloatBuffer/QuantizeByteBuffer bodies <-> this plan's counter-test meaningfulness -- against the Phase 10 identity stub, both cases would trivially pass without proving anything about the real, tightened tolerance."
    - "output.artifacts[0].artifactId (ComputeArtifactIdentity) <-> the D-01-compliant hash target for both new test cases -- never output.combinedHash / ExecutionManifest.manifestHash."
---

<objective>
Build the SECV-01 wrong-result-still-diverges counter-test that research and 12-CONTEXT.md flag as this milestone's central, non-optional risk: prove Plan 12-01's real quantization tolerance is not loose enough to also accept a substituted MNN model or a substituted render shader constant.

Purpose: The milestone's empirical cross-machine match test (Phase 13) only proves the tolerance is loose enough; this plan is the other half of the proof -- that it is not too loose. Must land in this phase, not deferred (D-12).
Output: New CTest target `processing_conformance_security_test`, two new fixture files, two TEST_F counter-test cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/PROJECT.md
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-PATTERNS.md
@.planning/workstreams/sgproc-render/phases/09-processor-pass-graph-conformance-suites/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the two corrupted-result fixtures</name>
  <files>SuperGenius/test/src/processing_conformance_security/fixtures/secv01-wrong-color.frag, SuperGenius/test/src/processing_conformance_security/fixtures/secv01-corrupted-float_model.mnn</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_regression/fixtures/passthrough.frag (the correct-result fragment shader this fixture must diverge from)
    SuperGenius/test/src/processing_conformance_regression/fixtures/passthrough.vert (confirms the vertex stage is unaffected and can be reused unchanged)
    SuperGenius/SGProcessingManager/include/shaders/shader_compiler.hpp (confirms ShaderSourceType::GLSL is a first-class, runtime-compiled-and-validated source type -- a plain-text GLSL fixture needs no precompiled .spv)
    SuperGenius/SGProcessingManager/generated/Generators.hpp (lines 853-863, confirms the JSON schema string for GLSL source type is literally "glsl")
    SuperGenius/test/src/processing_datatypes/float_model.mnn (the correct-result model this fixture must diverge from; 20496 bytes)
  </read_first>
  <action>
    Create secv01-wrong-color.frag as a copy of passthrough.frag's structure (#version 450, `layout(location = 0) out vec4 outColor;`, `void main()`), but with the body setting `outColor = vec4(0.5, 0.5, 0.5, 1.0)` instead of passthrough.frag's `vec4(1.0, 1.0, 1.0, 1.0)` -- a materially different, still well-formed solid color, standing in for D-11's "wrong shader constant." Add a header comment identifying this as the SECV-01 counter-test's deliberately-wrong fragment fixture. This file is referenced with `"type": "glsl"` (not "spirv") in Task 2's test, so it is compiled and spirv-val-validated at test runtime by the existing ShaderCompiler -- no separate GLSL-to-SPIR-V build step is needed for this fixture.
    Create secv01-corrupted-float_model.mnn as a byte-perturbed copy of float_model.mnn (D-10): copy the 20496 bytes, then flip one byte at offset 15360 by XORing it with 0xFF (this offset sits in the file's back ~25% region, where the two Linear layers' weight tensors -- roughly 16768 of the file's 20496 bytes -- are concentrated, well past the flatbuffers schema/vtable region near the front). Verify empirically that the corrupted file is still loadable: a quick standalone check that `MNN::Interpreter::createFromBuffer()` on the corrupted bytes returns non-null and a session can be created. If offset 15360 breaks parsing (interpreter returns null) or the model loads but produces byte-identical output to the original, try offset 18000, then 12000, then 10000 in that order, keeping the first offset that both loads successfully and produces materially different inference output for the same 512-float input (float_input.bin). Commit only the final working corrupted .mnn file, matching the same byte length as the original (20496 bytes) except for the single flipped byte.
  </action>
  <verify>
    <automated>MISSING -- Task 2 creates secv01_counter_test.cpp, which is the actual automated check that exercises and validates both fixtures end-to-end via the real ShaderCompiler and MNN::Interpreter load paths; there is no standalone check for the fixture files in isolation.</automated>
  </verify>
  <acceptance_criteria>
    - secv01-wrong-color.frag parses as valid GLSL (#version 450, single `out vec4 outColor`, `main()` assigns a vec4 literal different from `vec4(1.0, 1.0, 1.0, 1.0)`).
    - secv01-corrupted-float_model.mnn is exactly 20496 bytes (same size as float_model.mnn) and differs from float_model.mnn in at least one byte.
    - `MNN::Interpreter::createFromBuffer()` on secv01-corrupted-float_model.mnn's bytes returns non-null, and a session can be created from it (empirically confirmed before committing the file).
  </acceptance_criteria>
  <done>Both corrupted-result fixture files exist, are checked into git, and are confirmed loadable/compilable via their respective real code paths.</done>
</task>

<task type="auto">
  <name>Task 2: Write the SECV-01 counter-test suite + CTest registration</name>
  <files>SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp, SuperGenius/test/src/processing_conformance_security/CMakeLists.txt, SuperGenius/test/src/CMakeLists.txt</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_hashing/output_hashing_test.cpp (ContentHashChangesOnDifferentInput, lines 72-126 -- the two-manager-two-runs-compare-hashes shape to mirror for the MNN case)
    SuperGenius/test/src/processing_conformance_cancellation/cancellation_conformance_test.cpp (lines 177-260, RenderCancelBeforeStartProducesNoSuccessfulResult -- the GTEST_SKIP()-on-no-Vulkan-device + inline render JSON literal shape to mirror for the render case)
    SuperGenius/test/testutil/processing_conformance_fixture.hpp (ProcessorConformanceFixture base class -- PatchedJson, BinPath, PatchJsonUrisToAbsolute)
    SuperGenius/SGProcessingManager/include/processors/vulkan_gpu_probe.hpp (HasUsableVulkanDevice() declaration)
    SuperGenius/test/src/processing_datatypes/float-processing-definition.json (the exact JSON shape to mirror inline for both MNN cases, changing only the model's source_uri_param)
    SuperGenius/test/src/processing_conformance_regression/CMakeLists.txt (the addtest() + nested POST_BUILD fixture-copy pattern to mirror for this new suite's fixtures/ directory)
    SuperGenius/test/src/CMakeLists.txt (the add_subdirectory list to extend)
  </read_first>
  <action>
    Create secv01_counter_test.cpp under namespace sgns with `class Secv01CounterTest : public ProcessorConformanceFixture {}` (no SetUpTestSuite override -- use the base class's default data_path, since this suite constructs its job JSON as inline string literals rather than loading on-disk JSON fixtures).
    TEST_F(Secv01CounterTest, MnnCorruptedModelStillDiverges): construct two inline JSON string literals shaped exactly like float-processing-definition.json (same inputs/outputs/parameters/passes structure, width 512, block_len 64, chunk_stride 32, format FLOAT32) -- one with model.source_uri_param and the modelUri parameter's default both set to "file://processing_datatypes/float_model.mnn" (the correct model), the other set to "file://processing_conformance_security/fixtures/secv01-corrupted-float_model.mnn" (Task 1's corrupted copy); both reuse "file://processing_datatypes/float_input.bin" as the input, unchanged. Patch both via PatchJsonUrisToAbsolute(json_str, BinPath()). Create() + Process() (4-arg overload) each; ASSERT_TRUE both pr.has_value(). Assert `std::memcmp(prCorrect.value().artifacts[0].artifactId, prCorrupted.value().artifacts[0].artifactId, sgns::sgprocessing::SHA256_HASH_SIZE) != 0` (D-13's binary ASSERT_NE, applied to the D-01-compliant artifact-level hash, never to combinedHash).
    TEST_F(Secv01CounterTest, RenderWrongShaderConstantStillDiverges): `if (!sgns::sgprocessing::HasUsableVulkanDevice()) { GTEST_SKIP() << "No usable Vulkan device (DISCRETE_GPU/INTEGRATED_GPU) found on this host; skipping this GPU-dependent test, not failing it."; }` first. Construct two inline JSON string literals shaped exactly like cancellation_conformance_test.cpp's RenderCancelBeforeStartProducesNoSuccessfulResult render job (64x64 RGBA8/D32_SFLOAT render target, vertexData input from "file://processing_datatypes/float_input.bin") -- one with the fragment stage as `{"stage": "fragment", "type": "spirv", "source": "file://processing_conformance_regression/fixtures/passthrough.frag.spv", "entry_point": "main"}` (the correct shader), the other with `{"stage": "fragment", "type": "glsl", "source": "file://processing_conformance_security/fixtures/secv01-wrong-color.frag", "entry_point": "main"}` (Task 1's corrupted GLSL source -- compiled+validated at runtime by ShaderCompiler); both keep the vertex stage as the unmodified precompiled "file://processing_conformance_regression/fixtures/passthrough.vert.spv". Patch both via PatchJsonUrisToAbsolute. Create() with a ModelNode whose source is set to "input:vertexData" (matching the cancellation test's pattern) + Process() (4-arg overload) each; ASSERT_TRUE both pr.has_value(). Assert `std::memcmp(...artifactId..., ...artifactId..., SHA256_HASH_SIZE) != 0`, same binary check as the MNN case.
    Create SuperGenius/test/src/processing_conformance_security/CMakeLists.txt mirroring processing_conformance_regression/CMakeLists.txt's shape: `addtest(processing_conformance_security_test secv01_counter_test.cpp)`; `target_include_directories(processing_conformance_security_test PRIVATE ${AsyncIOManager_INCLUDE_DIR})`; `target_link_libraries(processing_conformance_security_test nlohmann_json ProcessingBase)`; one POST_BUILD `add_custom_command` copying `${CMAKE_CURRENT_SOURCE_DIR}/fixtures/` to `$<TARGET_FILE_DIR:processing_conformance_security_test>/processing_conformance_security/fixtures/` (this is the ONLY copy command needed here, since this suite never calls PatchedJson() against its own fixtures/ directory -- its JSON is all inline literals, and the only files that need copying are the two Task 1 fixtures referenced by nested "file://processing_conformance_security/fixtures/..." URIs).
    Add `add_subdirectory(processing_conformance_security)` to SuperGenius/test/src/CMakeLists.txt, alongside the existing Phase 09 `processing_conformance_*` entries.
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test && ctest --test-dir SuperGenius/build/Windows/Debug -R processing_conformance_security_test --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Both TEST_F cases assert on `.artifacts[0].artifactId` via std::memcmp, and neither references `.combinedHash` anywhere in this file.
    - RenderWrongShaderConstantStillDiverges calls HasUsableVulkanDevice() and GTEST_SKIP()s before constructing any render job, exactly mirroring the Phase 09 D-05 pattern.
    - The fragment-stage JSON literal for the corrupted render case uses `"type": "glsl"` (not "spirv") pointing at secv01-wrong-color.frag; the correct-render case keeps `"type": "spirv"` pointing at the existing passthrough.frag.spv.
    - `ctest --test-dir SuperGenius/build/Windows/Debug -R processing_conformance_security_test --output-on-failure` exits 0 -- either both cases pass, or (on a host with no usable Vulkan device) MnnCorruptedModelStillDiverges passes and RenderWrongShaderConstantStillDiverges reports SKIPPED, never FAILED.
    - `SuperGenius/test/src/CMakeLists.txt` contains a new `add_subdirectory(processing_conformance_security)` line.
  </acceptance_criteria>
  <done>A new CTest target proves, for both the MNN and render paths, that a deliberately wrong/corrupted result produces a different post-quantization artifact hash than the correct result -- SECV-01 satisfied for both paths in one CI-integrated suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Corrupted model/shader fixture -> real inference/render pipeline -> post-quantization hash | The deliberate adversarial-simulation boundary SECV-01 exists to test: does Plan 12-01's tolerance still reject a plausible-but-wrong result? |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Tampering / Spoofing | MNN model-file substitution (secv01-corrupted-float_model.mnn) | critical | mitigate | Secv01CounterTest.MnnCorruptedModelStillDiverges asserts the corrupted model's post-quantization artifactId differs from the correct model's (D-10); this is the direct empirical proof that Plan 12-01's tolerance is not loose enough to accept a substituted model. |
| T-12-07 | Tampering / Spoofing | Render shader-constant substitution (secv01-wrong-color.frag) | critical | mitigate | Secv01CounterTest.RenderWrongShaderConstantStillDiverges asserts the corrupted shader's post-quantization artifactId differs from the correct shader's (D-11), covering the render path independently of the MNN path. |
| T-12-08 | Denial of Service | GPU-dependent render counter-test case on a host with no usable Vulkan device | low | accept | GTEST_SKIP() (Phase 09 D-05 pattern) skips rather than fails on GPU-less hosts; the MNN counter-test case has no such dependency and always runs, so SECV-01 coverage for the MNN path is never silently lost. |
</threat_model>

<verification>
- `ctest --test-dir SuperGenius/build/Windows/Debug -R processing_conformance_security_test --output-on-failure` exits 0.
- `git diff` confirms no lines in ComputeManifestHash, ExecutionManifest, executorIdentity, or gpuMemoryUsedBytes were touched.
- `git diff` confirms neither new TEST_F case reads or asserts on `.combinedHash`.
</verification>

<success_criteria>
- A deliberately wrong/corrupted MNN model produces a different post-quantization artifact hash than the correct model's, proven by an automated CTest case.
- A deliberately wrong render shader constant produces a different post-quantization artifact hash than the correct shader's, proven by an automated CTest case.
- Both cases run automatically under `ctest`, registered alongside Phase 09's conformance suites, not as a standalone manual tool.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/12-quantization-normalization-implementation/12-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New CMake target `processing_conformance_security_test` under `SuperGenius/test/src/processing_conformance_security/`.
- New fixture files: `fixtures/secv01-wrong-color.frag`, `fixtures/secv01-corrupted-float_model.mnn`.
- New C++ class `sgns::Secv01CounterTest` (extends `ProcessorConformanceFixture`).
- New TEST_F cases: `Secv01CounterTest.MnnCorruptedModelStillDiverges`, `Secv01CounterTest.RenderWrongShaderConstantStillDiverges`.
- New `add_subdirectory(processing_conformance_security)` line in `SuperGenius/test/src/CMakeLists.txt`.
