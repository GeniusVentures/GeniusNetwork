---
phase: 11-empirical-cross-machine-capture-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/xhw-mnn-float_Fuus-Mac-mini.local---macOS_20260811T212907.cap
  - .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/xhw-mnn-float_Mofu---Windows_20260811T212812.cap
  - .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/xhw-render_Fuus-Mac-mini.local---macOS_20260811T213105.cap
  - .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/xhw-render_Mofu---Windows_20260811T213052.cap
  - .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/diff-mnn-float.json
  - .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/diff-render.json
  - .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md
  - .planning/workstreams/sgproc-render/ROADMAP.md
  - .planning/workstreams/sgproc-render/REQUIREMENTS.md
  - .planning/workstreams/sgproc-render/STATE.md
  - caps/ (deleted)
autonomous: true
# Phase 11 intentionally maps to zero of the 12 v2.1 requirements by design (ROADMAP.md's own
# Phase 11 "Requirements" note and REQUIREMENTS.md's traceability note both say this explicitly).
# It is a data-gathering checkpoint, not a coding phase; the empty list below is correct, not an
# oversight. Phase 12's QUANT-04 is the eventual consumer of this phase's output.
requirements: []

must_haves:
  truths:
    - "captures/ contains exactly the Mac and Windows .cap files for both fixtures (4 files) plus both diff JSONs (2 files) -- 6 files total -- relocated from repo-root caps/, which no longer exists (D-05: only Mac and Windows data survives into Phase 11's deliverable, for both fixtures)"
    - "The WSL/llvmpipe MNN-float .cap file is not present anywhere under captures/ and its measurements are not cited in 11-CAPTURE-RESULTS.md (no WSL diff JSON was ever produced, so this is structurally guaranteed as long as citations come only from diff-mnn-float.json and diff-render.json)"
    - "11-CAPTURE-RESULTS.md cites the exact maxAbsDelta, maxRelDelta, maxUlpDistance, and percentExceedingThreshold values from diff-mnn-float.json and diff-render.json, plus the three hash-match booleans (contentHashMatch, chunkHashesMatch, combinedHashMatch) reported per-hash-type rather than flattened into one boolean (D-08: this is Phase 11's deliverable doc, required to cite these exact numbers so Phase 12 can cite specific captured numbers rather than a guessed constant)"
    - "11-CAPTURE-RESULTS.md states the 2-machine scope decision (D-01) and the dropped-WSL/llvmpipe rationale (D-04) as their own required content, not a footnote"
    - "11-CAPTURE-RESULTS.md identifies both machines using their exact .cap-filename labels: Fuus-Mac-mini.local---macOS and Mofu---Windows"
    - "ROADMAP.md Phase 11 Success Criterion 1 and REQUIREMENTS.md QUANT-04 both read approximately '2 distinct physical machines' / '2-machine data' with the software-rasterizer-exclusion caveat, replacing the stale '3' wording"
    - "RenderProcessor::IsAcceptable() is unmodified -- this phase touches no SuperGenius/SGProcessingManager source file"
    - "No new capture_harness or capture_diff run is performed -- the existing 2 diff JSONs already satisfy pairwise coverage for both fixtures (D-06/D-07)"
    - "STATE.md's 'Operator Next Steps' section no longer contains the stale instruction to 'Get the third machine's .cap files ... before Phase 11 can close out' -- that line is superseded by D-01/D-02, and the adjacent 'Pending Todos' cross-reference to the not-yet-formalized discuss-phase step is also corrected"
  artifacts:
    - path: ".planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/"
      provides: "Phase-owned relocation of the 4 non-WSL .cap files + 2 diff JSONs, moved from repo-root caps/"
    - path: ".planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md"
      provides: "This phase's literal deliverable -- written cross-hardware divergence statistics Phase 12's normalization design can cite directly"
    - path: ".planning/workstreams/sgproc-render/ROADMAP.md"
      provides: "Phase 11 Success Criterion 1 reconciled to the accepted 2-machine scope"
    - path: ".planning/workstreams/sgproc-render/REQUIREMENTS.md"
      provides: "QUANT-04 reconciled to the accepted 2-machine scope"
    - path: ".planning/workstreams/sgproc-render/STATE.md"
      provides: "Operator Next Steps' stale third-machine mandate and the Pending Todos discuss-phase cross-reference reconciled to D-01/D-02's accepted 2-machine scope"
  key_links:
    - from: "captures/diff-mnn-float.json, captures/diff-render.json"
      to: "11-CAPTURE-RESULTS.md"
      via: "verbatim-cited JSON fields"
      pattern: "maxAbsDelta|maxRelDelta|maxUlpDistance|percentExceedingThreshold|contentHashMatch|combinedHashMatch"
    - from: "11-CONTEXT.md D-01/D-03/D-04"
      to: "ROADMAP.md Phase 11 SC1 + REQUIREMENTS.md QUANT-04"
      via: "text substitution reconciling roadmap literal wording with the accepted 2-machine scope"
      pattern: "at least 2 distinct physical machines|.2-machine data"
    - from: "11-CONTEXT.md D-02"
      to: "STATE.md Operator Next Steps + Pending Todos"
      via: "text substitution removing the stale 'get the third machine's .cap files' operator instruction and correcting the discuss-phase-pending cross-reference"
      pattern: "Get the third machine.s .cap files|formally kick off"
---

<objective>
Turn this session's already-completed ad-hoc cross-machine capture run (Mac + Windows, both Phase 09 fixtures, already diffed) into Phase 11's formal, traceable deliverable: relocate the raw evidence into a phase-owned directory, write the results document Phase 12 will cite, and reconcile ROADMAP.md/REQUIREMENTS.md's literal "3 machines" wording -- plus STATE.md's stale "get the third machine's .cap files" operator instruction -- with the accepted 2-machine scope decision (D-01/D-02/D-03).

Phase 11 maps to zero of the 12 v2.1 requirements by design -- see ROADMAP.md's own Phase 11 "Requirements" line and REQUIREMENTS.md's traceability note. This is a hands-on data-gathering checkpoint, not a coding phase: no SuperGenius/SGProcessingManager source file is created, modified, or needs to be. No new `capture_harness`/`capture_diff` run is required -- the empirical work already happened; this plan's job is documentation and data organization only.

Purpose: produce the empirical input Phase 12's QUANT-04 requirement depends on, and close Phase 11 without leaving the roadmap/requirements docs internally contradicting the scope decision made this session.
Output: `captures/` (relocated evidence), `11-CAPTURE-RESULTS.md` (new results doc), reconciled ROADMAP.md + REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-PATTERNS.md
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate capture evidence into the phase-owned captures/ directory</name>
  <files>.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/ (new, 6 files), caps/ (deleted)</files>
  <read_first>
    caps/diff-mnn-float.json
    caps/diff-render.json
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CONTEXT.md
  </read_first>
  <action>
Create the directory `.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/`. Move exactly 6 files into it from repo-root `caps/`, preserving filenames exactly (per D-09): both diff JSONs (`diff-mnn-float.json`, `diff-render.json`) and the 4 Mac/Windows `.cap` files for both fixtures (the MNN-float and render captures for `Fuus-Mac-mini.local---macOS` and for `Mofu---Windows`). This is a plain filesystem move, not a copy-then-edit -- byte content must be identical before and after.

Do not move the WSL/llvmpipe MNN-float capture (the fifth `.cap` file in `caps/`, timestamped `20260811T230448`, machine tag `Mofu---Linux`) into `captures/` under any circumstances -- per D-04 it is dropped from this phase's dataset entirely because it ran on a software rasterizer, not real hardware. D-09 leaves its disposal (delete vs. archive untracked) to executor discretion; since repo-root `caps/` is being removed in this same task and nothing downstream needs it, delete it along with the rest of `caps/`'s contents once the other 6 files are confirmed relocated.

After confirming the 6 target files exist at the new location, delete the now-empty repo-root `caps/` directory entirely (including the dropped file if not already removed). Do not create, modify, or touch any file under `SuperGenius/` -- this phase performs no code changes.
  </action>
  <verify>
    <automated>captures_dir=".planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures"; test -d "$captures_dir" && [ "$(ls "$captures_dir" | wc -l)" = "6" ] && test -f "$captures_dir/diff-mnn-float.json" && test -f "$captures_dir/diff-render.json" && test -f "$captures_dir/xhw-mnn-float_Fuus-Mac-mini.local---macOS_20260811T212907.cap" && test -f "$captures_dir/xhw-mnn-float_Mofu---Windows_20260811T212812.cap" && test -f "$captures_dir/xhw-render_Fuus-Mac-mini.local---macOS_20260811T213105.cap" && test -f "$captures_dir/xhw-render_Mofu---Windows_20260811T213052.cap" && [ ! -d caps ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `captures/` directory exists under the phase directory and contains exactly 6 files: the 4 Mac/Windows `.cap` files (MNN-float and render, one per machine) and the 2 diff JSONs (`diff-mnn-float.json`, `diff-render.json`).
    - The WSL/llvmpipe MNN-float `.cap` file does not exist anywhere under `captures/`.
    - Repo-root `caps/` directory no longer exists.
    - No file under `SuperGenius/` is created or modified by this task.
  </acceptance_criteria>
  <done>captures/ holds the phase's 6 relocated evidence files, repo-root caps/ is gone, and the WSL capture is excluded from the relocation entirely.</done>
</task>

<task type="auto">
  <name>Task 2: Write 11-CAPTURE-RESULTS.md citing both diff JSONs' exact numbers</name>
  <files>.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md (new)</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CONTEXT.md
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-PATTERNS.md
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/diff-mnn-float.json
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/diff-render.json
    .planning/workstreams/sgproc-render/phases/10-capture-harness-diff-tool-quantization-stub/10-VERIFICATION.md
  </read_first>
  <action>
Depends on Task 1 having already relocated the evidence -- read the two diff JSONs from their new `captures/` location, not from repo-root `caps/`.

Create `11-CAPTURE-RESULTS.md` in this phase directory. Open with a short metadata header (phase name, date captured, status noting this is a 2-machine dataset), matching the metadata-header convention used elsewhere in this workstream's phase docs (see `10-VERIFICATION.md` for the header-block style).

Include a "Scope Decision" section stating D-01's substance: this phase closes with 2 distinct real machines (Mac + Windows/"Mofu"), not the 3 originally stated in the roadmap, because WSL's only available Vulkan device is `llvmpipe` (a Mesa software rasterizer, `type=CPU`) -- not real hardware, and the same software-Vulkan-ICD tier Phase 4's D-31 already excludes project-wide; loosening `RenderProcessor::IsAcceptable()` to admit it was explicitly rejected as out of scope for this phase.

Include a "Dropped WSL/MNN Capture" section stating D-04's substance: a WSL/llvmpipe MNN-float capture was gathered ad hoc this session but is excluded from this phase's dataset entirely -- not relocated into `captures/`, not cited below as measured data -- because MNN has no device-type filter and silently ran on the same software rasterizer render already excludes project-wide, so comparing it against real GPU hardware would not be an apples-to-apples hardware comparison.

Identify both surviving machines using their exact `.cap`-filename labels -- `Fuus-Mac-mini.local---macOS` and `Mofu---Windows` -- and note both fixtures (MNN float32 and render uint8) were captured on each.

For each fixture, add a subsection that embeds the corresponding diff JSON's full contents verbatim as a fenced code block tagged json (copy `diff-mnn-float.json` and `diff-render.json`'s contents exactly, byte for byte, from the files relocated in Task 1 -- do not paraphrase, round, or omit any field), immediately followed by a short prose interpretation naming each field by its JSON key.

For the MNN float32 fixture's subsection: note `elementCount` 512, that every `chunkHashesMatch` entry is false and both `combinedHashMatch`/`contentHashMatch` are false -- expected, since Phase 10/11's quantization is still a no-op stub, so this measures raw cross-hardware float divergence rather than a normalization failure -- and cite `maxAbsDelta` (approximately 1.043e-07), `maxRelDelta` (approximately 7.27e-05), and `maxUlpDistance` (768) as the concrete numbers Phase 12's normalization design should cite.

For the render uint8 fixture's subsection: note `contentHashMatch` true (the raw output bytes matched byte-for-byte across Mac and Windows) alongside `combinedHashMatch` false -- report this pairing exactly as `capture_diff` structures it (two separate named booleans, not one flattened yes/no) and flag it as a nuance worth Phase 12's attention without asserting an unverified explanation for why they diverge, since resolving that mechanism is out of this phase's scope. Note `chunkHashesMatch` is an empty array for this fixture, and that `maxAbsDelta`/`maxRelDelta`/`maxUlpDistance`/`percentExceedingThreshold` are all 0 -- the raw pixel bytes are identical.

Close with a short "Implications for Phase 12" section connecting these numbers to QUANT-04: this data is the empirical 2-machine dataset Phase 12's normalization design must cite rather than a guessed constant, and note the MNN float-side threshold recorded in the JSON (`percentExceedingThreshold`) still uses Phase 10's stub relative-threshold default -- Phase 12 will revisit that constant, not this document.
  </action>
  <verify>
    <automated>file=".planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md"; grep -q "1.043081283569336e-07" "$file" && grep -q "7.269731577252969e-05" "$file" && grep -q "768" "$file" && grep -q "contentHashMatch" "$file" && grep -q "combinedHashMatch" "$file" && grep -q "chunkHashesMatch" "$file" && grep -q "Fuus-Mac-mini.local---macOS" "$file" && grep -q "Mofu---Windows" "$file" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `11-CAPTURE-RESULTS.md` exists and embeds the full verbatim JSON contents of both `diff-mnn-float.json` and `diff-render.json`.
    - Cites `maxAbsDelta`, `maxRelDelta`, `maxUlpDistance`, `percentExceedingThreshold`, `contentHashMatch`, `chunkHashesMatch`, and `combinedHashMatch` by name for both fixtures.
    - States the 2-machine scope decision (D-01) and the dropped-WSL rationale (D-04) as their own sections, not a footnote.
    - Identifies both machines using the exact labels `Fuus-Mac-mini.local---macOS` and `Mofu---Windows`.
    - States the render fixture's `contentHashMatch: true` / `combinedHashMatch: false` pairing explicitly, without asserting an unverified causal explanation.
  </acceptance_criteria>
  <done>11-CAPTURE-RESULTS.md exists, cites both diff JSONs' exact numbers and hash-match booleans, and documents the 2-machine scope decision and dropped-WSL rationale as required content.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP.md, REQUIREMENTS.md, and STATE.md wording to the 2-machine scope</name>
  <files>.planning/workstreams/sgproc-render/ROADMAP.md, .planning/workstreams/sgproc-render/REQUIREMENTS.md, .planning/workstreams/sgproc-render/STATE.md</files>
  <read_first>
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CONTEXT.md
    .planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-PATTERNS.md
    .planning/workstreams/sgproc-render/ROADMAP.md
    .planning/workstreams/sgproc-render/REQUIREMENTS.md
    .planning/workstreams/sgproc-render/STATE.md
  </read_first>
  <action>
Use the Edit tool (scoped replacement) on both files -- never Write a whole-file rewrite of either, since both contain many unrelated phase entries outside this plan's scope.

In ROADMAP.md, replace Phase 11 Success Criterion 1's current text -- which reads "at least 3 distinct physical machines, including the user's Mac and PC" -- so it reads "at least 2 distinct physical machines, including the user's Mac and PC" plus an added parenthetical caveat explaining that a third machine (WSL) was attempted but its only available Vulkan device was `llvmpipe`, a software rasterizer already excluded project-wide by `RenderProcessor::IsAcceptable()`'s device-type gate, and pointing to `11-CAPTURE-RESULTS.md` for the full rationale. This is D-03's explicitly mandated edit.

As a discretionary consistency fix in the same document (flagged by 11-PATTERNS.md as an adjacent inconsistency, not separately mandated by D-03 but reasonable to close in the same pass so the phase block does not contradict itself): also replace the Phase 11 Goal line's "(Mac + PC + a third)" wording with "(Mac + PC)" plus a short inline clause noting the third machine's only available device was a software rasterizer, excluded per this phase's scope decision.

In REQUIREMENTS.md, replace QUANT-04's current text -- which reads "empirical .3-machine data" (using the greater-than-or-equal symbol) -- so it reads "empirical .2-machine data (Mac + Windows)" plus a short parenthetical noting the excluded third-machine software-rasterizer caveat. This is D-03's explicitly mandated edit.

As the same discretionary consistency fix, also replace the Phase 11 traceability note's "run Phase 10's tooling on .3 real machines" wording (near the bottom of REQUIREMENTS.md) with "run Phase 10's tooling on .2 real machines (Mac + Windows)" plus the same excluded-third-machine caveat, pointing to `11-CAPTURE-RESULTS.md`.

Do NOT touch any other occurrence of "3 different machines" / "3 machines" in either file -- Phase 12's Success Criteria, Phase 13's Goal and Success Criteria, and REQUIREMENTS.md's VALD-01 all still read "3 different machines" and CONTEXT.md's D-03 only names ROADMAP Phase 11 SC1 and REQUIREMENTS QUANT-04 (plus the two immediately-adjacent stale mentions in the same phase block) as in-scope. Whether Phase 13's own eventual re-scoping revisits its "3-machine" re-validation criterion is that phase's own planning decision, not this plan's -- leave it untouched.

In STATE.md (`.planning/workstreams/sgproc-render/STATE.md`), fix two locations superseded by D-02 -- per CONTEXT.md D-02, the "Operator Next Steps" line telling the operator to "get the third machine's .cap files ... before Phase 11 can close out" predates D-01's 2-machine scope decision and is stale:

1. Under the `## Operator Next Steps` heading, the file currently has exactly this two-bullet block:
   `- Phase 10 complete. \`/gsd-discuss-phase 11 --ws sgproc-render\` when ready to formally scope Phase 11 (2 of 3 required machines already captured ad-hoc this session — see Pending Todos).`
   `- Get the third machine's \`.cap\` files (MNN-float + render, \`--repeat 2\` minimum) before Phase 11 can close out.`
   Replace both bullets verbatim with:
   `- Phase 10 complete. Phase 11 discussed and scoped (see 11-CONTEXT.md) — 2-machine dataset (Mac + Windows) accepted per D-01; Phase 11 is not blocked on a third physical machine (D-02).`
   `- Run \`/gsd-execute-phase 11 --ws sgproc-render\` per 11-01-PLAN.md to relocate the captured evidence into captures/, write 11-CAPTURE-RESULTS.md, and reconcile ROADMAP.md/REQUIREMENTS.md wording.`

2. Under the `### Pending Todos` heading, the first bullet currently ends with the parenthetical `(not yet moved into a phase-owned location)` -- replace that parenthetical with `(relocated into this phase's \`captures/\` directory per 11-01-PLAN.md Task 1)`. The last bullet in that same section currently reads exactly:
   `- Next: formally kick off \`/gsd-discuss-phase 11 --ws sgproc-render\` (user is doing this next) to capture this 2/3-machines-plus-labeled-caveat scope as the phase's actual context.`
   Replace it verbatim with:
   `- Done: \`/gsd-discuss-phase 11 --ws sgproc-render\` captured the 2-machine scope as 11-CONTEXT.md's D-01 through D-09; see 11-01-PLAN.md for the execution plan that closes out Phase 11.`

<!-- planner-discipline-allow: Get the third machine -->
<!-- planner-discipline-allow: formally kick off -->

Leave every other section of STATE.md (Key Decisions, Blockers/Concerns, Deferred Items, Session Continuity, the bottom-level Decisions log, etc.) untouched -- this task only corrects the two D-02-superseded locations named above.
  </action>
  <verify>
    <automated>roadmap=".planning/workstreams/sgproc-render/ROADMAP.md"; reqs=".planning/workstreams/sgproc-render/REQUIREMENTS.md"; state=".planning/workstreams/sgproc-render/STATE.md"; grep -q "at least 2 distinct physical machines" "$roadmap" && grep -q "software rasterizer" "$roadmap" && grep -q "Mac + PC)" "$roadmap" && grep -qE "..2-machine data" "$reqs" && grep -qE "..2 real machines" "$reqs" && grep -q "software rasterizer" "$reqs" && ! grep -q "Get the third machine" "$state" && ! grep -q "formally kick off" "$state" && grep -q "not blocked on a third physical machine" "$state" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 11 Success Criterion 1 reads "at least 2 distinct physical machines" with the software-rasterizer-exclusion caveat.
    - ROADMAP.md Phase 11 Goal line reads "(Mac + PC)" rather than "(Mac + PC + a third)".
    - REQUIREMENTS.md QUANT-04 reads with a "2-machine data" (or equivalent .2-machine) phrasing plus the Mac+Windows/software-rasterizer caveat.
    - REQUIREMENTS.md's Phase 11 traceability note reads "2 real machines" (or equivalent .2 real machines) with the same caveat.
    - Phase 12's Success Criteria, Phase 13's Goal/Success Criteria, and REQUIREMENTS.md's VALD-01 are unchanged.
    - STATE.md's "Operator Next Steps" section no longer contains the "Get the third machine's .cap files ... before Phase 11 can close out" instruction; it instead states the 2-machine scope is accepted (D-01) and Phase 11 is not blocked on a third machine (D-02).
    - STATE.md's "Pending Todos" section no longer says the discuss-phase step is still pending ("Next: formally kick off ...") and no longer says the captures are "not yet moved into a phase-owned location".
  </acceptance_criteria>
  <done>ROADMAP.md and REQUIREMENTS.md are reconciled to the 2-machine scope decision (D-03) in the two locations D-03 names plus the immediately-adjacent stale wording in the same phase block, with Phase 12/13's own wording left untouched; STATE.md's "Operator Next Steps" and "Pending Todos" sections no longer instruct gathering a third machine's captures, reconciled to D-01/D-02.</done>
</task>

</tasks>

## Artifacts this phase produces

This phase creates no code symbols -- no classes, functions, CLI flags, struct fields, or CMake targets. It is a documentation and data-organization phase. Artifacts:

- New directory: `.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/captures/` (holds 6 relocated evidence files: 4 `.cap` files + 2 diff JSONs)
- New file: `.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-CAPTURE-RESULTS.md`
- Edited file: `.planning/workstreams/sgproc-render/ROADMAP.md` (Phase 11 Success Criterion 1 + Goal line)
- Edited file: `.planning/workstreams/sgproc-render/REQUIREMENTS.md` (QUANT-04 + Phase 11 traceability note)
- Edited file: `.planning/workstreams/sgproc-render/STATE.md` (Operator Next Steps + Pending Todos, reconciled to D-01/D-02)
- Deleted directory: repo-root `caps/` (and the WSL/llvmpipe MNN-float `.cap` file it contained, dropped per D-04)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem move (repo-root caps/ -> phase-owned captures/) | Same-user, same-machine file relocation; no network or remote input crosses this boundary |
| Untracked binary capture data entering tracked git history | `.cap` files move from an untracked repo-root directory into a tracked `.planning/` path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | captures/ directory contents (relocated .cap files) | low | accept | Files are moved via a plain filesystem operation from a directory the same operator already produced this session; no external input crosses a trust boundary, and content was already verified unchanged by capture_harness's own CAPT-02 self-check before these files ever existed |
| T-11-02 | Repudiation | 11-CAPTURE-RESULTS.md's numeric citations | medium | mitigate | Task 2 requires embedding the raw diff JSON verbatim (not paraphrased or rounded) inside the results doc, so any future disagreement about what Phase 11 actually measured is resolvable by diffing the doc against the same captures/*.json files it cites |
| T-11-03 | Information Disclosure | Deleting the dropped WSL .cap file | low | accept | The dropped file is local test-fixture output (float tensor values from a public MNN test model run on a software rasterizer), not sensitive data; CONTEXT.md D-09 explicitly leaves its disposal method to executor discretion |

No package-manager installs occur in this plan -- it touches no build files, no dependencies. The npm/pip/cargo package legitimacy gate does not apply.
</threat_model>

<verification>
1. `captures/` contains exactly the 6 non-WSL evidence files (4 `.cap` + 2 diff JSONs); repo-root `caps/` no longer exists.
2. `11-CAPTURE-RESULTS.md` cites `diff-mnn-float.json` and `diff-render.json`'s exact fields (maxAbsDelta, maxRelDelta, maxUlpDistance, percentExceedingThreshold, contentHashMatch, chunkHashesMatch, combinedHashMatch) and documents D-01 (2-machine scope) and D-04 (dropped WSL) as required content.
3. ROADMAP.md Phase 11 SC1 and REQUIREMENTS.md QUANT-04 both read "2 distinct/real machines" with the software-rasterizer-exclusion caveat, reconciling the roadmap's literal text with the scope decision; Phase 12/13's own "3-machine" wording is left untouched (out of this plan's scope).
4. STATE.md's "Operator Next Steps" and "Pending Todos" sections no longer instruct the operator to gather a third machine's captures before Phase 11 can close out; both now reflect D-01's accepted 2-machine scope and D-02's supersession of that stale callout.
</verification>

<success_criteria>
Phase 11's ad-hoc capture run is now the phase's formal, traceable deliverable: raw evidence lives in a phase-owned `captures/` directory (WSL excluded), `11-CAPTURE-RESULTS.md` cites the real diff numbers Phase 12's normalization design needs, ROADMAP.md/REQUIREMENTS.md no longer contradict the accepted 2-machine scope decision, and STATE.md's operator-facing next-steps text no longer contradicts D-01/D-02 by demanding a third machine's captures.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/11-empirical-cross-machine-capture-run/11-01-SUMMARY.md` when done
</output>
