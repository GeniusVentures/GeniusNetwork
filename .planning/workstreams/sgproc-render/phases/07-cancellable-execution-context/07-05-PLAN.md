---
phase: 07-cancellable-execution-context
plan: 05
type: execute
wave: 3
depends_on: [07-01, 07-03]
files_modified:
  - SuperGenius/SGProcessingManager/include/processors/processing_processor.hpp
autonomous: true
requirements: [EXEC-07]
tags: [adapter, migration, cleanup]

must_haves:
  truths:
    - "Old 5-arg StartProcessing() pure virtual removed from ProcessingProcessor base class"
    - "Only the 6-arg StartProcessing(..., const ExecutionContext&) overload remains in the base class"
    - "All 15 processor subclasses compile after removal (verified by 07-03 completion)"
    - "D-19 satisfied: old signature removed before Phase 07 ships"
  artifacts:
    - path: "SuperGenius/SGProcessingManager/include/processors/processing_processor.hpp"
      provides: "Removal of old 5-arg StartProcessing; 6-arg is sole pure virtual"
      min_lines: 5
  key_links:
    - from: "processing_processor.hpp old StartProcessing removal"
      to: "all 15 processor subclasses (updated in 07-03)"
      via: "compilation verification — no subclass references removed signature"
      pattern: "StartProcessing"
---

<objective>
Remove the old 5-argument StartProcessing() adapter from ProcessingProcessor base class, finalizing the migration (D-19). After Plan 07-03 has updated all 15 processors to override the new ExecutionContext-taking overload, the old pure virtual signature is deleted and the new overload becomes the sole StartProcessing() entry point.

Purpose: Complete D-19 ("old StartProcessing() deprecated and removed before Phase 07 ships"). This is the final migration step — all processors already implement the new overload (07-03), so the base class no longer needs the backward-compat adapter.

Output: Updated `processing_processor.hpp` — old 5-arg pure virtual removed, new 6-arg overload is the only `= 0` StartProcessing.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
@.planning/workstreams/sgproc-render/phases/07-cancellable-execution-context/07-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/07-cancellable-execution-context/07-01-PLAN.md
@.planning/workstreams/sgproc-render/phases/07-cancellable-execution-context/07-03-PLAN.md

# Source of truth
@SuperGenius/SGProcessingManager/include/processors/processing_processor.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old StartProcessing() signature — finalize D-19 migration</name>
  <files>SuperGenius/SGProcessingManager/include/processors/processing_processor.hpp</files>
  <read_first>
Read these files before modifying anything:
- `SuperGenius/SGProcessingManager/include/processors/processing_processor.hpp` — current state after Plan 07-01: should have old pure virtual `StartProcessing(chunkhashes, proc, imageData, modelFile, parameters) = 0` and new overload `StartProcessing(..., const ExecutionContext&)` with default implementation delegating to old one
  </read_first>
  <behavior>
    - Test 1: After edit, `grep -c 'StartProcessing' include/processors/processing_processor.hpp` returns exactly 1 declaration (the new 6-arg overload)
    - Test 2: The remaining declaration has 6 parameters including `const ExecutionContext& execCtx` and is marked `= 0` (pure virtual)
    - Test 3: No 5-argument `StartProcessing` with parameters `(chunkhashes, proc, imageData, modelFile, parameters)` exists in the base class
    - Test 4: All 15 processor subclasses compile (verified by successful SGProcessingManager build)
  </behavior>
  <action>
Remove the old 5-argument StartProcessing() pure virtual from the ProcessingProcessor base class, leaving only the new 6-argument overload as the sole pure virtual (D-19: "old StartProcessing() deprecated and removed before Phase 07 ships"):

**Step 1 — Delete the old pure virtual declaration:**

Find and delete this line (or equivalent):
```cpp
virtual ProcessingResult StartProcessing(
    std::vector<std::vector<uint8_t>> &chunkhashes,
    const sgns::IoDeclaration         &proc,
    std::vector<char>                 &imageData,
    std::vector<char>                 &modelFile,
    const std::vector<sgns::Parameter> *parameters) = 0;
```

**Step 2 — Make the new overload pure virtual:**

The new 6-argument overload (added in 07-01) currently has a default inline implementation body that delegates to the old 5-arg overload. Remove that body and add `= 0`:

```cpp
// New overload — pure virtual, all processors must implement:
virtual ProcessingResult StartProcessing(
    std::vector<std::vector<uint8_t>> &chunkhashes,
    const sgns::IoDeclaration         &proc,
    std::vector<char>                 &imageData,
    std::vector<char>                 &modelFile,
    const std::vector<sgns::Parameter> *parameters,
    const ExecutionContext            &execCtx) = 0;
```

**Step 3 — Remove any stale comments:**

Remove any `// TODO: Remove after all processors implement new overload` or similar migration comments from 07-01.

**Verification after edit:**
- `grep -c 'StartProcessing.*= 0' include/processors/processing_processor.hpp` returns exactly 1 (the new 6-arg version)
- `grep -c 'StartProcessing.*IoDeclaration.*vector.*vector.*vector.*Parameter[^s]' include/processors/processing_processor.hpp` returns 0 (old 5-arg gone — note the `[^s]` to exclude the 6-arg match)
- Build completes: `cmake --build . --target SGProcessingManager` succeeds (all 15 subclasses already override the new overload per 07-03)
  </action>
  <verify>
    <automated>grep -c 'StartProcessing.*= 0' include/processors/processing_processor.hpp returns exactly 1. grep 'StartProcessing' include/processors/processing_processor.hpp shows exactly one declaration with 6 parameters including const ExecutionContext& execCtx. Build passes.</automated>
  </verify>
  <done>Old StartProcessing() 5-argument pure virtual removed from ProcessingProcessor. New 6-argument StartProcessing(chunkhashes, proc, imageData, modelFile, parameters, execCtx) is the only StartProcessing() in the base class, marked = 0 (pure virtual). All 15 processor subclasses override it (verified by successful compilation — dependency on 07-03 completion). D-19 satisfied.</done>
</task>

</tasks>

<verification>
1. `grep -c 'StartProcessing.*= 0' include/processors/processing_processor.hpp` returns exactly 1
2. Old 5-arg signature absent: `grep -c 'StartProcessing.*parameters)[[:space:]]*= 0' include/processors/processing_processor.hpp` returns 0
3. Build passes (all processor subclasses compile with new overload)
</verification>

<success_criteria>
1. Only the 6-argument StartProcessing(chunkhashes, proc, imageData, modelFile, parameters, execCtx) remains in ProcessingProcessor base class
2. The remaining overload is pure virtual (= 0)
3. D-19 satisfied: old adapter removed before Phase 07 ships
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/07-cancellable-execution-context/07-05-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| Old `StartProcessing()` 5-arg pure virtual | deletion | `processing_processor.hpp` | D-19: remove backward-compat adapter |
| New `StartProcessing()` 6-arg made `= 0` | modification | `processing_processor.hpp` | Sole StartProcessing entry point for all processors |
