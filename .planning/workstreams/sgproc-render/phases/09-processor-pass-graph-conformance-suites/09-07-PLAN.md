---
phase: 09-processor-pass-graph-conformance-suites
plan: 07
type: execute
wave: 3
depends_on: ["09-02", "09-03", "09-04", "09-05", "09-06"]
files_modified:
  - SuperGenius/test/src/CMakeLists.txt
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "ctest runs from standalone SGProcessingManager build and passes"
    - "Same tests run and pass when SGProcessingManager is consumed as submodule by SuperGenius/develop"
    - "All 6-7 new add_subdirectory() entries are registered in SuperGenius/test/src/CMakeLists.txt"
    - "No existing CTest targets are broken by the new registrations"
  artifacts:
    - path: "SuperGenius/test/src/CMakeLists.txt"
      provides: "Registration point for all new conformance test subdirectories"
      contains: "add_subdirectory(processing_conformance_"
  key_links:
    - from: "SuperGenius/test/src/CMakeLists.txt"
      to: "each processing_conformance_* subdirectory"
      via: "add_subdirectory()"
      pattern: 'add_subdirectory\(processing_conformance_'
</objective>

<objective>
Wire all new conformance test executables into the SuperGenius CTest build — add `add_subdirectory()` entries to `SuperGenius/test/src/CMakeLists.txt` and verify the full `ctest` suite passes (TEST-01).

Purpose: Plans 09-02 through 09-06 created the test executables and their CMakeLists.txt files, but they are not yet registered in the parent CMakeLists.txt. This plan connects everything and verifies the end-to-end CTest integration.

Output: Updated `SuperGenius/test/src/CMakeLists.txt` with 6 new `add_subdirectory()` entries. Verified `ctest` run.
---

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@SuperGenius/test/src/CMakeLists.txt
@SuperGenius/cmake/functions.cmake
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register all new conformance test subdirectories in parent CMakeLists.txt</name>
  <files>SuperGenius/test/src/CMakeLists.txt</files>
  <action>
Per D-04 and research recommendation: add 6 new `add_subdirectory()` entries to `SuperGenius/test/src/CMakeLists.txt`. Place them near the existing `add_subdirectory(processing_datatypes)` entry (around line 16) to keep processing tests grouped together.

New entries (in alphabetical order, matching existing convention):

```cmake
add_subdirectory(processing_conformance_cancellation)
add_subdirectory(processing_conformance_capability)
add_subdirectory(processing_conformance_executor)
add_subdirectory(processing_conformance_hashing)
add_subdirectory(processing_conformance_migration)
add_subdirectory(processing_conformance_regression)
add_subdirectory(processing_conformance_schema)
```

That's 7 entries, not 6 — because Plans 09-03 through 09-06 create 7 distinct test directories:
1. `processing_conformance_schema` (Plan 09-03, Task 1)
2. `processing_conformance_executor` (Plan 09-03, Task 2)
3. `processing_conformance_hashing` (Plan 09-04, Task 1)
4. `processing_conformance_migration` (Plan 09-04, Task 2)
5. `processing_conformance_cancellation` (Plan 09-05, Task 1)
6. `processing_conformance_capability` (Plan 09-05, Task 2)
7. `processing_conformance_regression` (Plan 09-06)

Insert these after the existing `add_subdirectory(processing_vulkan_concurrency)` line (line ~20). Add a comment block before them:

```cmake
# Phase 09: Processor & Pass-Graph Conformance Suites
```

Do NOT add entries for directories that don't exist yet — verify each directory exists before adding its `add_subdirectory()` call. If a directory from a prior plan doesn't exist yet, skip it and note the gap.

No other changes to CMakeLists.txt. The existing `add_subdirectory()` entries remain unchanged.
  </action>
  <verify>
    <automated>grep -c "processing_conformance_" SuperGenius/test/src/CMakeLists.txt</automated>
  </verify>
  <done>7 new add_subdirectory() entries registered. CMakeLists.txt configures without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full CTest suite passes</name>
  <files>None (verification only)</files>
  <action>
Per TEST-01: run the full CTest suite to verify all new conformance tests pass alongside existing tests.

Steps:
1. Configure the build: `cmake -S build/Windows -B build/Windows/Debug` (or the platform-appropriate path)
2. Build all test targets: `cmake --build build/Windows/Debug --target processing_conformance_schema_test processing_conformance_executor_test processing_conformance_hashing_test processing_conformance_migration_test processing_conformance_cancellation_test processing_conformance_capability_test processing_conformance_regression_test` (build all new targets explicitly to catch compile errors)
3. Run new conformance tests: `ctest --test-dir build/Windows/Debug -R "processing_conformance_" -V`
4. Run full suite to verify no regressions: `ctest --test-dir build/Windows/Debug -V`

Expected results:
- All new conformance tests pass (GPU tests may SKIP on headless CI — expected)
- No existing tests regress
- `ctest` exit code is 0

If compilation errors occur: fix include paths, missing link libraries, or namespace issues in the test .cpp files. Follow existing patterns from `processing_datatypes_test`.

If runtime failures occur: check fixture paths (POST_BUILD copy working?), JSON schema (valid per quicktype?), ProcessingManager initialization (all dependencies available?).

**TEST-01 submodule verification:** If SuperGenius builds as a submodule of GeniusSDK, verify the same tests compile and pass there. The `addtest()` macro uses generator expressions (`$<TARGET_FILE_DIR>`) which work correctly in submodule builds. Document the submodule build verification command.
  </action>
  <verify>
    <automated>ctest --test-dir build/Windows/Debug -R "processing_conformance_" -V</automated>
  </verify>
  <done>All new conformance tests pass via ctest. No existing test regressions. CTest exit code 0. TEST-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMake configure → add_subdirectory() | Each subdirectory must have a valid CMakeLists.txt; missing/invalid CMakeLists breaks the entire build |
| CTest runner → test executables | Test executables must be built before ctest runs them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-18 | Denial of Service | Missing subdirectory breaks CMake configure | mitigate | Task 1 verifies each directory exists before adding add_subdirectory(); Task 2 catches any remaining issues at build time |
| T-09-19 | Tampering | CMakeLists.txt merge conflicts | accept | Single file, single plan — no parallel modification risk in Wave 3 |
</threat_model>

<verification>
```bash
# Full suite
ctest --test-dir build/Windows/Debug -V
# Conformance-only
ctest --test-dir build/Windows/Debug -R "processing_conformance_" -V
```
All pass. Exit code 0.
</verification>

<success_criteria>
`SuperGenius/test/src/CMakeLists.txt` contains 7 new `add_subdirectory()` entries. `ctest` passes with all conformance tests. TEST-01 satisfied.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/09-processor-pass-graph-conformance-suites/09-07-SUMMARY.md` when done
</output>
