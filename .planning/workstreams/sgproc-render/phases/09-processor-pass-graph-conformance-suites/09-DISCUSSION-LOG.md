# Phase 09: Processor & Pass-Graph Conformance Suites - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-05
**Phase:** 09-Processor & Pass-Graph Conformance Suites
**Areas discussed:** Test Fixture Strategy, CTest Target Granularity, GPU-Dependent Test Tiers, Processor Conformance Contract Scope, Regression Bug Reproduction, Test Location (Submodule vs Standalone)

---

## Test Fixture Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Synthetic auto-generated | Generate minimal models/shaders at test time. No external files. | |
| Pre-baked test assets | Commit small real-but-minimal model/shader files to test/. Requires asset maintenance. | |
| Hybrid | Synthetic for MNN, pre-baked for RenderProcessor SPIR-V. | |
| You decide | Let Claude/researcher pick per processor type. | |

**User's choice:** "We already have SEVERAL generated mnn models in SuperGenius tests processing_datatypes? So I feel like that's already covered, we can expand the processing_datatypes tests with glsl and spir-v data right?"

**Notes:** The existing `processing_datatypes_test` pattern is the canonical approach — pre-baked `.mnn` models generated by Python scripts, `.raw` input/output files, JSON definitions, POST_BUILD copy. Expand this to cover the remaining 5 MNN types and add GLSL/SPIR-V fixtures for RenderProcessor.

---

## CTest Target Granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Per-category targets | ~6 CTest targets by test dimension (schema, hashing, cancel, etc.) | |
| Per-processor targets | ~20 CTest targets, one per processor running full contract | |
| Hybrid | Per-processor conformance + shared infrastructure tests | |
| You decide | Let Claude/researcher pick | |

**User's choice:** "Since we already have some tests, follow that setup I think."

**Notes:** The existing SGProcessingManager test pattern uses per-category executables (`capability_validator_test`, `sgprocmanagerexec_cancellation_test`, etc.) — not per-processor. Each is `add_executable` + `add_test` + `gtest_discover_tests`. Follow this pattern.

---

## GPU-Dependent Test Tiers

| Option | Description | Selected |
|--------|-------------|----------|
| GTEST_SKIP + device probe | Probe for Vulkan device at SetUp; skip with reason if unavailable | ✓ |
| CMake categories + ctest labels | Tag GPU tests; CI filters with ctest -LE gpu | |
| Both | Labels for CI + GTEST_SKIP as safety net | |
| You decide | Let Claude/researcher pick | |

**User's choice:** GTEST_SKIP + device probe

**Notes:** Straightforward, no CMake complexity. Can add labels later if CI needs them.

---

## Processor Conformance Contract Scope

| Option | Description | Selected |
|--------|-------------|----------|
| API shape only | Verify signatures, no GPU/MNN work | |
| Full pipeline | Each processor runs with fixture, verify output hash, verify teardown | ✓ |
| API + smoke test | API validation + one smoke dimension per processor | |
| You decide | Let Claude/researcher pick | |

**User's choice:** Full pipeline — each processor runs with known fixture, output hash verified, teardown verified.

**Notes:** This is the `processing_datatypes_test` approach: Process() with known input, compare to expected output, verify no leaks. Extend to all 18 MNN + RenderProcessor.

---

## Regression Bug Reproduction

| Option | Description | Selected |
|--------|-------------|----------|
| Synthetic triggers | Minimal inputs exercising exact code paths | ✓ |
| Historical repro | Actual inputs from production bugs | |
| Both | Synthetic where historical unavailable; historical where preserved | |
| You decide | Let Claude/researcher pick | |

**User's choice:** Synthetic triggers

**Notes:** Craft minimal inputs for each of 4 known bugs. Assert the FIXED behavior. Targeted, fast, no dependency on preserved historical data.

---

## Test Location (Submodule vs Standalone)

| Option | Description | Selected |
|--------|-------------|----------|
| add_subdirectory | SuperGenius/test picks up SGProcessingManager tests via add_subdirectory | |
| Follow existing | Follow processing_datatypes pattern in SuperGenius/test/src/ | |
| You decide | Let Claude/researcher pick | |

**User's choice:** "SGProcessingManager tests are SGProcessingManager tests, SuperGenius tests are SuperGenius tests. SuperGenius has no reason to consume SGProcessingManager tests. I think we just follow the existing pattern and create the tests in SuperGenius, mostly because it is compiled more often."

**Notes:** Conformance tests go in `SuperGenius/test/src/` alongside `processing_datatypes_test`. SGProcessingManager standalone tests (capability, execution, artifacts) remain separate. Tests are exercised more often in the SuperGenius build.

---

## Claude's Discretion

- Exact GTest suite and test case names
- Exact GLSL/SPIR-V fixture content for RenderProcessor (trivial pass-through shaders)
- Whether conformance tests share a common fixture base class
- CMake library target linkage per test executable
- GTEST_SKIP() reason strings
- Whether regression tests are separate executables or within category targets
- Python model-generation scripts for the 5 remaining MNN processor types

## Deferred Ideas

- Per-processor CTest targets — per-category is simpler, revisit if failure isolation needed
- CMake labels for GPU tests — GTEST_SKIP sufficient for now
- Cross-node/cross-vendor determinism tests — explicitly out of scope (v1.0 DETV-01)
- Coverage tooling (gcov/lcov) — not scoped for v2.0
- MoltenVK-specific conformance tests — macOS CI exercises MoltenVK automatically
