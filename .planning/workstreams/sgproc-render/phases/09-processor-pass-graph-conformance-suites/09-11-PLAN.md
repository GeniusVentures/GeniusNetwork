---
phase: 09-processor-pass-graph-conformance-suites
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - SuperGenius/test/src/processing_conformance_capability/capability_conformance_test.cpp
  - SuperGenius/test/src/processing_conformance_regression/regression_test.cpp
  - SuperGenius/test/src/processing_conformance_hashing/output_hashing_test.cpp
autonomous: true
gap_closure: true
requirements:
  - TEST-08
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "Capability conformance tests call ProcessingManager::CanExecute() and assert on the returned CanExecuteResult for at least a Vulkan-feature rejection, a valid-render acceptance, and a pass-type-registration rejection — not just ProcessingManager::Create()"
    - "RenderConformanceTest's RenderPassFullPipeline (and related cases) execute the real Vulkan pipeline via ProcessingManager::Process() and verify a rendered-output hash, skipping cleanly via HasUsableVulkanDevice()/GTEST_SKIP() when no GPU is present"
    - "Output hashing conformance covers artifact metadata (resource_name/artifact_id/pass_id/format/dimensions/byte_size/media_type) and execution-manifest field completeness and manifest-level deterministic serialization — not just combinedHash determinism"
  artifacts:
    - path: "SuperGenius/test/src/processing_conformance_capability/capability_conformance_test.cpp"
      provides: "3 new TEST_F cases that call ProcessingManager::CanExecute() directly"
    - path: "SuperGenius/test/src/processing_conformance_regression/regression_test.cpp"
      provides: "RenderConformanceTest cases that actually call Process() through the real Vulkan pipeline"
    - path: "SuperGenius/test/src/processing_conformance_hashing/output_hashing_test.cpp"
      provides: "3 new TEST_F cases covering artifact metadata and manifest-level serialization determinism"
  key_links:
    - from: "capability_conformance_test.cpp new TEST_F cases"
      to: "ProcessingManager::CanExecute()"
      via: "manager->CanExecute(pass, callback) with a CanExecuteResult assertion"
      pattern: "->CanExecute\\("
    - from: "regression_test.cpp RenderConformanceTest::SetUpTestSuite()"
      to: "HasUsableVulkanDevice()/GTEST_SKIP()"
      via: "D-05 GPU probe pattern from processing_dispatch_test.cpp"
      pattern: "HasUsableVulkanDevice"
    - from: "output_hashing_test.cpp ManifestDeterministicSerialization"
      to: "SerializeManifest()"
      via: "artifacts/artifact_serializer.hpp free function, called on a timing-zeroed manifest copy"
      pattern: "SerializeManifest"
---

<objective>
Close three test-authoring-only gaps found by 09-VERIFICATION.md's goal-backward audit (Gap 1, Gap 3, Gap 4) that the earlier 09-UAT.md pass/fail-count check never caught: capability conformance tests never call `ProcessingManager::CanExecute()` (only `Create()`), RenderProcessor GPU conformance tests never touch a real Vulkan device (only `Create()`), and output-hashing conformance tests implement 4 of 7 planned cases (missing artifact-metadata and manifest-serialization coverage). All three fixes are pure test-authoring against APIs that already exist and work correctly — no production code changes.

Purpose: make TEST-08 (capability acceptance/rejection), TEST-05 (Vulkan/MoltenVK conformance), and TEST-06 (output hashing/artifact metadata/manifest serialization) actually exercise the behavior their names and the roadmap's success criteria claim, not just a schema-parsing duplicate.

Output: `capability_conformance_test.cpp` gains 3 cases that call `ProcessingManager::CanExecute()`; `regression_test.cpp`'s `RenderConformanceTest` gains a real GPU probe and actually executes `Process()` through the Vulkan pipeline; `output_hashing_test.cpp` gains the 3 missing ARTF-01/02/04/05 cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/PROJECT.md
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/STATE.md
@.planning/workstreams/sgproc-render/phases/09-processor-pass-graph-conformance-suites/09-VERIFICATION.md
@.planning/workstreams/sgproc-render/phases/09-processor-pass-graph-conformance-suites/09-CONTEXT.md
</context>

## Artifacts This Phase Produces

New symbols created by this plan (excluded from drift verification by the plan-review-convergence source-grounding pass):

- `CapabilityConformanceTest::AcceptValidRenderJobViaCanExecute` (new TEST_F case)
- `CapabilityConformanceTest::RejectUnsupportedVulkanFeature` (new TEST_F case — the plan-mandated missing case)
- `CapabilityConformanceTest::InferenceCanExecuteReflectsPassTypeRegistryGap` (new TEST_F case)
- `OutputHashingTest::ArtifactMetadataPresent` (new TEST_F case)
- `OutputHashingTest::ManifestFieldsPresent` (new TEST_F case)
- `OutputHashingTest::ManifestDeterministicSerialization` (new TEST_F case)

No production (non-test) symbols are created or modified by this plan. `RenderConformanceTest`'s 5 existing TEST_F case names (`RenderPassShaderFixturesValid`, `RenderPassFullPipeline`, `RenderPassOutputHashDeterministic`, `RenderPassTeardownVerified`, `MoltenVkEquivalentPath`) are retained — only their bodies are rewritten.

<tasks>

<task type="auto">
  <name>Task 1: Wire capability_conformance_test.cpp to actually call ProcessingManager::CanExecute()</name>
  <files>SuperGenius/test/src/processing_conformance_capability/capability_conformance_test.cpp</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_capability/capability_conformance_test.cpp
    SuperGenius/SGProcessingManager/src/capability/capability_validator.cpp
    SuperGenius/SGProcessingManager/include/capability/capability_types.hpp
    SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp (lines 414-448, the real registry built in Init())
    SuperGenius/SGProcessingManager/include/processors/vulkan_gpu_probe.hpp
  </read_first>
  <action>
Add three new `TEST_F` cases to the existing `CapabilityConformanceTest` fixture (after the existing 6 cases, before the closing namespace brace). Each calls the real `sgns::sgprocessing::ProcessingManager::CanExecute()` member function via a `CanExecuteCallback` lambda and asserts on the returned `CanExecuteResult` — this is the exact production entry point the verification report found was never exercised (only `Create()` was called by all 6 existing cases).

1. `AcceptValidRenderJobViaCanExecute`: add `#include <processors/vulkan_gpu_probe.hpp>` at the top of the file. Guard the test body with `if ( !sgns::sgprocessing::HasUsableVulkanDevice() ) { GTEST_SKIP() << "No Vulkan device available"; }` (D-05 pattern). Build the manager from the exact same inline JSON literal already used by `AcceptValidRenderJob` (the 64x64 render target, lines 89-142). Extract `auto pass = manager->GetProcessingData().get_passes()[0];`. Call `manager->CanExecute( pass, [&]( sgns::sgprocessing::CanExecuteResult r ) { called = true; result = std::move( r ); } );`. Assert `called` is true, `result.executable` is true, `result.executorId` is non-empty, `result.unmet.empty()`. A 64x64 render target is executable on any real Vulkan-capable GPU with generous device limits — no mocking is needed.

2. `RejectUnsupportedVulkanFeature` (the specific case the verification report flagged as missing — no `HasUsableVulkanDevice()` guard needed, works identically with or without a real GPU): duplicate the `AcceptValidRenderJob` JSON literal, changing only `render_target.width` and `render_target.height` from `64` to `999999999` — a value that exceeds any real Vulkan device's `maxImageDimension2D` and also exceeds the zero-valued default `CapabilitySnapshot.vulkanProps.limits` used when no GPU is present (`CheckProcessValidity()`'s RENDER case only checks presence of `render_shader`/`render_target`/`vertex_buffer`/`vertex_layout`, never bounds-checks width/height, so `Create()` still succeeds with this value). Extract the pass, call `CanExecute()`, assert `result.executable` is false and at least one entry in `result.unmet` has `category == sgns::sgprocessing::UnmetRequirementCategory::VULKAN` and `detail.find( "maxImageDimension2D" ) != std::string::npos`.

3. `InferenceCanExecuteReflectsPassTypeRegistryGap`: build the manager from the exact `AcceptValidInferenceJob` JSON literal (a schema-valid MNN inference pass). Extract the pass, call `CanExecute()`. Assert `result.executable` is false and `result.unmet[0].category == sgns::sgprocessing::UnmetRequirementCategory::PASS_TYPE`, with `result.unmet[0].detail.find( "PassType" ) != std::string::npos || result.unmet[0].detail.find( "registered" ) != std::string::npos`. Add a comment explaining WHY: `ProcessingManager::Init()` (`ProcessingManager.cpp` line 414) only ever calls `RegisterPassProcessorFactory( PassType::RENDER, ... )` — INFERENCE passes dispatch through the separate `DataType`-keyed `m_processorFactories` map and are never entered into the `PassType`-keyed capability registry that `CapabilityValidator::BuildSnapshot()` reads. This test honestly documents today's real, verified behavior (INFERENCE passes are schema-valid but not yet capability-registered); it is not a bug this task fixes — registering INFERENCE for capability purposes would be a separate, larger cross-phase production change, out of this task's authorized scope. Note this finding in the task's completion summary as a discovered follow-up item.

Add a one-line clarifying comment immediately above each of the 3 pre-existing schema-layer cases (`RejectUnsupportedModelFormat`, `RejectUnsupportedPassType`, `RejectionReasonsAreDistinct`) noting they test the pre-`CanExecute()` `Create()`/schema validation gate (correctly fixed by Plan 09-09's Gap 2/4/5) — a distinct, earlier defense layer from the `CanExecute()`/capability gate exercised by the three new cases above. Do not change their assertions or behavior, only add the comment.
  </action>
  <verify>
    <automated>ctest --test-dir SuperGenius/build/Windows/Release -C Release -R "processing_conformance_capability_test" -V</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CanExecute(" SuperGenius/test/src/processing_conformance_capability/capability_conformance_test.cpp` returns at least 3 (previously 0)
    - `RejectUnsupportedVulkanFeature` asserts `result.executable == false` with a `VULKAN`-category `unmet` entry mentioning `maxImageDimension2D`
    - `AcceptValidRenderJobViaCanExecute` is guarded by `HasUsableVulkanDevice()` and asserts `result.executable == true` when a GPU is present
    - All 6 pre-existing test cases remain present, unremoved, and passing
    - ctest verbose output shows 9 `[ OK ]` results for `CapabilityConformanceTest` (6 existing + 3 new)
  </acceptance_criteria>
  <done>All 9 CapabilityConformanceTest cases pass (or the 2 GPU-dependent new cases show `[ SKIPPED ]` with an explicit reason on a GPU-less host). `RejectUnsupportedVulkanFeature` deterministically rejects regardless of host hardware.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RenderConformanceTest to a real Vulkan device probe and actual Process() execution</name>
  <files>SuperGenius/test/src/processing_conformance_regression/regression_test.cpp</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_regression/regression_test.cpp (RenderConformanceTest, lines 146-267)
    SuperGenius/test/src/processing_conformance_regression/fixtures/regression-b-parseblocksize-model-only.json
    SuperGenius/test/src/processing_dispatch/processing_dispatch_test.cpp (lines 266-354 — the exact HasUsableVulkanDevice()/GTEST_SKIP()/ModelNode.set_source()/Process() precedent to copy)
    SuperGenius/SGProcessingManager/include/processors/vulkan_gpu_probe.hpp
  </read_first>
  <action>
Replace `RenderConformanceTest::SetUpTestSuite()`'s placeholder comment block (the lines that say "This is a placeholder — the actual probe depends on the Vulkan GPU probe API...") with a real GPU probe. Add `#include <processors/vulkan_gpu_probe.hpp>` at the top of the file. As the first statement inside `SetUpTestSuite()` (after setting `binary_path`/`data_path`), add `if ( !sgns::sgprocessing::HasUsableVulkanDevice() ) { GTEST_SKIP() << "No usable Vulkan device (DISCRETE_GPU/INTEGRATED_GPU) found on this host; skipping RenderConformanceTest, not failing it."; }` — matching D-05 and `processing_dispatch_test.cpp`'s established pattern. `GTEST_SKIP()` inside a static `SetUpTestSuite()` skips every `TEST_F` in the class.

The `regression-b-parseblocksize-model-only.json` fixture already declares a complete, real render pass (SPIR-V shaders `passthrough.vert.spv`/`passthrough.frag.spv`, a `render_target`, and an input named `vertexData` bound as the vertex buffer) — reuse it as-is; no new fixture is needed. Rewrite the 5 test bodies:

- `RenderPassShaderFixturesValid`: leave unchanged (its purpose is fixture/schema validity via `Create()`, not full-pipeline execution; it now benefits from the class-level GPU-skip guard being real).
- `RenderPassFullPipeline`: after `Create()` succeeds, build `sgns::ModelNode model_node; model_node.set_source( "input:vertexData" );`, an `ioc`, `chunkhashes`, `output_locations` (matching `processing_dispatch_test.cpp`'s pattern exactly), call `manager->Process( ioc, chunkhashes, model_node, output_locations )`, `ASSERT_TRUE( pr.has_value() ) << pr.error().message();`, and `ASSERT_EQ( pr.value().combinedHash.size(), 32u );` — the rendered-output hash the gap requires.
- `RenderPassOutputHashDeterministic`: build TWO separate `ProcessingManager` instances from the same JSON, call `Process()` on each with its own `ModelNode`/`ioc`/`chunkhashes`, and `ASSERT_EQ( pr1.value().combinedHash, pr2.value().combinedHash );` — replacing the current no-op "`Create()` twice" check with a real same-node bit-exact determinism check.
- `RenderPassTeardownVerified`: call `Process()` (not just `Create()`) on two separate manager instances built from the same JSON, in sequence; `ASSERT_TRUE( pr.has_value() )` for both. The second call succeeding proves the first call's `RunTeardown()` released Vulkan resources without leaking, mirroring `CancellationConformanceTest::ResourcesCleanedUpAfterProcess`'s existing pattern.
- `MoltenVkEquivalentPath`: call `Process()` once, assert success and a 32-byte `combinedHash`. Add a comment clarifying that "equivalent" is proven by exercising the identical platform-agnostic Vulkan/RenderProcessor code path on whichever backend (native Vulkan or MoltenVK) the host provides — per 09-CONTEXT.md's Deferred Ideas, no separate MoltenVK-specific fixture is needed or in scope.
  </action>
  <verify>
    <automated>ctest --test-dir SuperGenius/build/Windows/Release -C Release -R "processing_conformance_regression_test" -V</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HasUsableVulkanDevice" SuperGenius/test/src/processing_conformance_regression/regression_test.cpp` returns at least 1
    - `grep -c "->Process(" SuperGenius/test/src/processing_conformance_regression/regression_test.cpp` returns at least 4 (previously 0 inside `RenderConformanceTest`)
    - The "This is a placeholder" comment text no longer appears in the file
    - `RenderPassFullPipeline` asserts `combinedHash.size() == 32`
  </acceptance_criteria>
  <done>On a host with a usable Vulkan device, all 9 `RegressionTest`+`RenderConformanceTest` cases show `[ OK ]`. On a GPU-less host, all 5 `RenderConformanceTest` cases show `[ SKIPPED ]` with the explicit reason, while the 4 unrelated `RegressionTest` cases still run and pass normally.</done>
</task>

<task type="auto">
  <name>Task 3: Add the 3 missing output-hashing conformance cases (artifact metadata + manifest fields + manifest-serialization determinism)</name>
  <files>SuperGenius/test/src/processing_conformance_hashing/output_hashing_test.cpp</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_hashing/output_hashing_test.cpp
    SuperGenius/SGProcessingManager/include/artifacts/artifact_types.hpp
    SuperGenius/SGProcessingManager/include/artifacts/execution_manifest.hpp
    SuperGenius/SGProcessingManager/include/artifacts/artifact_serializer.hpp
    SuperGenius/SGProcessingManager/src/processingbase/ProcessingManager.cpp (lines 1318-1464, how ProcessOutput.artifacts/manifest are populated, and the timing-zeroed-copy pattern from Plan 09-10)
  </read_first>
  <action>
Add `#include <artifacts/artifact_serializer.hpp>` to the top of the file (needed for `SerializeManifest`/`MANIFEST_SERIALIZED_SIZE`; `execution_manifest.hpp`/`artifact_types.hpp` are already transitively included via `processingbase/ProcessingManager.hpp`). Add 3 new `TEST_F` cases to `OutputHashingTest`, after `PersistenceRoundTrip`, all reusing `buffer-processing-definition.json` and the identical `Create()`+`Process()` setup boilerplate already established in this file:

1. `ArtifactMetadataPresent` (ARTF-01/ARTF-02): after a successful `Process()`, assert `pr.value().artifacts.size() >= 1`. On `artifacts[0]` assert: `std::strlen( art.resourceName ) > 0`, `art.artifactId` is not all-zero (compare the 32 bytes against an all-zero array), `std::strlen( art.passId ) > 0`, `std::string( art.outputBinding ).rfind( "output:", 0 ) == 0`, `std::strlen( art.dataType ) > 0`, `std::strlen( art.format ) > 0`, `art.byteSize > 0`, `std::string( art.mediaType ) == "application/octet-stream"`.

2. `ManifestFieldsPresent` (ARTF-04): after `Process()`, assert on `pr.value().manifest`: `std::strlen( manifest.executionId ) > 0`, `std::strlen( manifest.passId ) > 0`, `manifest.executorIdentity` not all-zero (32 bytes), `manifest.outputArtifactCount >= 1`, `manifest.outputArtifactHashes[0]` not all-zero, `manifest.startTimeUsec > 0`, `manifest.endTimeUsec >= manifest.startTimeUsec`, `manifest.terminalState == sgns::sgprocessing::TerminalState::Success`, `manifest.manifestHash` not all-zero.

3. `ManifestDeterministicSerialization` (ARTF-05 — distinct from Plan 09-10's Gap-3 fix, which only made `manifest.manifestHash`'s VALUE deterministic; this verifies the full serialized BYTE LAYOUT is deterministic): run `Process()` twice on two separate manager instances built from the same JSON (mirroring `ContentHashDeterministic`'s existing two-manager pattern). Take local copies `sgns::sgprocessing::ExecutionManifest m1 = pr1.value().manifest;` and `m2 = pr2.value().manifest;`. Zero `m1.startTimeUsec`, `m1.endTimeUsec`, `m1.wallClockUsec` and the same three fields on `m2` — mirroring the exact timing-zeroed-copy pattern `ProcessingManager::Process()` already applies internally to compute `manifestHash` (Plan 09-10's `hashInput`), proving that pattern holds at the full-serialization level, not just at the derived-hash level. Call `sgns::sgprocessing::SerializeManifest( m1 )` and `SerializeManifest( m2 )`. Assert `bytes1.size() == sgns::sgprocessing::MANIFEST_SERIALIZED_SIZE` (5649) and `ASSERT_EQ( bytes1, bytes2 );` — byte-identical `std::vector<uint8_t>` comparison.
  </action>
  <verify>
    <automated>ctest --test-dir SuperGenius/build/Windows/Release -C Release -R "processing_conformance_hashing_test" -V</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TEST_F( OutputHashingTest," SuperGenius/test/src/processing_conformance_hashing/output_hashing_test.cpp` returns exactly 7 (4 existing + 3 new)
    - `ManifestDeterministicSerialization` asserts byte-vector equality via `SerializeManifest()` after zeroing timing fields on both copies, not just hash-digest equality
    - `ArtifactMetadataPresent` and `ManifestFieldsPresent` each assert on at least 6 distinct struct fields
  </acceptance_criteria>
  <done>ctest shows 7 `OutputHashingTest` cases, all `[ OK ]`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-supplied JSON job definitions → ProcessingManager::Create()/CanExecute() | Checked-in fixtures only in this plan; the same code path handles network-submitted jobs in production |
| Rendered pipeline output bytes → SHA-256 artifact/manifest hashes | Content-integrity boundary already established by Phase 08; this plan only adds test coverage against it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-26 | Denial of Service | `RenderConformanceTest` executing a real GPU pipeline could hang on a misbehaving driver | low | accept | Bounded by the existing 64x64 render target and `HasUsableVulkanDevice()` pre-probe (D-05); no new unbounded workload introduced |
| T-09-27 | Tampering | `RejectUnsupportedVulkanFeature`'s 999999999 dimension value could be misread as a real production limit elsewhere | low | mitigate | Value is confined to this test file, commented as deliberately-extreme-for-determinism, never propagated to production schema/config |
| T-09-28 | Information Disclosure | `ManifestDeterministicSerialization` asserts on full serialized manifest bytes, which could mask a future accidental inclusion of sensitive data in the manifest layout | low | accept | `ExecutionManifest`'s fixed-field layout (Phase 08) contains no secrets — only IDs, hashes, and timing; this test only strengthens detection of unintended layout drift |

</threat_model>

<verification>
Run all three modified ctest binaries after rebuilding:
```
ctest --test-dir SuperGenius/build/Windows/Release -C Release -R "processing_conformance_(capability|regression|hashing)_test" -V
```
All 3 binaries report 0 failures (GPU-dependent cases show `[ SKIPPED ]` with an explicit reason on hosts without a usable Vulkan device).
</verification>

<success_criteria>
`capability_conformance_test` gains 3 cases that genuinely call `ProcessingManager::CanExecute()`, including the previously-missing `RejectUnsupportedVulkanFeature`. `RenderConformanceTest` genuinely executes the Vulkan pipeline via `Process()` (or cleanly skips) instead of only re-parsing JSON. `output_hashing_test` covers all 7 originally-planned ARTF-01/02/04/05 cases instead of 4.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/09-processor-pass-graph-conformance-suites/09-11-SUMMARY.md` when done
</output>
