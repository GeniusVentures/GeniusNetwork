---
phase: 15-validation-comparison-mechanism
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - SuperGenius/test/src/processing_conformance_security/secv02_counter_test.cpp
  - SuperGenius/test/src/processing_conformance_security/CMakeLists.txt
autonomous: true
requirements: [SECV-02]

must_haves:
  truths:
    - "A deliberately wrong/corrupted subtask result, run through a real ProcessingManager::Process() pipeline exactly as SECV-01 does, and fed through SubTaskQueueAccessorImpl's real public API (AssignSubTasks/GrabSubTask, not ValidateResults called directly), is still caught as invalid by the fixed ValidateResults plus tolerance fallback acting together (SC5, D-05)."
    - "The test exercises the tolerance-fallback fetch+diff mechanism for real (a working fetchOutputData reaching real file:// output data written by ProcessingManager::Process()), not a trivially-absent-capability path -- otherwise the test would only re-prove XNODE-01b in isolation, not the joint XNODE-01b+XNODE-02 claim SC5/SECV-02 actually requires."
    - "The two subtasks sharing one chunk are produced via ProcessTaskSplitter::SplitTask's existing addvalidationsubtask=true production code path, not hand-built protobuf objects -- reusing the real mechanism that would eventually back XNODE-01c, per D-05's full-pipeline mandate."
  artifacts:
    - SuperGenius/test/src/processing_conformance_security/secv02_counter_test.cpp
  key_links:
    - "SplitTask(task, subTasks, json_data, numchunks=1, addvalidationsubtask=true, ipfsId) <-> the shared ProcessingChunk both resulting subtasks reference -- this is what makes 'two subtasks, one chunk' true without hand-constructing protobuf objects."
    - "outputLocationsCorrect[0]/outputLocationsCorrect (real file:// paths ProcessingManager::Process() already wrote) <-> SubTaskResult.ipfs_results_data_id <-> FinalizeQueueProcessing's real FileManager::LoadASync fetch (Plan 15-03) -- the full chain that must all actually work for this test to be a meaningful proof, not a vacuous one."
    - "sgns::test::ProcessingCoreImpl (processing_mock.hpp's mock, whose base ProcessingCore::GetTaskQueue() default returns nullptr) <-> jobParameters resolving to nullptr <-> Plan 15-01's D-04 fixed-constant tolerance fallback -- this test deliberately exercises the mechanism in its loosest legitimate configuration, the strongest form of the SECV-02 proof."
---

<objective>
Build the SECV-02 counter-test: a full, real pipeline (correct vs. deliberately corrupted MNN model, mirroring SECV-01's exact job-pair methodology) whose two subtask results reach `SubTaskQueueAccessorImpl`'s real public API with a genuine chunk-hash mismatch, proving the fixed `ValidateResults` plus its tolerance fallback (Plans 15-02/15-03) still catch the corruption -- not just tolerate it.

Purpose: this is the phase's final, mandatory proof (per D-05, mirroring SECV-01's discipline) that XNODE-01b's fix and XNODE-02's tolerance layer are not, together, loose enough to mask a real defect. Everything built in Plans 15-01 through 15-03 exists to make this test both possible and meaningful.
Output: `secv02_counter_test.cpp`, wired into the existing `processing_conformance_security_test` CTest target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgproc-render/ROADMAP.md
@.planning/workstreams/sgproc-render/phases/15-validation-comparison-mechanism/15-CONTEXT.md
@.planning/workstreams/sgproc-render/phases/15-validation-comparison-mechanism/15-RESEARCH.md
@.planning/workstreams/sgproc-render/phases/15-validation-comparison-mechanism/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create secv02_counter_test.cpp and wire it into processing_conformance_security_test</name>
  <files>SuperGenius/test/src/processing_conformance_security/secv02_counter_test.cpp, SuperGenius/test/src/processing_conformance_security/CMakeLists.txt</files>
  <read_first>
    SuperGenius/test/src/processing_conformance_security/secv01_counter_test.cpp (full file -- the exact correct-vs-corrupted MNN job-pair JSON shape, PatchJsonUrisToAbsolute/BinPath usage, ProcessingManager::Create()+Process(ioc, chunkhashes, modelNode, outputLocations) call shape, and where the corrupted-model fixture URI points)
    SuperGenius/test/src/processing_conformance_security/CMakeLists.txt (full file -- current addtest() sources/target_link_libraries/fixture copy_directory command)
    SuperGenius/src/processing/processing_tasksplit.cpp (full file -- ProcessTaskSplitter::SplitTask's addvalidationsubtask=true code path, lines 44-98: produces one main subtask plus a second "subtask_validation" subtask, both referencing chunk index 0's identical ProcessingChunk -- the exact two-subtask-one-chunk construction this test reuses instead of hand-building protobuf objects)
    SuperGenius/src/processing/processing_subtask_queue_accessor_impl.hpp (post-Plan-15-03 state -- constructor's 6 parameters including the new processingCore, AssignSubTasks/GrabSubTask/GetResults public surface)
    SuperGenius/test/src/processing/processing_mock.hpp (full file -- SubTaskResultStorageMock, ProcessingSubTaskQueueChannelImpl, sgns::test::ProcessingCoreImpl -- the exact local, no-real-P2P-networking mocks this test constructs; note its ProcessingCoreImpl does NOT override GetTaskQueue(), so it inherits ProcessingCore's default nullptr -- deliberately exercising Plan 15-01's D-04 fallback)
    SuperGenius/src/processing/processing_subtask_queue_manager.hpp (ProcessingSubTaskQueueManager's constructor signature: channel, io_context, nodeId, errorSink)
  </read_first>
  <action>
    Build two inline JSON job definitions in `secv02_counter_test.cpp`, mirroring `secv01_counter_test.cpp`'s exact correct-vs-corrupted MNN inference job shape (same `float_input.bin`/`float_model.mnn` input fixture, same `secv01-corrupted-float_model.mnn` corrupted-model fixture already present under `processing_conformance_security/fixtures/`), but with `"dimensions": { "width": 64, "block_len": 64 }` and NO `"chunk_stride"` key -- this yields exactly one, non-overlapping chunk (nChunks=1), distinct from SECV-01's own overlapping `width:512/block_len:64/chunk_stride:32` fixture, per RESEARCH.md's Pitfall-3 scoping recommendation (single-chunk subtasks are the tractable, exact case for blob-to-chunk slicing; this test must stay in that case). Patch both JSONs via `PatchJsonUrisToAbsolute(..., BinPath())`, run each through `ProcessingManager::Create()` + `Process(ioc, chunkhashes, modelNode, outputLocations)` exactly as `secv01_counter_test.cpp` does, capturing `chunkhashesCorrect[0]`/`chunkhashesCorrupted[0]` (each a `std::vector&lt;uint8_t&gt;`, convert to `std::string` the same way `ProcessingCoreImpl::ProcessSubTask` does for `chunk_hashes`) and `outputLocationsCorrect[0]`/`outputLocationsCorrupted[0]` (the real `file://...` paths each run wrote its output to).

    Build the shared task/subtask pair via `sgns::processing::ProcessTaskSplitter().SplitTask(task, subTasksList, modelJsonForThisSubtask, /*numchunks=*/1, /*addvalidationsubtask=*/true, someIpfsId)` where `task` is a minimal `SGProcessing::Task` with `set_ipfs_block_id(someIpfsId)` -- this produces exactly two `SGProcessing::SubTask`s in `subTasksList`: the first with a real generated UUID subtaskid, the second literally named `"subtask_validation"`, both referencing the identical `ProcessingChunk` (chunk index 0). Read back each subtask's `subtaskid()` from `subTasksList` (do not assume the UUID value) for use below.

    Construct two `SGProcessing::SubTaskResult` objects: one for the first subtask's real id, with `add_chunk_hashes(...)` set to the correct job's real `chunkhashesCorrect[0]` string and `set_ipfs_results_data_id(outputLocationsCorrect[0])`; one for `"subtask_validation"`, with the corrupted job's real `chunkhashesCorrupted[0]` and `outputLocationsCorrupted[0]` -- these two results genuinely differ (real chunk hashes from two different model runs), giving `ValidateResults` a real mismatch to resolve, not a synthetic one.

    Construct, entirely locally with no real P2P networking: a fresh `boost::asio::io_context`; a `sgns::test::ProcessingSubTaskQueueChannelImpl` stub channel (from `processing_mock.hpp`); a `ProcessingSubTaskQueueManager` bound to that channel, the io_context, a node id string, and a no-op error sink; a `sgns::test::SubTaskResultStorageMock`; a `sgns::test::ProcessingCoreImpl` mock instance (its inherited `GetTaskQueue()` returns `nullptr` -- deliberately exercising the D-04 fallback-configuration path, the strongest form of this proof per this plan's `must_haves`); and a `SubTaskQueueAccessorImpl` built from `(nullptr /*gossipPubSub*/, queueManager, resultStorage, taskResultSink, processingErrorSink, processingCoreMock)`, where `processingErrorSink` is a lambda that sets a local `bool errorFired` and captures the message string. Call `accessor-&gt;AssignSubTasks(subTasksList)` (invokes `queueManager-&gt;CreateQueue`), then `resultStorage-&gt;AddSubTaskResult(...)` for both constructed results, then `accessor-&gt;GrabSubTask([](auto){});` -- this public call internally runs `UpdateResultsFromStorage` (loads both results from the mock storage) then, once `IsProcessed()` is true, `FinalizeQueueProcessing` -&gt; the fixed `ValidateResults` with a REAL fetch of both on-disk `file://` outputs (via Plan 15-03's `FileManager::LoadASync`-backed capability) and a REAL `ComputeFloat32Diff` over genuinely different correct-vs-corrupted MNN output bytes.

    Assert `errorFired == true` and the captured message equals `"Invalid results for the entire task"` -- proving the deliberately corrupted subtask result was still caught by the fixed `ValidateResults` plus its tolerance fallback acting together (SECV-02/SC5), even while the fallback is operating in its D-04 fallback-constant configuration (no schema-declared `quantScale`/`byteQuantMode` resolvable for this mock's `ProcessingCore`).

    In `CMakeLists.txt`: add `secv02_counter_test.cpp` to the existing `addtest(processing_conformance_security_test ...)` sources list, and add `processing_service` to its `target_link_libraries` (this transitively brings `SubTaskQueueAccessorImpl`/`ProcessingSubTaskQueueManager`/`SGProcessingProto`, and re-confirms `ProcessingBase`, already present via `processing_service`'s own PUBLIC link).
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target processing_conformance_security_test --config Debug && ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv02CounterTest -V</automated>
  </verify>
  <acceptance_criteria>
    - `secv02_counter_test.cpp` contains `"width": 64` and `"block_len": 64` with no `"chunk_stride"` key in both job JSON definitions (single, non-overlapping chunk).
    - `secv02_counter_test.cpp` calls `ProcessTaskSplitter().SplitTask(` with its 5th argument `true` (addvalidationsubtask) -- `grep -c "SplitTask(" SuperGenius/test/src/processing_conformance_security/secv02_counter_test.cpp` returns at least 1, and the call includes `true` for the validation-subtask flag.
    - `secv02_counter_test.cpp` never calls `ValidateResults` directly (`grep -c "ValidateResults(" SuperGenius/test/src/processing_conformance_security/secv02_counter_test.cpp` returns 0) -- confirms the real `SubTaskQueueAccessorImpl`/`GrabSubTask` public-API path is exercised, per D-05.
    - `CMakeLists.txt` in `processing_conformance_security` contains both `secv02_counter_test.cpp` and `processing_service` in the relevant target's sources/link list.
    - `ctest --test-dir SuperGenius/build/Windows/Debug -C Debug -R Secv02CounterTest -V` passes.
  </acceptance_criteria>
  <done>SECV-02 exists as a real, end-to-end pipeline test (two genuine MNN runs, real subtask-sharing via ProcessTaskSplitter, real SubTaskQueueAccessorImpl public API, real file-backed fetch) proving the fixed ValidateResults plus tolerance fallback still catch a deliberately corrupted result -- the phase's final, mandatory security proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full pipeline: corrupted MNN model -> real subtask result -> SubTaskQueueAccessorImpl -> ValidateResults | The complete, end-to-end trust boundary this whole phase exists to defend -- this plan is the test that proves it holds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-11 | Tampering | The combined XNODE-01b+XNODE-02 mechanism, exercised end-to-end against a real corrupted model | critical | mitigate | This test IS the mitigation -- a failing `Secv02CounterTest` would mean the phase's central security claim (SC5) is false. Passing proves the concatenation-bug fix plus tolerance fallback, acting together against real MNN output data, still flag a genuine defect. |

This test introduces no new production code path and no package-manager installs -- it is a test-only artifact exercising already-mitigated production logic (Plans 15-01/15-02/15-03).
</threat_model>

<verification>
1. `secv02_counter_test.cpp` runs two real `ProcessingManager::Process()` jobs (correct vs. deliberately corrupted model), reusing `ProcessTaskSplitter::SplitTask`'s production two-subtask/one-chunk construction.
2. The two subtask results reach `ValidateResults` exclusively via `SubTaskQueueAccessorImpl`'s real public API (`AssignSubTasks`/`GrabSubTask`), never a direct `ValidateResults` call.
3. `ctest -R Secv02CounterTest` passes, proving the corrupted result is still caught by the combined fixed-comparison + tolerance-fallback mechanism.
</verification>

<success_criteria>
SECV-02 is proven: a deliberately corrupted subtask result, run through a real pipeline and validated via `SubTaskQueueAccessorImpl`'s real public API, is still flagged invalid by the phase's fixed `ValidateResults` -- closing the phase's three requirements (XNODE-01b, XNODE-02, SECV-02) with a concrete, passing, end-to-end test.
</success_criteria>

<output>
Create `.planning/workstreams/sgproc-render/phases/15-validation-comparison-mechanism/15-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- New file `SuperGenius/test/src/processing_conformance_security/secv02_counter_test.cpp` with `TEST_F(Secv02CounterTest, ...)` (or equivalent) full-pipeline counter-test.
- `processing_conformance_security_test`'s CMakeLists.txt gains `secv02_counter_test.cpp` as a source and `processing_service` as a link dependency.
