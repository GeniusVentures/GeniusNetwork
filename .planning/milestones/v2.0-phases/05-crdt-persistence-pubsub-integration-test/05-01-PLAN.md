---
phase: 05-crdt-persistence-pubsub-integration-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SuperGenius/src/account/TransactionManager.cpp
  - SuperGenius/src/account/TransactionManager.hpp
  - SuperGenius/src/account/GeniusNode.cpp
  - SuperGenius/src/account/GeniusNode.hpp
autonomous: true
requirements: [RIMPL-04, RIMPL-07]

must_haves:
  truths:
    - "FilterRegistration rejects a RegistrationTx whose sequence is not strictly higher than the stored reg/ record (gate d)"
    - "FilterRegistration rejects a RegistrationTx with sequence == 0 (gate d well-formed check per D-46)"
    - "RegisterChild called with 2 arguments auto-derives sequence from stored reg/ record (1 if none exists)"
    - "RegisterChild called with 3 arguments preserves the caller-supplied sequence (for tests and replay)"
    - "The SendTransactionItem reg/ path diversion has a null check after dynamic_pointer_cast (Phase 4 advisory fix 1)"
    - "RegistrationTx serialization failure in SendTransactionItem is explicitly checked, not silently swallowed (Phase 4 advisory fix 2)"
  artifacts:
    - path: "SuperGenius/src/account/TransactionManager.cpp"
      provides: "Gate (d) sequence monotonicity inside FilterRegistration; RegisterChild 2-arg auto-derive overload; advisory fixes at SendTransactionItem reg/ diversion"
      min_lines: 15
      contains: "GetBlockChainBase() + \"reg/\""
    - path: "SuperGenius/src/account/TransactionManager.hpp"
      provides: "RegisterChild 2-arg declaration with doxygen"
      contains: "RegisterChild( std::string main_address, SGTransaction::RegistrationMetadata metadata )"
    - path: "SuperGenius/src/account/GeniusNode.cpp"
      provides: "RegisterChild 2-arg wrapper"
      contains: "GeniusNode::RegisterChild( const std::string &main_address, SGTransaction::RegistrationMetadata metadata )"
    - path: "SuperGenius/src/account/GeniusNode.hpp"
      provides: "RegisterChild 2-arg wrapper declaration"
      contains: "RegisterChild( const std::string &main_address, SGTransaction::RegistrationMetadata metadata )"
  key_links:
    - from: "FilterRegistration gate (d)"
      to: "globaldb_m->Get(reg_key)"
      via: "CRDT value read during filter execution"
      pattern: "globaldb_m->Get\\(.*reg/"
    - from: "RegisterChild 2-arg overload"
      to: "RegisterChild 3-arg"
      via: "delegation after auto-derive"
      pattern: "return RegisterChild\\(.*main_address.*metadata.*sequence\\)"

---

<objective>
Complete the FilterRegistration element filter with gate (d) sequence monotonicity + sequence > 0 well-formed check, add a RegisterChild auto-derive overload that reads stored sequence from the reg/ CRDT, and fix two Phase 4 advisory warnings touching the SendTransactionItem reg/ diversion path.

Purpose: Gate (d) is the final filter gate — without it, a stale or replayed registration can overwrite a newer one in the reg/ CRDT namespace. The auto-derive overload removes the caller's burden of tracking sequences. The advisory fixes harden the SendTransactionItem path against null-pointer dereference and silent serialization failure.

Output: FilterRegistration enforcing all four gates (a–d) per D-46; RegisterChild available in both 3-arg (caller-supplied) and 2-arg (auto-derive) forms per D-47; SendTransactionItem reg/ diversion path with proper null-check and error propagation.
</objective>

<execution_context>
@C:/Users/Fuu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Fuu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-crdt-persistence-pubsub-integration-test/05-CONTEXT.md
@.planning/phases/05-crdt-persistence-pubsub-integration-test/05-PATTERNS.md

# Source files being modified — read these first
@SuperGenius/src/account/TransactionManager.cpp
@SuperGenius/src/account/TransactionManager.hpp
@SuperGenius/src/account/GeniusNode.cpp
@SuperGenius/src/account/GeniusNode.hpp

# Reference — FilterTransaction KeyExistsInDB lookup pattern (analog for gate d)
@SuperGenius/src/account/TransactionManager.cpp (line 2729, KeyExistsInDB; lines 2770–2820, FilterRegistration gates a–c; lines 4862–4871, KeyExistsInDB implementation)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate (d) — sequence monotonicity + sequence > 0 check in FilterRegistration</name>
  <files>SuperGenius/src/account/TransactionManager.cpp</files>
  <read_first>
    - SuperGenius/src/account/TransactionManager.cpp — lines 2770–2820 (FilterRegistration gates a–c + Phase 5 comment marker at line 2809), lines 4862–4871 (KeyExistsInDB implementation — the analog for CRDT Get during filter execution), lines 1421–1430 (DeSerializeTransaction string overload)
    - SuperGenius/src/account/RegistrationTransaction.hpp — GetSrcAddress(), GetSequence(), GetMainAddress()
    - .planning/phases/05-crdt-persistence-pubsub-integration-test/05-PATTERNS.md — Pattern 1: Gate (d) pattern (lines 86–119)
    - .planning/phases/05-crdt-persistence-pubsub-integration-test/05-CONTEXT.md — D-46 (gate d specification)
  </read_first>
  <behavior>
    - Test 1: A RegistrationTx with sequence=5 arriving when reg/ has sequence=4 is accepted (filter returns nullopt, not tombstone)
    - Test 2: A RegistrationTx with sequence=3 arriving when reg/ has sequence=4 is rejected (filter returns empty tombstone vector)
    - Test 3: A RegistrationTx with sequence=0 is rejected (filter returns empty tombstone vector)
    - Test 4: A RegistrationTx with no prior reg/ record (first registration) and sequence > 0 is accepted
    - Test 5: A RegistrationTx with sequence > 0 but deserialization of stored reg/ record fails is still accepted (graceful — gate d skips sequence check when existing record is unreadable)
  </behavior>
  <action>
    Insert gate (d) at the Phase 5 comment marker in TransactionManager.cpp line 2809 (`// Phase 5 adds: (d) sequence monotonicity check`).

    D-46 specification: gate (d) reads the stored record via a direct CRDT Get inside FilterRegistration — fetch the value at `GetBlockChainBase() + "reg/" + child_addr`, deserialize the stored RegistrationTx, and reject unless the incoming sequence is strictly higher (and sequence > 0). No in-memory cache.

    Implementation steps following the 05-PATTERNS.md Pattern 1:
    1. Add a `sequence == 0` check before the CRDT read — early reject with error log "Zero sequence in registration {key}"
    2. Build the reg/ key: `std::string reg_key = GetBlockChainBase() + "reg/" + reg_tx->GetSrcAddress();`
    3. Call `globaldb_m->Get(reg_key)` — the string implicitly converts to HierarchicalKey per hierarchical_key.hpp:32
    4. If `existing_data.has_value()`, call `DeSerializeTransaction(existing_data.value())` (string overload at line 1421)
    5. If deserialization succeeds AND the tx type is "registration", dynamic_pointer_cast to RegistrationTransaction
    6. If the existing_reg exists AND `reg_tx->GetSequence() <= existing_reg->GetSequence()`, emit error log "Non-monotonic sequence in registration {key}: incoming={seq}, stored={stored_seq}" and break

    Use `m_logger->error(...)` for rejections — match the existing FilterRegistration logging style (lines 2798, 2805). Follow the FilterTransaction's KeyExistsInDB pattern (`globaldb_m->Get(key).has_value()`) for the CRDT read.

    The gate (d) block replaces the `// Phase 5 adds: (d) sequence monotonicity check` comment (line 2809). The break statements inside gate (d) feed into the existing `should_delete = false` (line 2811) bypass — rejected registrations get empty tombstone vector per lines 2814–2818.

    Update the Phase 5 comment in FilterRegistration (TransactionManager.hpp line 606) from a placeholder to a short description:
    "@note Phase 5 adds: (d) sequence monotonicity check — reads existing reg/{child_addr} via CRDT Get, rejects non-monotonic or zero sequences."

    No changes to the filter registration pattern in TransactionManager::New() — the reg/ filter lambda (around lines 222–233) already delegates to FilterRegistration and needs no modification.
  </action>
  <verify>
    <automated>Run the existing Phase 4 FilterRegistration tests via: ctest -R registration_transaction_test --output-on-failure (in the SuperGenius test build directory)</automated>
  </verify>
  <done>
    All five behavior test scenarios pass via the existing FilterRegistration test suite. An incoming RegistrationTx with sequence 0 or <= stored sequence produces a tombstone; a strictly higher sequence is accepted. Existing gates a–c continue to work unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RegisterChild two-argument auto-derive overload (TM + GeniusNode + headers)</name>
  <files>
    SuperGenius/src/account/TransactionManager.cpp,
    SuperGenius/src/account/TransactionManager.hpp,
    SuperGenius/src/account/GeniusNode.cpp,
    SuperGenius/src/account/GeniusNode.hpp
  </files>
  <read_first>
    - SuperGenius/src/account/TransactionManager.cpp — lines 578–592 (existing 3-arg RegisterChild), lines 575–576 (transfer hash return pattern)
    - SuperGenius/src/account/TransactionManager.hpp — lines 125–134 (existing RegisterChild declaration with doxygen)
    - SuperGenius/src/account/GeniusNode.cpp — lines 2285–2298 (existing RegisterChild wrapper), lines 2258–2283 (TransferFunds wrapper for two-layer pattern reference)
    - SuperGenius/src/account/GeniusNode.hpp — lines 513–522 (existing RegisterChild declaration)
    - SuperGenius/src/account/RegistrationTransaction.hpp — GetSrcAddress() for building reg/ key
    - .planning/phases/05-crdt-persistence-pubsub-integration-test/05-PATTERNS.md — Pattern 2 (auto-derive pattern lines 147–184), Pattern 5 (GeniusNode wrapper pattern lines 344–357), Patterns 4+6 (header declarations)
    - .planning/phases/05-crdt-persistence-pubsub-integration-test/05-CONTEXT.md — D-47 (auto-derive spec: optional overload per planner discretion)
  </read_first>
  <behavior>
    - Test 1: RegisterChild(main_addr, metadata) with no prior registration creates a tx with sequence=1
    - Test 2: RegisterChild(main_addr, metadata) after a prior sequence=1 registration creates a tx with sequence=2
    - Test 3: RegisterChild(main_addr, metadata, 5) preserves caller-supplied sequence=5
    - Test 4: RegisterChild(main_addr, metadata) when reg/ record exists but deserialization fails still defaults to sequence=1 (graceful fallback)
  </behavior>
  <action>
    Per D-47, implement auto-derive as a 2-argument overload that delegates to the 3-argument version. The overload reads `GetBlockChainBase() + "reg/" + account_m->GetAddress()`, calls `globaldb_m->Get(reg_key)`, deserializes the stored RegistrationTx, and uses stored_sequence + 1 (or 1 if no prior record).

    Decision (planner discretion, per D-47): use a **2-arg overload** (not std::optional parameter) — matches the existing codebase convention of function overloading (TransferFunds has multiple overloads).

    **TransactionManager.cpp — after the existing 3-arg RegisterChild (after line 592):**

    New method: `TransactionManager::RegisterChild(std::string main_address, SGTransaction::RegistrationMetadata metadata)`
    - Guard: `if (GetState() != State::READY) return outcome::failure(...);`
    - Initialize `uint64_t sequence = 1;`
    - Build `std::string reg_key = GetBlockChainBase() + "reg/" + account_m->GetAddress();`
    - Call `auto existing_data = globaldb_m->Get(reg_key);`
    - If `existing_data.has_value()`: deserialize via `DeSerializeTransaction(existing_data.value())`, check type == "registration", dynamic_pointer_cast to RegistrationTransaction, read `existing_reg->GetSequence() + 1`
    - Delegate: `return RegisterChild(std::move(main_address), std::move(metadata), sequence);`

    Follow the 05-PATTERNS.md Pattern 2 exactly (lines 147–184).

    **TransactionManager.hpp — after the existing 3-arg RegisterChild declaration (after line 134):**

    New doxygen declaration per 05-PATTERNS.md Pattern 4 (lines 296–309):
    ```
    /**
     * @brief Creates and enqueues a child-wallet registration transaction with auto-derived sequence.
     *
     * Reads the existing reg/{child_addr} CRDT record and uses stored sequence + 1
     * (or 1 if no prior registration exists).
     *
     * @param[in] main_address Main wallet public address (128-hex).
     * @param[in] metadata      Optional registration metadata (game_id, publisher_id, dev_wallet, peers_cut).
     * @return Transaction hash on success.
     */
    outcome::result<std::string> RegisterChild( std::string main_address, SGTransaction::RegistrationMetadata metadata );
    ```

    **GeniusNode.cpp — after the existing 3-arg RegisterChild wrapper (after line 2298):**

    New wrapper per 05-PATTERNS.md Pattern 5 (lines 344–357):
    - Guard: `GetTransactionManagerState() != TransactionManager::State::READY`
    - `BOOST_OUTCOME_TRY(auto manager, GetTransactionManager());`
    - `BOOST_OUTCOME_TRY(auto tx_id, manager->RegisterChild(main_address, metadata));`
    - Debug log and return tx_id

    **GeniusNode.hpp — after the existing RegisterChild declaration (after line 522):**

    New doxygen declaration per 05-PATTERNS.md Pattern 6 (lines 396–408):
    ```
    /**
     * @brief Registers this node as a child of main_address with auto-derived sequence.
     * ...
     */
    outcome::result<std::string> RegisterChild( const std::string &main_address, SGTransaction::RegistrationMetadata metadata );
    ```
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test cases in registration_transaction_test.cpp for the auto-derive overload. Test name: RegisterChildAutoDeriveSequence. After test creation: ctest -R registration_transaction_test --output-on-failure</automated>
  </verify>
  <done>
    Both the 2-arg and 3-arg RegisterChild overloads compile and link at the TransactionManager and GeniusNode layers. The 2-arg overload auto-derives sequence=1 for first registration, sequence=N+1 when a prior registration with sequence=N exists in the reg/ CRDT. The 3-arg overload preserves exact caller-supplied sequence. Both return the transaction hash string.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix Phase 4 advisory warnings — null-check dynamic_pointer_cast and SerializeByteVector error handling in SendTransactionItem</name>
  <files>SuperGenius/src/account/TransactionManager.cpp</files>
  <read_first>
    - SuperGenius/src/account/TransactionManager.cpp — lines 1214–1230 (SendTransactionItem reg/ path diversion with unchecked dynamic_pointer_cast at line 1217 and unchecked SerializeByteVector at line 1229)
    - SuperGenius/src/account/TransactionManager.cpp — lines 2789–2793 (FilterRegistration's correct dynamic_pointer_cast null-check pattern to replicate)
    - .planning/phases/05-crdt-persistence-pubsub-integration-test/05-PATTERNS.md — lines 815–820 (opportunistic fixes description)
    - .planning/phases/05-crdt-persistence-pubsub-integration-test/05-CONTEXT.md — line 102 (Phase 4 advisory warnings)
  </read_first>
  <behavior>
    - Test 1: When dynamic_pointer_cast returns nullptr (unexpected tx type in registration branch), the path returns an error instead of dereferencing null
    - Test 2: When SerializeByteVector returns empty bytes, the path returns an error instead of silently committing empty data to CRDT
  </behavior>
  <action>
    Two targeted fixes in the SendTransactionItem reg/ diversion block at lines 1214–1230 of TransactionManager.cpp.

    **Fix 1 — Null-check after dynamic_pointer_cast (line 1217):**

    Current code at lines 1215–1219:
    ```
    if ( transaction->GetType() == "registration" )
    {
        auto reg_tx = std::dynamic_pointer_cast<RegistrationTransaction>( transaction );
        transaction_path = GetBlockChainBase() + "reg/" + reg_tx->GetSrcAddress();
    }
    ```

    Add a null check after the cast, matching the FilterRegistration pattern at lines 2789–2793:
    ```
    if ( transaction->GetType() == "registration" )
    {
        auto reg_tx = std::dynamic_pointer_cast<RegistrationTransaction>( transaction );
        if ( !reg_tx )
        {
            m_logger->error( "SendTransactionItem: dynamic_pointer_cast<RegistrationTransaction> returned null" );
            return outcome::failure( boost::system::errc::make_error_code( boost::system::errc::invalid_argument ) );
        }
        transaction_path = GetBlockChainBase() + "reg/" + reg_tx->GetSrcAddress();
    }
    ```

    **Fix 2 — SerializeByteVector error check (line 1229):**

    Current code at line 1229:
    ```
    data_transaction.put( transaction->SerializeByteVector() );
    ```

    Wrap with an error check: call `SerializeByteVector()` into a local vector first, verify it is non-empty, and only then call `.put()`. If the vector is empty, log an error and return failure:
    ```
    auto serializedBytes = transaction->SerializeByteVector();
    if ( serializedBytes.empty() )
    {
        m_logger->error( "SendTransactionItem: SerializeByteVector returned empty for transaction {}", transaction->GetHash() );
        return outcome::failure( boost::system::errc::make_error_code( boost::system::errc::invalid_argument ) );
    }
    data_transaction.put( serializedBytes );
    ```

    Both fixes follow the existing error-return pattern used at lines 1210–1212 (early `return outcome::failure` with `invalid_argument` error code).
  </action>
  <verify>
    <automated>Build the genius_node_test target and run: ctest -R registration_transaction_test --output-on-failure. The existing ChildRegistrationEndToEnd test exercises the SendTransactionItem path and must continue to pass.</automated>
  </verify>
  <done>
    The SendTransactionItem reg/ path diversion at lines 1215–1230 has a null check after dynamic_pointer_cast (matching FilterRegistration pattern) and an explicit non-empty check on SerializeByteVector output. Existing Phase 4 E2E test still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRDT merge path → FilterRegistration | Untrusted reg/ deltas from peer nodes enter the element filter callback |
| SendTransactionItem → CRDT write | Locally signed RegistrationTx bytes pass through serialization before CRDT commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | reg/ CRDT element | mitigate | FilterRegistration gate (b) validates child signature; gate (d) prevents replay of stale registrations via sequence monotonicity |
| T-05-02 | Tampering | CRDT element value | mitigate | Gate (a) deserialization rejects malformed protobuf bytes; gate (b) signature verification catches tampered content |
| T-05-03 | Repudiation | RegistrationTx submission | accept | Child-only signature provides non-repudiation; main node does not sign (by design per D-04/D-05) |
| T-05-04 | Information Disclosure | reg/ CRDT storage | accept | reg/ values contain only public addresses and metadata; no secrets stored in registration records |
| T-05-05 | Denial of Service | FilterRegistration gate (d) CRDT read | mitigate | Gate (d) performs a single Get per filter invocation — established precedent via FilterTransaction's KeyExistsInDB; rejects non-monotonic/zero sequences to prevent reg/ key pollution |
| T-05-06 | Elevation of Privilege | Registration claiming arbitrary main_address | mitigate | Gate (c) validates main_address format (128-hex); authority gate (CheckParentChildAuthority) deferred to later milestone — accepted risk for v2.0 per ROADMAP |
</threat_model>

<verification>
Run the existing Phase 4 FilterRegistration E2E test suite: `ctest -R registration_transaction_test --output-on-failure` in the SuperGenius test build directory. All tests from Phase 4 (ChildRegistrationEndToEnd, FilterRegistrationRejectsTamperedSignature, FilterRegistrationRejectsBadMainAddress) must pass unchanged, proving gates a–c still work after gate (d) insertion.

Write Phase 5-specific FilterRegistration gate (d) tests — zero sequence rejection and non-monotonic sequence rejection — using the existing RegistrationE2ETestAccess friend accessor pattern (lines 164–181 of registration_transaction_test.cpp). Gate (d) tests inject CRDT elements at various sequence values and assert FilterRegistration returns tombstone or nullopt as expected.
</verification>

<success_criteria>
1. `FilterRegistration` in TransactionManager.cpp contains gate (d) at line ~2809 with: `sequence == 0` → tombstone, non-monotonic `sequence <= stored_sequence` → tombstone, strictly higher → accept
2. `RegisterChild(main_address, metadata)` compiles at both TransactionManager and GeniusNode layers, auto-derives sequence from reg/ CRDT
3. `SendTransactionItem` reg/ path has `if (!reg_tx)` null check and `if (serializedBytes.empty())` error check
4. All Phase 4 `registration_transaction_test` cases pass without modification
5. Gate (d) behavior test cases pass (zero sequence reject, replay reject, first-registration accept)
</success_criteria>

<output>
Create `.planning/phases/05-crdt-persistence-pubsub-integration-test/05-01-SUMMARY.md` when done
</output>
