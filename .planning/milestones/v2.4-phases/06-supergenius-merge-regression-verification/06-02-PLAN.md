---
phase: 06-supergenius-merge-regression-verification
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - SuperGenius/test/src/account/network_config_precedence_test.cpp
  - SuperGenius/test/src/account/node_type_derivation_test.cpp
  - SuperGenius/test/src/blockchain/blockchain_genesis_test.cpp
  - SuperGenius/test/src/blockchain/consensus_subject_test.cpp
  - SuperGenius/test/src/bridge_e2e/bridge_e2e_test.cpp
  - SuperGenius/test/src/multiaccount/multi_account_sync.cpp
  - SuperGenius/test/src/multiaccount/regtest/child_registration.cpp
  - SuperGenius/test/src/processing_multi/processing_multi_test.cpp
  - SuperGenius/test/src/processing_nodes/child_tokens_test.cpp
  - SuperGenius/test/src/processing_nodes/full_node_test.cpp
  - SuperGenius/test/src/processing_nodes/processing_nodes_test.cpp
  - SuperGenius/test/src/transaction_sync/migration_sync_test.cpp
  - SuperGenius/test/src/transaction_sync/transaction_crash_test.cpp
  - SuperGenius/test/src/transaction_sync/transaction_sync_test.cpp
  - SuperGenius/example/node_test/NodeExample.cpp
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "Zero DevConfig_st references remain anywhere in src/, test/, or example/ — the type is spelled GeniusNodeConfig everywhere, fields unchanged"
  artifacts:
    - "15 rename-swept files (14 test fixtures + 1 example) using GeniusNodeConfig in place of DevConfig_st"
    - "SuperGenius/src/account/GeniusNode.hpp and GeniusNode.cpp confirmed already using GeniusNodeConfig (landed automatically via Plan 01's merge, re-verified here as part of the same zero-hit grep)"
  key_links:
    - "GeniusNode.hpp/.cpp's auto-merged rename (the source of truth for the new type name) must match exactly what the 15 swept files now reference, or the sweep introduces a new mismatch instead of resolving the old one"
</must_haves>
---

<objective>
Perform the mechanical `DevConfig_st` -> `GeniusNodeConfig` global rename sweep across the 15 files that reference the old struct name but were never touched by `origin/develop`'s diff (so git's merge in Plan 01 could not update them automatically). This is a pure type-name rename — struct fields are unchanged — executed strictly after Plan 01's conflict resolution is complete and staged (never before, and never mid-conflict-marker).

Purpose: completes MERGE-01's "adapting all DevConfig_st references (~17 files) to the renamed GeniusNodeConfig typedef" requirement; an incomplete sweep would leave these 15 files failing to compile.
Output: 15 files edited (word-boundary rename of the bare identifier `DevConfig_st` only, `DEV_CONFIG` variable untouched); zero `DevConfig_st` hits repo-wide.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-supergenius-merge-regression-verification/06-RESEARCH.md
@.planning/phases/06-supergenius-merge-regression-verification/06-PATTERNS.md
@.planning/phases/06-supergenius-merge-regression-verification/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DevConfig_st -> GeniusNodeConfig across the 15 enumerated files</name>
  <files>
    SuperGenius/test/src/account/network_config_precedence_test.cpp, SuperGenius/test/src/account/node_type_derivation_test.cpp, SuperGenius/test/src/blockchain/blockchain_genesis_test.cpp, SuperGenius/test/src/blockchain/consensus_subject_test.cpp, SuperGenius/test/src/bridge_e2e/bridge_e2e_test.cpp, SuperGenius/test/src/multiaccount/multi_account_sync.cpp, SuperGenius/test/src/multiaccount/regtest/child_registration.cpp, SuperGenius/test/src/processing_multi/processing_multi_test.cpp, SuperGenius/test/src/processing_nodes/child_tokens_test.cpp, SuperGenius/test/src/processing_nodes/full_node_test.cpp, SuperGenius/test/src/processing_nodes/processing_nodes_test.cpp, SuperGenius/test/src/transaction_sync/migration_sync_test.cpp, SuperGenius/test/src/transaction_sync/transaction_crash_test.cpp, SuperGenius/test/src/transaction_sync/transaction_sync_test.cpp, SuperGenius/example/node_test/NodeExample.cpp
  </files>
  <read_first>
    06-RESEARCH.md section "The DevConfig_st -> GeniusNodeConfig Rename" (full enumeration table, 17 total matches, 2 of which already landed via the merge). 06-PATTERNS.md section "`DevConfig_st` -> `GeniusNodeConfig` mechanical rename sweep (15 files)" for the 3 representative excerpts (`network_config_precedence_test.cpp:20`, `child_registration.cpp:170`, `NodeExample.cpp:481`) showing the exact before/after pattern, including the explicit warning to NOT touch the `DEV_CONFIG` variable name in `NodeExample.cpp`.
  </read_first>
  <action>
    For each of the 15 files listed above, replace every bare-word occurrence of the type token `DevConfig_st` with `GeniusNodeConfig`. Use a word-boundary match so the unrelated global variable identifier `DEV_CONFIG` (spelled differently, all-caps with underscore) is never touched — only the exact token `DevConfig_st` changes. This affects local variable declarations (e.g. `DevConfig_st MakeDevConfig(...)` -> `GeniusNodeConfig MakeDevConfig(...)`), inline stack variables (e.g. `DevConfig_st devConfig = {...}` -> `GeniusNodeConfig devConfig = {...}`), and one global (`DevConfig_st DEV_CONFIG{...}` -> `GeniusNodeConfig DEV_CONFIG{...}` — only the leading type token changes, the variable name `DEV_CONFIG` stays exactly as-is). Field usage (`.BaseWritePath`, constructor argument order, brace-init contents) is unaffected since only the type name changes, not the struct layout — do not alter any field access or initializer values. `SuperGenius/test/src/multiaccount/regtest/child_registration.cpp` is also part of the MVER-03 regression-gate file set (Plan 04 will run it) — sweep it with the same mechanical care as the other 14.
  </action>
  <!-- planner-discipline-allow: DevConfig_st -->
  <!-- planner-discipline-allow: GeniusNodeConfig -->
  <!-- planner-discipline-allow: DEV_CONFIG -->
  <verify>
    <automated>grep -c 'DevConfig_st' SuperGenius/test/src/account/network_config_precedence_test.cpp</automated>
  </verify>
  <acceptance_criteria>
    For each of the 15 files, run two independent commands: `grep -c 'DevConfig_st' &lt;file&gt;` (must return 0) and `grep -c 'GeniusNodeConfig' &lt;file&gt;` (must return 1 or more, confirming the replacement landed rather than a bare deletion). Separately, `grep -c 'DEV_CONFIG' SuperGenius/example/node_test/NodeExample.cpp` returns 1 or more (confirms the `DEV_CONFIG` variable identifier itself is untouched).
  </acceptance_criteria>
  <done>All 15 files use GeniusNodeConfig with zero remaining DevConfig_st references; DEV_CONFIG variable name preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Repo-wide post-sweep verification and staging</name>
  <files>SuperGenius/ (verification + staging, no further source edits)</files>
  <read_first>
    06-PATTERNS.md "Post-sweep verification command" and "Common Pitfalls" Pitfall 3 (do not confuse "0 matches because already renamed by the merge" in GeniusNode.hpp/.cpp with an actual completed sweep step for those 2 files — they need no edit, just confirmation).
  </read_first>
  <action>
    Run the repo-wide verification: `grep -rln "DevConfig_st" --include="*.hpp" --include="*.cpp" --include="*.h" --include="*.cc" SuperGenius/src SuperGenius/test SuperGenius/example`. This must return zero file paths — covering both the 15 just-swept files AND `SuperGenius/src/account/GeniusNode.hpp`/`.cpp` (which needed no edit here since Plan 01's merge already renamed them). If any path is still returned, fix it before proceeding. Then stage the 15 modified files explicitly by path: `git add test/src/account/network_config_precedence_test.cpp test/src/account/node_type_derivation_test.cpp test/src/blockchain/blockchain_genesis_test.cpp test/src/blockchain/consensus_subject_test.cpp test/src/bridge_e2e/bridge_e2e_test.cpp test/src/multiaccount/multi_account_sync.cpp test/src/multiaccount/regtest/child_registration.cpp test/src/processing_multi/processing_multi_test.cpp test/src/processing_nodes/child_tokens_test.cpp test/src/processing_nodes/full_node_test.cpp test/src/processing_nodes/processing_nodes_test.cpp test/src/transaction_sync/migration_sync_test.cpp test/src/transaction_sync/transaction_crash_test.cpp test/src/transaction_sync/transaction_sync_test.cpp example/node_test/NodeExample.cpp` (run from `SuperGenius/`). Confirm `docs` still untouched/unstaged via `git status`.
  </action>
  <verify>
    <automated>grep -rln "DevConfig_st" --include="*.hpp" --include="*.cpp" --include="*.h" --include="*.cc" SuperGenius/src SuperGenius/test SuperGenius/example</automated>
  </verify>
  <acceptance_criteria>
    `grep -rln "DevConfig_st" --include="*.hpp" --include="*.cpp" --include="*.h" --include="*.cc" SuperGenius/src SuperGenius/test SuperGenius/example` returns zero lines (empty output). `git status` (inside `SuperGenius/`) shows all 15 files under "Changes to be committed", zero unmerged paths, `docs` still listed separately as unstaged.
  </acceptance_criteria>
  <done>Zero DevConfig_st references anywhere in the repo; 15 sweep files staged; merge still open (uncommitted), ready for build verification in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mechanical rename script/edit -> compiled test binaries | An incomplete or over-broad rename could either leave a stale type name that still compiles against a shim (masking a break) or accidentally rename an unrelated identifier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | 15 rename-swept files + GeniusNode.hpp/.cpp | high | mitigate | Task 2's acceptance criteria hard-gates on a repo-wide `grep -rln "DevConfig_st" ... src/ test/ example/` returning zero file paths — not merely "the 15 listed files compile." If any file outside the enumerated 15 (or a future file) still references the old name, the grep catches it before the build step in Plan 03 rather than allowing a stale name to silently mask a real integration break |
| T-06-04 | Tampering | example/node_test/NodeExample.cpp (DEV_CONFIG variable adjacency) | low | mitigate | Task 1 explicitly requires word-boundary matching so the global variable `DEV_CONFIG` is never renamed alongside the type `DevConfig_st`; Task 1's acceptance criteria explicitly re-confirms `DEV_CONFIG` (the variable) is still present after the sweep |
</threat_model>

<verification>
Repo-wide `grep -rln "DevConfig_st"` across `src/`, `test/`, `example/` returns zero results. All 15 files staged. Merge remains open (not yet committed).
</verification>

<success_criteria>
Every reference to the retired `DevConfig_st` type name is gone from the tree; `GeniusNodeConfig` is used consistently. The merge is still mid-flight (uncommitted) — Plan 03 builds against this state next.
</success_criteria>

<output>
Create `.planning/phases/06-supergenius-merge-regression-verification/06-02-SUMMARY.md` when done
</output>
