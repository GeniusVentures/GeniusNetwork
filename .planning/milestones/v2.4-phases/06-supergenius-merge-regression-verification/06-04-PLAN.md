---
phase: 06-supergenius-merge-regression-verification
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified: []
autonomous: true
requirements: [MVER-03, MVER-04]

must_haves:
  truths:
    - "The full pre-existing child-wallet test suite (registration_transaction_test 37 cases + child_registration_test 4 cases, covering v2.0 registration, v2.1 balance query, v2.2 GeniusSDK wrapper interfaces, v2.3 CONS-01/CONS-02 transfer authority and transfer wrappers, and Phase 5 lifecycle Detach/Revoke/ReplaceMain) passes with zero regressions"
    - "CheckParentChildAuthority, FilterRegistration, CheckCertifiedParent, and the transaction_parsers registration/revoke dispatch entries are confirmed unmodified by develop's diff, and the adjacency-risk EvaluateTransactionReplayProtection rewrite does not reject any legitimate lifecycle transaction"
  artifacts:
    - "GTest run output/logs confirming 37/37 registration_transaction_test cases and 4/4 child_registration_test cases pass"
    - "Diff confirmation that CheckParentChildAuthority and FilterRegistration function bodies are byte-identical between pre-merge (5fd137dc) and merged HEAD"
  key_links:
    - "ValidateTransactionForConsensus pipeline: CheckParentChildAuthority is called immediately before the rewritten EvaluateTransactionReplayProtection — this adjacency is the single highest-value verification target in the whole phase"
</must_haves>
---

<objective>
Run the full pre-existing child-wallet regression suite against the merged, built SuperGenius tree, and explicitly verify MVER-04's 4 named consensus-gate targets — not merely "the suite passed in aggregate," but targeted confirmation that `CheckParentChildAuthority`, `FilterRegistration`, `CheckCertifiedParent`, and the `transaction_parsers` registration/revoke dispatch entries still function as designed, with special attention to the adjacent `EvaluateTransactionReplayProtection` rewrite.

Purpose: MVER-03 (zero regressions across v2.0-v2.3/Phase 5) and MVER-04 (consensus gate compatibility) are both proven here, against the merged tree built in Plan 03.
Output: full GTest pass confirmation (37+4 cases), plus a targeted re-run of the 6 lifecycle-adjacent test cases and a direct byte-diff proof that the two unmodified gate functions are unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-supergenius-merge-regression-verification/06-RESEARCH.md
@.planning/phases/06-supergenius-merge-regression-verification/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full regression suite (MVER-03)</name>
  <files>None (test execution only)</files>
  <read_first>
    06-RESEARCH.md "Regression Test Inventory" section (both binaries, case counts, the pre-existing benign teardown-segfault caveat).
  </read_first>
  <action>
    Run `SuperGenius/build/Windows/Debug/test_bin/Debug/registration_transaction_test.exe` and capture full output. Run `SuperGenius/build/Windows/Debug/test_bin/Debug/child_registration_test.exe` and capture full output. For each binary, confirm the GTest summary line reports all cases passed (`[==========] 37 tests from ... ran.` / `[  PASSED  ] 37 tests.` for registration_transaction_test; `[  PASSED  ] 4 tests.` for child_registration_test) with zero `[  FAILED  ]` entries. Per the known pre-existing issue documented in STATE.md and 06-RESEARCH.md, a process exit code of 139 (segfault) occurring AFTER the GTest PASSED summary has already printed is a pre-existing teardown-lifecycle issue unrelated to this merge — treat it as a pass, not a regression. Any `[  FAILED  ]` line in the GTest summary itself, regardless of exit code, is a real regression and must be investigated and fixed before proceeding (trace back to the merge conflict resolution, rename sweep, or a genuine develop-side behavior change).
  </action>
  <verify>
    <automated>SuperGenius/build/Windows/Debug/test_bin/Debug/registration_transaction_test.exe</automated>
  </verify>
  <acceptance_criteria>
    `registration_transaction_test.exe`'s GTest summary shows `[  PASSED  ] 37 tests.` and zero `[  FAILED  ]` lines. `child_registration_test.exe`'s GTest summary shows `[  PASSED  ] 4 tests.` and zero `[  FAILED  ]` lines. (A trailing non-zero process exit code after either PASSED summary is acceptable per the documented pre-existing teardown issue; it does not count against this criterion.)
  </acceptance_criteria>
  <done>Both regression binaries report 100% pass (37/37 and 4/4) with zero GTest FAILED entries, confirming MVER-03.</done>
</task>

<task type="auto">
  <name>Task 2: Targeted MVER-04 consensus-gate verification</name>
  <files>None (verification only — grep, diff, targeted test re-run)</files>
  <read_first>
    06-RESEARCH.md "MVER-04: Consensus Gate Compatibility Analysis" section (exact symbol table and the EvaluateTransactionReplayProtection adjacency-risk paragraph naming the 6 lifecycle-adjacent test cases).
  </read_first>
  <action>
    Confirm each of the 4 MVER-04 targets is present and unmodified: run `grep -n 'CheckParentChildAuthority' SuperGenius/src/account/TransactionManager.cpp` (expect both the definition and its call site inside `ValidateTransactionForConsensus`), `grep -n 'FilterRegistration' SuperGenius/src/account/TransactionManager.cpp` (expect the definition and its CRDT-filter-callback registration), `grep -n 'CheckCertifiedParent' SuperGenius/src/blockchain/impl/Blockchain.cpp` (expect the definition), and `grep -n '"registration"\|"revoke"\|kRegistration\|kRevoke' SuperGenius/src/account/TransactionManager.cpp` (expect the `RegisterDeserializer` calls and switch-case dispatch arms all present). Then extract and diff the `CheckParentChildAuthority` and `FilterRegistration` function bodies between the pre-merge tree and the current merged working tree: run (inside `SuperGenius/`) `git show 5fd137dc:src/account/TransactionManager.cpp > pre-merge-tm.cpp.tmp` to snapshot the pre-merge file into a scratch file in the working directory, then extract each function's body from both `pre-merge-tm.cpp.tmp` and the current `src/account/TransactionManager.cpp` and diff them — confirm zero line differences (proving develop's diff never touched these two function bodies, matching 06-RESEARCH.md's "no hunk overlap" finding). Finally, re-run the regression binary with `--gtest_filter=RegistrationTransactionE2ETest.DetachChildEndToEnd:RegistrationTransactionE2ETest.ReplaceMainEndToEnd:RegistrationTransactionE2ETest.ReRegistrationAfterDetachViaReplaceMain:RegistrationTransactionE2ETest.LifecycleChangeReplayRejectedByNonceChain:RegistrationTransactionE2ETest.RevokeChildEndToEnd:RegistrationTransactionE2ETest.ReRegistrationAfterRevoke` and confirm all 6 pass individually (these are exactly the cases exercising the EvaluateTransactionReplayProtection adjacency risk — Task 1 already proved this as part of the full 37, this is an explicit named re-confirmation, not redundant busywork). Delete the scratch file `pre-merge-tm.cpp.tmp` once the diff is confirmed — it must never be staged or committed.
  </action>
  <verify>
    <automated>grep -n 'CheckParentChildAuthority' SuperGenius/src/account/TransactionManager.cpp</automated>
  </verify>
  <acceptance_criteria>
    All 4 grep checks return non-empty results at the expected locations (definition + call/registration site for each). The `CheckParentChildAuthority` and `FilterRegistration` function-body diff (pre-merge vs. merged) shows zero line differences. The targeted `--gtest_filter` run reports `[  PASSED  ] 6 tests.` with zero `[  FAILED  ]` lines for exactly: `DetachChildEndToEnd`, `ReplaceMainEndToEnd`, `ReRegistrationAfterDetachViaReplaceMain`, `LifecycleChangeReplayRejectedByNonceChain`, `RevokeChildEndToEnd`, `ReRegistrationAfterRevoke`.
  </acceptance_criteria>
  <done>All 4 MVER-04 symbols confirmed present and correctly wired; CheckParentChildAuthority/FilterRegistration proven byte-identical pre/post merge; all 6 lifecycle-adjacent tests individually confirmed passing, closing out the EvaluateTransactionReplayProtection adjacency risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Merged consensus pipeline (`ValidateTransactionForConsensus`) -> accepted/rejected transactions | The boundary where develop's rewritten replay-protection check could silently reject a legitimate child-wallet lifecycle transaction, or (worse) silently accept one it shouldn't |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | TransactionManager.cpp `ValidateTransactionForConsensus` / `EvaluateTransactionReplayProtection` | high | mitigate | Task 2 requires named, individual re-confirmation of the 6 lifecycle-adjacent test cases via explicit `--gtest_filter`, not just trusting the aggregate 37/37 pass from Task 1 — directly targets 06-RESEARCH.md's Assumption A2 (highest residual risk in the entire phase) |
| T-06-08 | Tampering | TransactionManager.cpp `CheckParentChildAuthority`/`FilterRegistration`, Blockchain.cpp `CheckCertifiedParent` | high | mitigate | Task 2's byte-identical function-body diff (pre-merge vs. merged) is a stronger acceptance criterion than "no conflict marker" — it directly proves develop's diff never altered these gates' semantics, rather than inferring it from hunk-range non-overlap alone |
| T-06-09 | Denial of Service | Regression test process teardown (registration_transaction_test.exe / child_registration_test.exe) | low | accept | Pre-existing, documented libp2p/boost::asio thread-join lifecycle issue (STATE.md Blockers/Concerns) causing a benign exit 139 after the GTest PASSED summary prints; explicitly not a new regression, accepted as a known issue rather than blocking this phase |
</threat_model>

<verification>
37/37 registration_transaction_test cases pass, 4/4 child_registration_test cases pass, zero GTest FAILED entries. CheckParentChildAuthority/FilterRegistration confirmed byte-identical pre/post merge. All 6 lifecycle-adjacent tests individually confirmed passing.
</verification>

<success_criteria>
MVER-03 and MVER-04 both proven: zero regressions across the full pre-existing child-wallet suite, and all 4 consensus-gate targets confirmed functioning as designed against the merged origin/develop changes. The merge remains uncommitted — Plan 05 finalizes it next.
</success_criteria>

<output>
Create `.planning/phases/06-supergenius-merge-regression-verification/06-04-SUMMARY.md` when done
</output>
