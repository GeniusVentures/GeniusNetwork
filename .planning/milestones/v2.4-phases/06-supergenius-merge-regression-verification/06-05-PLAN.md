---
phase: 06-supergenius-merge-regression-verification
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - SuperGenius (outer repo submodule pointer)
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "SuperGenius's dev_childwallet branch has a two-parent merge commit with origin/develop as a parent, pushed to origin/dev_childwallet"
    - "The outer GeniusNetwork repo's submodule pointer for SuperGenius is bumped to reference the new merge commit"
  artifacts:
    - "SuperGenius merge commit object (2 parents: prior dev_childwallet HEAD 5fd137dc and origin/develop 2981cd83)"
    - "Outer repo commit bumping the SuperGenius submodule pointer, following the established 'chore(0X-0Y): bump SuperGenius submodule...' message pattern"
  key_links:
    - "The merge commit hash produced inside SuperGenius must exactly match the hash recorded in the outer repo's updated submodule pointer"
</must_haves>
---

<objective>
Finalize the SuperGenius merge commit (all conflicts resolved, rename swept, build clean, full regression suite + MVER-04 gates verified in Plans 01-04), push it to `origin/dev_childwallet`, then bump the outer `GeniusNetwork` repo's submodule pointer to reference the new merge commit — following the exact pattern used for every prior SuperGenius submodule bump this milestone (`chore(0X-0Y): bump SuperGenius submodule...`).

Purpose: MERGE-01 explicitly requires the merge commit be pushed, not just created locally; the outer-repo submodule bump is required so downstream Phase 7 (GeniusSDK merge) and any other consumer of this repo see the merged SuperGenius state.
Output: two-parent merge commit on SuperGenius's `dev_childwallet`, pushed to origin; one new commit in the outer `GeniusNetwork` repo bumping the submodule pointer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-supergenius-merge-regression-verification/06-RESEARCH.md
@.planning/phases/06-supergenius-merge-regression-verification/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the merge commit and push to origin/dev_childwallet</name>
  <files>SuperGenius/ (git commit only, no source edits)</files>
  <read_first>
    06-RESEARCH.md "Refreshed Git State" (expected parent hashes: prior dev_childwallet HEAD `5fd137dcd13475114b070e070cda374850f8e699`, origin/develop `2981cd83abb08aba6fdb594103fddb89f8b5dd64`) and "Open Questions" item 1 (fold the DevConfig_st rename sweep into the single merge commit, per REQUIREMENTS.md's framing of it as part of ordinary conflict resolution).
  </read_first>
  <action>
    From `SuperGenius/`, confirm `git status` shows no remaining unmerged paths and all 9 shared files + 15 rename-swept files are staged (from Plans 01-02), and the `docs` submodule pointer is still separately unstaged (not part of this commit). Run `git commit` with a message describing the merge: summarize that this merges `origin/develop` (162 commits) into `dev_childwallet`, resolves the 2 real conflicts by retiring `ProcessingTransaction` (`TransactionManager.cpp` include block, `CMakeLists.txt` source list), and includes the mechanical `DevConfig_st` -> `GeniusNodeConfig` rename sweep across the 15 non-conflicting files. Confirm the resulting commit has exactly 2 parents via `git log -1 --format=%P` (expect two hashes: the prior `dev_childwallet` HEAD and `origin/develop`'s HEAD). Then push: `git push origin dev_childwallet`. Confirm the push succeeded by running `git log origin/dev_childwallet -1 --format=%H` and confirming it matches the local merge commit's hash.
  </action>
  <verify>
    <automated>git -C SuperGenius log -1 --format=%P</automated>
  </verify>
  <acceptance_criteria>
    `git log -1 --format=%P` (inside `SuperGenius/`, post-commit) returns exactly 2 hashes. `git log -1 --format=%H` matches `git log origin/dev_childwallet -1 --format=%H` after the push (i.e., the remote branch tip equals the new local merge commit). `git status` shows a clean working tree except for the pre-existing, still-untouched `docs` submodule pointer modification.
  </acceptance_criteria>
  <done>Two-parent merge commit created and pushed; origin/dev_childwallet's tip matches the new commit hash; docs pointer bump remains isolated and unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Bump the outer repo's SuperGenius submodule pointer</name>
  <files>SuperGenius (submodule pointer reference inside the outer GeniusNetwork repo)</files>
  <read_first>
    Outer-repo commit `0d2bd92` (`feat(quick-2tc): bump GeniusSDK submodule pointer for Detach/ReplaceMain/RevokeChild wrappers`) as the established message-style precedent for a submodule-bump commit in this repo, and STATE.md's Performance Metrics table entries showing the prior `chore(0X-0Y): bump SuperGenius submodule...` pattern used throughout Phase 05.
  </read_first>
  <action>
    From the outer `GeniusNetwork` repo root, run `git status` to confirm only the `SuperGenius` submodule pointer shows as modified (new commits) — this reflects Task 1's new merge commit. Stage it explicitly: `git add SuperGenius` (do not use `git add -A`, to avoid picking up unrelated outer-repo working-tree state). Commit with a message following the established pattern, e.g. `chore(06): bump SuperGenius submodule to include origin/develop merge (DevConfig_st rename, ProcessingTransaction retirement)`, describing that this brings SuperGenius current with `origin/develop` per MERGE-01/MVER-01/03/04.
  </action>
  <verify>
    <automated>git show --stat HEAD</automated>
  </verify>
  <acceptance_criteria>
    `git show --stat HEAD` (in the outer repo, post-commit) shows exactly one file changed: `SuperGenius`. `git diff HEAD~1 HEAD -- SuperGenius` shows the submodule pointer moving from the prior hash (`5fd137dcd13475114b070e070cda374850f8e699`) to the new merge commit hash produced in Task 1.
  </acceptance_criteria>
  <done>Outer repo has one new commit bumping the SuperGenius submodule pointer to the pushed merge commit, following the established commit-message pattern for this milestone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local merge commit -> shared origin/dev_childwallet branch | Once pushed, this becomes the shared history other developers and Phase 7 build on; premature or unverified push here is difficult to cleanly undo on a shared branch |
| SuperGenius merge commit hash -> outer repo submodule pointer | These two must reference the exact same commit or the outer repo silently points at an unverified/stale SuperGenius state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Tampering | origin/dev_childwallet (SuperGenius remote branch) | high | mitigate | Task 1 only runs after Plans 01-04 have already verified conflict resolution, rename sweep, clean build, full regression pass, and MVER-04 gate compatibility — the push is the last step in the sequence, never first; acceptance criteria explicitly re-verify the pushed remote tip matches the locally-verified commit hash |
| T-06-11 | Tampering | Outer repo SuperGenius submodule pointer | medium | mitigate | Task 2 uses `git add SuperGenius` (not `git add -A`) and explicitly diffs the pointer change to confirm it moved to exactly the Task-1-verified merge commit hash, not some other unintended ref |
</threat_model>

<verification>
`git log -1 --format=%P` shows 2 parents. `origin/dev_childwallet` tip matches the new local merge commit. Outer repo's submodule pointer diff shows the SuperGenius hash moving to that same merge commit.
</verification>

<success_criteria>
MERGE-01 is fully satisfied: a two-parent merge commit exists on SuperGenius's `dev_childwallet`, pushed to origin, and the outer `GeniusNetwork` repo's submodule pointer reflects it — completing Phase 6 and unblocking Phase 7 (GeniusSDK Merge & Build Verification).
</success_criteria>

<output>
Create `.planning/phases/06-supergenius-merge-regression-verification/06-05-SUMMARY.md` when done
</output>
