---
phase: 06-supergenius-merge-regression-verification
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: true
requirements: [MVER-01]

must_haves:
  truths:
    - "SuperGenius builds cleanly across all targets post-merge (genius_node core lib, registration_transaction_test, child_registration_test), with no new compiler/linker errors introduced by conflict resolution or the rename sweep"
    - "The 4 large auto-merged (zero-conflict-marker) files carrying substantial develop refactors have been manually read through, not just build-verified — the fact that they compile does not by itself prove no semantic regression"
  artifacts:
    - "Built artifacts: genius_node static lib, build/Windows/Debug/test_bin/Debug/registration_transaction_test.exe, build/Windows/Debug/test_bin/Debug/child_registration_test.exe (all rebuilt post-merge, replacing the stale pre-merge binaries)"
  key_links:
    - "Successful compilation of GeniusNode.hpp/.cpp, Blockchain.cpp, and TransactionManager.cpp's EvaluateTransactionReplayProtection depends on both Plan 01's conflict resolution and Plan 02's rename sweep being fully complete first"
</must_haves>
---

<objective>
Build SuperGenius across all relevant targets against the merged, rename-swept, still-uncommitted working tree, and manually read through the auto-merged files that carry the largest semantic refactors from `origin/develop` even though git accepted them with zero conflict markers (git's line-based merge does not catch semantic interaction between non-overlapping hunks in the same function/pipeline).

Purpose: MVER-01 requires a clean build with no new compiler/linker errors post-merge; this is also the first point at which a compile error would surface an incomplete conflict resolution or rename-sweep miss from Plans 01-02. The manual read-through targets the specific "no conflict marker != no review needed" risk flagged in research (Pitfall 1).
Output: genius_node, registration_transaction_test, and child_registration_test all rebuilt successfully; a documented read-through of GeniusNode.hpp/.cpp, Blockchain.cpp, and the EvaluateTransactionReplayProtection rewrite in TransactionManager.cpp, confirming no textual interaction with child-wallet consensus code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-supergenius-merge-regression-verification/06-RESEARCH.md
@.planning/phases/06-supergenius-merge-regression-verification/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build all targets post-merge</name>
  <files>SuperGenius/build/Windows/Debug/ (build outputs only, no source edits expected)</files>
  <read_first>
    06-RESEARCH.md sections "Build Environment" and "Standard build/verify commands" for the exact `cmake --build` invocations and confirmed generator/config (`Visual Studio 17 2022`, `build/Windows/Debug` already configured with `TESTING=ON`). Note the "Stale-install risk" callout: this plan performs NO `cmake --install` step (that is explicitly Phase 7's concern).
  </read_first>
  <action>
    From the repo root, run in sequence: `cmake --build SuperGenius/build/Windows/Debug --target genius_node --config Debug`, then `cmake --build SuperGenius/build/Windows/Debug --target registration_transaction_test --config Debug`, then `cmake --build SuperGenius/build/Windows/Debug --target child_registration_test --config Debug`. If any target fails, read the compiler/linker error, identify whether it traces back to an incomplete conflict resolution (Plan 01 — check for a lingering `ProcessingTransaction` reference) or an incomplete rename sweep (Plan 02 — rerun `grep -rln "DevConfig_st"`), fix the specific file, and rebuild that target before moving to the next. Do not run `cmake --install` at any point in this task.
  </action>
  <verify>
    <automated>cmake --build SuperGenius/build/Windows/Debug --target genius_node --config Debug</automated>
  </verify>
  <acceptance_criteria>
    All three `cmake --build` invocations exit with code 0. `build/Windows/Debug/test_bin/Debug/registration_transaction_test.exe` and `build/Windows/Debug/test_bin/Debug/child_registration_test.exe` exist with a modification timestamp newer than the merge start (confirming they were actually rebuilt, not stale pre-merge binaries).
  </acceptance_criteria>
  <done>genius_node, registration_transaction_test, and child_registration_test all build with exit code 0; both test binaries are freshly rebuilt.</done>
</task>

<task type="auto">
  <name>Task 2: Manual read-through of the 4 large auto-merged files (semantic regression check)</name>
  <files>SuperGenius/src/account/GeniusNode.hpp, SuperGenius/src/account/GeniusNode.cpp, SuperGenius/src/blockchain/impl/Blockchain.cpp, SuperGenius/src/account/TransactionManager.cpp</files>
  <read_first>
    06-RESEARCH.md "Common Pitfalls" Pitfall 1 (treating "no conflict markers" as "no review needed") and the "MVER-04: Consensus Gate Compatibility Analysis" section's "Adjacency risk" paragraph on `EvaluateTransactionReplayProtection`.
  </read_first>
  <action>
    Read the merged (current working-tree) versions of `SuperGenius/src/account/GeniusNode.hpp` and `SuperGenius/src/account/GeniusNode.cpp` end-to-end, confirming the bridge-catchup rewrite (`PerformStartupCatchupScan` moved into a new `BridgeCatchupWatcher` class) has zero textual overlap with registration/transfer/lifecycle code (no `RegisterChild`, `DetachChild`, `RevokeChild`, `ReplaceMain`, `RecoverFromChild`, `FundChild`, or `CheckParentChildAuthority`/`FilterRegistration` symbols appear inside the touched bridge-catchup regions). Read the merged `SuperGenius/src/blockchain/impl/Blockchain.cpp`, confirming the multi-address genesis registry change (`StoreGenesisRegistry` call sites) and deferred-start logic sit entirely above `CheckCertifiedParent`'s definition and do not alter its body. Read the merged `EvaluateTransactionReplayProtection` function body in `SuperGenius/src/account/TransactionManager.cpp` in full — confirm it now calls the new `GetOutgoingPreviousHash(nonce)` helper and applies the new own-address previous-hash consistency check — this function is intentionally NOT modified in this task (targeted test execution against it happens in Plan 04); this read is to build confidence for that verification, and to catch an obviously-wrong merge (e.g. a truncated function body, mismatched braces) before wasting a build/test cycle later.
  </action>
  <verify>
    <automated>grep -c 'CheckParentChildAuthority\|FilterRegistration\|RegisterChild\|DetachChild\|RevokeChild\|ReplaceMain\|RecoverFromChild' SuperGenius/src/account/GeniusNode.cpp</automated>
  </verify>
  <acceptance_criteria>
    `grep -c 'CheckParentChildAuthority\|FilterRegistration\|RegisterChild\|DetachChild\|RevokeChild\|ReplaceMain\|RecoverFromChild' SuperGenius/src/account/GeniusNode.cpp` returns 0 (confirms the bridge-catchup refactor never references child-wallet consensus symbols). `grep -n 'CheckCertifiedParent' SuperGenius/src/blockchain/impl/Blockchain.cpp` returns exactly one definition line, with the function body syntactically intact (balanced braces — spot-checked by reading the function in full). `EvaluateTransactionReplayProtection`'s merged body in `SuperGenius/src/account/TransactionManager.cpp` compiles (already proven by Task 1's exit-0 build) and is confirmed by direct read to reference `GetOutgoingPreviousHash`.
  </acceptance_criteria>
  <done>All 4 files read through in full; zero unexpected symbol overlap between develop's refactors and child-wallet consensus code; EvaluateTransactionReplayProtection's rewrite understood and flagged for Plan 04's targeted test execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auto-merged (non-conflicting) source -> compiled artifact | Git's line-based merge accepts these files with zero markers, but semantic interaction between non-overlapping hunks in the same function/pipeline is not something a build alone can prove absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | GeniusNode.hpp/.cpp, Blockchain.cpp, TransactionManager.cpp (EvaluateTransactionReplayProtection) | high | mitigate | Task 2 requires an explicit manual read-through of all 4 files plus a symbol-overlap grep, rather than treating Task 1's exit-0 build as sufficient proof of no regression — directly addresses 06-RESEARCH.md's Pitfall 1 ("no conflict markers" != "no review needed") |
| T-06-06 | Denial of Service | Build pipeline (stale binaries) | low | mitigate | Task 1's acceptance criteria explicitly checks the rebuilt `.exe` timestamps are newer than merge start, preventing a false-pass from accidentally running stale pre-merge test binaries in Plan 04 |
</threat_model>

<verification>
`cmake --build` exits 0 for all three targets. Both `.exe` files freshly rebuilt. Manual read-through of the 4 large auto-merged files confirms zero symbol overlap with child-wallet consensus code.
</verification>

<success_criteria>
SuperGenius builds cleanly post-merge across genius_node, registration_transaction_test, and child_registration_test, satisfying MVER-01. The 4 large auto-merged files are confirmed free of unexpected semantic interaction with child-wallet logic, setting up Plan 04's regression suite run and targeted MVER-04 verification.
</success_criteria>

<output>
Create `.planning/phases/06-supergenius-merge-regression-verification/06-03-SUMMARY.md` when done
</output>
